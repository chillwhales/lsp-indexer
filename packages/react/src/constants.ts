/** Default number of items per page for paginated queries (useInfinite* hooks). */
export const DEFAULT_PAGE_SIZE = 20;

/** Default maximum items returned in subscription results. */
export const DEFAULT_SUBSCRIPTION_LIMIT = 10;
