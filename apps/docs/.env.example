# ============================================================================
# @lsp-indexer Test App — Environment Variables
# ============================================================================
#
# Copy this file to .env and fill in your Hasura GraphQL endpoint(s).
# You need at least ONE of NEXT_PUBLIC_INDEXER_URL or INDEXER_URL to use the app.
#
# Client mode (@lsp-indexer/react): Browser connects to Hasura directly
# Server mode (@lsp-indexer/next): Browser → Next.js Server Action → Hasura
#
# ============================================================================

# --- Client-side (exposed to browser) ---

# HTTP endpoint for @lsp-indexer/react hooks (browser → Hasura directly)
NEXT_PUBLIC_INDEXER_URL=https://your-hasura-instance.com/v1/graphql

# Required v3 network scope for browser helpers (no implicit default)
NEXT_PUBLIC_INDEXER_NETWORK=lukso-mainnet

# WebSocket endpoint for subscriptions (browser connects here)
# Point at the WS proxy to keep the upstream Hasura URL hidden.
# Falls back to NEXT_PUBLIC_INDEXER_URL with wss:// protocol if not set.
# NEXT_PUBLIC_INDEXER_WS_URL=wss://your-ws-proxy.example.com/v1/graphql

# --- Server-side only (never exposed to browser) ---

# HTTP endpoint for @lsp-indexer/next server actions (server → Hasura)
INDEXER_URL=https://your-hasura-instance.com/v1/graphql

# Required v3 server network scope (falls back only to NEXT_PUBLIC_INDEXER_NETWORK)
INDEXER_NETWORK=lukso-mainnet

# Upstream WebSocket endpoint for the WS proxy (proxy → Hasura)
# Falls back to INDEXER_URL with wss:// protocol if not set
# INDEXER_WS_URL=wss://your-hasura-instance.com/v1/graphql

# Allowed origins for the WS proxy CORS validation (comma-separated)
# Required when running the WS proxy (dev and production)
# INDEXER_ALLOWED_ORIGINS=http://localhost:3000,https://your-app.com

# Port for the WebSocket subscription proxy (default: 4000)
# WS_PROXY_PORT=4000

# --- Indexer v3 alpha development ---
# These variables are not used by the docs app or the production v2 indexer.

# INDEXER_NETWORK=ethereum-mainnet
# INDEXER_FROM_BLOCK=22000000
# INDEXER_TO_BLOCK=22000010
# SQD_PORTAL_URL=https://portal.sqd.dev/datasets/ethereum-mainnet
# RPC_URL=https://ethereum-rpc.publicnode.com
# RPC_URL_LUKSO_MAINNET=https://rpc.mainnet.lukso.network
# RPC_URL_ETHEREUM_MAINNET=https://ethereum-rpc.publicnode.com
# RPC_URL_ETHEREUM_SEPOLIA=https://ethereum-sepolia-rpc.publicnode.com
# INDEXER_ALLOW_HISTORICAL_SOURCE=false
# INDEXER_METRICS_PORT=9090
# DATABASE_URL=postgresql://lsp_v3_runtime:secret@localhost:5432/lsp_indexer_v3
# DATABASE_URL_LUKSO_MAINNET=
# DATABASE_URL_ETHEREUM_MAINNET=
# DATABASE_URL_ETHEREUM_SEPOLIA=
# DATABASE_ADMIN_URL=postgresql://migration_admin:secret@localhost:5432/lsp_indexer_v3
# DATABASE_MIGRATION_NETWORKS=lukso-mainnet,ethereum-mainnet,ethereum-sepolia
# DATABASE_RUNTIME_LOGIN_LUKSO_MAINNET=lsp_v3_lukso_runtime
# DATABASE_RUNTIME_LOGIN_ETHEREUM_MAINNET=lsp_v3_ethereum_runtime
# DATABASE_RUNTIME_LOGIN_ETHEREUM_SEPOLIA=lsp_v3_sepolia_runtime
# DATABASE_API_LOGIN=lsp_v3_hasura
# HASURA_GRAPHQL_V3_DATABASE_URL=postgresql://lsp_v3_hasura:secret@localhost:5432/lsp_indexer_v3
# HASURA_GRAPHQL_UNAUTHORIZED_ROLE=public
# HASURA_GRAPHQL_ENDPOINT=http://localhost:8080
# HASURA_GRAPHQL_ADMIN_SECRET=operator-secret
# DATABASE_POOL_MAX=10
# DATABASE_CONNECTION_TIMEOUT_MS=10000
# DATABASE_IDLE_TIMEOUT_MS=30000
# DATABASE_STATEMENT_TIMEOUT_MS=60000
# DATABASE_LOCK_TIMEOUT_MS=10000
# DATABASE_IDLE_TRANSACTION_TIMEOUT_MS=60000
# DATABASE_UNFINALIZED_BLOCKS_RETENTION=1000
# METADATA_CONCURRENCY=8
# METADATA_POLL_INTERVAL_MS=1000
# METADATA_REQUEST_TIMEOUT_MS=15000
# METADATA_MAX_RESPONSE_BYTES=2097152
# METADATA_MAX_REDIRECTS=3
# METADATA_MAX_ATTEMPTS=6
# METADATA_RETRY_BASE_MS=5000
# METADATA_RETRY_MAX_MS=1800000
# METADATA_LEASE_TIMEOUT_MS=300000
# METADATA_METRICS_PORT=9091
# METADATA_IPFS_GATEWAYS=https://ipfs.io/ipfs,https://dweb.link/ipfs
# METADATA_ALLOW_HTTP=false
# METADATA_RUN_ONCE=false
# TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres
# TEST_HASURA_GRAPHQL_ENDPOINT=http://localhost:8080/v1/graphql
# TEST_HASURA_ADMIN_SECRET=
