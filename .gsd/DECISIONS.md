# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M004 | arch | How to compute follower set intersections | Hasura nested `followed`/`followedBy` relationship filters on `universal_profile` query | Server-side SQL joins, no client-side intersection needed. Schema verified to support this. | No | agent |
| D002 | M004 | data | Return type for mutual follow hooks | `Profile[]` (via `ProfileResult<I>`) not `Follower[]` | The consumer wants to know *who* the mutual connections are, not the follow relationship metadata | No | agent |
| D003 | M004 | pattern | Query approach | Query `universal_profile` table (not `follower` table) with nested relationship filters | The result is profiles, not follow records. Reuses existing profile parser and include machinery. | No | agent |
| D004 | M004/S01 | pattern | How mutual follow service functions build where-clauses | Call execute() directly with composed _and where-clauses instead of using fetchProfiles | fetchProfiles builds its own where from ProfileFilter, but mutual follow queries need two simultaneous followedBy/followed relationship conditions that can't be expressed through ProfileFilter. Direct execute() with Universal_Profile_Bool_Exp gives full control. | Yes | agent |
| D005 | M004/S01 | api | Whether mutual follow hook factories should default params to empty object | Mandatory addressA/addressB (or myAddress/targetAddress) params with no default = {} | Unlike profile queries which can be called without filters, mutual follow queries always require two addresses to compute an intersection — calling without them is nonsensical. Mandatory params prevent accidental misuse. | Yes | agent |
| D006 | M005 | pattern | How to batch-fetch encrypted assets by tuple | Separate `fetchEncryptedAssetsBatch` using `_or`/`_and` Hasura where-clauses, not an extension of `fetchEncryptedAssets` | Input shape (tuple array vs single filter) and query shape (_or/_and exact tuples vs single _ilike filter) are fundamentally different. Matches precedent: fetchIsFollowingBatch is separate from fetchFollowers. | No | agent |
| D007 | M005 | data | Address comparison in batch tuple queries | `_ilike` for address, `_eq` for contentId and revision | Non-checksummed address is exactly the same as checksummed — case-insensitive comparison required. contentId and revision are exact-match fields. | No | agent |
| D008 | M005/S01 | pattern | Whether batch hook factory should use createUseList or direct useQuery | Direct useQuery with manual 3-overload signatures, not createUseList | Batch results have no totalCount or pagination — createUseList adds unnecessary pagination machinery. Direct useQuery with enabled guard on tuples.length and stable EMPTY array is simpler and matches the createUseIsFollowingBatch precedent. | Yes | agent |
| D009 | M006/S01 | pattern | How to handle non-boolean hex values from supportsInterface multicall responses | Create a `safeHexToBool` utility that wraps viem's `hexToBool` in try-catch, returning `false` for any `InvalidHexBooleanError`. Place it in `packages/indexer/src/utils/index.ts`. | viem's hexToBool is strict (only 0x0/0x1). Rogue contracts can return arbitrary data for supportsInterface. Wrapping in try-catch is the simplest defensive fix — any non-boolean response means the contract doesn't properly implement the interface, so false is correct. | No | collaborative |
| D010 | M007 | api | How to fetch collection attribute facets for filter dropdowns | Use distinct_on with lsp4_metadata_attribute to get unique {key, value} pairs per collection | Hasura supports distinct_on for lsp4_metadata_attribute. Querying distinct attributes server-side is more efficient than fetching all attributes and deduplicating client-side. Total NFT count via nft_aggregate in the same query. | Yes | collaborative |
| D011 |  | requirement | R025 — Full stack propagation for all changes | validated | pnpm build exits 0 across all 9 workspace projects. All type changes (NftInclude, NftFilter, NftSortField, CollectionAttribute, OwnedTokenNftInclude) propagate cleanly through types → node → react → next. | Yes | agent |
| D012 |  | requirement | R026 — Documentation updated for all changes | validated | All 4 docs pages updated: node (fetchCollectionAttributes, collectionAttributeKeys, 7 NFT include fields, 4 filter fields, score sort), react (useCollectionAttributes, NFT include/filter/sort), next (getCollectionAttributes, NFT include/filter/sort), home (Collection Attributes domain row). All grep checks pass. 22/22 static pages build. | Yes | agent |
