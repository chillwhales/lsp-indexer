- 'queueDelete() separate from removeEntity() to distinguish DB-level vs in-memory bag removal'
- 'postVerification as opt-in boolean flag — existing handlers unaffected'
- 'topologicalSort called on both discoverHandlers() and registerEntityHandler()'
- 'OwnedAsset FK on OwnedToken set directly (not via enrichment queue) since OwnedAsset is handler-created, not a verified core entity'
- 'Dual-trigger handlers read from ALL bags each invocation to ensure consistency regardless of trigger order'
- 'tokenId null sentinel used to mark OwnedTokens for deletion (matching V1 pattern)'
- 'Decimals uses postVerification: true to run after DA verification in Step 5.5'
- 'FormattedTokenId mutates NFT entities in-place in BatchContext for Path 1'
- 'FormattedTokenId adds reformatted existing NFTs to BatchContext for Path 2 persistence'
- "JSDoc 'Port from v1' references annotated with deletion note rather than removed entirely — preserves provenance trail"
- "Dual-output via Subsquid Logger.child() + pino file transport — keeps stdout/stderr under Subsquid control while adding independent file rotation"
- "LOG_LEVEL env var overrides NODE_ENV-based default regardless of environment"
- "Four severity levels only (debug, info, warn, error) — no trace/fatal per user decision"
- "DualLogger wrapper passes attributes as objects directly — never JSON.stringify"
- 'vitest @/* alias uses lib/ directory (compiled JS) since src/ is incomplete'
- 'CJS require resolution for @/* handled via Module._resolveFilename hook in vitest.setup.ts'
- 'Follower handler uses generateFollowId from @/utils for V1-compatible deterministic IDs'
- "Used type assertions (null as unknown as undefined) for entity FK fields where TypeORM model types don't include null but compiled JS sets null at runtime"
- 'Cast dataKey/dataValue to 0x${string} template literals for viem hexToBytes compatibility'
- 'Mocked mergeEntitiesFromBatchAndDb at module level to isolate handler logic from DB dependencies'
- 'Step loggers created once per step section (outside loops) for efficiency'
- "Helper modules (verification.ts) use inline step field since they don't have direct createStepLogger access"
- "Handler log calls use step='HANDLE' + handler='{name}' for dual-level filtering"
- 'decimals.handler.ts and formattedTokenId.handler.ts deferred — no TS sources exist yet (Phase 1 scope)'
- 'Type-only imports (import type) for @lukso/lsp2-contracts and @lukso/lsp3-contracts to avoid runtime dependency'
- 'Type assertion for discriminated union narrowing due to TS strictNullChecks being off'
- 'EntityConstructor used for entity updates (new config.entityClass({...})) to preserve TypeORM decorators'
- 'LSP29 AccessControlCondition not in SubEntityDescriptors — cleared via encryption cascade'
- 'LSP29 entityUpdates returns version, contentId, revision, createdAt for parent entity'
- 'LSP3 profile images treated as flat arrays (matching V1 structure, unlike LSP4 nested arrays)'
- 'Icons mapped without isFileImage filter to match V1 behavior exactly'
- 'Category entity always created even when value is undefined'
- 'Score uses parseInt, Rank uses parseInt, attribute.rarity uses parseFloat'
- 'Test through shared utility (integration-style) rather than mocking handleMetadataFetch'
- 'Mock store.find and workerPool.fetchBatch as the external boundaries'
- 'Verify entity creation by count and field values rather than ID matching'
- 'Type assertions for entity FK null vs undefined (matching 02-03 pattern)'
- 'createComponentLogger uses Logger.child() pattern for automatic component field injection'
- 'DEBUG_COMPONENTS documented for post-hoc filtering (not runtime filtering) - advisory only'
- "All debug logs use isLevelEnabled('debug') check for zero-cost when disabled"
- 'Worker pool logs batch scheduling, distribution, completion, retries, and shutdown'
- 'Metadata handlers log entry, duration, and entity counts (IDs/counts only, not full objects)'
- 'metadataWorkerPool.ts uses relative path ./logger (both files in core/ directory)'
- 'Handler files use absolute path @/core/logger (handlers/ to core/ requires parent navigation)'
- 'Test file adds to existing import from ../logger (test in __tests__ subdirectory)'
- 'Consolidate type assertions to factory function instead of removing entirely'
- 'Use if/else pattern in handlers to avoid code duplication'
- 'Keep startTime in worker pool (minimal overhead), only calculate duration inside guard'
- 'MockLogger interface with explicit vi.fn() types eliminates all type assertions'
- "Check hctx.context.log.isLevelEnabled('debug') before creating component logger"
- 'Logger variable scope minimized to debug block only'
- 'RPC_ENDPOINT defaults to LUKSO mainnet RPC (https://rpc.lukso.network)'
- 'ARCHIVE_URL defaults to LUKSO Subsquid archive endpoint'
- 'Finality confirmation set to 75 blocks per LUKSO requirements'
- 'Block range starts from genesis (block 0) by default'
- 'createRegistry() takes Logger parameter for structured boot logging'
- 'Registry.discover() and discoverHandlers() called with resolved __dirname paths'
- 'Log subscriptions configured via for-loop calling processor.addLog()'
- 'Bootstrap happens before processor.run() to ensure all subscriptions configured'
- 'MetadataWorkerPool pool size configurable via METADATA_WORKER_POOL_SIZE env var (default: 4)'
- 'createPipelineConfig() assembles all dependencies for processBatch in one function'
- 'BOOTSTRAP added to PipelineStep union for structured boot logging'
- 'createLogger() uses sqd:processor namespace for Subsquid convention'
- 'Fixtures are synthetic (not real blocks) for deterministic testing'
- 'Fixtures formatted as minimal Subsquid block structure: header + logs array'
- 'Multi-event fixture includes LSP7 Transfer, DataChanged, and LSP8 Transfer events'
- 'Event topic0 values are real (from LUKSO LSP contracts)'
- 'Addresses and hashes are synthetic placeholders'
- 'Mock store tracks inserted/upserted/removed entities for verification'
- 'Mock verification function simulates supportsInterface checks without RPC calls'
- 'Tests validate handler execution order matches V1 dependency graph'
- 'Integration tests run without network dependency using committed fixtures'
- 'Snake case conversion preserves LSP prefixes (LSP3ProfileImage → lsp3_profile_image)'
- 'GraphQL client returns -1 for missing tables instead of throwing errors'
- 'Field introspection filters to scalar types only (excludes object/array relations)'
- 'Field cache per table to avoid repeated introspection queries'
- 'Fixed address comparison at pipeline filter level (not in constants)'
- 'Owner handlers use postVerification: true (need verified UP/DA entities)'
- 'Entity id = emitting address, address field = newOwner (matches V1 exactly)'
- 'Two-phase pattern: create entities with value=false on mint, verify and update to value=true at chain head'
- 'Batch size 500 with 1-second rate limiting matches V1 behavior'
- 'CHILL contract uses getClaimedStatusFor; ORBS contract uses getChillwhaleClaimStatus'
- 'Mint detection via LSP8Transfer from zero address matches V1 behavior exactly'
- 'Default entity overwrites via addEntity() when TokenIdDataChanged arrives (Map.set() semantics)'
- 'Both mint and TokenIdDataChanged branches queue same enrichment for digitalAsset/nft FKs'
- 'Entity ID format: "BaseURI - {address} - {tokenId}" matching V1 exactly'
- 'URL derivation: baseUri.endsWith("/") ? baseUri + tokenId : baseUri + "/" + tokenId'
- 'formattedTokenId fallback: use raw tokenId when formattedTokenId is null'
- 'Base URI change path queries ALL NFTs from DB + batch for comprehensive derivation'
- 'Mint path checks DB + batch for base URI (batch takes priority)'
- 'Deduplication via Map prevents duplicate entities in same batch'
- 'resolveEntities has identical semantics to mergeEntitiesFromBatchAndDb (preserves all batch entities, not just requested IDs)'
- 'resolveEntity is new single-entity variant for handlers that only need one lookup'
- 'Deleted old function immediately to force migration via compile errors'
- 'Removed stale TODO referencing Phase 4 bootstrap wiring (already completed)'
- 'Deleted mergeEntitiesFromBatchAndDb after verifying zero callers'
- 'Applied lsp6Controllers.handler.ts structured logging pattern to all handler log calls'
- 'DataChangedEvent base fields: address, dataKey, dataValue, dataKeyName — dataKeyName is parser-derived (resolved name for known ERC725Y keys, null if unknown)'
- 'TokenIdDataChangedEvent base fields: address, dataKey, dataValue, tokenId, dataKeyName — same parser-derived dataKeyName plus tokenId'
- 'NFT include is boolean-only (not NftInclude) — TokenIdDataChangedEventNftSchema is a lightweight 5-field sub-type, not the full Nft domain type'
- 'No singular hooks for either domain — event tables have no natural single-entity key (opaque Hasura ID only)'
- 'Both domains created in single plan since they share ~90% identical structure'
- 'resolveDataKeyName returns human-readable name for known ERC725Y hex keys from 10 @lukso/lsp*-contracts packages, null for unknown keys'
- 'Array keys (e.g., LSP4Creators[]) resolved as .length and .index sub-entries'
- 'Map built at module load time — small constant map, no lazy init needed'
- 'NFT name filter uses _or pattern for lsp4Metadata/lsp4MetadataBaseUri dual sources (matching nfts service)'
- 'DA include vars use prefix replacement: include* → includeDigitalAsset*'
- 'UP include vars use prefix replacement: includeProfile* → includeUniversalProfile*'
- 'All hooks follow encrypted-assets pattern exactly — consistent API across all domains'
- 'React hooks use getClientUrl() for direct browser queries, Next.js hooks route through server actions'
- 'Both domains use same flat return shape: { items, totalCount, ...queryState } for useQuery, { items, hasNextPage, fetchNextPage, isFetchingNextPage, ...queryState } for useInfiniteQuery'
- 'dataKeyName displayed as bold resolved name with raw hex in mono below — Unknown Key shown as muted italic when null'
- 'Separate pages per domain (/data-changed-events, /token-id-data-changed-events) with 3 tabs each (Latest, List, Infinite)'
- 'NFT info section uses field-presence guards on Record<string, unknown> — no import from nfts domain types'
- 'nft include toggle in scalar include fields array (schema supports z.union([z.boolean(), OwnedTokenNftIncludeSchema]) but playground uses boolean toggle)'
- '3 relation resolvers (UP, FromProfile, FromAsset) — most relation-heavy domain in the project'
- 'Both UP sub-selections (receiving and sender) use followed_aggregate for following count — matching established pattern'
- '46 GraphQL variables — highest variable count of any document in the project'
- 'Parser calls sub-parsers (parseProfile, parseDigitalAsset) without sub-include forwarding — matching data-changed-events pattern. stripExcluded handles field removal at the parent level.'
- '3 prefix replacement loops in buildUniversalReceiverEventIncludeVars — unique to this domain (most relation-heavy in project)'
- 'Followed issued-assets hooks pattern exactly (list + infinite, no singular, no latest)'
- 'React hooks use getClientUrl() for direct browser → Hasura; Next.js hooks route through server action'
- "Used ArrowDownFromLine lucide icon for 'receiving' semantic (ArrowDownToLine not exported in v0.574.0)"
- '3 SubIncludeSection panels — most of any domain page (receiving UP, sender UP, sender DA)'
- 'ExpandableHex reused for receivedData and returnedValue hex display'
- 'newest/oldest sort fields replace timestamp/blockNumber — abstracts blockchain terminology from consumers'
- 'buildBlockOrderSort returns Record<string, Order_By>[] for structural compatibility with all Hasura order_by types'
- 'direction and nulls ignored for newest/oldest — self-describing fields'
- 'Default sort fallback uses ?? operator with typed cast: buildBlockOrderSort("desc") as X_Order_By[]'
- 'Parser block fields use proper typed access after codegen (removed Record<string, unknown> workaround from Plan 01)'
- 'hideDirectionAndNulls is a simple boolean prop computed from sortField at usage site — keeps SortControls generic and reusable'
- 'newest/oldest added at top of SORT_OPTIONS arrays — most common sort first for quick selection'
- 'Extracted Lsp4ImageSchema/Lsp4LinkSchema/Lsp4AttributeSchema to common.ts — eliminates duplication between digital-assets.ts and nfts.ts, reusable by future domains'
- 'Kept DigitalAssetImageSchema etc. as backward-compat aliases (const alias = Lsp4Schema) — zero breaking changes'
- 'NftFilter.collectionAddress enables useNftsByCollection pattern (QUERY-03)'
- 'NftOwner has address + timestamp (from owned_token table) — different from DigitalAssetOwner which is the contract owner'
- 'Extracted parseImage to parsers/utils.ts for cross-domain reuse (digital-assets + nfts share identical LSP4 image parsing)'
- 'parseImage returns Lsp4Image type (structurally identical to DigitalAssetImage alias)'
- 'Boolean filter fields (isBurned, isMinted) use !== undefined check to allow filtering by false values'
- 'Node index already had NFT exports from plan 02 — no modification needed'
- 'Followed digital asset hook patterns exactly for consistency'
- 'Single NFT lookup supports tokenId OR formattedTokenId (not both required) — stacked inputs'
- 'Name and formattedTokenId filters added for list view (name searches both metadata sources via _or)'
- 'Collection is full DigitalAsset with sub-include toggles (17 collection variables in GraphQL)'
- 'Holder terminology used throughout (owner = contract deployer, holder = token possessor)'
- 'BaseUri fallback pattern: check lsp4Metadata first → lsp4MetadataBaseUri second → null'
- 'OwnedAsset.balance uses z.bigint() per locked decision — parser returns bigint, consumer formats'
- 'OwnedAssetFilter exposes 4 string fields (owner, address, digitalAssetId, universalProfileId) — balance/timestamp range filters deferred to future'
- 'OwnedAssetSortField includes nested sorts: digitalAssetName and tokenIdCount — mapped at service layer'
- 'OwnedToken NFT relation includes full lsp4Metadata + lsp4MetadataBaseUri for fallback parsing'
- 'Universal profile fragment in nested includes fetches all LSP3 fields (name, description, tags, links, avatar, profileImage, backgroundImage) without follower/following counts'
- 'Type casts (as any) for cross-domain parser calls where nested sub-selections omit fields like id'
- 'buildDigitalAssetIncludeVars reused directly (not prefixed like NFT collection) since owned asset DA vars share same include* names'
- 'Single lookup uses natural keys (holder + asset address) via useOwnedAssets/useOwnedTokens with filter + limit:1, not opaque Hasura IDs'
- 'Preload prevention: limit:0 when no query present (prevents useOwnedAssets firing on empty filter)'
- 'Full addresses displayed everywhere (no truncation) with break-all wrapping'
- 'All collapsible triggers use ghost Button style matching RawJsonToggle pattern'
- 'NFT holder Universal Profile fetched as full UP block (not per-field @include)'
- 'NftHolderSchema includes universalProfile: ProfileSchema.nullable()'
- 'parseHolderProfile() uses inline raw type derivation from codegen types (differs from RawProfile)'
- 'Collection section moved to last position in NftCard (after NFT Metadata)'
- 'IncludeResult uses ActiveFields helper to extract truthy include keys via conditional mapped types'
- 'const I generic parameter on all functions for literal type inference preservation'
- 'as ProfileResult<I> cast at service boundaries since parser returns Profile (full type) before strip'
- 'stripExcluded accepts derivedFields param for future cross-field dependencies (e.g., standard derives from decimals)'
- 'ResolveStandard<I> as intersection type — standard field added via & when decimals is included'
- "derivedFields { standard: 'decimals' } in stripExcluded call — runtime stripping mirrors type-level derivation"
- 'buildDigitalAssetIncludeVars signature unchanged — public API for other domains (NFTs, OwnedAssets, OwnedTokens)'
- 'NFT base fields: address, tokenId, isBurned, isMinted (identity + boolean flags always present)'
- 'OwnedAsset base fields: id, digitalAssetAddress, holderAddress (identity fields always present)'
- 'Nested relation narrowing via intersection types (ResolveNftCollection, ResolveNftHolder, etc.)'
- 'NftHolder timestamp handled via ProfileResult<H> & { timestamp: string } intersection'
- 'Recursive stripping: parser delegates to parseDigitalAsset/parseProfile with sub-include param'
- 'OwnedTokenNftScalarFieldMap uses IncludeResult<Nft> directly (not NftResult) since collection/holder are absent in owned-token context'
- 'OwnedTokenOwnedAssetFieldMap uses IncludeResult<OwnedAsset> directly (not OwnedAssetResult) since only 3 scalar fields available'
- 'OwnedTokenNftInclude and OwnedTokenOwnedAssetInclude are subtypes of NftInclude and OwnedAssetInclude — safely passable to parseNft and parseOwnedAsset'
- 'Card prop types changed from domain types to Record<string, unknown> for conditional rendering'
- 'Pages cast narrowed result types at card boundaries with as Record<string, unknown>'
- 'Removed explicit ResultsList<T> generics in favor of inference from items prop'
- 'No changes needed to include-toggles.tsx — existing toggle component already produces correct include shape'
- 'FollowerResult uses dual profile resolvers (ResolveFollowerProfile + ResolveFollowedProfile) — each side independently narrowable'
- 'GetFollowersDocument serves all list queries (followers, following, isFollowing) — service layer varies $where'
- 'GetFollowCountDocument uses aliased follower_aggregate for separate follower/following counts'
- '22 @include variables: 2 scalars + 2×9 profile sub-includes + 2 top-level profile toggles'
- 'fetchFollowers uses direction param (followers/following) to select primary field — single service function for 4 list hooks'
- 'parseProfile called with sub-include params for recursive nested stripping — follower and followed profiles independently narrowable'
- 'fetchIsFollowing sets all 22 include variables to false explicitly for maximum efficiency'
- 'Primary address filter uses exact escapeLike (no % wrapping) while secondary filters use partial match (% wrapping)'
- 'getFollowers server action passes direction through to fetchFollowers — single action serves both useFollowers and useFollowing'
- 'useFollowing returns { following } not { followers } for semantic clarity — maps internally from fetchFollowers result'
- 'useFollowCount returns 0 defaults — safe for UI rendering before data loads'
- 'useIsFollowing enabled guard: Boolean(followerAddress) && Boolean(followedAddress) — prevents queries with empty addresses'
- 'FollowerCard accepts Record<string, unknown> — consistent with other domain cards (DX-04 pattern)'
- 'Two SubIncludeSections for followerProfile and followedProfile — each independently controllable'
- 'CountTab shows followerCount + followingCount in large text with labels'
- 'IsFollowingTab shows YES/NO Badge with descriptive text'
- 'Each list tab has its own address input — no shared state across tabs (per CONTEXT.md)'
- 'No implementation body changes — only overload declarations modified'
- 'Generic overload always position 2 of 3 (between no-include and fallback)'
- 'Followed followers gold standard pattern exactly — 3 overloads per function, generic first'
- 'Return type aliases defined per-file (not shared) to keep each hook file self-contained'
- 'Removed explicit type annotations on intermediate variables (let return type alias handle typing)'
- 'Missing type imports from 09.6-02 fixed as blocking issue — 15 files needed UseQueryResult, InfiniteData, UseInfiniteQueryResult, FetchXResult, and XResult type imports'
- 'CreatorResult uses dual relation resolvers (ResolveCreatorProfile + ResolveCreatorDigitalAsset) — each side independently narrowable'
- 'GetCreatorsDocument has 31 @include variables: 3 scalars + 10 creator profile + 18 digital asset'
- 'Base fields creatorAddress + digitalAssetAddress always present (no natural single key — opaque Hasura ID excluded)'
- 'No singular useCreator hook — creator records identified by composite context only'
- 'parseCreator uses as any cast for creatorProfile sub-selection (matching cross-domain pattern from owned-assets)'
- 'stripExcluded nestedConfig includes derivedFields for digitalAsset (standard derives from decimals)'
- 'Name sorts default nulls: last (profiles/assets without names sort last)'
- 'Followed followers hook pattern exactly — consistent DX across all domains'
- 'DEFAULT_PAGE_SIZE = 20 matching all other domains'
- 'Followed FollowerCard collapsible pattern exactly — consistent UX across domains'
- 'Two heterogeneous collapsible sections (Profile + Digital Asset) vs Follows two homogeneous (both Profile)'
- '7 filter fields split into 3 rows: addresses+interfaceId, names, dates'
- 'IssuedAsset base fields are issuerAddress and assetAddress (composite natural key)'
- 'Hasura field universalProfile → domain issuerProfile, Hasura field issuedAsset → domain digitalAsset'
- '31 include variables (3 scalar + 10 issuerProfile + 18 digitalAsset) matching Creator document pattern'
- "followed_aggregate used for following count (not following_aggregate which doesn't exist in schema)"
- 'Parser maps Hasura address → issuerAddress (not issuer_address — Hasura field IS address)'
- 'Parser maps universalProfile → issuerProfile and issuedAsset → digitalAsset (Hasura→domain rename)'
- 'buildIssuedAssetIncludeVars replaces includeProfile→includeIssuerProfile and include→includeDigitalAsset prefixes'
- 'Followed Creator hooks pattern exactly — same 3-overload structure, return type aliases, destructured infinite query properties'
- 'IssuedAssetCard uses PartialExcept<IssuedAsset> domain type — NOT Record<string, unknown>'
- 'Followed CreatorCard pattern exactly — shared CollapsibleProfileSection and CollapsibleDigitalAssetSection'
- 'FileOutput lucide icon for Issued Assets nav link'
- 'Encryption include uses z.union([z.boolean(), EncryptedAssetEncryptionIncludeSchema]) — true = full, object = fine-grained, false/omitted = skip'
- 'Base fields: address, contentId, revision (per CONTEXT.md locked decision)'
- 'No singular hook — no reliable natural key exists'
- 'LSP29 images have unique fields (verificationSource, imageIndex) unlike LSP4 images — separate schema'
- 'LSP29 images use domain-specific parseEncryptedAssetImage (not shared parseImage) — different schema with imageIndex and verificationSource'
- 'fileSize filter uses _gte with String() conversion for Hasura numeric scalar input type'
- 'revision filter uses _eq (exact numeric match) not _ilike (it is Int, not String)'
- 'universalProfile parsed without sub-include in result construction — stripExcluded nestedConfig handles recursive stripping'
- 'No singular hook — encrypted assets have no reliable natural key (same pattern as creators/issued assets)'
- 'Server action uses fetchEncryptedAssets(url, params) passthrough — no branching needed'
- 'ImageList accepts generic { url?: string | null; [key: string]: unknown }[] — works for both LSP4 and LSP29 image arrays'
- 'EncryptedAssetCard uses PartialExcept<EncryptedAsset, address | contentId | revision> — NOT Record<string, unknown>'
- 'Encryption sub-include uses useSubInclude for accessControlConditions toggle'
- 'Card title shows title text (or truncated address) + truncated contentId for identification'
- 'Ciphertext truncated at 100 chars in encryption collapsible section'
- 'File size displayed with toLocaleString() for human-readable formatting'
- 'SubscriptionConfig moved to @lsp-indexer/node (depends on TypedDocumentString from codegen)'
- 'SubscriptionClient interface in types package uses structural { toString(): string } for document to avoid TypedDocumentString import'
- 'TResult threads through executeSubscription via graphql-ws Client.subscribe<Data>() — zero casts in data path'
- 'Migrated encrypted-assets hooks from flat file to directory structure (react + next) to accommodate subscription hook alongside query hooks'
- 'Entity domain uses Hasura default ordering (order_by: undefined) — matching creators/issued-assets pattern'
- '31 include variables in subscription document matching query document (2 scalar + 6 encryption + 4 file + 3 chunks + 1 images + 2 title/desc + 10 universal profile + 3 relation toggles)'
- 'Migrated data-changed-events hooks from flat file to directory structure (react + next) to accommodate subscription hook alongside query hooks'
- 'Event domain uses block-order desc default sort (block_number desc, transaction_index desc, log_index desc) — matching followers subscription pattern'
- '~35 include variables in subscription document matching query document (4 scalar + 10 UP + 18 DA + 3 control variables)'
- 'Migrated token-id-data-changed-events hooks from flat file to directory structure (react + next) to accommodate subscription hook alongside factory-based query hooks'
- 'Event domain uses block-order desc default sort (block_number desc, transaction_index desc, log_index desc) — matching data-changed-events subscription pattern'
- '~31 include variables in subscription document matching query document (4 scalar + 18 DA + 1 NFT + 8 NFT sub variables)'
- 'Followed established factory pattern: createUseUniversalReceiverEventSubscription + thin wrappers in react/next'
- 'Migrated universal-receiver-events hooks from flat file to directory (matching all other subscription domains)'
- 'EVENT domain uses block-order desc sort: block_number desc, transaction_index desc, log_index desc'
- '3-overload type narrowing: include: I → narrowed, no include → full, optional include → partial'
- 'createUseProfileSubscription factory pattern — mirrors createUseSubscription, both react and next call factory with their own useSubscription'
- 'useQueryClient passed as optional factory param — Rules of Hooks compliant, only used when invalidate=true'
- 'Entity domain uses Hasura default ordering (order_by: undefined)'
- 'Name filter used for search field (reusing buildProfileWhere name → _ilike pattern)'
- 'Refactored all 3 existing digital asset query hooks from monolith to factory pattern alongside adding subscription'
- 'Next.js useDigitalAsset wrapper destructures params to match getDigitalAsset(address, include) positional signature'
- 'Exported buildDigitalAssetOrderBy which was previously private, needed by subscription config builder'
- 'Refactored all 3 existing NFT query hooks from monolith to factory pattern alongside adding subscription'
- 'Next.js useNft wrapper destructures params to match getNft(address, tokenId, formattedTokenId, include) positional signature'
- 'Exported buildNftOrderBy which was previously private, needed by subscription config builder'
- 'Converted owned-assets single files to directory modules in both react and next packages'
- 'Only subscription factory needed — existing query hooks already have factories in digital-assets/'
- 'Fixed relative import paths after file-to-directory conversion (../actions/ → ../../actions/)'
- 'Converted owned-tokens single files to directory modules in both react and next packages'
- 'Only subscription factory needed — existing query hooks do not have factories in this directory'
- 'Fixed relative import paths after file-to-directory conversion (../actions/ → ../../actions/)'
- 'Converted followers single files to directory modules in both react and next packages'
- 'Only subscription factory needed — existing query hooks do not have factories in a separate directory'
- 'Fixed relative import paths after file-to-directory conversion (../actions/ → ../../actions/ in next package)'
- "Event domain block-order desc default sort: buildFollowerOrderBy(params.sort) ?? buildBlockOrderSort('desc')"
- 'Entity domain uses Hasura default ordering (order_by: undefined when no sort) — matching profiles pattern'
- 'Converted creators hooks from monolith to directory structure (matching 10.3-10.7 precedent)'
- 'Factory pattern with 3-overload type narrowing for include-based type inference'
- 'Migrated issued-assets hooks from flat file to directory structure (react + next) to accommodate subscription hook alongside query hooks'
- 'Entity domain uses Hasura default ordering (order_by: undefined) — matching creators pattern'
- '31 include variables in subscription document matching query document'
- 'PropertyKey[] for fromValidationError issues param — Zod v4 uses PropertyKey (includes symbol) instead of (string | number)[]'
- 'Separate runtime import for schemas — import type for types, import for Zod schema values, matching isolatedModules requirement'
- "Added typesVersions to @lsp-indexer/next for node10 resolution of ./server entry — attw flagged NoResolution for node10 without it"
- 'Import DataKeyNameSchema and TypeIdNameSchema directly from @chillwhales/erc725 and @chillwhales/lsp1 (both use Zod 4, same as this repo)'
- 'No additional @chillwhales package swaps warranted — lsp-indexer utilities operate at Hasura/GraphQL read layer while @chillwhales packages operate at on-chain interaction layer'
- 'No upstream PRs warranted — all local utilities are either Hasura-specific, tightly coupled to internal include system, already covered by @chillwhales/utils, or trivially simple'
- "Dropped '. ./env.sh &&' prefix from root scripts — env.sh only existed at docker/v1/env.sh (deleted), indexer loads env via dotenv internally"
- 'Updated CI branch triggers from refactor/indexer-v2 to refactor/indexer-v2-react (active working branch)'
- 'Removed eslint ignore for packages/indexer/ — now the active code, should be linted'
- 'Removed KnownDivergence type and V1_V2_DIVERGENCES entirely — all diffs are now unexpected since v1 no longer exists'
- 'Simplified RowDiff to single diffs field — no known/unexpected split needed without v1-v2 mode'
- 'FK coverage always checks both endpoints — no v1-v2 branch that only checks target'
- 'Kept refactor/indexer-v2-react branch name in AGENTS.md — it is the actual working branch name'
- 'JSDoc on z.infer type aliases uses {@link XSchema} cross-reference pattern for discoverability'
- 'Barrel re-exports (export * from) do not need individual JSDoc — they inherit from source'
- 'Function overloads only need JSDoc on the first overload (TypeScript convention)'
- 'Page header comments follow template: hooks demonstrated, patterns shown, tab layout'
- 'Card JSDoc describes props interface (PartialExcept pattern), sections, and rendering behavior'
- 'Pre-existing ESLint errors (empty object type in include types) noted but not fixed (out of scope)'
- "Followed 09.5 VERIFICATION.md format exactly — same frontmatter, tables, section headings"
- "Included PAGE-01 contribution in 09.1 verification (useInfiniteDigitalAssets exists)"
- "PAGE-01 traceability maps to Phase 9.* (all domain sub-phases) not Phase 16 — Phase 16 is gap closure, not implementation"
- "Followers useInfiniteFollows (with direction param) counts as 1 infinite hook covering both followers and following"
- "Reset private package versions to 0.1.0 as clean semver baseline for v1.2"
- "Hardcode HASURA_GRAPHQL_METADATA_DEFAULTS as single-quoted YAML string — data connector URIs reference internal service names that won't change"
- "PostgreSQL port not exposed in production — access only via internal Docker network"
- "All 3 required vars use ${VAR:?message} syntax for fail-loud behavior"
- "Used composite @index on (blockNumber, transactionIndex, logIndex) for all 61 modified entities"
- "OwnedAsset/OwnedToken 'block' field renamed to 'blockNumber' for consistency with all other entities"
- "Placeholder blockNumber:0 values used in queueEnrichment calls (replaced in Plans 02/03)"
- "BlockPosition type defined in core/types/verification.ts (central types barrel) rather than verification.ts to avoid circular imports"
- "Block position map is optional param on VerifyFn to maintain backward compatibility"
- "Fallback to 0 for block fields when no block position available (blockPos?.blockNumber ?? 0)"
- "Merge/spread patterns preserve existing entity block fields (lsp5ReceivedAssets, lsp4Creators, lsp12IssuedAssets) — only new entities get event block fields"
- "Post-verification handlers cast verified entities to access block fields: (da as { id: string; blockNumber?: number; ... })"
- "Metadata fetch handlers propagate parent entity block fields to sub-entities rather than using any separate source"
- "lsp6Controllers shared helper getOrCreateController receives event parameter to thread block fields through"
- "lsp4MetadataBaseUri treated as Category B (transfer-derived) not Category A (data-key) despite similar name"
- "Handlers cast at call site rather than re-introducing generic on interface — makes type-erasure boundary explicit"
- "Test entities use variable extraction + as Entity cast to bypass excess property checking on object literals"
- "Used wget for monitoring healthchecks (Alpine images lack curl)"
- "Alloy River syntax for unified log+metric collection pipeline"
- "Grafana anonymous Viewer access with admin login for editing"
- "Optional Logger in PluginRegistry constructor so tests work without logger"
- "Used warn (not error) for invalid METADATA_WORKER_POOL_SIZE since it's recoverable"
- "Keep 1 console.error in metadataWorker.ts (parentPort null check — impossible to relay)"
- "Worker LOG messages use type discrimination (array = results, object with type LOG = log relay)"
- "metadataFetch.ts error branch keeps template string for entity fetchErrorMessage field but uses structured attrs for log call"
- "Used performance.now() for sub-millisecond timing precision over Date.now()"
- "Timing for VERIFY step includes single-pass grouping, verification, post-verify handlers, and late enrichment processing as one unit"
- "RESOLVE timing logged via separate resolveTimingLog since fkResolution.ts creates its own internal logger"
- "Used stacked bars for pipeline step latency to show relative contribution of each step"
- "Added threshold coloring (green/yellow/red) on batch time and backlog panels for at-a-glance health"
- "Used dual-axis on Metadata Fetch Performance (ms left, counts right) to combine duration and throughput"
- "Used byRegexp matcher for override rules to apply to all backlog/processed/failed series"
- "Removed block, timestamp, transactionIndex, logIndex as individual sort fields — replaced by deterministic newest/oldest"
- "Block-order tiebreaker appended to ALL non-block sort fields for deterministic pagination"
- "Default sort is buildBlockOrderSort('desc') when no sort parameter passed"
- "Minor version bump 1.0.0 → 1.1.0 for sorting feature (removals not breaking since no external consumers yet)"
- "Used changesets fixed group to version all 4 packages together from single changeset"
- "Used @chillwhales/lsp31@0.1.2 instead of @0.1.0 (plan version not published, latest used)"
- "Stored per-backend chunk references as JSON strings (ipfsChunks, lumeraChunks, s3Chunks, arweaveChunks) rather than separate entities"
- "Encryption params stored as flat discriminated entity with nullable method-specific fields"
- "Used isLsp29Asset() type guard for schema validation instead of decodeLsp29Metadata() — type guard returns boolean for graceful error handling vs throwing"
- "Kept isFileImage() guard for images — shared utility used by LSP3/LSP4 domains, not LSP29-specific"
- "Updated node/schema.graphql locally to add new Hasura types for codegen validation (will be overwritten on next schema:dump)"
- "Fixed test app encrypted-asset-card.tsx as blocking deviation — it referenced old AccessControlCondition types"
- "IPFS backend preferred via selectBackend(entries, 'ipfs') — IPFS is the primary storage for encrypted asset metadata"
- "hasServerUrl checks only INDEXER_URL (no NEXT_PUBLIC fallback) per PUB-05 — Server tab only when explicit server env is set"
- "Removed hardcoded proxyUrl='ws://localhost:4000' from NextSubscriptionProvider — let it use default /api/graphql for non-localhost deployments"
- "No-mode scenario shows warning with setup instructions rather than crashing"
