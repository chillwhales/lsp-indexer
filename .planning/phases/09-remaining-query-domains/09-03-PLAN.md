---
phase: 09-remaining-query-domains
plan: 03
type: execute
wave: 2
depends_on: ['09-01']
files_modified:
  - packages/types/src/nfts.ts
  - packages/types/src/index.ts
  - packages/node/src/documents/nfts.ts
  - packages/node/src/parsers/nfts.ts
  - packages/node/src/services/nfts.ts
  - packages/node/src/keys/nfts.ts
  - packages/node/src/index.ts
  - packages/react/src/hooks/nfts.ts
  - packages/react/src/index.ts
  - packages/next/src/actions/nfts.ts
  - packages/next/src/hooks/nfts.ts
  - packages/next/src/index.ts
  - apps/test/src/app/nfts/page.tsx
autonomous: true
must_haves:
  truths:
    - 'Developer can call useNft({ address, tokenId }) and get typed Nft data'
    - 'Developer can call useNfts({ filter, sort, limit }) and get a paginated NFT list'
    - 'Developer can call useNftsByCollection({ collectionAddress }) as a convenience wrapper'
    - 'Developer can call useInfiniteNfts() and use fetchNextPage for infinite scroll'
    - 'Playground page at /nfts shows Client/Server mode toggle and working results'
  artifacts:
    - path: 'packages/types/src/nfts.ts'
      provides: 'Nft Zod schema + inferred types'
      contains: 'NftSchema'
    - path: 'packages/node/src/services/nfts.ts'
      provides: 'fetchNft, fetchNfts service functions'
      exports: ['fetchNft', 'fetchNfts']
    - path: 'packages/react/src/hooks/nfts.ts'
      provides: 'useNft, useNfts, useNftsByCollection, useInfiniteNfts hooks'
      exports: ['useNft', 'useNfts', 'useNftsByCollection', 'useInfiniteNfts']
    - path: 'packages/next/src/actions/nfts.ts'
      provides: 'getNft, getNfts server actions'
      exports: ['getNft', 'getNfts']
    - path: 'apps/test/src/app/nfts/page.tsx'
      provides: 'NFTs playground page'
      contains: 'useNft'
  key_links:
    - from: 'packages/react/src/hooks/nfts.ts'
      to: 'packages/node/src/services/nfts.ts'
      via: 'fetchNft import'
      pattern: 'fetchNft'
    - from: 'packages/node/src/services/nfts.ts'
      to: 'packages/node/src/client/execute.ts'
      via: 'execute() call'
      pattern: "execute\\(url"
    - from: 'packages/node/src/services/nfts.ts'
      to: 'packages/node/src/parsers/nfts.ts'
      via: 'parseNft import'
      pattern: 'parseNft'
---

<objective>
Build the complete NFT domain vertical slice: types → documents → parsers → services → keys → hooks (react + next) → actions → playground page.

Purpose: Delivers QUERY-03 — developer can use `useNft`, `useNfts`, `useNftsByCollection` for NFT data. `useNftsByCollection` is a convenience wrapper around `useNfts` with a pre-filled collectionAddress filter. Also delivers PAGE-01 infinite scroll for NFTs.

Output: 13 files across 5 packages. NFTs playground page at /nfts with Client/Server mode toggle.
</objective>

<execution_context>
@/home/coder/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@packages/types/src/profiles.ts
@packages/node/src/documents/profiles.ts
@packages/node/src/parsers/profiles.ts
@packages/node/src/services/profiles.ts
@packages/node/src/keys/profiles.ts
@packages/react/src/hooks/profiles.ts
@packages/next/src/actions/profiles.ts
@packages/next/src/hooks/profiles.ts
@apps/test/src/app/profiles/page.tsx
@packages/node/src/graphql/graphql.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types + Node layer (types, documents, parser, service, keys + index exports)</name>
  <files>
    packages/types/src/nfts.ts
    packages/types/src/index.ts
    packages/node/src/documents/nfts.ts
    packages/node/src/parsers/nfts.ts
    packages/node/src/services/nfts.ts
    packages/node/src/keys/nfts.ts
    packages/node/src/index.ts
  </files>
  <action>
    **packages/types/src/nfts.ts:**
    Define Zod schemas for the `nft` Hasura table:
    ```ts
    NftSchema = z.object({
      address: z.string(),          // collection contract address
      tokenId: z.string(),          // token ID (string, may be large)
      tokenIdFormat: z.string().nullable(), // hex, number, etc.
      name: z.string().nullable(),  // LSP4 token name for this specific token
      symbol: z.string().nullable(),
      baseUri: z.string().nullable(), // base URI for metadata
      ownerAddress: z.string().nullable(), // current owner UP address
    })
    ```
    Filter schema `NftFilterSchema`: collectionAddress (string), ownerAddress (string), tokenId (string)
    Sort schema `NftSortFieldSchema`: z.enum(['tokenId', 'name'])
    Hook param schemas: UseNftParams { address, tokenId }, UseNftsParams { filter?, sort?, limit?, offset? }, UseInfiniteNftsParams { filter?, sort?, pageSize? }
    Export all schemas + inferred types.

    **packages/node/src/documents/nfts.ts:**
    `GetNftDocument`: query `nft(where: { address: { _ilike }, token_id: { _ilike } }, limit: 1)`
    Fields: address, token_id, token_id_format, lsp4TokenName { value }, lsp4TokenSymbol { value }, baseUri { value }, owner_address
    `GetNftsDocument`: paginated with $where, $order_by, $limit, $offset + nft_aggregate for count.

    **packages/node/src/parsers/nfts.ts:**
    `parseNft(raw)` → Nft: map snake_case fields to camelCase, handle nullable values.
    `parseNfts(raw[])` → Nft[].

    **packages/node/src/services/nfts.ts:**
    `buildNftWhere(filter?)` → Nft_Bool_Exp:
    - filter.collectionAddress → `{ address: { _ilike: collectionAddress } }`
    - filter.ownerAddress → `{ owner_address: { _ilike: ownerAddress } }`
    - filter.tokenId → `{ token_id: { _ilike: tokenId } }`
    `buildNftOrderBy(sort?)` → Nft_Order_By[] | undefined
    `fetchNft(url, { address, tokenId })` → Nft | null (WHERE address _ilike AND token_id _ilike)
    `FetchNftsResult` interface: { nfts: Nft[]; totalCount: number }
    `fetchNfts(url, params)` → FetchNftsResult

    **packages/node/src/keys/nfts.ts:**
    `nftKeys` factory:
    - `.all` → ['nfts']
    - `.detail(address, tokenId)` → [...all, 'detail', { address, tokenId }]
    - `.byCollection(collectionAddress)` → [...all, 'list', { filter: { collectionAddress } }] — convenience key
    - `.list(filter, sort, limit, offset)` → [...all, 'list', { filter, sort, limit, offset }]
    - `.infinite(filter, sort)` → [...all, 'infinite', { filter, sort }]

    **Index exports:**
    - `packages/types/src/index.ts`: add `export * from './nfts'`
    - `packages/node/src/index.ts`: add all nft exports

  </action>
  <verify>pnpm build in packages/types and packages/node succeeds with exit code 0</verify>
  <done>All Node-layer NFT files created and packages build cleanly</done>
</task>

<task type="auto">
  <name>Task 2: React hooks + Next.js actions and hooks + index exports</name>
  <files>
    packages/react/src/hooks/nfts.ts
    packages/react/src/index.ts
    packages/next/src/actions/nfts.ts
    packages/next/src/hooks/nfts.ts
    packages/next/src/index.ts
  </files>
  <action>
    **packages/react/src/hooks/nfts.ts:**
    const DEFAULT_PAGE_SIZE = 20;
    `useNft({ address, tokenId })`: useQuery wrapping fetchNft, enabled: Boolean(address && tokenId), returns { nft: data ?? null, ...rest }
    `useNfts(params = {})`: useQuery wrapping fetchNfts, returns { nfts, totalCount, ...rest }
    `useNftsByCollection(collectionAddress: string, params = {})`: convenience wrapper — calls useNfts with params.filter merged to include collectionAddress. Signature: `useNftsByCollection(collectionAddress, { sort, limit, offset } = {})`. Internally merges filter: { ...params.filter, collectionAddress }.
    `useInfiniteNfts(params = {})`: useInfiniteQuery with offset pagination, DEFAULT_PAGE_SIZE=20, returns { nfts (flattened), hasNextPage, fetchNextPage, isFetchingNextPage, ...rest }.

    **packages/next/src/actions/nfts.ts:**
    'use server' directive.
    `getNft(address: string, tokenId: string)`: fetchNft(getServerUrl(), { address, tokenId })
    `getNfts(params?)`: fetchNfts(getServerUrl(), params ?? {})

    **packages/next/src/hooks/nfts.ts:**
    Mirror of react hooks but uses getNft/getNfts server actions.
    `useNft`, `useNfts`, `useNftsByCollection`, `useInfiniteNfts` — same signatures, same return shapes.

    **Index exports:**
    - `packages/react/src/index.ts`: export useNft, useNfts, useNftsByCollection, useInfiniteNfts
    - `packages/next/src/index.ts`: export getNft, getNfts + all 4 hooks

  </action>
  <verify>pnpm build in packages/react and packages/next succeeds with exit code 0</verify>
  <done>All NFT hook and action files created, both packages build cleanly</done>
</task>

<task type="auto">
  <name>Task 3: NFTs playground page</name>
  <files>apps/test/src/app/nfts/page.tsx</files>
  <action>
    Create the playground page following the profiles/page.tsx pattern.

    **Domain config:**
    ```ts
    const NFT_FILTERS: FilterFieldConfig[] = [
      { key: 'collectionAddress', label: 'Collection', placeholder: '0x... (collection address)', mono: true },
      { key: 'ownerAddress', label: 'Owner', placeholder: '0x... (owner address)', mono: true },
      { key: 'tokenId', label: 'Token ID', placeholder: 'e.g. 0x0001...', mono: true },
    ];
    const NFT_SORT_OPTIONS: SortOption[] = [
      { value: 'tokenId', label: 'Token ID' },
      { value: 'name', label: 'Name' },
    ];
    ```

    **NftCard component:** Shows collection address (truncated, monospace), tokenId (monospace), name (if available), symbol (badge), ownerAddress (truncated, monospace). Use shadcn Card, Badge.

    **Single NFT tab:** Two inputs — collection address + token ID — then fetch button. Shows NftCard result.

    **Three tabs:** Single (by address + tokenId), List (paginated with filters), Infinite Scroll.

    Client/Server mode toggle matching profiles page pattern exactly. key={mode} on Tabs component.

  </action>
  <verify>pnpm build in apps/test succeeds, or /nfts route renders without TypeScript errors</verify>
  <done>NFTs playground page exists at apps/test/src/app/nfts/page.tsx with Client/Server toggle and three tabs</done>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds in all 4 packages
2. `useNft`, `useNfts`, `useNftsByCollection`, `useInfiniteNfts` exported from both @lsp-indexer/react and @lsp-indexer/next
3. `getNft`, `getNfts` exported from @lsp-indexer/next
4. `nftKeys.byCollection(address)` returns a typed query key array
5. Playground page exists with 'use client', imports from both packages
</verification>

<success_criteria>
Developer can import `useNft` from `@lsp-indexer/react` or `@lsp-indexer/next` and call it with address + tokenId to get typed NFT data. `useNftsByCollection(address)` returns NFTs filtered by collection. QUERY-03 delivered.
</success_criteria>

<output>
After completion, create `.planning/phases/09-remaining-query-domains/09-03-SUMMARY.md` following the summary template. Note any NFT schema field name discrepancies (actual Hasura names may differ from expected).
</output>
