---
phase: 09-remaining-query-domains
plan: 07
type: execute
wave: 2
depends_on: ['09-01']
files_modified:
  - packages/types/src/encrypted-assets.ts
  - packages/types/src/index.ts
  - packages/node/src/documents/encrypted-assets.ts
  - packages/node/src/parsers/encrypted-assets.ts
  - packages/node/src/services/encrypted-assets.ts
  - packages/node/src/keys/encrypted-assets.ts
  - packages/node/src/index.ts
  - packages/react/src/hooks/encrypted-assets.ts
  - packages/react/src/index.ts
  - packages/next/src/actions/encrypted-assets.ts
  - packages/next/src/hooks/encrypted-assets.ts
  - packages/next/src/index.ts
  - apps/test/src/app/encrypted-assets/page.tsx
autonomous: true
must_haves:
  truths:
    - 'Developer can call useEncryptedAsset({ address }) and get typed EncryptedAsset data'
    - 'Developer can call useEncryptedAssets({ filter, sort, limit }) and get a paginated list'
    - 'Developer can call useInfiniteEncryptedAssets() for infinite scroll'
    - 'Playground page at /encrypted-assets shows Client/Server toggle and working results'
  artifacts:
    - path: 'packages/types/src/encrypted-assets.ts'
      provides: 'EncryptedAsset Zod schema + inferred types'
      contains: 'EncryptedAssetSchema'
    - path: 'packages/node/src/services/encrypted-assets.ts'
      provides: 'fetchEncryptedAsset, fetchEncryptedAssets service functions'
      exports: ['fetchEncryptedAsset', 'fetchEncryptedAssets']
    - path: 'packages/react/src/hooks/encrypted-assets.ts'
      provides: 'useEncryptedAsset, useEncryptedAssets, useInfiniteEncryptedAssets hooks'
      exports: ['useEncryptedAsset', 'useEncryptedAssets', 'useInfiniteEncryptedAssets']
    - path: 'packages/next/src/actions/encrypted-assets.ts'
      provides: 'getEncryptedAsset, getEncryptedAssets server actions'
      exports: ['getEncryptedAsset', 'getEncryptedAssets']
    - path: 'apps/test/src/app/encrypted-assets/page.tsx'
      provides: 'Encrypted Assets playground page'
      contains: 'useEncryptedAsset'
  key_links:
    - from: 'packages/react/src/hooks/encrypted-assets.ts'
      to: 'packages/node/src/services/encrypted-assets.ts'
      via: 'fetchEncryptedAsset import'
      pattern: 'fetchEncryptedAsset'
    - from: 'packages/node/src/services/encrypted-assets.ts'
      to: 'packages/node/src/client/execute.ts'
      via: 'execute() call'
      pattern: "execute\\(url"
---

<objective>
Build the LSP29 Encrypted Assets domain vertical slice using the `lsp29_encrypted_asset` Hasura table.

Purpose: Delivers QUERY-07 — developer can use `useEncryptedAsset` and `useEncryptedAssets` for LSP29 encrypted asset data. This is the individual encrypted asset record (not the feed entries). PAGE-01 infinite scroll included.

Output: 13 files across 5 packages. Encrypted Assets playground at /encrypted-assets.
</objective>

<execution_context>
@/home/coder/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@packages/types/src/profiles.ts
@packages/node/src/services/profiles.ts
@packages/node/src/keys/profiles.ts
@packages/react/src/hooks/profiles.ts
@packages/next/src/actions/profiles.ts
@packages/next/src/hooks/profiles.ts
@apps/test/src/app/profiles/page.tsx
@packages/node/src/graphql/graphql.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types + Node layer (types, documents, parser, service, keys + index exports)</name>
  <files>
    packages/types/src/encrypted-assets.ts
    packages/types/src/index.ts
    packages/node/src/documents/encrypted-assets.ts
    packages/node/src/parsers/encrypted-assets.ts
    packages/node/src/services/encrypted-assets.ts
    packages/node/src/keys/encrypted-assets.ts
    packages/node/src/index.ts
  </files>
  <action>
    **packages/types/src/encrypted-assets.ts:**
    Check the `lsp29_encrypted_asset` table schema. Likely fields:
    ```ts
    EncryptedAssetSchema = z.object({
      address: z.string(),              // encrypted asset contract address
      name: z.string().nullable(),      // encrypted asset name
      symbol: z.string().nullable(),
      entryCount: z.number(),           // number of feed entries (aggregate)
      ownerAddress: z.string().nullable(), // owner UP address
    })
    ```
    Adapt field list based on actual schema in graphql.ts — check for `lsp29_encrypted_asset` type definition.
    Filter schema `EncryptedAssetFilterSchema`: name (string), ownerAddress (string)
    Sort: `EncryptedAssetSortFieldSchema`: z.enum(['name', 'entryCount'])
    Hook param schemas: UseEncryptedAssetParams { address }, UseEncryptedAssetsParams { filter?, sort?, limit?, offset? }, UseInfiniteEncryptedAssetsParams { filter?, sort?, pageSize? }
    Export all schemas + inferred types.

    **packages/node/src/documents/encrypted-assets.ts:**
    `GetEncryptedAssetDocument`: query `lsp29_encrypted_asset(where: { address: { _ilike: $address } }, limit: 1)`
    Fields based on actual schema. Include lsp29EncryptedAssetEntries_aggregate for entryCount if available.
    `GetEncryptedAssetsDocument`: paginated list with $where, $order_by, $limit, $offset + aggregate for count.

    **packages/node/src/parsers/encrypted-assets.ts:**
    `parseEncryptedAsset(raw)` → EncryptedAsset
    `parseEncryptedAssets(raw[])` → EncryptedAsset[]

    **packages/node/src/services/encrypted-assets.ts:**
    `buildEncryptedAssetWhere(filter?)` → Lsp29_Encrypted_Asset_Bool_Exp
    `buildEncryptedAssetOrderBy(sort?)` → Lsp29_Encrypted_Asset_Order_By[] | undefined
    `fetchEncryptedAsset(url, { address })` → EncryptedAsset | null
    `FetchEncryptedAssetsResult` interface: { encryptedAssets: EncryptedAsset[]; totalCount: number }
    `fetchEncryptedAssets(url, params)` → FetchEncryptedAssetsResult

    **packages/node/src/keys/encrypted-assets.ts:**
    `encryptedAssetKeys` factory:
    - `.all` → ['encrypted-assets']
    - `.detail(address)` → [...all, 'detail', { address }]
    - `.list(filter, sort, limit, offset)` → [...all, 'list', { filter, sort, limit, offset }]
    - `.infinite(filter, sort)` → [...all, 'infinite', { filter, sort }]

    **Index exports:**
    - `packages/types/src/index.ts`: add `export * from './encrypted-assets'`
    - `packages/node/src/index.ts`: add all encrypted-assets exports

  </action>
  <verify>pnpm build in packages/types and packages/node succeeds with exit code 0</verify>
  <done>Encrypted Assets Node-layer files created, packages build cleanly</done>
</task>

<task type="auto">
  <name>Task 2: React hooks + Next.js actions and hooks + index exports</name>
  <files>
    packages/react/src/hooks/encrypted-assets.ts
    packages/react/src/index.ts
    packages/next/src/actions/encrypted-assets.ts
    packages/next/src/hooks/encrypted-assets.ts
    packages/next/src/index.ts
  </files>
  <action>
    **packages/react/src/hooks/encrypted-assets.ts:**
    const DEFAULT_PAGE_SIZE = 20;
    `useEncryptedAsset({ address })`: useQuery, enabled: Boolean(address), returns { encryptedAsset: data ?? null, ...rest }
    `useEncryptedAssets(params = {})`: useQuery, returns { encryptedAssets, totalCount, ...rest }
    `useInfiniteEncryptedAssets(params = {})`: useInfiniteQuery, returns { encryptedAssets (flattened), hasNextPage, fetchNextPage, isFetchingNextPage, ...rest }

    **packages/next/src/actions/encrypted-assets.ts:**
    'use server' directive.
    `getEncryptedAsset(address: string)`: fetchEncryptedAsset(getServerUrl(), { address })
    `getEncryptedAssets(params?)`: fetchEncryptedAssets(getServerUrl(), params ?? {})

    **packages/next/src/hooks/encrypted-assets.ts:**
    Mirror of react hooks, uses getEncryptedAsset/getEncryptedAssets actions.

    **Index exports:**
    - `packages/react/src/index.ts`: export useEncryptedAsset, useEncryptedAssets, useInfiniteEncryptedAssets
    - `packages/next/src/index.ts`: export getEncryptedAsset, getEncryptedAssets + all 3 hooks

  </action>
  <verify>pnpm build in packages/react and packages/next succeeds with exit code 0</verify>
  <done>Encrypted Assets hooks and actions created and exported, both packages build cleanly</done>
</task>

<task type="auto">
  <name>Task 3: Encrypted Assets playground page</name>
  <files>apps/test/src/app/encrypted-assets/page.tsx</files>
  <action>
    Create playground page at apps/test/src/app/encrypted-assets/page.tsx.

    **Domain config:**
    ```ts
    const ENCRYPTED_ASSET_FILTERS: FilterFieldConfig[] = [
      { key: 'name', label: 'Name', placeholder: 'Search by name...' },
      { key: 'ownerAddress', label: 'Owner', placeholder: '0x... (owner address)', mono: true },
    ];
    const ENCRYPTED_ASSET_SORT_OPTIONS: SortOption[] = [
      { value: 'name', label: 'Name' },
      { value: 'entryCount', label: 'Entry Count' },
    ];
    ```

    **EncryptedAssetCard:** Shows address (mono, truncated), name, symbol (badge), entryCount (badge showing "N entries"), ownerAddress (mono, truncated if available).

    **Three tabs:** Single (by address), List (paginated), Infinite Scroll.
    Client/Server toggle. key={mode} on Tabs.

  </action>
  <verify>pnpm build in apps/test succeeds, /encrypted-assets route renders without TypeScript errors</verify>
  <done>Encrypted Assets playground page at apps/test/src/app/encrypted-assets/page.tsx with three tabs</done>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds in all 4 packages
2. `useEncryptedAsset`, `useEncryptedAssets`, `useInfiniteEncryptedAssets` exported from both packages
3. `getEncryptedAsset`, `getEncryptedAssets` exported from @lsp-indexer/next
4. Playground page exists with Client/Server toggle
</verification>

<success_criteria>
Developer can call `useEncryptedAsset({ address: '0x...' })` and get typed EncryptedAsset data. QUERY-07 delivered.
</success_criteria>

<output>
After completion, create `.planning/phases/09-remaining-query-domains/09-07-SUMMARY.md` following the summary template. Note any LSP29 schema field names that differ from expected.
</output>
