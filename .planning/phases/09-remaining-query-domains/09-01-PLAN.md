---
phase: 09-remaining-query-domains
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/node/schema.graphql
  - packages/node/src/graphql/graphql.ts
  - packages/node/src/graphql/gql.ts
autonomous: true
must_haves:
  truths:
    - 'Hasura schema is freshly introspected and written to packages/node/schema.graphql'
    - 'TypeScript codegen types are regenerated in packages/node/src/graphql/graphql.ts'
    - 'packages/node builds successfully with pnpm build after codegen'
  artifacts:
    - path: 'packages/node/schema.graphql'
      provides: 'Hasura GraphQL schema (introspected)'
      contains: 'type query_root'
    - path: 'packages/node/src/graphql/graphql.ts'
      provides: 'Codegen-generated TypeScript types'
      contains: 'Digital_Asset_Bool_Exp'
  key_links:
    - from: 'packages/node/src/documents/{domain}.ts'
      to: 'packages/node/src/graphql/graphql.ts'
      via: 'graphql() tagged template'
      pattern: "graphql\\(`"
---

<objective>
Run schema introspection and codegen once upfront so all 10 domain plans (Wave 2) start from a consistent generated state.

Purpose: All domain plans depend on up-to-date Hasura types (Digital_Asset_Bool_Exp, Nft_Bool_Exp, etc.) being available before they write documents and services. Running codegen once here prevents redundant schema dumps across 10 parallel plans.

Output: Updated schema.graphql + regenerated graphql.ts/gql.ts committed as the baseline for all Wave 2 domain plans.
</objective>

<execution_context>
@/home/coder/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/node/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run schema:dump to refresh Hasura schema</name>
  <files>packages/node/schema.graphql</files>
  <action>
    From the packages/node directory, run `pnpm schema:dump` to introspect the Hasura endpoint (http://192.168.0.21:18716/v1/graphql) and regenerate schema.graphql.

    If the command is unavailable or the Hasura endpoint is unreachable, check packages/node/package.json for the exact script name — it may be `schema:dump`, `generate:schema`, or similar. The schema.graphql should be ~25k lines covering all Hasura tables.

    After the dump completes, verify schema.graphql exists and contains the expected tables for Phase 9 domains by checking for these type names:
    - `digital_asset` (or `digital_asset_bool_exp`)
    - `nft_bool_exp`
    - `owned_asset_bool_exp`
    - `follower_bool_exp`
    - `lsp4_creator_bool_exp`
    - `lsp29_encrypted_asset_bool_exp`
    - `lsp29_encrypted_asset_entry_order_by` (for feed domain)
    - `data_changed_bool_exp`
    - `universal_receiver_bool_exp`

    If any are missing, note this in the SUMMARY.md but do not block — the domain plans will work with whatever the schema contains.

  </action>
  <verify>grep -c "bool_exp" packages/node/schema.graphql (expect > 50 matches)</verify>
  <done>schema.graphql exists, is non-empty, and contains bool_exp types for the expected domains</done>
</task>

<task type="auto">
  <name>Task 2: Run codegen to regenerate TypeScript types</name>
  <files>
    packages/node/src/graphql/graphql.ts
    packages/node/src/graphql/gql.ts
  </files>
  <action>
    From the packages/node directory, run `pnpm codegen` (check package.json for exact script name — may be `codegen`, `generate`, or `graphql:codegen`).

    Codegen reads the updated schema.graphql and all document files in src/documents/ to regenerate:
    - `src/graphql/graphql.ts` — Hasura filter/order types used by service layers
    - `src/graphql/gql.ts` — TypedDocumentString registry

    After codegen completes, verify the following types exist in src/graphql/graphql.ts:
    - `Digital_Asset_Bool_Exp`
    - `Nft_Bool_Exp`
    - `Owned_Asset_Bool_Exp`
    - `Owned_Token_Bool_Exp`
    - `Follower_Bool_Exp`
    - `Lsp4_Creator_Bool_Exp`
    - `Lsp29_Encrypted_Asset_Bool_Exp`
    - `Lsp29_Encrypted_Asset_Entry_Order_By`
    - `Data_Changed_Bool_Exp`
    - `Universal_Receiver_Bool_Exp`

    These types are what Wave 2 domain plans import in their service files (e.g., `import type { Digital_Asset_Bool_Exp } from '../graphql/graphql'`).

  </action>
  <verify>grep "Digital_Asset_Bool_Exp\|Nft_Bool_Exp\|Follower_Bool_Exp" packages/node/src/graphql/graphql.ts | wc -l (expect > 5 matches)</verify>
  <done>graphql.ts contains all 10 domain Hasura bool_exp and order_by types needed by Wave 2 plans</done>
</task>

<task type="auto">
  <name>Task 3: Verify packages/node builds cleanly after codegen</name>
  <files>packages/node/src/graphql/graphql.ts</files>
  <action>
    Run `pnpm build` in packages/node to confirm the codegen output is syntactically valid and the package builds without TypeScript errors.

    This is the "does the baseline compile?" check before all 10 domain plans begin. If the build fails, diagnose and fix before proceeding — likely causes:
    1. Codegen wrote invalid TypeScript (schema introspection mismatch)
    2. An existing document in src/documents/ references a field that no longer exists

    Do NOT proceed to domain plans if this build fails — fix it here.

    After a successful build, commit the updated schema.graphql and graphql.ts/gql.ts:
    ```
    git add packages/node/schema.graphql packages/node/src/graphql/graphql.ts packages/node/src/graphql/gql.ts
    git commit -m "chore(09): refresh schema and regenerate codegen types"
    ```

  </action>
  <verify>pnpm build succeeds in packages/node with exit code 0, dist/ output exists</verify>
  <done>packages/node builds cleanly, schema + codegen types committed — Wave 2 domain plans can start</done>
</task>

</tasks>

<verification>
1. `packages/node/schema.graphql` exists and contains bool_exp types for all 10 Phase 9 domains
2. `packages/node/src/graphql/graphql.ts` contains `Digital_Asset_Bool_Exp`, `Nft_Bool_Exp`, `Follower_Bool_Exp`, `Data_Changed_Bool_Exp`, `Universal_Receiver_Bool_Exp`
3. `pnpm build` in packages/node exits with code 0
4. Updated files committed to git
</verification>

<success_criteria>
All 10 Wave 2 domain plans can start immediately after this plan completes — they have consistent generated types available in packages/node/src/graphql/graphql.ts to import in their service files.
</success_criteria>

<output>
After completion, create `.planning/phases/09-remaining-query-domains/09-01-SUMMARY.md` following the summary template. Key fields: phase=09-remaining-query-domains, plan=01, subsystem=codegen. Note any schema changes or missing domain tables discovered during schema:dump.
</output>
