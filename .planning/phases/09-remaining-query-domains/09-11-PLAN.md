---
phase: 09-remaining-query-domains
plan: 11
type: execute
wave: 3
depends_on: ['09-02', '09-03', '09-04', '09-05', '09-06', '09-07', '09-08', '09-09', '09-10']
files_modified:
  - packages/types/src/index.ts
  - packages/node/src/index.ts
  - packages/react/src/index.ts
  - packages/next/src/index.ts
  - apps/test/src/app/page.tsx
  - apps/test/src/app/layout.tsx
autonomous: true
must_haves:
  truths:
    - 'All 4 package index.ts files export all 9 new domains without conflicts or missing exports'
    - 'Test app home page lists all 10 domain playground pages with working links'
    - 'Full repo pnpm build succeeds with zero TypeScript errors across all packages'
    - 'Nav routes for all 9 new domains are set to available:true so they appear in the nav'
  artifacts:
    - path: 'packages/types/src/index.ts'
      provides: 'All 10 domain type exports'
      contains: "export * from './digital-assets'"
    - path: 'packages/node/src/index.ts'
      provides: 'All 10 domain service/parser/key/document exports'
      contains: 'fetchDigitalAsset'
    - path: 'packages/react/src/index.ts'
      provides: 'All 10 domain hook exports'
      contains: 'useDigitalAsset'
    - path: 'packages/next/src/index.ts'
      provides: 'All 10 domain action + hook exports'
      contains: 'getDigitalAsset'
    - path: 'apps/test/src/app/page.tsx'
      provides: 'Test app home page with links to all 10 domain playgrounds'
      contains: 'digital-assets'
  key_links:
    - from: 'apps/test/src/app/page.tsx'
      to: 'apps/test/src/app/profiles/page.tsx'
      via: 'Next.js Link href'
      pattern: 'href.*profiles'
    - from: 'apps/test/src/app/page.tsx'
      to: 'apps/test/src/app/digital-assets/page.tsx'
      via: 'Next.js Link href'
      pattern: 'href.*digital-assets'
---

<objective>
Integration plan: Audit all 4 package index.ts files for missing exports, update test app nav and home page, and validate the full build.

Purpose: Catches any exports missed by the 9 parallel domain plans, ensures the test app home page lists all 10 domains, and validates the complete Phase 9 build. This is the "did everything wire up?" verification before Phase 9 is declared complete.

Output: Audited + fixed index.ts files for all 4 packages, updated test app home page with all 10 domain links, full build validation passing.
</objective>

<execution_context>
@/home/coder/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@packages/types/src/index.ts
@packages/node/src/index.ts
@packages/react/src/index.ts
@packages/next/src/index.ts
@apps/test/src/app/page.tsx
@apps/test/src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all 4 package index.ts files + add /feed nav route</name>
  <files>
    packages/types/src/index.ts
    packages/node/src/index.ts
    packages/react/src/index.ts
    packages/next/src/index.ts
    apps/test/src/app/layout.tsx
  </files>
  <action>
    **Audit packages/types/src/index.ts:**
    Verify it exports from all 10 domains (profiles + 9 new):
    - `export * from './profiles'` (already exists)
    - `export * from './digital-assets'`
    - `export * from './nfts'`
    - `export * from './owned-assets'`
    - `export * from './social'`
    - `export * from './creators'`
    - `export * from './encrypted-assets'`
    - `export * from './encrypted-feed'`
    - `export * from './data-changed'`
    - `export * from './universal-receiver'`
    Add any missing exports.

    **Audit packages/node/src/index.ts:**
    Verify it exports from all 10 domains. Each domain should export: documents, parsers, services (fetch functions + result interfaces), keys.
    Check for: fetchDigitalAsset, fetchDigitalAssets, fetchNft, fetchNfts, fetchOwnedAssets, fetchOwnedTokens, fetchFollowers, fetchFollowing, fetchFollowCount, fetchCreatorAddresses, fetchEncryptedAsset, fetchEncryptedAssets, fetchEncryptedAssetFeed, fetchDataChangedEvents, fetchUniversalReceiverEvents.
    Add any missing exports.

    **Audit packages/react/src/index.ts:**
    Verify it exports all hooks from all 10 domains. Check for: useDigitalAsset, useDigitalAssets, useInfiniteDigitalAssets, useNft, useNfts, useNftsByCollection, useInfiniteNfts, useOwnedAssets, useOwnedTokens, useInfiniteOwnedAssets, useInfiniteOwnedTokens, useFollowers, useFollowing, useFollowCount, useInfiniteFollowers, useInfiniteFollowing, useCreatorAddresses, useInfiniteCreatorAddresses, useEncryptedAsset, useEncryptedAssets, useInfiniteEncryptedAssets, useEncryptedAssetFeed, useInfiniteEncryptedAssetFeed, useDataChangedEvents, useInfiniteDataChangedEvents, useUniversalReceiverEvents, useInfiniteUniversalReceiverEvents.
    Note: useFollowCount has NO infinite variant.
    Add any missing exports.

    **Audit packages/next/src/index.ts:**
    Same check for @lsp-indexer/next — both server actions (getX) and hooks (useX) for all 10 domains.
    Add any missing exports.

    **Check for export conflicts:** If any name appears in multiple domain files (e.g., SortDirection from profiles.ts), ensure it's exported only once. Re-exports across domains can cause conflicts. If SortDirectionSchema is in multiple files, keep it only in profiles.ts and import from there in other type files.

    **Update nav to use long route names:**
    The nav component is at `apps/test/src/components/nav.tsx`. It currently has 8 placeholder routes with short names. Update them to use the long routes that match the page directories the domain plans created:
    - `{ href: '/assets', ... }` → `{ href: '/digital-assets', ... }`
    - `{ href: '/owned', ... }` → `{ href: '/owned-assets', ... }`
    - `{ href: '/follows', ... }` → keep as `/follows` (matches plan 09-05's page path)
    - `{ href: '/encrypted', ... }` → `{ href: '/encrypted-assets', ... }`
    - `{ href: '/events', ... }` → replace with two entries: `/data-changed` (Data Changed) and `/universal-receiver` (Universal Receiver)
    - `{ href: '/stats', ... }` → remove entirely (profile stats removed from Phase 9)
    - `/nfts`, `/creators` — already correct, no change needed
    Set `available: true` on all 9 domain entries.
    Add new entry: `{ href: '/feed', label: 'Encrypted Feed', icon: <appropriate icon>, available: true }`.
    The nav file is `apps/test/src/components/nav.tsx` (not layout.tsx).

  </action>
  <verify>
    grep -c "export" packages/types/src/index.ts (expect >= 10 domain export lines)
    grep -c "export" packages/react/src/index.ts (expect >= 24 hook exports)
    grep -r "available.*true\|/feed" apps/test/src/components/nav.tsx
  </verify>
  <done>All 4 package index.ts files audited and have complete exports for all 10 domains. Nav updated with long route names, /feed route added, all 9 new domain routes set to available:true. /stats entry removed.</done>
</task>

<task type="auto">
  <name>Task 2: Update test app home page + run full build validation</name>
  <files>
    apps/test/src/app/page.tsx
  </files>
  <action>
    **Update apps/test/src/app/page.tsx:**
    The current home page likely shows a minimal placeholder. Update it to list all 11 domain playground pages with descriptions and links.

    Create a grid of domain cards. Group them logically:

    **Domain Cards (group by category):**
    - Profiles: Universal Profiles (/profiles)
    - Assets & Tokens: Digital Assets (/digital-assets), NFTs (/nfts), Owned Assets (/owned-assets)
    - Social: Follows (/follows), Creators (/creators)
    - LSP29 Encrypted: Encrypted Assets (/encrypted-assets), Encrypted Feed (/feed)
    - Events: Data Changed Events (/data-changed), Universal Receiver Events (/universal-receiver)

    Each card should show:
    - Domain name (bold)
    - Brief description (1 sentence)
    - Hook names it provides (small badges or code snippets)
    - Link button: "Open Playground →"

    Use shadcn Card, Badge, Button components. Link via Next.js `<Link>` component.

    **Full build validation:**
    After updating the home page, run the full build across all packages and the test app:
    ```bash
    # Option A: from repo root (if turborepo build script exists)
    pnpm build

    # Option B: per-package build
    cd packages/types && pnpm build
    cd packages/node && pnpm build
    cd packages/react && pnpm build
    cd packages/next && pnpm build
    cd apps/test && pnpm build
    ```

    If build fails:
    - TypeScript errors in packages: fix missing exports, wrong types, import paths
    - TypeScript errors in apps/test: fix incorrect hook import names, prop types
    - Build errors: check for missing files (a domain plan may not have created all 13 files)

    After a clean build, commit all changes:
    ```bash
    git add packages/*/src/index.ts apps/test/src/app/page.tsx apps/test/src/app/layout.tsx
    git commit -m "feat(09): complete phase 9 - all 10 remaining query domains + integration"
    ```

  </action>
  <verify>
    pnpm build succeeds with exit code 0 for all packages and apps/test
    apps/test/src/app/page.tsx contains links to all 10 domain pages (/profiles through /universal-receiver and /feed)
  </verify>
  <done>Test app home page lists all 10 domain playgrounds. Full build succeeds with zero TypeScript errors. Phase 9 build validated.</done>
</task>

</tasks>

<verification>
1. All 4 package index.ts files export from all 10 domains
2. No duplicate export conflicts (especially SortDirection, SortDirectionSchema which may exist in multiple type files)
3. packages/next/src/index.ts exports both server actions (getX) and hooks (useX) for all 10 domains
4. Test app home page lists all 10 domain pages
5. Nav uses long route names (/digital-assets, /owned-assets, /follows, /encrypted-assets, /data-changed, /universal-receiver) with available:true. /stats entry removed. /feed entry added.
6. Full pnpm build exits with code 0 — zero TypeScript errors across all packages and test app
</verification>

<success_criteria>
Phase 9 complete: All 10 query domains accessible via type-safe hooks from @lsp-indexer/react and @lsp-indexer/next. Full build validates the entire system works together. Developer can navigate to any domain playground page and test hooks in Client or Server mode. Requirements QUERY-02 through QUERY-10 and PAGE-01 all delivered.
</success_criteria>

<output>
After completion, create `.planning/phases/09-remaining-query-domains/09-11-SUMMARY.md` following the summary template. Include a complete list of all exports added to each package's index.ts and the final build output. Mark Phase 9 as complete in STATE.md.
</output>
