---
phase: 09-remaining-query-domains
plan: 12
type: execute
wave: 3
depends_on:
  ['09-02', '09-03', '09-04', '09-05', '09-06', '09-07', '09-08', '09-09', '09-10', '09-11']
files_modified:
  - packages/types/src/index.ts
  - packages/node/src/index.ts
  - packages/react/src/index.ts
  - packages/next/src/index.ts
  - apps/test/src/app/page.tsx
  - apps/test/src/app/layout.tsx
autonomous: true
must_haves:
  truths:
    - 'All 4 package index.ts files export all 10 new domains without conflicts or missing exports'
    - 'Test app home page lists all 11 domain playground pages with working links'
    - 'Full repo pnpm build succeeds with zero TypeScript errors across all packages'
    - 'Nav routes for all 10 domains are set to available:true so they appear in the nav'
  artifacts:
    - path: 'packages/types/src/index.ts'
      provides: 'All 11 domain type exports'
      contains: "export * from './digital-assets'"
    - path: 'packages/node/src/index.ts'
      provides: 'All 11 domain service/parser/key/document exports'
      contains: 'fetchDigitalAsset'
    - path: 'packages/react/src/index.ts'
      provides: 'All 11 domain hook exports'
      contains: 'useDigitalAsset'
    - path: 'packages/next/src/index.ts'
      provides: 'All 11 domain action + hook exports'
      contains: 'getDigitalAsset'
    - path: 'apps/test/src/app/page.tsx'
      provides: 'Test app home page with links to all 11 domain playgrounds'
      contains: 'digital-assets'
  key_links:
    - from: 'apps/test/src/app/page.tsx'
      to: 'apps/test/src/app/profiles/page.tsx'
      via: 'Next.js Link href'
      pattern: 'href.*profiles'
    - from: 'apps/test/src/app/page.tsx'
      to: 'apps/test/src/app/digital-assets/page.tsx'
      via: 'Next.js Link href'
      pattern: 'href.*digital-assets'
---

<objective>
Integration plan: Audit all 4 package index.ts files for missing exports, update test app nav and home page, and validate the full build.

Purpose: Catches any exports missed by the 10 parallel domain plans, ensures the test app home page lists all 11 domains, and validates the complete Phase 9 build. This is the "did everything wire up?" verification before Phase 9 is declared complete.

Output: Audited + fixed index.ts files for all 4 packages, updated test app home page with all 11 domain links, full build validation passing.
</objective>

<execution_context>
@/home/coder/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@packages/types/src/index.ts
@packages/node/src/index.ts
@packages/react/src/index.ts
@packages/next/src/index.ts
@apps/test/src/app/page.tsx
@apps/test/src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all 4 package index.ts files + add /feed nav route</name>
  <files>
    packages/types/src/index.ts
    packages/node/src/index.ts
    packages/react/src/index.ts
    packages/next/src/index.ts
    apps/test/src/app/layout.tsx
  </files>
  <action>
    **Audit packages/types/src/index.ts:**
    Verify it exports from all 11 domains (profiles + 10 new):
    - `export * from './profiles'` (already exists)
    - `export * from './digital-assets'`
    - `export * from './nfts'`
    - `export * from './owned-assets'`
    - `export * from './social'`
    - `export * from './creators'`
    - `export * from './encrypted-assets'`
    - `export * from './encrypted-feed'`
    - `export * from './data-changed'`
    - `export * from './universal-receiver'`
    - `export * from './profile-stats'`
    Add any missing exports.

    **Audit packages/node/src/index.ts:**
    Verify it exports from all 11 domains. Each domain should export: documents, parsers, services (fetch functions + result interfaces), keys.
    Check for: fetchDigitalAsset, fetchDigitalAssets, fetchNft, fetchNfts, fetchOwnedAssets, fetchOwnedTokens, fetchFollowers, fetchFollowing, fetchFollowCount, fetchCreatorAddresses, fetchEncryptedAsset, fetchEncryptedAssets, fetchEncryptedAssetFeed, fetchDataChangedEvents, fetchUniversalReceiverEvents, fetchProfileStats.
    Add any missing exports.

    **Audit packages/react/src/index.ts:**
    Verify it exports all hooks from all 11 domains. Check for: useDigitalAsset, useDigitalAssets, useDigitalAssetSearch, useInfiniteDigitalAssets, useNft, useNfts, useNftsByCollection, useInfiniteNfts, useOwnedAssets, useOwnedTokens, useInfiniteOwnedAssets, useInfiniteOwnedTokens, useFollowers, useFollowing, useFollowCount, useInfiniteFollowers, useInfiniteFollowing, useCreatorAddresses, useInfiniteCreatorAddresses, useEncryptedAsset, useEncryptedAssets, useInfiniteEncryptedAssets, useEncryptedAssetFeed, useInfiniteEncryptedAssetFeed, useDataChangedEvents, useInfiniteDataChangedEvents, useUniversalReceiverEvents, useInfiniteUniversalReceiverEvents, useProfileStats.
    Note: useProfileStats has NO infinite variant. useFollowCount has NO infinite variant.
    Add any missing exports.

    **Audit packages/next/src/index.ts:**
    Same check for @lsp-indexer/next — both server actions (getX) and hooks (useX) for all 11 domains.
    Add any missing exports.

    **Check for export conflicts:** If any name appears in multiple domain files (e.g., SortDirection from profiles.ts), ensure it's exported only once. Re-exports across domains can cause conflicts. If SortDirectionSchema is in multiple files, keep it only in profiles.ts and import from there in other type files.

    **Add /feed nav route:**
    Research found "Nav has 9 existing placeholder routes (available: false) — integration plan flips them all to true and adds /feed for Domain 7."
    Check apps/test/src/app/layout.tsx (or wherever the nav/sidebar is defined). Find the nav links array and:
    1. Set available: true for all 10 domain routes: /digital-assets, /nfts, /owned-assets, /social, /creators, /encrypted-assets, /universal-receiver, /data-changed, /profile-stats
    2. Add the new /feed route: { href: '/feed', label: 'Encrypted Feed', ... }
    If the nav is not in layout.tsx, check for a navigation component (sidebar.tsx, nav.tsx, or similar in apps/test/src/components/).

  </action>
  <verify>
    grep -c "export" packages/types/src/index.ts (expect >= 11 domain export lines)
    grep -c "export" packages/react/src/index.ts (expect >= 25 hook exports)
    grep "available.*true\|/feed" apps/test/src/app/layout.tsx (or nav file)
  </verify>
  <done>All 4 package index.ts files audited and have complete exports for all 11 domains. Nav updated with /feed route and all 10 domain routes set to available:true.</done>
</task>

<task type="auto">
  <name>Task 2: Update test app home page + run full build validation</name>
  <files>
    apps/test/src/app/page.tsx
  </files>
  <action>
    **Update apps/test/src/app/page.tsx:**
    The current home page likely shows a minimal placeholder. Update it to list all 11 domain playground pages with descriptions and links.

    Create a grid of domain cards. Group them logically:

    **Domain Cards (group by category):**
    - Identity & Profiles: Universal Profiles (/profiles), Profile Stats (/profile-stats)
    - Assets & Tokens: Digital Assets (/digital-assets), NFTs (/nfts), Owned Assets (/owned-assets)
    - Social: Social/Follows (/social), Creators (/creators)
    - LSP29 Encrypted: Encrypted Assets (/encrypted-assets), Encrypted Feed (/feed)
    - Events: Data Changed Events (/data-changed), Universal Receiver Events (/universal-receiver)

    Each card should show:
    - Domain name (bold)
    - Brief description (1 sentence)
    - Hook names it provides (small badges or code snippets)
    - Link button: "Open Playground →"

    Use shadcn Card, Badge, Button components. Link via Next.js `<Link>` component.

    **Full build validation:**
    After updating the home page, run the full build across all packages and the test app:
    ```bash
    # Option A: from repo root (if turborepo build script exists)
    pnpm build

    # Option B: per-package build
    cd packages/types && pnpm build
    cd packages/node && pnpm build
    cd packages/react && pnpm build
    cd packages/next && pnpm build
    cd apps/test && pnpm build
    ```

    If build fails:
    - TypeScript errors in packages: fix missing exports, wrong types, import paths
    - TypeScript errors in apps/test: fix incorrect hook import names, prop types
    - Build errors: check for missing files (a domain plan may not have created all 13 files)

    After a clean build, commit all changes:
    ```bash
    git add packages/*/src/index.ts apps/test/src/app/page.tsx apps/test/src/app/layout.tsx
    git commit -m "feat(09): complete phase 9 - all 10 remaining query domains + integration"
    ```

  </action>
  <verify>
    pnpm build succeeds with exit code 0 for all packages and apps/test
    apps/test/src/app/page.tsx contains links to all 11 domain pages (/profiles through /profile-stats and /feed)
  </verify>
  <done>Test app home page lists all 11 domain playgrounds. Full build succeeds with zero TypeScript errors. Phase 9 build validated.</done>
</task>

</tasks>

<verification>
1. All 4 package index.ts files export from all 11 domains
2. No duplicate export conflicts (especially SortDirection, SortDirectionSchema which may exist in multiple type files)
3. packages/next/src/index.ts exports both server actions (getX) and hooks (useX) for all 11 domains
4. Test app home page lists all 11 domain pages
5. Nav shows all 10 new domain routes as active (available: true) + /feed route added
6. Full pnpm build exits with code 0 — zero TypeScript errors across all packages and test app
</verification>

<success_criteria>
Phase 9 complete: All 11 query domains accessible via type-safe hooks from @lsp-indexer/react and @lsp-indexer/next. Full build validates the entire system works together. Developer can navigate to any domain playground page and test hooks in Client or Server mode. Requirements QUERY-02 through QUERY-11 and PAGE-01 all delivered.
</success_criteria>

<output>
After completion, create `.planning/phases/09-remaining-query-domains/09-12-SUMMARY.md` following the summary template. Include a complete list of all exports added to each package's index.ts and the final build output. Mark Phase 9 as complete in STATE.md.
</output>
