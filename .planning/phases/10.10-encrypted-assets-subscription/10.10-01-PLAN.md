---
phase: 10.10-encrypted-assets-subscription
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/react/src/subscriptions/encrypted-assets.ts
  - packages/react/src/index.ts
  - apps/test/src/app/subscriptions/page.tsx
autonomous: true
requirements: [SUB-02, SUB-03]

must_haves:
  truths:
    - 'Developer can call useEncryptedAssetSubscription() and get { data, isConnected, isSubscribed, error }'
    - 'All type params inferred — zero explicit type arguments at call sites'
    - 'Encrypted asset subscription data appears in the playground page'
    - 'Cache invalidation works when invalidate: true'
  artifacts:
    - path: 'packages/react/src/subscriptions/encrypted-assets.ts'
      provides: 'useEncryptedAssetSubscription hook'
      contains: 'useEncryptedAssetSubscription'
    - path: 'apps/test/src/app/subscriptions/page.tsx'
      provides: 'Encrypted assets subscription demo section'
      contains: 'useEncryptedAssetSubscription'
  key_links:
    - from: 'packages/react/src/subscriptions/encrypted-assets.ts'
      to: '@lsp-indexer/node'
      via: 'buildEncryptedAssetWhere + encryptedAssetKeys + parseEncryptedAssets + EncryptedAssetSubscriptionDocument'
      pattern: 'buildEncryptedAssetWhere|encryptedAssetKeys|parseEncryptedAssets|EncryptedAssetSubscriptionDocument'
    - from: 'packages/react/src/subscriptions/encrypted-assets.ts'
      to: 'packages/react/src/subscriptions/use-subscription.ts'
      via: 'useSubscription call'
      pattern: 'useSubscription'
---

<objective>
Create the `useEncryptedAssetSubscription` hook and add a working encrypted assets subscription demo to the playground page.

Purpose: Domain subscription hook for encrypted assets (LSP29). **This is the PRIMARY use case from CONTEXT.md — "global feed of new LSP29 encrypted assets."** Follows the exact pattern established by profiles (10.2).

Output: `useEncryptedAssetSubscription` exported from `@lsp-indexer/react`, encrypted assets subscription demo visible in the test app.
</objective>

<execution_context>
@/home/coder/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-subscriptions/10-CONTEXT.md
@.planning/phases/10.1-subscription-foundation/10.1-01-SUMMARY.md
@.planning/phases/10.2-profiles-subscription/10.2-01-SUMMARY.md
@packages/node/src/services/encrypted-assets.ts
@packages/node/src/keys/encrypted-assets.ts
@packages/node/src/documents/encrypted-assets.ts
@packages/node/src/parsers/encrypted-assets.ts
@packages/react/src/index.ts
@apps/test/src/app/subscriptions/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useEncryptedAssetSubscription hook + playground demo + export</name>
  <files>
    packages/react/src/subscriptions/encrypted-assets.ts
    packages/react/src/index.ts
    apps/test/src/app/subscriptions/page.tsx
  </files>
  <action>
    **Create `packages/react/src/subscriptions/encrypted-assets.ts`:**

    Follow the exact pattern from the 10.2 profiles SUMMARY:
    - Import `EncryptedAsset`, `EncryptedAssetFilter` from `@lsp-indexer/types`
    - Import `buildEncryptedAssetWhere`, `encryptedAssetKeys`, `parseEncryptedAssets`, `EncryptedAssetSubscriptionDocument` from `@lsp-indexer/node`
    - `extract: (result) => result.lsp29_encrypted_asset` — Hasura table extraction
    - `parser: (raw) => parseEncryptedAssets(raw)`
    - `order_by: undefined` — entity domain, Hasura default sort
    - Params interface: `UseEncryptedAssetSubscriptionParams` with `{ filter?, limit?, enabled?, invalidate?, onData?, onReconnect? }`
    - `useQueryClient()` called unconditionally (Rules of Hooks), only passed when `invalidate: true`

    **Update `packages/react/src/index.ts`:**

    Add export:
    ```typescript
    export * from './subscriptions/encrypted-assets';
    ```

    **Add encrypted assets demo to playground page:**

    Update `apps/test/src/app/subscriptions/page.tsx` — add an encrypted assets subscription section showing:
    1. Connection/subscription status badges
    2. Data count and list of encrypted asset addresses/names
    3. Error display
    4. Filter input if relevant
    5. Limit selector
    6. Note: This is the primary use case — "global feed of new LSP29 encrypted assets"

    **Build validation:**
    ```bash
    pnpm build
    ```

    Commit: `feat(10.10-encrypted-assets): add useEncryptedAssetSubscription hook + playground demo`

  </action>
  <verify>
    `pnpm build` — all packages + test app build.
    `grep "useEncryptedAssetSubscription" packages/react/dist/index.js` — exported.
    `grep "useEncryptedAssetSubscription" apps/test/src/app/subscriptions/page.tsx` — used in playground.
  </verify>
  <done>useEncryptedAssetSubscription hook exported from @lsp-indexer/react with full type inference. Encrypted assets (LSP29) subscription demo visible in test app playground at /subscriptions — primary CONTEXT.md use case.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes for all packages + test app
- `useEncryptedAssetSubscription` is importable from `@lsp-indexer/react`
- Hook accepts { filter?, limit?, enabled?, invalidate?, onData?, onReconnect? }
- Hook returns { data: EncryptedAsset[] | null, isConnected, isSubscribed, error: Error | null }
- All types inferred — no explicit type arguments
- Playground shows encrypted assets subscription demo
</verification>

<success_criteria>

1. useEncryptedAssetSubscription created with type-safe SubscriptionConfig (extract + parser, zero assertions)
2. Hook exported from @lsp-indexer/react
3. Encrypted assets subscription demo works in playground
4. All packages build with zero type errors
   </success_criteria>

<output>
After completion, create `.planning/phases/10.10-encrypted-assets-subscription/10.10-01-SUMMARY.md`
</output>
