---
phase: 16-v1.1-verification-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-first-vertical-slice/08-VERIFICATION.md
  - .planning/phases/09.1-digital-assets/09.1-VERIFICATION.md
autonomous: true
requirements: [QUERY-01, DX-01, DX-02, QUERY-02]

must_haves:
  truths:
    - "Phase 08 has a VERIFICATION.md with status: passed"
    - "QUERY-01 is marked satisfied with file path evidence for useProfile, useProfiles, useInfiniteProfiles"
    - "DX-01 is marked satisfied with evidence that all domain types export clean camelCase from @lsp-indexer/types"
    - "DX-02 is marked satisfied with evidence that profileKeys cache factory exists and is exported"
    - "Phase 09.1 has a VERIFICATION.md with status: passed"
    - "QUERY-02 is marked satisfied with evidence for useDigitalAsset, useDigitalAssets, useInfiniteDigitalAssets"
  artifacts:
    - path: ".planning/phases/08-first-vertical-slice/08-VERIFICATION.md"
      provides: "Verification evidence for QUERY-01, DX-01, DX-02"
      contains: "status: passed"
    - path: ".planning/phases/09.1-digital-assets/09.1-VERIFICATION.md"
      provides: "Verification evidence for QUERY-02"
      contains: "status: passed"
  key_links:
    - from: "08-VERIFICATION.md"
      to: "packages/react/src/hooks/profiles/"
      via: "Observable truth citing file paths + line numbers"
      pattern: "useProfile.*useProfiles.*useInfiniteProfiles"
    - from: "08-VERIFICATION.md"
      to: "packages/types/src/profiles.ts"
      via: "Artifact verification with export evidence"
      pattern: "Profile.*ProfileFilter.*ProfileSort"
    - from: "09.1-VERIFICATION.md"
      to: "packages/react/src/hooks/digital-assets/"
      via: "Observable truth citing file paths + line numbers"
      pattern: "useDigitalAsset.*useDigitalAssets.*useInfiniteDigitalAssets"
---

<objective>
Create VERIFICATION.md files for Phase 08 (First Vertical Slice — Profiles) and Phase 09.1 (Digital Assets), confirming that requirements QUERY-01, DX-01, DX-02, and QUERY-02 are satisfied by existing code artifacts.

Purpose: Close 4 of 7 verification gaps identified by the v1.1 milestone audit. These phases have complete implementations but no formal verification evidence.
Output: Two VERIFICATION.md files with structured evidence (observable truths, required artifacts, key links, requirements coverage).
</objective>

<execution_context>
@/home/coder/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.1-MILESTONE-AUDIT.md

Use this VERIFICATION.md as the template for format and thoroughness:
@.planning/phases/09.5-social-follows/09.5-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 08 VERIFICATION.md (QUERY-01, DX-01, DX-02)</name>
  <files>.planning/phases/08-first-vertical-slice/08-VERIFICATION.md</files>
  <action>
Create a VERIFICATION.md for Phase 08 (First Vertical Slice — Profiles) that covers three requirements: QUERY-01, DX-01, DX-02.

**Follow the exact format of `.planning/phases/09.5-social-follows/09.5-VERIFICATION.md`** — same frontmatter structure, same table formats, same section headings.

**Frontmatter:**
```yaml
---
phase: 08-first-vertical-slice
verified: [current ISO timestamp]
status: passed
score: N/N must-haves verified
---
```

**Phase Goal:** "Developer can use complete Universal Profile hooks in a real app — validating the full document → parser → service → hook architecture end-to-end."

**For QUERY-01** — verify these artifacts exist and are wired:
1. Read `packages/types/src/profiles.ts` — confirm Profile, ProfileFilter, ProfileSort, ProfileInclude types exist with clean camelCase fields
2. Read `packages/node/src/documents/profiles.ts` — confirm GraphQL documents with @include directives
3. Read `packages/node/src/parsers/profiles.ts` — confirm parseProfile transforms Hasura snake_case → camelCase, supports include parameter
4. Read `packages/node/src/services/profiles.ts` — confirm fetchProfile (detail) and fetchProfiles (list) service functions
5. Read `packages/node/src/keys/profiles.ts` — confirm profileKeys cache key factory
6. Read `packages/react/src/hooks/profiles/` — confirm useProfile, useProfiles, useInfiniteProfiles hooks exist
7. Read `packages/next/src/actions/profiles.ts` — confirm server actions with 'use server'
8. Read `packages/next/src/hooks/profiles/` — confirm Next.js hooks routing through server actions
9. Read `apps/test/src/app/profiles/page.tsx` — confirm playground page exists

**For DX-01** — verify clean camelCase types are importable:
1. Read `packages/types/src/index.ts` — confirm `export *` from all domain type files
2. Spot-check 3-4 domain type files — confirm types use camelCase properties (not snake_case)

**For DX-02** — verify query key factories:
1. Read `packages/node/src/keys/profiles.ts` — confirm profileKeys with hierarchical TkDodo pattern (all, lists, details, etc.)
2. Read `packages/node/src/index.ts` — confirm keys are exported from the package

**Observable Truths:** Create 15-20 truths covering the profile domain end-to-end + DX-01/DX-02. Each truth must cite specific file paths and line numbers as evidence.

**Required Artifacts:** List all files that must exist with their purpose, line counts, and key exports.

**Key Link Verification:** Trace import chains: hooks → services → documents, hooks → services → parsers, Next.js hooks → server actions → services, playground → hooks.

**Requirements Coverage:** Table showing QUERY-01, DX-01, DX-02 each marked ✓ SATISFIED with notes.
  </action>
  <verify>
File `.planning/phases/08-first-vertical-slice/08-VERIFICATION.md` exists with:
- Frontmatter containing `status: passed`
- Observable Truths table with all truths marked `✓ VERIFIED`
- Required Artifacts table with all artifacts marked `✓ VERIFIED`
- Key Link Verification table with all links marked `✓ WIRED`
- Requirements Coverage table with QUERY-01, DX-01, DX-02 all marked `✓ SATISFIED`
  </verify>
  <done>Phase 08 has a complete VERIFICATION.md confirming QUERY-01, DX-01, DX-02 are satisfied with evidence from existing code artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 09.1 VERIFICATION.md (QUERY-02)</name>
  <files>.planning/phases/09.1-digital-assets/09.1-VERIFICATION.md</files>
  <action>
Create a VERIFICATION.md for Phase 09.1 (Digital Assets) that covers requirement QUERY-02.

**Follow the exact format of `.planning/phases/09.5-social-follows/09.5-VERIFICATION.md`.**

**Frontmatter:**
```yaml
---
phase: 09.1-digital-assets
verified: [current ISO timestamp]
status: passed
score: N/N must-haves verified
---
```

**Phase Goal:** "Developer can use `useDigitalAsset`, `useDigitalAssets`, `useInfiniteDigitalAssets` for LSP7/LSP8 digital asset metadata."

**For QUERY-02** — verify these artifacts exist and are wired:
1. Read `packages/types/src/digital-assets.ts` — confirm DigitalAsset, DigitalAssetFilter, DigitalAssetSort, DigitalAssetInclude types
2. Read `packages/node/src/documents/digital-assets.ts` — confirm GraphQL documents
3. Read `packages/node/src/parsers/digital-assets.ts` — confirm parseDigitalAsset with standard derivation from decimals
4. Read `packages/node/src/services/digital-assets.ts` — confirm fetchDigitalAsset (detail) and fetchDigitalAssets (list)
5. Read `packages/node/src/keys/digital-assets.ts` — confirm digitalAssetKeys cache factory
6. Read `packages/react/src/hooks/digital-assets/` — confirm useDigitalAsset, useDigitalAssets, useInfiniteDigitalAssets hooks
7. Read `packages/next/src/actions/digital-assets.ts` — confirm server actions
8. Read `packages/next/src/hooks/digital-assets/` — confirm Next.js hooks
9. Read `apps/test/src/app/digital-assets/page.tsx` — confirm playground page

**Also verify PAGE-01 contribution** — confirm useInfiniteDigitalAssets exists in both @lsp-indexer/react and @lsp-indexer/next.

**Observable Truths:** Create 12-18 truths. Include standard derivation logic (decimals → standard field mapping), DigitalAssetResult<I> conditional type narrowing, and the `standard` derived field pattern.

**Required Artifacts:** Same structure as 09.5 verification — list all files with purpose and key exports.

**Key Link Verification:** Trace full import chains. Include the standard derivation link (parser → decimals field → standard enum).

**Requirements Coverage:** QUERY-02 marked ✓ SATISFIED, PAGE-01 contribution noted.
  </action>
  <verify>
File `.planning/phases/09.1-digital-assets/09.1-VERIFICATION.md` exists with:
- Frontmatter containing `status: passed`
- Observable Truths table with all truths marked `✓ VERIFIED`
- Required Artifacts table with all artifacts marked `✓ VERIFIED`
- Key Link Verification table with all links marked `✓ WIRED`
- Requirements Coverage table with QUERY-02 marked `✓ SATISFIED`
  </verify>
  <done>Phase 09.1 has a complete VERIFICATION.md confirming QUERY-02 is satisfied with evidence from existing code artifacts.</done>
</task>

</tasks>

<verification>
1. Both VERIFICATION.md files exist in their respective phase directories
2. Both have `status: passed` in frontmatter
3. Every observable truth cites a real file path that exists in the codebase
4. Requirements QUERY-01, DX-01, DX-02, QUERY-02 each have a `✓ SATISFIED` entry
5. Format matches the 09.5-social-follows/09.5-VERIFICATION.md template
</verification>

<success_criteria>
- Phase 08 VERIFICATION.md created with evidence for QUERY-01, DX-01, DX-02
- Phase 09.1 VERIFICATION.md created with evidence for QUERY-02
- Both files follow the established VERIFICATION.md format with observable truths, artifacts, key links
- All referenced file paths are real (not invented)
</success_criteria>

<output>
After completion, create `.planning/phases/16-v1.1-verification-gap-closure/16-01-SUMMARY.md`
</output>
