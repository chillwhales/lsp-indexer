---
phase: 09.4-conditional-include-types
plan: 02
subsystem: types, api
tags: [typescript, conditional-types, digital-assets, derived-fields, include-narrowing]

# Dependency graph
requires:
  - phase: 09.4-01
    provides: IncludeResult utility type, stripExcluded runtime utility, generic pattern for Profile domain
provides:
  - DigitalAssetResult<I> conditional type with standard↔decimals derivation
  - Generic fetchDigitalAsset<I> and fetchDigitalAssets<I> service functions
  - Generic digital asset hooks in react and next packages
affects: [09.4-03, 09.4-04, 09.4-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 'derivedFields parameter on stripExcluded for cross-field dependencies (standard→decimals)'
    - 'ResolveStandard<I> intersection type for derived field inclusion'

key-files:
  created: []
  modified:
    - packages/types/src/digital-assets.ts
    - packages/node/src/parsers/digital-assets.ts
    - packages/node/src/services/digital-assets.ts
    - packages/react/src/hooks/digital-assets.ts
    - packages/next/src/actions/digital-assets.ts
    - packages/next/src/hooks/digital-assets.ts

key-decisions:
  - 'ResolveStandard<I> as intersection type — standard field added via & when decimals is included'
  - "derivedFields { standard: 'decimals' } in stripExcluded call — runtime stripping mirrors type-level derivation"
  - 'buildDigitalAssetIncludeVars signature unchanged — public API for other domains (NFTs, OwnedAssets, OwnedTokens)'

patterns-established:
  - 'Derived field pattern: ResolveX<I> intersection type + derivedFields parameter in stripExcluded'
  - 'Same const I generic parameter pattern applied consistently across services/hooks/actions'

# Metrics
duration: 7min
completed: 2026-02-22
---

# Phase 9.4 Plan 02: Digital Assets Conditional Include Types Summary

**DigitalAssetResult<I> type with standard↔decimals derivation, threaded through all 4 packages via const I generic parameter**

## Performance

- **Duration:** 7 min
- **Started:** 2026-02-22T11:13:38Z
- **Completed:** 2026-02-22T11:20:54Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- DigitalAssetResult<I> type correctly narrows digital asset return types based on include parameter
- Standard↔decimals derived field handling works at both type and runtime level
- Generic I parameter threaded through services, hooks, and server actions in all 4 packages
- buildDigitalAssetIncludeVars signature unchanged (backward compatible public API)

## Task Commits

Each task was committed atomically:

1. **Task 1: DigitalAssetResult type + parser stripping with derived field handling** - `4828def` (feat)
2. **Task 2: Thread DigitalAsset generic through services, hooks, and server actions** - `5c30a31` (feat)

## Files Created/Modified

- `packages/types/src/digital-assets.ts` - Added DigitalAssetIncludeFieldMap, ResolveStandard<I>, and DigitalAssetResult<I> export
- `packages/node/src/parsers/digital-assets.ts` - Added include parameter and stripExcluded with derivedFields
- `packages/node/src/services/digital-assets.ts` - Made fetchDigitalAsset/fetchDigitalAssets generic, FetchDigitalAssetsResult<I> generic
- `packages/react/src/hooks/digital-assets.ts` - Made all 3 hooks generic with DigitalAssetResult<I> return types
- `packages/next/src/actions/digital-assets.ts` - Made getDigitalAsset/getDigitalAssets generic
- `packages/next/src/hooks/digital-assets.ts` - Made all 3 hooks generic with DigitalAssetResult<I> return types

## Decisions Made

- Used `ResolveStandard<I>` as intersection type with `IncludeResult` — this cleanly separates the derived field logic from the base field mapping
- Used `derivedFields: { standard: 'decimals' }` parameter in `stripExcluded` — this mirrors the type-level derivation at runtime, ensuring Object.keys consistency
- Left `buildDigitalAssetIncludeVars` signature unchanged — it's a public export used by NFTs, OwnedAssets, OwnedTokens domains and doesn't need generics

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Digital Asset conditional include types complete
- Ready for 09.4-03-PLAN.md (NFTs + Owned Assets domains with nested relation narrowing)
- The derived field pattern (ResolveStandard) is available as a reference for any future domains that need similar cross-field dependencies

## Self-Check: PASSED

---

_Phase: 09.4-conditional-include-types_
_Completed: 2026-02-22_
