---
phase: 09.4-conditional-include-types
verified: 2026-02-22T12:11:48Z
status: passed
score: 14/14 must-haves verified
---

# Phase 9.4: Conditional Include Types Verification Report

**Phase Goal:** Developer gets TypeScript return types that are narrowed based on the `include` parameter — excluded fields are absent from the type (not `null`), so autocomplete only shows what was requested. Applies to all 5 existing domains (Profiles, Digital Assets, NFTs, Owned Assets, Owned Tokens) and establishes the pattern for all future domains.

**Requirement:** DX-04 — Developer gets TypeScript return types narrowed by `include` parameter — excluded fields are absent from the type, not `null`

**Verified:** 2026-02-22T12:11:48Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                | Status     | Evidence                                                                                                                                                                                                                                                             |
| --- | ------------------------------------------------------------------------------------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `IncludeResult` utility type exists and is exported from `@lsp-indexer/types`        | ✓ VERIFIED | `packages/types/src/include-types.ts` (63 lines), exported via `packages/types/src/index.ts` line 4: `export * from './include-types'`, confirmed in built `dist/index.d.ts`                                                                                         |
| 2   | `stripExcluded` runtime utility exists in `packages/node/src/parsers/strip.ts`       | ✓ VERIFIED | 61-line implementation with base field retention, truthy include checking, and derived field support. Exported from `packages/node/src/index.ts`                                                                                                                     |
| 3   | `ProfileResult<I>` type narrows Profile based on include param                       | ✓ VERIFIED | `packages/types/src/profiles.ts` lines 190-195: `IncludeResult<Profile, 'address', ProfileIncludeFieldMap, I>`. Imports `IncludeResult`. undefined→Profile, {}→{address}                                                                                             |
| 4   | `DigitalAssetResult<I>` type narrows DigitalAsset with standard↔decimals derivation | ✓ VERIFIED | `packages/types/src/digital-assets.ts` lines 291-295: `IncludeResult<...> & ResolveStandard<NonNullable<I>>`. `ResolveStandard` (line 270) adds `standard` when `decimals: true`. Runtime `derivedFields: { standard: 'decimals' }` in parser                        |
| 5   | `NftResult<I>` type narrows Nft with nested collection and holder narrowing          | ✓ VERIFIED | `packages/types/src/nfts.ts` lines 281-290: `IncludeResult<Nft, base, NftScalarIncludeFieldMap, I> & ResolveNftCollection<...> & ResolveNftHolder<...>`. Collection uses `DigitalAssetResult<C>`, Holder uses `ProfileResult<H> & { timestamp }`                     |
| 6   | `OwnedAssetResult<I>` type narrows OwnedAsset with nested digitalAsset and holder    | ✓ VERIFIED | `packages/types/src/owned-assets.ts` lines 235-245: `IncludeResult<OwnedAsset, base, OwnedAssetScalarIncludeFieldMap, I> & ResolveOwnedAssetDA<...> & ResolveOwnedAssetHolder<...>`                                                                                  |
| 7   | `OwnedTokenResult<I>` type narrows OwnedToken with 4 nested relations                | ✓ VERIFIED | `packages/types/src/owned-tokens.ts` lines 348-360: `IncludeResult<...> & ResolveOwnedTokenDA & ResolveOwnedTokenNft & ResolveOwnedTokenOA & ResolveOwnedTokenHolder`. Most complex domain — 4 `Resolve*` intersection types                                         |
| 8   | All 5 domain services are generic (`fetchProfile<I>`, etc.)                          | ✓ VERIFIED | All services use `<const I extends XInclude \| undefined = undefined>`: profiles.ts:191, digital-assets.ts:189, nfts.ts:224+284, owned-assets.ts:202+241, owned-tokens.ts:208+247                                                                                    |
| 9   | All 5 domain hooks are generic in `@lsp-indexer/react`                               | ✓ VERIFIED | All 15 hooks (useX, useXs, useInfiniteXs × 5 domains) use `<const I extends XInclude \| undefined = undefined>` pattern. Confirmed in profiles.ts:47+96+158, digital-assets.ts:52+101+165, nfts.ts:48+105+175, owned-assets.ts:47+96+158, owned-tokens.ts:48+97+159  |
| 10  | All 5 domain hooks are generic in `@lsp-indexer/next`                                | ✓ VERIFIED | All 15 hooks use same `<const I>` pattern. Confirmed in profiles.ts:49+96+155, digital-assets.ts:49+96+157, nfts.ts:50+105+172, owned-assets.ts:49+96+155, owned-tokens.ts:50+97+156                                                                                 |
| 11  | All 5 domain server actions are generic in `@lsp-indexer/next`                       | ✓ VERIFIED | All 10 actions (getX, getXs × 5 domains) use `<const I>` pattern. Confirmed in profiles.ts:19+37, digital-assets.ts:24+42, nfts.ts:23+43, owned-assets.ts:24+42, owned-tokens.ts:24+42                                                                               |
| 12  | All 5 playground cards use `'key' in obj` field-presence checks                      | ✓ VERIFIED | profile-card.tsx: 9 checks, digital-asset-card.tsx: 25 checks, nft-card.tsx: 14 checks, owned-asset-card.tsx: 10 checks, owned-token-card.tsx: 13 checks. All use `Record<string, unknown>` prop types                                                               |
| 13  | `pnpm build` succeeds for all 4 packages and apps/test                               | ✓ VERIFIED | `@lsp-indexer/types`, `@lsp-indexer/node`, `@lsp-indexer/react`, `@lsp-indexer/next` all build cleanly (ESM+CJS+DTS). `apps/test` Next.js production build compiles and generates all 5 domain pages. Pre-existing `packages/indexer` errors unrelated to this phase |
| 14  | Backward compatibility — omitting include returns full types                         | ✓ VERIFIED | All 5 `XResult<I>` types default `I = undefined` and use `I extends undefined ? Full : Narrowed`. All service/hook/action generics default to `undefined`. `stripExcluded` returns obj unchanged when `include` is `undefined` (line 32: `if (!include) return obj`) |

**Score:** 14/14 truths verified

### Required Artifacts

| Artifact                                          | Expected                                             | Status                 | Details                                                                                                                                                              |
| ------------------------------------------------- | ---------------------------------------------------- | ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/types/src/include-types.ts`             | `IncludeResult` utility type + `ActiveFields` helper | ✓ VERIFIED (63 lines)  | Substantive: 63 lines, generic mapped conditional type. Wired: imported by all 5 domain type files                                                                   |
| `packages/node/src/parsers/strip.ts`              | `stripExcluded` runtime field stripping              | ✓ VERIFIED (61 lines)  | Substantive: 61 lines with base field logic, truthy checking, derived fields. Wired: imported by all 5 domain parsers + exported from node index                     |
| `packages/types/src/profiles.ts`                  | `ProfileResult<I>` conditional type                  | ✓ VERIFIED (195 lines) | Has `ProfileIncludeFieldMap` (9 fields), `ProfileResult<I>` type alias using `IncludeResult`                                                                         |
| `packages/types/src/digital-assets.ts`            | `DigitalAssetResult<I>` with `ResolveStandard`       | ✓ VERIFIED (295 lines) | Has `DigitalAssetIncludeFieldMap` (17 fields), `ResolveStandard<I>`, `DigitalAssetResult<I>`                                                                         |
| `packages/types/src/nfts.ts`                      | `NftResult<I>` with nested collection/holder         | ✓ VERIFIED (290 lines) | Has `NftScalarIncludeFieldMap` (8 fields), `ResolveNftCollection`, `ResolveNftHolder`, `NftResult<I>`                                                                |
| `packages/types/src/owned-assets.ts`              | `OwnedAssetResult<I>` with nested DA/holder          | ✓ VERIFIED (245 lines) | Has `OwnedAssetScalarIncludeFieldMap` (4 fields), `ResolveOwnedAssetDA`, `ResolveOwnedAssetHolder`, `OwnedAssetResult<I>`                                            |
| `packages/types/src/owned-tokens.ts`              | `OwnedTokenResult<I>` with 4 nested relations        | ✓ VERIFIED (360 lines) | Has 4 Resolve types (`DA`, `Nft`, `OA`, `Holder`), 2 custom scalar field maps (`OwnedTokenNftScalarFieldMap`, `OwnedTokenOwnedAssetFieldMap`), `OwnedTokenResult<I>` |
| `packages/node/src/services/profiles.ts`          | Generic `fetchProfile<I>`                            | ✓ VERIFIED (265 lines) | `<const I extends ProfileInclude \| undefined = undefined>`, returns `ProfileResult<I>`, calls `parseProfile(raw, params.include)` with `as ProfileResult<I>` cast   |
| `packages/react/src/hooks/profiles.ts`            | Generic hooks (3)                                    | ✓ VERIFIED             | `useProfile<I>`, `useProfiles<I>`, `useInfiniteProfiles<I>` all generic                                                                                              |
| `packages/next/src/hooks/profiles.ts`             | Generic hooks (3)                                    | ✓ VERIFIED             | Same pattern as react hooks                                                                                                                                          |
| `packages/next/src/actions/profiles.ts`           | Generic actions (2)                                  | ✓ VERIFIED             | `getProfile<I>`, `getProfiles<I>` both generic                                                                                                                       |
| `apps/test/src/components/profile-card.tsx`       | Field-presence checks                                | ✓ VERIFIED             | `Record<string, unknown>` prop, 9 `'key' in obj` checks                                                                                                              |
| `apps/test/src/components/digital-asset-card.tsx` | Field-presence checks                                | ✓ VERIFIED             | 25 `'key' in obj` checks                                                                                                                                             |
| `apps/test/src/components/nft-card.tsx`           | Field-presence checks                                | ✓ VERIFIED             | 14 `'key' in obj` checks, nested `'collection' in nft` and `'holder' in nft`                                                                                         |
| `apps/test/src/components/owned-asset-card.tsx`   | Field-presence checks                                | ✓ VERIFIED             | 10 `'key' in obj` checks, nested `'digitalAsset' in obj` and `'holder' in obj`                                                                                       |
| `apps/test/src/components/owned-token-card.tsx`   | Field-presence checks                                | ✓ VERIFIED             | 13 `'key' in obj` checks                                                                                                                                             |

### Key Link Verification

| From                                                 | To                                        | Via                                                                                             | Status  | Details                                                                                              |
| ---------------------------------------------------- | ----------------------------------------- | ----------------------------------------------------------------------------------------------- | ------- | ---------------------------------------------------------------------------------------------------- |
| `include-types.ts`                                   | `profiles.ts`                             | `import type { IncludeResult }`                                                                 | ✓ WIRED | Also imported in digital-assets.ts, nfts.ts, owned-assets.ts, owned-tokens.ts (5 consumers)          |
| `strip.ts`                                           | All 5 parsers                             | `import { stripExcluded }`                                                                      | ✓ WIRED | profiles, digital-assets, nfts, owned-assets, owned-tokens parsers all import and call stripExcluded |
| Profile types → Profile service                      | `ProfileResult<I>`                        | `import type { ProfileResult }`                                                                 | ✓ WIRED | Service returns `ProfileResult<I>`, calls `parseProfile(raw, params.include)`                        |
| Profile service → React hooks                        | Generic `I` flows through                 | `fetchProfile<I>` called in `useQuery`                                                          | ✓ WIRED | Hook signature `useProfile<const I>` → `fetchProfile<I>` in queryFn                                  |
| Profile service → Next hooks                         | Same pattern                              | `fetchProfile<I>`                                                                               | ✓ WIRED | Same as React but in Next package                                                                    |
| Profile service → Next actions                       | Same pattern                              | `fetchProfile<I>`                                                                               | ✓ WIRED | Server actions call `fetchProfile<I>`                                                                |
| Digital asset parser → strip.ts                      | `derivedFields: { standard: 'decimals' }` | `stripExcluded(result, include, ['address'], { standard: 'decimals' })`                         | ✓ WIRED | Runtime derivation matches type-level `ResolveStandard<I>`                                           |
| `NftResult` → `DigitalAssetResult` + `ProfileResult` | Nested narrowing                          | `ResolveNftCollection` uses `DigitalAssetResult<C>`, `ResolveNftHolder` uses `ProfileResult<H>` | ✓ WIRED | Cross-domain type composition working                                                                |
| `OwnedTokenResult` → 4 nested domains                | 4 `Resolve*` types                        | Each uses appropriate sub-result type                                                           | ✓ WIRED | Most complex wiring — all 4 nested relation resolvers verified                                       |

### Requirements Coverage

| Requirement                                                     | Status      | Notes                                                                                                                                                                                                                                                                 |
| --------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DX-04 — TypeScript return types narrowed by `include` parameter | ✓ SATISFIED | All 5 domains have `XResult<I>` types. Types narrow correctly (undefined→full, {}→base, {field:true}→base+field). Runtime stripping via `stripExcluded` ensures Object.keys matches type. Pattern established for future domains via reusable `IncludeResult` utility |

### Anti-Patterns Found

| File         | Line | Pattern | Severity | Impact                                                         |
| ------------ | ---- | ------- | -------- | -------------------------------------------------------------- |
| _None found_ | —    | —       | —        | Zero TODO/FIXME/placeholder/stub patterns in any modified file |

### Human Verification Required

### 1. Autocomplete Narrowing in IDE

**Test:** Open a file that calls `useProfile({ address: '0x...', include: { name: true } })` and hover over the return type or use autocomplete on the result
**Expected:** IDE shows only `address` and `name` fields — no `description`, `tags`, etc.
**Why human:** Requires IDE to evaluate conditional types and show narrowed autocomplete

### 2. Nested Type Narrowing in IDE

**Test:** Call `useNft({ address: '0x...', tokenId: '1', include: { collection: { name: true } } })` and check the type of `result.collection`
**Expected:** Collection shows only `address` and `name` — not all DigitalAsset fields
**Why human:** Deeply nested conditional types may behave differently in IDE vs. compilation

### 3. Runtime Field Stripping

**Test:** Run playground app, toggle include fields on/off, inspect returned data objects
**Expected:** `Object.keys()` on returned objects only shows base fields + included fields — excluded fields are completely absent (not `null`)
**Why human:** Runtime behavior requires running the app against a live GraphQL endpoint

### Gaps Summary

No gaps found. All 14 must-haves verified against the actual codebase:

- **Type layer:** `IncludeResult` utility type + 5 domain `XResult<I>` types with proper narrowing (including nested relations and derived fields)
- **Runtime layer:** `stripExcluded` utility called in all 5 parsers
- **Service layer:** All 10 service functions (fetchX + fetchXs × 5) are generic with `<const I>`
- **Hook layer:** All 30 hooks (useX + useXs + useInfiniteXs × 5 domains × 2 packages) are generic
- **Action layer:** All 10 server actions (getX + getXs × 5) are generic
- **UI layer:** All 5 playground cards use `'key' in obj` field-presence checks
- **Build:** All 4 packages + apps/test build cleanly
- **Backward compat:** Omitting `include` returns full types via `I = undefined` default

---

_Verified: 2026-02-22T12:11:48Z_
_Verifier: Claude (gsd-verifier)_
