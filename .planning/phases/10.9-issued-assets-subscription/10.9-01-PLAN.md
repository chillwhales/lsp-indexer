---
phase: 10.9-issued-assets-subscription
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/react/src/subscriptions/issued-assets.ts
  - packages/react/src/index.ts
  - apps/test/src/app/subscriptions/page.tsx
autonomous: true
requirements: [SUB-02, SUB-03]

must_haves:
  truths:
    - 'Developer can call useIssuedAssetSubscription() and get { data, isConnected, isSubscribed, error }'
    - 'All type params inferred — zero explicit type arguments at call sites'
    - 'Issued asset subscription data appears in the playground page'
    - 'Cache invalidation works when invalidate: true'
  artifacts:
    - path: 'packages/react/src/subscriptions/issued-assets.ts'
      provides: 'useIssuedAssetSubscription hook'
      contains: 'useIssuedAssetSubscription'
    - path: 'apps/test/src/app/subscriptions/page.tsx'
      provides: 'Issued assets subscription demo section'
      contains: 'useIssuedAssetSubscription'
  key_links:
    - from: 'packages/react/src/subscriptions/issued-assets.ts'
      to: '@lsp-indexer/node'
      via: 'buildIssuedAssetWhere + issuedAssetKeys + parseIssuedAssets + IssuedAssetSubscriptionDocument'
      pattern: 'buildIssuedAssetWhere|issuedAssetKeys|parseIssuedAssets|IssuedAssetSubscriptionDocument'
    - from: 'packages/react/src/subscriptions/issued-assets.ts'
      to: 'packages/react/src/subscriptions/use-subscription.ts'
      via: 'useSubscription call'
      pattern: 'useSubscription'
---

<objective>
Create the `useIssuedAssetSubscription` hook and add a working issued assets subscription demo to the playground page.

Purpose: Domain subscription hook for issued assets (LSP12). Follows the exact pattern established by profiles (10.2).

Output: `useIssuedAssetSubscription` exported from `@lsp-indexer/react`, issued assets subscription demo visible in the test app.
</objective>

<execution_context>
@/home/coder/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-subscriptions/10-CONTEXT.md
@.planning/phases/10.1-subscription-foundation/10.1-01-SUMMARY.md
@.planning/phases/10.2-profiles-subscription/10.2-01-SUMMARY.md
@packages/node/src/services/issued-assets.ts
@packages/node/src/keys/issued-assets.ts
@packages/node/src/documents/issued-assets.ts
@packages/node/src/parsers/issued-assets.ts
@packages/react/src/index.ts
@apps/test/src/app/subscriptions/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useIssuedAssetSubscription hook + playground demo + export</name>
  <files>
    packages/react/src/subscriptions/issued-assets.ts
    packages/react/src/index.ts
    apps/test/src/app/subscriptions/page.tsx
  </files>
  <action>
    **Create `packages/react/src/subscriptions/issued-assets.ts`:**

    Follow the exact pattern from the 10.2 profiles SUMMARY:
    - Import `IssuedAsset`, `IssuedAssetFilter` from `@lsp-indexer/types`
    - Import `buildIssuedAssetWhere`, `issuedAssetKeys`, `parseIssuedAssets`, `IssuedAssetSubscriptionDocument` from `@lsp-indexer/node`
    - `extract: (result) => result.lsp12_issued_asset` — Hasura table extraction
    - `parser: (raw) => parseIssuedAssets(raw)`
    - `order_by: undefined` — entity domain, Hasura default sort
    - Params interface: `UseIssuedAssetSubscriptionParams` with `{ filter?, limit?, enabled?, invalidate?, onData?, onReconnect? }`
    - `useQueryClient()` called unconditionally (Rules of Hooks), only passed when `invalidate: true`

    **Update `packages/react/src/index.ts`:**

    Add export:
    ```typescript
    export * from './subscriptions/issued-assets';
    ```

    **Add issued assets demo to playground page:**

    Update `apps/test/src/app/subscriptions/page.tsx` — add an issued assets subscription section showing:
    1. Connection/subscription status badges
    2. Data count and list of issued asset addresses
    3. Error display
    4. Filter input if relevant
    5. Limit selector

    **Build validation:**
    ```bash
    pnpm build
    ```

    Commit: `feat(10.9-issued-assets): add useIssuedAssetSubscription hook + playground demo`

  </action>
  <verify>
    `pnpm build` — all packages + test app build.
    `grep "useIssuedAssetSubscription" packages/react/dist/index.js` — exported.
    `grep "useIssuedAssetSubscription" apps/test/src/app/subscriptions/page.tsx` — used in playground.
  </verify>
  <done>useIssuedAssetSubscription hook exported from @lsp-indexer/react with full type inference. Issued assets subscription demo visible in test app playground at /subscriptions.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes for all packages + test app
- `useIssuedAssetSubscription` is importable from `@lsp-indexer/react`
- Hook accepts { filter?, limit?, enabled?, invalidate?, onData?, onReconnect? }
- Hook returns { data: IssuedAsset[] | null, isConnected, isSubscribed, error: Error | null }
- All types inferred — no explicit type arguments
- Playground shows issued assets subscription demo
</verification>

<success_criteria>

1. useIssuedAssetSubscription created with type-safe SubscriptionConfig (extract + parser, zero assertions)
2. Hook exported from @lsp-indexer/react
3. Issued assets subscription demo works in playground
4. All packages build with zero type errors
   </success_criteria>

<output>
After completion, create `.planning/phases/10.9-issued-assets-subscription/10.9-01-SUMMARY.md`
</output>
