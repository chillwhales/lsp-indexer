---
phase: 10.9-issued-assets-subscription
plan: 01
subsystem: subscriptions
tags: [react, websocket, graphql-ws, subscription, issued-assets, lsp12]

# Dependency graph
requires:
  - phase: 10.1-subscription-foundation
    provides: useSubscription hook factory, SubscriptionConfig, IndexerSubscriptionProvider
  - phase: 10.2-profiles-subscription
    provides: Domain subscription hook factory pattern (createUseXSubscription)
provides:
  - useIssuedAssetSubscription hook in both @lsp-indexer/react and @lsp-indexer/next
  - createUseIssuedAssetSubscription factory for cross-package reuse
  - IssuedAssetSubscriptionDocument (GraphQL subscription)
  - buildIssuedAssetSubscriptionConfig (node service)
  - Subscription tab in issued-assets playground page
affects: [10.10-encrypted-assets-subscription, 10.11-data-changed-events-subscription]

# Tech tracking
tech-stack:
  added: []
  patterns:
    [domain subscription hook with directory migration (flat file → directory for subscription)]

key-files:
  created:
    [
      packages/react/src/hooks/factories/issued-assets/create-use-issued-asset-subscription.ts,
      packages/react/src/hooks/issued-assets/use-issued-asset-subscription.ts,
      packages/react/src/hooks/types/issued-assets.ts,
      packages/next/src/hooks/issued-assets/use-issued-asset-subscription.ts,
    ]
  modified:
    [
      packages/node/src/documents/issued-assets.ts,
      packages/node/src/services/issued-assets.ts,
      packages/node/src/graphql/graphql.ts,
      packages/node/src/graphql/gql.ts,
      packages/react/src/hooks/factories/index.ts,
      packages/react/src/hooks/types/index.ts,
      apps/test/src/app/issued-assets/page.tsx,
    ]

key-decisions:
  - 'Migrated issued-assets hooks from flat file to directory structure (react + next) to accommodate subscription hook alongside query hooks'
  - 'Entity domain uses Hasura default ordering (order_by: undefined) — matching creators pattern'
  - '31 include variables in subscription document matching query document'

patterns-established:
  - 'Domain flat-file to directory migration pattern: move queries.ts into domain/ dir, add subscription wrapper + index'

requirements-completed: [SUB-02, SUB-03]

# Metrics
duration: 7min
completed: 2026-03-05
---

# Phase 10.9 Plan 01: Issued Assets Subscription Summary

**useIssuedAssetSubscription hook with 3-overload type narrowing, directory refactoring of issued-assets hooks (react + next), subscription tab in playground**

## Performance

- **Duration:** 7 min
- **Started:** 2026-03-05T06:56:29Z
- **Completed:** 2026-03-05T07:04:10Z
- **Tasks:** 1
- **Files modified:** 16

## Accomplishments

- Created `IssuedAssetSubscriptionDocument` — subscription variant of `GetIssuedAssetsDocument` with 31 include variables
- Created `buildIssuedAssetSubscriptionConfig` in node service layer with extract + parser config
- Created `createUseIssuedAssetSubscription` factory with 3-overload type narrowing (full, include-narrowed, partial)
- Exported `useIssuedAssetSubscription` from both `@lsp-indexer/react` and `@lsp-indexer/next`
- Migrated issued-assets hooks from flat file to directory structure in both react and next packages
- Added Subscription tab to issued-assets playground page with connection/subscription status badges, cache invalidation toggle, and full filter/sort/include controls

## Task Commits

Each task was committed atomically:

1. **Task 1: Create useIssuedAssetSubscription hook + playground demo + export** - `a055f9c` (feat)

## Files Created/Modified

- `packages/node/src/documents/issued-assets.ts` - Added IssuedAssetSubscriptionDocument (subscription GraphQL document)
- `packages/node/src/services/issued-assets.ts` - Added buildIssuedAssetSubscriptionConfig + IssuedAssetSubscriptionSubscription import
- `packages/node/src/graphql/graphql.ts` - Codegen-generated subscription types
- `packages/node/src/graphql/gql.ts` - Codegen-generated graphql() overload for subscription
- `packages/react/src/hooks/factories/issued-assets/create-use-issued-asset-subscription.ts` - Factory with 3-overload type narrowing
- `packages/react/src/hooks/factories/issued-assets/index.ts` - Factory barrel export
- `packages/react/src/hooks/issued-assets/queries.ts` - Existing query hooks (renamed from flat file)
- `packages/react/src/hooks/issued-assets/use-issued-asset-subscription.ts` - Thin wrapper calling factory with React useSubscription
- `packages/react/src/hooks/issued-assets/index.ts` - Directory barrel export
- `packages/react/src/hooks/types/issued-assets.ts` - UseIssuedAssetSubscriptionParams interface
- `packages/react/src/hooks/types/index.ts` - Added issued-assets type export
- `packages/react/src/hooks/factories/index.ts` - Added issued-assets factory export
- `packages/next/src/hooks/issued-assets/queries.ts` - Existing query hooks (renamed, fixed import path)
- `packages/next/src/hooks/issued-assets/use-issued-asset-subscription.ts` - Next.js thin wrapper
- `packages/next/src/hooks/issued-assets/index.ts` - Directory barrel export
- `apps/test/src/app/issued-assets/page.tsx` - Added Subscription tab with status badges, invalidation toggle

## Decisions Made

- Migrated issued-assets hooks from flat file to directory structure (react + next) to accommodate subscription hook alongside query hooks — same migration pattern as creators in 10.8
- Entity domain uses Hasura default ordering (order_by: undefined) — matching creators and profiles subscription patterns
- 31 include variables in subscription document matching the query document (3 scalar + 10 issuer profile + 18 digital asset)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Migrated issued-assets from flat file to directory structure**

- **Found during:** Task 1
- **Issue:** Plan assumed old architecture with `packages/react/src/subscriptions/issued-assets.ts`. Actual pattern uses `hooks/` directory with factory pattern. Flat file `issued-assets.ts` needed to become a directory to add subscription hook.
- **Fix:** Migrated to directory: `issued-assets.ts` → `issued-assets/queries.ts` + `issued-assets/use-issued-asset-subscription.ts` + `issued-assets/index.ts` (both react and next packages)
- **Files modified:** packages/react/src/hooks/issued-assets/, packages/next/src/hooks/issued-assets/
- **Verification:** `pnpm build` passes for all packages + test app
- **Committed in:** a055f9c

**2. [Rule 3 - Blocking] Fixed relative import path in next package after directory migration**

- **Found during:** Task 1
- **Issue:** After moving `issued-assets.ts` into `issued-assets/queries.ts`, the relative import `../actions/issued-assets` became invalid (needed `../../actions/issued-assets`)
- **Fix:** Updated import path from `../actions/issued-assets` to `../../actions/issued-assets`
- **Files modified:** packages/next/src/hooks/issued-assets/queries.ts
- **Verification:** Next package builds successfully
- **Committed in:** a055f9c

---

**Total deviations:** 2 auto-fixed (2 blocking)
**Impact on plan:** Both auto-fixes necessary for correct directory structure migration. No scope creep.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Issued assets subscription pattern complete — ready for replication across remaining domain subscription sub-phases (10.10–10.13)
- All packages build cleanly (node, react, next, types, test app)

---

_Phase: 10.9-issued-assets-subscription_
_Completed: 2026-03-05_
