---
phase: 10.5-owned-assets-subscription
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/react/src/subscriptions/owned-assets.ts
  - packages/react/src/index.ts
  - apps/test/src/app/subscriptions/page.tsx
autonomous: true
requirements: [SUB-02, SUB-03]

must_haves:
  truths:
    - 'Developer can call useOwnedAssetSubscription() and get { data, isConnected, isSubscribed, error }'
    - 'All type params inferred — zero explicit type arguments at call sites'
    - 'Owned asset subscription data appears in the playground page'
    - 'Cache invalidation works when invalidate: true'
  artifacts:
    - path: 'packages/react/src/subscriptions/owned-assets.ts'
      provides: 'useOwnedAssetSubscription hook'
      contains: 'useOwnedAssetSubscription'
    - path: 'apps/test/src/app/subscriptions/page.tsx'
      provides: 'Owned assets subscription demo section'
      contains: 'useOwnedAssetSubscription'
  key_links:
    - from: 'packages/react/src/subscriptions/owned-assets.ts'
      to: '@lsp-indexer/node'
      via: 'buildOwnedAssetWhere + ownedAssetKeys + parseOwnedAssets + OwnedAssetSubscriptionDocument'
      pattern: 'buildOwnedAssetWhere|ownedAssetKeys|parseOwnedAssets|OwnedAssetSubscriptionDocument'
    - from: 'packages/react/src/subscriptions/owned-assets.ts'
      to: 'packages/react/src/subscriptions/use-subscription.ts'
      via: 'useSubscription call'
      pattern: 'useSubscription'
---

<objective>
Create the `useOwnedAssetSubscription` hook and add a working owned assets subscription demo to the playground page.

Purpose: Domain subscription hook for owned assets. Follows the exact pattern established by profiles (10.2).

Output: `useOwnedAssetSubscription` exported from `@lsp-indexer/react`, owned assets subscription demo visible in the test app.
</objective>

<execution_context>
@/home/coder/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-subscriptions/10-CONTEXT.md
@.planning/phases/10.1-subscription-foundation/10.1-01-SUMMARY.md
@.planning/phases/10.2-profiles-subscription/10.2-01-SUMMARY.md
@packages/node/src/services/owned-assets.ts
@packages/node/src/keys/owned-assets.ts
@packages/node/src/documents/owned-assets.ts
@packages/node/src/parsers/owned-assets.ts
@packages/react/src/index.ts
@apps/test/src/app/subscriptions/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useOwnedAssetSubscription hook + playground demo + export</name>
  <files>
    packages/react/src/subscriptions/owned-assets.ts
    packages/react/src/index.ts
    apps/test/src/app/subscriptions/page.tsx
  </files>
  <action>
    **Create `packages/react/src/subscriptions/owned-assets.ts`:**

    Follow the exact pattern from the 10.2 profiles SUMMARY:
    - Import `OwnedAsset`, `OwnedAssetFilter` from `@lsp-indexer/types`
    - Import `buildOwnedAssetWhere`, `ownedAssetKeys`, `parseOwnedAssets`, `OwnedAssetSubscriptionDocument` from `@lsp-indexer/node`
    - `extract: (result) => result.owned_asset` — Hasura table extraction
    - `parser: (raw) => parseOwnedAssets(raw)`
    - `order_by: undefined` — entity domain, Hasura default sort
    - Params interface: `UseOwnedAssetSubscriptionParams` with `{ filter?, limit?, enabled?, invalidate?, onData?, onReconnect? }`
    - `useQueryClient()` called unconditionally (Rules of Hooks), only passed when `invalidate: true`

    **Update `packages/react/src/index.ts`:**

    Add export:
    ```typescript
    export * from './subscriptions/owned-assets';
    ```

    **Add owned assets demo to playground page:**

    Update `apps/test/src/app/subscriptions/page.tsx` — add an owned assets subscription section showing:
    1. Connection/subscription status badges
    2. Data count and list of owned asset addresses
    3. Error display
    4. Filter input (address filter)
    5. Limit selector

    **Build validation:**
    ```bash
    pnpm build
    ```

    Commit: `feat(10.5-owned-assets): add useOwnedAssetSubscription hook + playground demo`

  </action>
  <verify>
    `pnpm build` — all packages + test app build.
    `grep "useOwnedAssetSubscription" packages/react/dist/index.js` — exported.
    `grep "useOwnedAssetSubscription" apps/test/src/app/subscriptions/page.tsx` — used in playground.
  </verify>
  <done>useOwnedAssetSubscription hook exported from @lsp-indexer/react with full type inference. Owned assets subscription demo visible in test app playground at /subscriptions.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes for all packages + test app
- `useOwnedAssetSubscription` is importable from `@lsp-indexer/react`
- Hook accepts { filter?, limit?, enabled?, invalidate?, onData?, onReconnect? }
- Hook returns { data: OwnedAsset[] | null, isConnected, isSubscribed, error: Error | null }
- All types inferred — no explicit type arguments
- Playground shows owned assets subscription demo
</verification>

<success_criteria>

1. useOwnedAssetSubscription created with type-safe SubscriptionConfig (extract + parser, zero assertions)
2. Hook exported from @lsp-indexer/react
3. Owned assets subscription demo works in playground
4. All packages build with zero type errors
   </success_criteria>

<output>
After completion, create `.planning/phases/10.5-owned-assets-subscription/10.5-01-SUMMARY.md`
</output>
