---
phase: 10.5-owned-assets-subscription
plan: 01
subsystem: subscriptions
tags: [react, next, websocket, graphql-ws, subscription, owned-assets, factory-pattern]

# Dependency graph
requires:
  - phase: 10.4-nfts-subscription
    provides: Factory pattern template, domain subscription replication pattern
  - phase: 10.1-subscription-foundation
    provides: useSubscription hook, SubscriptionConfig<TResult,TVariables,TRaw,TParsed>, IndexerSubscriptionProvider
provides:
  - createUseOwnedAssetSubscription factory for react + next
  - useOwnedAssetSubscription hook in both @lsp-indexer/react and @lsp-indexer/next
  - Owned asset hooks refactored from monolith to directory pattern (queries + subscription)
  - OwnedAssetSubscriptionDocument + buildOwnedAssetSubscriptionConfig in node service layer
  - Subscription tab in /owned-assets playground page
affects: [10.6-owned-tokens-subscription]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Owned asset domain directory refactoring pattern (single file → directory with queries + subscription)
    - buildOwnedAssetSubscriptionConfig in node service layer

key-files:
  created:
    - packages/react/src/hooks/factories/owned-assets/create-use-owned-asset-subscription.ts
    - packages/react/src/hooks/factories/owned-assets/index.ts
    - packages/react/src/hooks/owned-assets/use-owned-asset-subscription.ts
    - packages/react/src/hooks/owned-assets/index.ts
    - packages/react/src/hooks/types/owned-assets.ts
    - packages/next/src/hooks/owned-assets/use-owned-asset-subscription.ts
    - packages/next/src/hooks/owned-assets/index.ts
  modified:
    - packages/node/src/documents/owned-assets.ts
    - packages/node/src/services/owned-assets.ts
    - packages/node/src/graphql/gql.ts
    - packages/node/src/graphql/graphql.ts
    - packages/react/src/hooks/factories/index.ts
    - packages/react/src/hooks/types/index.ts
    - packages/react/src/hooks/owned-assets/queries.ts (renamed from owned-assets.ts)
    - packages/next/src/hooks/owned-assets/queries.ts (renamed from owned-assets.ts, fixed import path)
    - apps/test/src/app/owned-assets/page.tsx

key-decisions:
  - 'Converted owned-assets single files to directory modules in both react and next packages'
  - 'Only subscription factory needed — existing query hooks already have factories in digital-assets/'
  - 'Fixed relative import paths after file-to-directory conversion (../actions/ → ../../actions/)'

patterns-established:
  - 'Domain subscription replication confirmed for fourth domain (profiles → digital assets → NFTs → owned assets)'

requirements-completed: [SUB-02, SUB-03]

# Metrics
duration: 12min
completed: 2026-03-04
---

# Phase 10.5 Plan 01: Owned Assets Subscription Summary

**useOwnedAssetSubscription with 3-overload type narrowing + directory refactoring of owned-assets hooks in react/next**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-03-04T11:05:00Z
- **Completed:** 2026-03-04T11:16:16Z
- **Tasks:** 1
- **Files modified:** 16

## Accomplishments

- Added `OwnedAssetSubscriptionDocument` with 32 include variables mirroring `GetOwnedAssetsDocument`
- Ran codegen to generate `OwnedAssetSubscriptionSubscription` and `OwnedAssetSubscriptionSubscriptionVariables` types
- Added `buildOwnedAssetSubscriptionConfig` to node service layer
- Created `createUseOwnedAssetSubscription` factory with 3-overload type narrowing
- Added `useOwnedAssetSubscription` thin wrappers in both `@lsp-indexer/react` and `@lsp-indexer/next`
- Converted owned-assets single files to directory modules (queries.ts + use-owned-asset-subscription.ts + index.ts)
- Fixed relative import paths after file-to-directory conversion in next package
- Added subscription tab to `/owned-assets` playground with connection status badges, filter/sort/include/invalidate controls
- Added `UseOwnedAssetSubscriptionParams` type with proper exports
- All 4 packages + test app build with zero errors

## Task Commits

Each task was committed atomically:

1. **Task 1: Subscription document + codegen + config builder + factory + hooks + playground** - `04d3369` (feat)

## Files Created/Modified

- `packages/node/src/documents/owned-assets.ts` - Added OwnedAssetSubscriptionDocument (32 include variables)
- `packages/node/src/services/owned-assets.ts` - Added buildOwnedAssetSubscriptionConfig + imports
- `packages/node/src/graphql/gql.ts` - Codegen output (subscription document mapping)
- `packages/node/src/graphql/graphql.ts` - Codegen output (OwnedAssetSubscriptionSubscription type)
- `packages/react/src/hooks/factories/owned-assets/` - Factory + barrel export
- `packages/react/src/hooks/types/owned-assets.ts` - UseOwnedAssetSubscriptionParams interface
- `packages/react/src/hooks/owned-assets/` - queries.ts (renamed) + subscription wrapper + barrel
- `packages/next/src/hooks/owned-assets/` - queries.ts (renamed, fixed import) + subscription wrapper + barrel
- `packages/react/src/hooks/factories/index.ts` - Added owned-assets barrel export
- `packages/react/src/hooks/types/index.ts` - Added owned-assets type export
- `apps/test/src/app/owned-assets/page.tsx` - Added SubscriptionTab with full controls

## Decisions Made

- Converted owned-assets single files to directory modules (consistent with NFTs pattern from 10.4)
- Only created subscription factory — existing query hooks already have factories in the digital-assets factories directory
- Fixed relative import paths after file-to-directory conversion (`../actions/` → `../../actions/` in next package)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] File structure differs from plan**

- **Found during:** Task 1
- **Issue:** Plan referenced `packages/react/src/subscriptions/owned-assets.ts` but actual codebase uses factory pattern in `hooks/factories/` + thin wrappers in `hooks/`
- **Fix:** Created files following actual project structure (factories + thin wrappers)
- **Files modified:** All created files use correct paths
- **Commit:** 04d3369

**2. [Rule 3 - Blocking] File-to-directory conversion broke imports**

- **Found during:** Task 1
- **Issue:** Converting `owned-assets.ts` → `owned-assets/` directory broke relative imports in next package (`../actions/` needed to become `../../actions/`)
- **Fix:** Updated import paths in renamed queries.ts files
- **Files modified:** `packages/next/src/hooks/owned-assets/queries.ts`
- **Commit:** 04d3369

## Issues Encountered

- Edit tool matched wrong location when using generic `oldString` — required more specific context to target correct insertion point in documents file
- Pre-existing build failures in `packages/indexer` (unrelated typeorm/abi issues) — worked around by building individual packages

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Owned assets subscription pattern confirmed — fourth domain replicated from established template
- Ready for 10.6 (Owned Tokens Subscription) and remaining domain subscription phases
- All packages build cleanly

## Self-Check: PASSED

- All 7 created files verified present on disk
- Commit `04d3369` verified in git log
- node, react, next packages + test app all build successfully
- `useOwnedAssetSubscription` exported in react dist
- `useOwnedAssetSubscription` exported in next dist
- `buildOwnedAssetSubscriptionConfig` exported in node dist
- `OwnedAssetSubscriptionDocument` exported in node dist

---

_Phase: 10.5-owned-assets-subscription_
_Completed: 2026-03-04_
