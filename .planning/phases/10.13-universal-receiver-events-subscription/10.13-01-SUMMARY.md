---
phase: 10.13-universal-receiver-events-subscription
plan: 01
subsystem: subscriptions
tags: [react, websocket, graphql-ws, subscription, universal-receiver-events, tanstack-query]

# Dependency graph
requires:
  - phase: 10.1-subscription-foundation
    provides: useSubscription hook factory, SubscriptionConfig<TResult,TVariables,TRaw,TParsed>, IndexerSubscriptionProvider
  - phase: 10.2-profiles-subscription
    provides: Factory pattern template (createUseXSubscription + thin wrappers in react/next)
provides:
  - createUseUniversalReceiverEventSubscription factory for react + next
  - useUniversalReceiverEventSubscription hook in both @lsp-indexer/react and @lsp-indexer/next
  - UniversalReceiverEventSubscriptionDocument (GraphQL subscription document)
  - buildUniversalReceiverEventSubscriptionConfig (node service config builder)
  - Universal receiver events subscription playground demo at /universal-receiver-events (Subscription tab)
affects: [11-server-actions-publish-readiness]

# Tech tracking
tech-stack:
  added: []
  patterns:
    [
      universal-receiver-event subscription hook with 3-overload type narrowing and event-domain block-order desc sort,
    ]

key-files:
  created:
    [
      packages/react/src/hooks/factories/universal-receiver-events/create-use-universal-receiver-event-subscription.ts,
      packages/react/src/hooks/factories/universal-receiver-events/index.ts,
      packages/react/src/hooks/universal-receiver-events/use-universal-receiver-event-subscription.ts,
      packages/react/src/hooks/universal-receiver-events/index.ts,
      packages/react/src/hooks/types/universal-receiver-events.ts,
      packages/next/src/hooks/universal-receiver-events/use-universal-receiver-event-subscription.ts,
      packages/next/src/hooks/universal-receiver-events/index.ts,
    ]
  modified:
    [
      packages/node/src/documents/universal-receiver-events.ts,
      packages/node/src/services/universal-receiver-events.ts,
      packages/node/src/graphql/graphql.ts,
      packages/node/src/graphql/gql.ts,
      packages/react/src/hooks/factories/index.ts,
      packages/react/src/hooks/types/index.ts,
      packages/next/src/hooks/universal-receiver-events/use-universal-receiver-events.ts,
      apps/test/src/app/universal-receiver-events/page.tsx,
    ]

key-decisions:
  - 'Followed established factory pattern: createUseUniversalReceiverEventSubscription + thin wrappers in react/next'
  - 'Migrated universal-receiver-events hooks from flat file to directory (matching all other subscription domains)'
  - 'EVENT domain uses block-order desc sort: block_number desc, transaction_index desc, log_index desc'
  - '3-overload type narrowing: include: I → narrowed, no include → full, optional include → partial'

patterns-established:
  - 'Last domain subscription hook completes the 10.x subscription sub-phase pattern'

requirements-completed: [SUB-02, SUB-03]

# Metrics
duration: 7min
completed: 2026-03-05
---

# Phase 10.13 Plan 01: Universal Receiver Events Subscription Summary

**useUniversalReceiverEventSubscription hook with 3-overload type narrowing, factory pattern, directory migration, and playground Subscription tab with event-domain block-order desc default sort**

## Performance

- **Duration:** 7 min
- **Started:** 2026-03-05T12:09:04Z
- **Completed:** 2026-03-05T12:16:48Z
- **Tasks:** 1
- **Files modified:** 16

## Accomplishments

- Created `UniversalReceiverEventSubscriptionDocument` — subscription mirror of the query document (~45 variables, no $offset, no aggregate)
- Added `buildUniversalReceiverEventSubscriptionConfig` to node service layer (assembles document + variables + extract + parser)
- Created `createUseUniversalReceiverEventSubscription` factory with 3-overload type narrowing (include: I → narrowed, no include → full, optional → partial)
- `useUniversalReceiverEventSubscription` exported from both `@lsp-indexer/react` and `@lsp-indexer/next` (thin wrappers calling factory)
- Migrated universal-receiver-events hooks from flat file to directory structure (react + next) to accommodate subscription hook alongside query hooks
- Added Subscription tab to universal-receiver-events playground with connection/subscription status badges, invalidate cache toggle, filter/sort/limit/include controls

## Task Commits

Each task was committed atomically:

1. **Task 1: Create useUniversalReceiverEventSubscription hook + playground demo + export** - `12e479e` (feat)

## Files Created/Modified

- `packages/node/src/documents/universal-receiver-events.ts` - Added UniversalReceiverEventSubscriptionDocument
- `packages/node/src/services/universal-receiver-events.ts` - Added buildUniversalReceiverEventSubscriptionConfig
- `packages/node/src/graphql/graphql.ts` - Codegen: subscription types
- `packages/node/src/graphql/gql.ts` - Codegen: document map
- `packages/react/src/hooks/factories/universal-receiver-events/create-use-universal-receiver-event-subscription.ts` - Factory with 3 overloads
- `packages/react/src/hooks/factories/universal-receiver-events/index.ts` - Barrel export
- `packages/react/src/hooks/universal-receiver-events/use-universal-receiver-event-subscription.ts` - React thin wrapper
- `packages/react/src/hooks/universal-receiver-events/index.ts` - Directory barrel
- `packages/react/src/hooks/types/universal-receiver-events.ts` - UseUniversalReceiverEventSubscriptionParams
- `packages/next/src/hooks/universal-receiver-events/use-universal-receiver-event-subscription.ts` - Next.js thin wrapper
- `packages/next/src/hooks/universal-receiver-events/index.ts` - Directory barrel
- `apps/test/src/app/universal-receiver-events/page.tsx` - Added Subscription tab

## Decisions Made

- Followed established factory pattern (createUseXSubscription + thin wrappers) matching all other domain subscriptions
- Migrated universal-receiver-events from flat file to directory in both react and next packages to accommodate subscription alongside query hooks (matching other domains like token-id-data-changed-events, data-changed-events)
- EVENT domain uses block-order desc default sort (block_number desc, transaction_index desc, log_index desc)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Adapted to codebase factory pattern instead of plan's file paths**

- **Found during:** Task 1
- **Issue:** Plan specified `packages/react/src/subscriptions/universal-receiver-events.ts` but the codebase uses a factory pattern under `packages/react/src/hooks/factories/` with thin wrappers in `packages/react/src/hooks/{domain}/`
- **Fix:** Created files following the established factory + directory pattern matching all 11 other domain subscription hooks
- **Files modified:** All factory/hook/type files
- **Verification:** pnpm build passes for all packages + test app
- **Committed in:** 12e479e

**2. [Rule 3 - Blocking] Migrated universal-receiver-events to directory structure**

- **Found during:** Task 1
- **Issue:** Existing hooks were in flat files (`universal-receiver-events.ts`) but subscription hooks require directory structure for multiple files
- **Fix:** Migrated to directory with index.ts barrel in both react and next packages, fixed relative import path in next package (`../../actions/` instead of `../actions/`)
- **Files modified:** packages/react/src/hooks/universal-receiver-events/, packages/next/src/hooks/universal-receiver-events/
- **Verification:** pnpm build passes, exports resolve correctly
- **Committed in:** 12e479e

**3. [Rule 3 - Blocking] Added @lsp-indexer/next subscription hook**

- **Found during:** Task 1
- **Issue:** Plan didn't mention @lsp-indexer/next package, but all domain subscription hooks must exist in both react and next
- **Fix:** Created thin wrapper in next package following established pattern
- **Files modified:** packages/next/src/hooks/universal-receiver-events/use-universal-receiver-event-subscription.ts
- **Verification:** @lsp-indexer/next builds and exports the hook
- **Committed in:** 12e479e

---

**Total deviations:** 3 auto-fixed (3 blocking — adapting to actual codebase architecture)
**Impact on plan:** All deviations were necessary to match the established codebase patterns. No scope creep — same functionality delivered via the correct architecture.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Universal receiver events subscription complete — this is the LAST domain subscription sub-phase (10.13)
- All 12 domain subscription hooks now complete (profiles, digital-assets, nfts, owned-assets, owned-tokens, followers, creators, issued-assets, encrypted-assets, data-changed-events, token-id-data-changed-events, universal-receiver-events)
- Ready for Phase 11: Server Actions & Publish Readiness

---

_Phase: 10.13-universal-receiver-events-subscription_
_Completed: 2026-03-05_
