---
phase: 10.13-universal-receiver-events-subscription
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/react/src/subscriptions/universal-receiver-events.ts
  - packages/react/src/index.ts
  - apps/test/src/app/subscriptions/page.tsx
autonomous: true
requirements: [SUB-02, SUB-03]

must_haves:
  truths:
    - 'Developer can call useUniversalReceiverEventSubscription() and get { data, isConnected, isSubscribed, error }'
    - 'All type params inferred — zero explicit type arguments at call sites'
    - 'Universal receiver event subscription data appears in the playground page'
    - 'Cache invalidation works when invalidate: true'
    - 'Results sorted by block_number desc, transaction_index desc, log_index desc (event domain)'
  artifacts:
    - path: 'packages/react/src/subscriptions/universal-receiver-events.ts'
      provides: 'useUniversalReceiverEventSubscription hook'
      contains: 'useUniversalReceiverEventSubscription'
    - path: 'apps/test/src/app/subscriptions/page.tsx'
      provides: 'Universal receiver events subscription demo section'
      contains: 'useUniversalReceiverEventSubscription'
  key_links:
    - from: 'packages/react/src/subscriptions/universal-receiver-events.ts'
      to: '@lsp-indexer/node'
      via: 'buildUniversalReceiverEventWhere + universalReceiverEventKeys + parseUniversalReceiverEvents + UniversalReceiverEventSubscriptionDocument'
      pattern: 'buildUniversalReceiverEventWhere|universalReceiverEventKeys|parseUniversalReceiverEvents|UniversalReceiverEventSubscriptionDocument'
    - from: 'packages/react/src/subscriptions/universal-receiver-events.ts'
      to: 'packages/react/src/subscriptions/use-subscription.ts'
      via: 'useSubscription call'
      pattern: 'useSubscription'
---

<objective>
Create the `useUniversalReceiverEventSubscription` hook and add a working universal receiver events subscription demo to the playground page.

Purpose: Domain subscription hook for universal receiver events. This is an EVENT domain — results must be sorted by block-order desc (`block_number desc, transaction_index desc, log_index desc`). Follows the profiles pattern (10.2) with the event-domain sort addition.

Output: `useUniversalReceiverEventSubscription` exported from `@lsp-indexer/react`, universal receiver events subscription demo visible in the test app.
</objective>

<execution_context>
@/home/coder/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-subscriptions/10-CONTEXT.md
@.planning/phases/10.1-subscription-foundation/10.1-01-SUMMARY.md
@.planning/phases/10.2-profiles-subscription/10.2-01-SUMMARY.md
@packages/node/src/services/universal-receiver-events.ts
@packages/node/src/keys/universal-receiver-events.ts
@packages/node/src/documents/universal-receiver-events.ts
@packages/node/src/parsers/universal-receiver-events.ts
@packages/react/src/index.ts
@apps/test/src/app/subscriptions/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useUniversalReceiverEventSubscription hook + playground demo + export</name>
  <files>
    packages/react/src/subscriptions/universal-receiver-events.ts
    packages/react/src/index.ts
    apps/test/src/app/subscriptions/page.tsx
  </files>
  <action>
    **Create `packages/react/src/subscriptions/universal-receiver-events.ts`:**

    Follow the exact pattern from the 10.2 profiles SUMMARY, with the EVENT domain sort:
    - Import `UniversalReceiverEvent`, `UniversalReceiverEventFilter` from `@lsp-indexer/types`
    - Import `buildUniversalReceiverEventWhere`, `universalReceiverEventKeys`, `parseUniversalReceiverEvents`, `UniversalReceiverEventSubscriptionDocument` from `@lsp-indexer/node`
    - `extract: (result) => result.universal_receiver` — Hasura table extraction
    - `parser: (raw) => parseUniversalReceiverEvents(raw)`
    - **`order_by: [{ block_number: 'desc' as const }, { transaction_index: 'desc' as const }, { log_index: 'desc' as const }]`** — EVENT domain, block-order desc sort
    - Params interface: `UseUniversalReceiverEventSubscriptionParams` with `{ filter?, limit?, enabled?, invalidate?, onData?, onReconnect? }`
    - `useQueryClient()` called unconditionally (Rules of Hooks), only passed when `invalidate: true`

    **Update `packages/react/src/index.ts`:**

    Add export:
    ```typescript
    export * from './subscriptions/universal-receiver-events';
    ```

    **Add universal receiver events demo to playground page:**

    Update `apps/test/src/app/subscriptions/page.tsx` — add a universal receiver events subscription section showing:
    1. Connection/subscription status badges
    2. Data count and list of events (address, type ID, block number)
    3. Error display
    4. Filter input (address filter)
    5. Limit selector

    **Build validation:**
    ```bash
    pnpm build
    ```

    Commit: `feat(10.13-universal-receiver-events): add useUniversalReceiverEventSubscription hook + playground demo`

  </action>
  <verify>
    `pnpm build` — all packages + test app build.
    `grep "useUniversalReceiverEventSubscription" packages/react/dist/index.js` — exported.
    `grep "useUniversalReceiverEventSubscription" apps/test/src/app/subscriptions/page.tsx` — used in playground.
  </verify>
  <done>useUniversalReceiverEventSubscription hook exported from @lsp-indexer/react with full type inference and event-domain block-order desc sort. Universal receiver events subscription demo visible in test app playground at /subscriptions.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes for all packages + test app
- `useUniversalReceiverEventSubscription` is importable from `@lsp-indexer/react`
- Hook accepts { filter?, limit?, enabled?, invalidate?, onData?, onReconnect? }
- Hook returns { data: UniversalReceiverEvent[] | null, isConnected, isSubscribed, error: Error | null }
- All types inferred — no explicit type arguments
- order_by uses block-order desc (event domain)
- Playground shows universal receiver events subscription demo
</verification>

<success_criteria>

1. useUniversalReceiverEventSubscription created with type-safe SubscriptionConfig (extract + parser, zero assertions)
2. Hook exported from @lsp-indexer/react
3. Event domain sort applied: block_number desc, transaction_index desc, log_index desc
4. Universal receiver events subscription demo works in playground
5. All packages build with zero type errors
   </success_criteria>

<output>
After completion, create `.planning/phases/10.13-universal-receiver-events-subscription/10.13-01-SUMMARY.md`
</output>
