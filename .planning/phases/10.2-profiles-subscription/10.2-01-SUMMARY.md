---
phase: 10.2-profiles-subscription
plan: 01
subsystem: subscriptions
tags: [react, websocket, graphql-ws, subscription, profiles, tanstack-query]

# Dependency graph
requires:
  - phase: 10.1-subscription-foundation
    provides: useSubscription hook factory, SubscriptionConfig<TResult,TVariables,TRaw,TParsed>, IndexerSubscriptionProvider
provides:
  - createUseProfileSubscription factory for react + next
  - useProfileSubscription hook in both @lsp-indexer/react and @lsp-indexer/next
  - Profiles subscription playground demo at /subscriptions
  - Factory pattern template for all subsequent domain subscription hooks (10.3–10.13)
affects: [10.3-digital-assets-subscription, 10.4-nfts-subscription, 10.5-owned-assets-subscription]

# Tech tracking
tech-stack:
  added: []
  patterns:
    [domain subscription hook factory (createUseXSubscription + thin wrappers in react/next)]

key-files:
  created:
    [
      packages/react/src/subscriptions/create-use-profile-subscription.ts,
      packages/react/src/subscriptions/profiles.ts,
      packages/next/src/subscriptions/profiles.ts,
    ]
  modified:
    [
      packages/react/src/index.ts,
      packages/next/src/index.ts,
      apps/test/src/app/subscriptions/page.tsx,
    ]

key-decisions:
  - 'createUseProfileSubscription factory pattern — mirrors createUseSubscription, both react and next call factory with their own useSubscription'
  - 'useQueryClient passed as optional factory param — Rules of Hooks compliant, only used when invalidate=true'
  - 'Entity domain uses Hasura default ordering (order_by: undefined)'
  - 'Name filter used for search field (reusing buildProfileWhere name → _ilike pattern)'

patterns-established:
  - 'Domain subscription hook factory: createUseXSubscription(useSubscription, useQueryClient?) in @lsp-indexer/react, thin wrappers in react + next'

requirements-completed: [SUB-02, SUB-03]

# Metrics
duration: 2min
completed: 2026-03-02
---

# Phase 10.2 Plan 01: Profiles Subscription Summary

**createUseProfileSubscription factory + useProfileSubscription in both react/next, playground demo with filter/limit/invalidate controls**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-02T21:35:46Z
- **Completed:** 2026-03-02T21:38:14Z
- **Tasks:** 1
- **Files modified:** 6

## Accomplishments

- Created `createUseProfileSubscription` factory (mirrors `createUseSubscription` pattern) — accepts `useSubscription` + optional `useQueryClient`
- `useProfileSubscription` exported from both `@lsp-indexer/react` and `@lsp-indexer/next` (thin wrappers calling factory with package-specific context)
- `createUseProfileSubscription` exported from `@lsp-indexer/react` for reuse by `@lsp-indexer/next`
- Added interactive profiles subscription playground demo with name filter, limit selector, cache invalidation toggle, connection/subscription status badges, and profile result cards

## Task Commits

Each task was committed atomically:

1. **Task 1: Create useProfileSubscription hook + playground demo + export** - `8b88a03` (feat)

## Files Created/Modified

- `packages/react/src/subscriptions/create-use-profile-subscription.ts` - Factory: createUseProfileSubscription + UseProfileSubscriptionParams + UseSubscriptionFn type
- `packages/react/src/subscriptions/profiles.ts` - Thin wrapper: calls factory with react's useSubscription + useQueryClient
- `packages/next/src/subscriptions/profiles.ts` - Thin wrapper: calls factory with next's useSubscription + useQueryClient
- `packages/react/src/index.ts` - Added factory + hook exports
- `packages/next/src/index.ts` - Added hook export
- `apps/test/src/app/subscriptions/page.tsx` - Profiles subscription demo with filter controls and result display

## Decisions Made

- createUseProfileSubscription factory pattern — mirrors createUseSubscription, both react and next call factory with their own useSubscription
- useQueryClient passed as optional factory param — Rules of Hooks compliant, only used when invalidate=true
- Entity domain uses Hasura default ordering (order_by: undefined) per CONTEXT.md locked decision
- Name filter reuses existing buildProfileWhere name → \_ilike pattern from query service

## Deviations from Plan

- Refactored from direct hook to factory pattern (createUseProfileSubscription) so both @lsp-indexer/react and @lsp-indexer/next can produce useProfileSubscription from a single source

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Profile subscription pattern established — ready for replication across 10.3–10.13 domain subscription sub-phases
- All packages build cleanly (node, react, types, test app)

---

_Phase: 10.2-profiles-subscription_
_Completed: 2026-03-02_
