---
phase: 10.7-followers-subscription
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/react/src/subscriptions/followers.ts
  - packages/react/src/index.ts
  - apps/test/src/app/subscriptions/page.tsx
autonomous: true
requirements: [SUB-02, SUB-03]

must_haves:
  truths:
    - 'Developer can call useFollowerSubscription() and get { data, isConnected, isSubscribed, error }'
    - 'All type params inferred — zero explicit type arguments at call sites'
    - 'Follower subscription data appears in the playground page'
    - 'Cache invalidation works when invalidate: true'
    - 'Results sorted by block_number desc, transaction_index desc, log_index desc (event domain)'
  artifacts:
    - path: 'packages/react/src/subscriptions/followers.ts'
      provides: 'useFollowerSubscription hook'
      contains: 'useFollowerSubscription'
    - path: 'apps/test/src/app/subscriptions/page.tsx'
      provides: 'Followers subscription demo section'
      contains: 'useFollowerSubscription'
  key_links:
    - from: 'packages/react/src/subscriptions/followers.ts'
      to: '@lsp-indexer/node'
      via: 'buildFollowerWhere + followerKeys + parseFollowers + FollowerSubscriptionDocument'
      pattern: 'buildFollowerWhere|followerKeys|parseFollowers|FollowerSubscriptionDocument'
    - from: 'packages/react/src/subscriptions/followers.ts'
      to: 'packages/react/src/subscriptions/use-subscription.ts'
      via: 'useSubscription call'
      pattern: 'useSubscription'
---

<objective>
Create the `useFollowerSubscription` hook and add a working followers subscription demo to the playground page.

Purpose: Domain subscription hook for followers. This is an EVENT domain — results must be sorted by block-order desc (`block_number desc, transaction_index desc, log_index desc`). Follows the profiles pattern (10.2) with the event-domain sort addition.

Output: `useFollowerSubscription` exported from `@lsp-indexer/react`, followers subscription demo visible in the test app.
</objective>

<execution_context>
@/home/coder/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-subscriptions/10-CONTEXT.md
@.planning/phases/10.1-subscription-foundation/10.1-01-SUMMARY.md
@.planning/phases/10.2-profiles-subscription/10.2-01-SUMMARY.md
@packages/node/src/services/followers.ts
@packages/node/src/keys/followers.ts
@packages/node/src/documents/followers.ts
@packages/node/src/parsers/followers.ts
@packages/react/src/index.ts
@apps/test/src/app/subscriptions/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useFollowerSubscription hook + playground demo + export</name>
  <files>
    packages/react/src/subscriptions/followers.ts
    packages/react/src/index.ts
    apps/test/src/app/subscriptions/page.tsx
  </files>
  <action>
    **Create `packages/react/src/subscriptions/followers.ts`:**

    Follow the exact pattern from the 10.2 profiles SUMMARY, with the EVENT domain sort:
    - Import `Follower`, `FollowerFilter` from `@lsp-indexer/types`
    - Import `buildFollowerWhere`, `followerKeys`, `parseFollowers`, `FollowerSubscriptionDocument` from `@lsp-indexer/node`
    - `extract: (result) => result.follower` — Hasura table extraction
    - `parser: (raw) => parseFollowers(raw)`
    - **`order_by: [{ block_number: 'desc' as const }, { transaction_index: 'desc' as const }, { log_index: 'desc' as const }]`** — EVENT domain, block-order desc sort
    - Params interface: `UseFollowerSubscriptionParams` with `{ filter?, limit?, enabled?, invalidate?, onData?, onReconnect? }`
    - `useQueryClient()` called unconditionally (Rules of Hooks), only passed when `invalidate: true`

    **Update `packages/react/src/index.ts`:**

    Add export:
    ```typescript
    export * from './subscriptions/followers';
    ```

    **Add followers demo to playground page:**

    Update `apps/test/src/app/subscriptions/page.tsx` — add a followers subscription section showing:
    1. Connection/subscription status badges
    2. Data count and list of follower addresses
    3. Error display
    4. Filter input (address filter)
    5. Limit selector

    **Build validation:**
    ```bash
    pnpm build
    ```

    Commit: `feat(10.7-followers): add useFollowerSubscription hook + playground demo`

  </action>
  <verify>
    `pnpm build` — all packages + test app build.
    `grep "useFollowerSubscription" packages/react/dist/index.js` — exported.
    `grep "useFollowerSubscription" apps/test/src/app/subscriptions/page.tsx` — used in playground.
  </verify>
  <done>useFollowerSubscription hook exported from @lsp-indexer/react with full type inference and event-domain block-order desc sort. Followers subscription demo visible in test app playground at /subscriptions.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes for all packages + test app
- `useFollowerSubscription` is importable from `@lsp-indexer/react`
- Hook accepts { filter?, limit?, enabled?, invalidate?, onData?, onReconnect? }
- Hook returns { data: Follower[] | null, isConnected, isSubscribed, error: Error | null }
- All types inferred — no explicit type arguments
- order_by uses block-order desc (event domain)
- Playground shows followers subscription demo
</verification>

<success_criteria>

1. useFollowerSubscription created with type-safe SubscriptionConfig (extract + parser, zero assertions)
2. Hook exported from @lsp-indexer/react
3. Event domain sort applied: block_number desc, transaction_index desc, log_index desc
4. Followers subscription demo works in playground
5. All packages build with zero type errors
   </success_criteria>

<output>
After completion, create `.planning/phases/10.7-followers-subscription/10.7-01-SUMMARY.md`
</output>
