---
phase: 10.7-followers-subscription
plan: 01
subsystem: subscriptions
tags: [react, next, websocket, graphql-ws, subscription, followers, factory-pattern, event-domain]

# Dependency graph
requires:
  - phase: 10.6-owned-tokens-subscription
    provides: Factory pattern template, domain subscription replication pattern
  - phase: 10.1-subscription-foundation
    provides: useSubscription hook, SubscriptionConfig<TResult,TVariables,TRaw,TParsed>, IndexerSubscriptionProvider
provides:
  - createUseFollowerSubscription factory for react + next
  - useFollowerSubscription hook in both @lsp-indexer/react and @lsp-indexer/next
  - Follower hooks refactored from monolith to directory pattern (queries + subscription)
  - FollowerSubscriptionDocument + buildFollowerSubscriptionConfig in node service layer
  - Subscription tab in /follows playground page
affects: [10.8-creators-subscription]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Follower domain directory refactoring pattern (single file → directory with queries + subscription)
    - buildFollowerSubscriptionConfig with event domain block-order desc default sort

key-files:
  created:
    - packages/react/src/hooks/factories/followers/create-use-follower-subscription.ts
    - packages/react/src/hooks/factories/followers/index.ts
    - packages/react/src/hooks/followers/use-follower-subscription.ts
    - packages/react/src/hooks/followers/index.ts
    - packages/react/src/hooks/types/followers.ts
    - packages/next/src/hooks/followers/use-follower-subscription.ts
    - packages/next/src/hooks/followers/index.ts
  modified:
    - packages/node/src/documents/followers.ts
    - packages/node/src/services/followers.ts
    - packages/node/src/graphql/gql.ts
    - packages/node/src/graphql/graphql.ts
    - packages/react/src/hooks/factories/index.ts
    - packages/react/src/hooks/types/index.ts
    - packages/react/src/hooks/followers/queries.ts (renamed from followers.ts)
    - packages/next/src/hooks/followers/queries.ts (renamed from followers.ts, fixed import path)
    - apps/test/src/app/follows/page.tsx

key-decisions:
  - 'Converted followers single files to directory modules in both react and next packages'
  - 'Only subscription factory needed — existing query hooks do not have factories in a separate directory'
  - 'Fixed relative import paths after file-to-directory conversion (../actions/ → ../../actions/ in next package)'
  - "Event domain block-order desc default sort: buildFollowerOrderBy(params.sort) ?? buildBlockOrderSort('desc')"

patterns-established:
  - 'Domain subscription replication confirmed for sixth domain (profiles → digital assets → NFTs → owned assets → owned tokens → followers)'

requirements-completed: [SUB-02, SUB-03]

# Metrics
duration: 8min
completed: 2026-03-04
---

# Phase 10.7 Plan 01: Followers Subscription Summary

**useFollowerSubscription with 3-overload type narrowing, event domain block-order desc default sort, and directory refactoring of followers hooks in react/next**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-03-04T20:36:59Z
- **Completed:** 2026-03-04T20:45:50Z
- **Tasks:** 1
- **Files modified:** 16

## Accomplishments

- Added `FollowerSubscriptionDocument` with 26 variables (23 include + 3 pagination) — subscription operation with `id` field for Hasura requirement
- Ran codegen to generate `FollowerSubscriptionSubscription` and `FollowerSubscriptionSubscriptionVariables` types
- Added `buildFollowerSubscriptionConfig` to node service layer with EVENT DOMAIN block-order desc default sort (`buildBlockOrderSort('desc')` fallback)
- Created `createUseFollowerSubscription` factory with 3-overload type narrowing (FollowerResult<I>, Follower, PartialFollower)
- Added `useFollowerSubscription` thin wrappers in both `@lsp-indexer/react` and `@lsp-indexer/next`
- Converted followers single files to directory modules (queries.ts + use-follower-subscription.ts + index.ts)
- Fixed relative import paths after file-to-directory conversion in next package
- Added subscription tab to `/follows` playground with connection status badges, filter/sort/include/invalidate controls including both nested profile sub-includes
- Added `UseFollowerSubscriptionParams` type with proper exports
- All 4 packages + test app build with zero errors

## Task Commits

Each task was committed atomically:

1. **Task 1: Subscription document + codegen + config builder + factory + hooks + directory conversion + playground** - `226c5ec` (feat)

## Files Created/Modified

- `packages/node/src/documents/followers.ts` - Added FollowerSubscriptionDocument (26 variables)
- `packages/node/src/services/followers.ts` - Added buildFollowerSubscriptionConfig with event domain default sort + imports
- `packages/node/src/graphql/gql.ts` - Codegen output (subscription document mapping)
- `packages/node/src/graphql/graphql.ts` - Codegen output (FollowerSubscriptionSubscription type)
- `packages/react/src/hooks/factories/followers/` - Factory + barrel export
- `packages/react/src/hooks/types/followers.ts` - UseFollowerSubscriptionParams interface + query return types
- `packages/react/src/hooks/followers/` - queries.ts (renamed) + subscription wrapper + barrel
- `packages/next/src/hooks/followers/` - queries.ts (renamed, fixed import) + subscription wrapper + barrel
- `packages/react/src/hooks/factories/index.ts` - Added followers barrel export
- `packages/react/src/hooks/types/index.ts` - Added followers type export
- `apps/test/src/app/follows/page.tsx` - Added SubscriptionTab with full controls

## Decisions Made

- Converted followers single files to directory modules (consistent with owned-tokens pattern from 10.6)
- Only created subscription factory — followers does not have query factories in the factories directory
- Fixed relative import paths after file-to-directory conversion (`../actions/` → `../../actions/` in next package)
- Used event domain block-order desc default sort (`buildFollowerOrderBy(params.sort) ?? buildBlockOrderSort('desc')`)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- Initial edit tool applications for imports (lucide icons, package imports) required re-application due to duplicate `oldString` match conflicts — resolved by applying edits sequentially with verified file state
- Pre-existing build failures in `packages/indexer` (unrelated typeorm/abi issues) — worked around by building individual packages

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Followers subscription pattern confirmed — sixth domain replicated from established template
- Ready for 10.8 (Creators Subscription) and remaining domain subscription phases
- All packages build cleanly

## Self-Check: PASSED

- All 7 created files verified present on disk
- Commit `226c5ec` verified in git log
- node, react, next packages + test app all build successfully
- `useFollowerSubscription` exported in react dist
- `useFollowerSubscription` exported in next dist
- `buildFollowerSubscriptionConfig` exported in node dist
- `FollowerSubscriptionDocument` exported in node dist
- Old monolithic files (followers.ts) removed from both react and next packages

---

_Phase: 10.7-followers-subscription_
_Completed: 2026-03-04_
