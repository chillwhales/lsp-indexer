---
phase: 10.7-followers-subscription
verified: 2026-03-04T20:50:59Z
status: passed
score: 7/7 must-haves verified
re_verification: false
---

# Phase 10.7: Followers Subscription Verification Report

**Phase Goal:** Developer can use `useFollowerSubscription` for live follower events with block-order desc sorting.
**Verified:** 2026-03-04T20:50:59Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                              | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                     |
| --- | ------------------------------------------------------------------------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Developer can call useFollowerSubscription() and get { data, isConnected, isSubscribed, error }                    | ✓ VERIFIED | Factory at `create-use-follower-subscription.ts:70` returns `useSubscription(config, options)` which yields `UseSubscriptionReturn<T>` containing `{ data, isConnected, isSubscribed, error }`. Playground at `follows/page.tsx:508` destructures exactly `{ data, isConnected, isSubscribed, error }`.                                                                                                      |
| 2   | All type params inferred — zero explicit type arguments at call sites                                              | ✓ VERIFIED | React wrapper (`use-follower-subscription.ts:12`) and Next.js wrapper (`use-follower-subscription.ts:15`) both call `createUseFollowerSubscription(useSubscription)` with zero type arguments. Playground usage at line 508 also has zero type args. No `as` casts found in factory.                                                                                                                         |
| 3   | Follower subscription tab appears in /follows playground page                                                      | ✓ VERIFIED | Tab registered at `follows/page.tsx:629-633` with value `'subscription'`, label `'Subscription'`, `Radio` icon. `SubscriptionTab` component at lines 502-584 renders connection badges, filter/sort/include controls, invalidate switch, and `ResultsList`.                                                                                                                                                  |
| 4   | Cache invalidation works when invalidate: true                                                                     | ✓ VERIFIED | Factory at lines 71-74: `invalidate` passed to `useSubscription`, `invalidateKeys: invalidate ? [followerKeys.all] : undefined`, `queryClient: invalidate ? queryClient : undefined`. Playground has invalidate `Switch` control at line 534.                                                                                                                                                                |
| 5   | Hook available from both @lsp-indexer/react and @lsp-indexer/next                                                  | ✓ VERIFIED | React: `packages/react/src/hooks/followers/use-follower-subscription.ts` exports hook; barrel chain: `followers/index.ts → hooks/index.ts → package`. Next: `packages/next/src/hooks/followers/use-follower-subscription.ts` exports hook with `'use client'` directive. Both confirmed in dist: `grep "useFollowerSubscription"` found in `packages/react/dist/index.js` and `packages/next/dist/index.js`. |
| 6   | Results default to block-order desc sort (event domain: block_number desc, transaction_index desc, log_index desc) | ✓ VERIFIED | `buildFollowerSubscriptionConfig` at `services/followers.ts:406`: `buildFollowerOrderBy(params.sort) ?? buildBlockOrderSort('desc')`. The `buildBlockOrderSort` import confirmed at line 24. Fallback triggers when no sort provided — event domain default.                                                                                                                                                 |
| 7   | 3-overload type narrowing works: include → FollowerResult\<I\>, no include → Follower, catch-all → PartialFollower | ✓ VERIFIED | Factory has 4 function declarations (3 overloads + 1 implementation). Overload 1 (line 32): `include: I` → `UseSubscriptionReturn<FollowerResult<I>>`. Overload 2 (line 38): `include?: never` → `UseSubscriptionReturn<Follower>`. Overload 3 (line 44): `include?: FollowerInclude` → `UseSubscriptionReturn<PartialFollower>`. Implementation (line 53): `any[]` onData avoids contravariance.            |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact                                                                           | Expected                                                            | Status     | Details                                                                                                                                                                                               |
| ---------------------------------------------------------------------------------- | ------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/node/src/documents/followers.ts`                                         | FollowerSubscriptionDocument (subscription, 28 vars)                | ✓ VERIFIED | Subscription operation at line 189. 28 variables (3 pagination + 25 include). No $offset, no aggregate. Has `id` field for Hasura.                                                                    |
| `packages/node/src/services/followers.ts`                                          | buildFollowerSubscriptionConfig with event domain default sort      | ✓ VERIFIED | Function at line 399. Uses `buildBlockOrderSort('desc')` as fallback. Returns `{ document, variables, extract, parser }`.                                                                             |
| `packages/react/src/hooks/factories/followers/create-use-follower-subscription.ts` | Factory with 3-overload type narrowing                              | ✓ VERIFIED | 81 lines. 3 overloads + implementation. Imports `buildFollowerSubscriptionConfig`, `followerKeys` from node. Zero `as` casts.                                                                         |
| `packages/react/src/hooks/followers/use-follower-subscription.ts`                  | React thin wrapper                                                  | ✓ VERIFIED | 12 lines. `createUseFollowerSubscription(useSubscription)` — single line export.                                                                                                                      |
| `packages/next/src/hooks/followers/use-follower-subscription.ts`                   | Next.js thin wrapper with use client directive                      | ✓ VERIFIED | 15 lines. `'use client'` at line 1. Imports factory from `@lsp-indexer/react`. Uses package-specific `useSubscription`.                                                                               |
| `packages/react/src/hooks/types/followers.ts`                                      | UseFollowerSubscriptionParams interface + query return type aliases | ✓ VERIFIED | 46 lines. Has `UseFollowerSubscriptionParams` interface with filter, sort, limit, include, enabled, invalidate, onReconnect. Also has `UseFollowsReturn` and `UseInfiniteFollowsReturn` type aliases. |
| `apps/test/src/app/follows/page.tsx`                                               | Subscription tab in /follows playground                             | ✓ VERIFIED | `SubscriptionTab` component at line 502. Registered as 5th tab at line 629. Has connection badges, filter rows, sort controls, include sections, invalidate switch, ResultsList with FollowerCard.    |

### Key Link Verification

| From                                  | To                       | Via                                              | Status  | Details                                                                |
| ------------------------------------- | ------------------------ | ------------------------------------------------ | ------- | ---------------------------------------------------------------------- |
| `create-use-follower-subscription.ts` | `@lsp-indexer/node`      | `buildFollowerSubscriptionConfig + followerKeys` | ✓ WIRED | Import at line 14, usage at lines 68 and 73.                           |
| `create-use-follower-subscription.ts` | `use-subscription.ts`    | `UseSubscriptionFn parameter`                    | ✓ WIRED | Type import at line 24, factory parameter at line 31, call at line 70. |
| `services/followers.ts`               | `documents/followers.ts` | `FollowerSubscriptionDocument import`            | ✓ WIRED | Import at line 12, usage at line 410.                                  |
| `next/use-follower-subscription.ts`   | `@lsp-indexer/react`     | `createUseFollowerSubscription factory import`   | ✓ WIRED | Import at line 3, invocation at line 15.                               |
| `services/followers.ts`               | `buildBlockOrderSort`    | `Default event domain sort fallback`             | ✓ WIRED | Import at line 24, usage at lines 277 and 406.                         |

### Barrel Export Chain Verification

| Level              | File                           | Export                                                                      | Status     |
| ------------------ | ------------------------------ | --------------------------------------------------------------------------- | ---------- |
| Factory barrel     | `factories/followers/index.ts` | `export * from './create-use-follower-subscription'`                        | ✓ VERIFIED |
| Factories barrel   | `factories/index.ts`           | `export * from './followers'` (line 9)                                      | ✓ VERIFIED |
| React hooks barrel | `hooks/followers/index.ts`     | `export * from './queries'` + `export * from './use-follower-subscription'` | ✓ VERIFIED |
| Next hooks barrel  | `hooks/followers/index.ts`     | `export * from './queries'` + `export * from './use-follower-subscription'` | ✓ VERIFIED |
| Types barrel       | `types/index.ts`               | `export * from './followers'` (line 2)                                      | ✓ VERIFIED |

### Directory Conversion Verification

| Package              | Old monolith         | New directory                                                             | Status                                                                        |
| -------------------- | -------------------- | ------------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| `@lsp-indexer/react` | `hooks/followers.ts` | `hooks/followers/` (queries.ts + use-follower-subscription.ts + index.ts) | ✓ VERIFIED — old file removed                                                 |
| `@lsp-indexer/next`  | `hooks/followers.ts` | `hooks/followers/` (queries.ts + use-follower-subscription.ts + index.ts) | ✓ VERIFIED — old file removed, import path fixed to `../../actions/followers` |

### Requirements Coverage

| Requirement | Source Plan  | Description                                                                                | Status      | Evidence                                                                                                                                                                                                        |
| ----------- | ------------ | ------------------------------------------------------------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **SUB-02**  | 10.7-01-PLAN | Developer can use `use*Subscription` hooks for all 11 domains (live data via WebSocket)    | ✓ SATISFIED | `useFollowerSubscription` hook created and exported from both react and next packages. Factory follows established pattern. Hook wired through `useSubscription` → `IndexerSubscriptionProvider` → WebSocket.   |
| **SUB-03**  | 10.7-01-PLAN | Subscription updates automatically invalidate/update relevant TanStack Query cache entries | ✓ SATISFIED | `invalidate` param wired through factory (lines 71-74). When `invalidate: true`, passes `invalidateKeys: [followerKeys.all]` and `queryClient` to `useSubscription`. Playground has Switch control for testing. |

No orphaned requirements found — both SUB-02 and SUB-03 are claimed and satisfied.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact                    |
| ---- | ---- | ------- | -------- | ------------------------- |
| —    | —    | —       | —        | No anti-patterns detected |

All key files scanned for TODO/FIXME/PLACEHOLDER/stubs — clean. The `return {}` patterns in `services/followers.ts` (lines 54, 98, 170) are legitimate early returns for empty filter/include objects, not stubs.

### Human Verification Required

#### 1. Live Subscription Data Flow

**Test:** Open `/follows` playground → Subscription tab → verify data appears when WebSocket connects to Hasura
**Expected:** Connection badge shows "Connected", subscription badge shows "Subscribed", follower data renders in list
**Why human:** Requires live WebSocket connection to Hasura instance

#### 2. Cache Invalidation Toggle

**Test:** With Subscription tab active and "Invalidate Cache" switch ON, trigger a follower event → switch to Follows tab
**Expected:** Follows tab data refreshes automatically (stale query re-fetched)
**Why human:** Requires live data mutation + cross-tab cache observation

#### 3. Sort Default Behavior

**Test:** Open Subscription tab with no explicit sort → verify results appear in block-order desc
**Expected:** Newest block events appear first (block_number descending)
**Why human:** Requires live data to verify ordering

### Gaps Summary

No gaps found. All 7 observable truths verified. All 7 artifacts exist, are substantive, and are properly wired. All 5 key links confirmed. Both requirement IDs (SUB-02, SUB-03) satisfied. Build outputs confirmed in all 3 package dist files. Directory conversions complete with old monoliths removed. Zero anti-patterns detected.

**Minor note:** The subscription document has 28 variables (25 include + 3 pagination), not 26 as estimated in the plan. The difference is 2 extra avatar include toggles ($includeFollowerProfileAvatar, $includeFollowedProfileAvatar) that exist in the GetFollowersDocument but were undercounted in the plan. This is correct behavior — the subscription mirrors the query's field selection.

---

_Verified: 2026-03-04T20:50:59Z_
_Verifier: Claude (gsd-verifier)_
