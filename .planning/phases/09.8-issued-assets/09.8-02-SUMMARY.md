---
phase: 09.8-issued-assets
plan: 02
subsystem: api
tags: [typescript, graphql, hasura, lsp12, issued-assets, parser, service, query-keys]

# Dependency graph
requires:
  - phase: 09.8-01
    provides: IssuedAsset types + GetIssuedAssetsDocument + codegen types
  - phase: 09.7-creators
    provides: Creator domain pattern (closest analog — same dual heterogeneous relation structure)
  - phase: 09.6-generic-type-propagation
    provides: 3-overload <const I> generic pattern (DX-05)
provides:
  - parseIssuedAsset/parseIssuedAssets parsers with Hasura→domain field mapping
  - fetchIssuedAssets service function with filter/sort/include translation
  - buildIssuedAssetIncludeVars for nested profile and DA include variable generation
  - issuedAssetKeys query key factory with list/infinite namespaces
  - Full barrel exports from @lsp-indexer/node
affects: [09.8-03-hooks-actions, 09.8-04-playground]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created:
    - packages/node/src/keys/issued-assets.ts
    - packages/node/src/parsers/issued-assets.ts
    - packages/node/src/services/issued-assets.ts
  modified:
    - packages/node/src/index.ts

key-decisions:
  - 'Parser maps Hasura address → issuerAddress (not issuer_address — Hasura field IS address)'
  - 'Parser maps universalProfile → issuerProfile and issuedAsset → digitalAsset (Hasura→domain rename)'
  - 'buildIssuedAssetIncludeVars replaces includeProfile→includeIssuerProfile and include→includeDigitalAsset prefixes'

patterns-established:
  - 'IssuedAsset follows exact Creator service/parser pattern: dual nested relation include builders with prefix replacement'

# Metrics
duration: 5min
completed: 2026-02-23
---

# Phase 9.8 Plan 02: Issued Asset Parsers + Services + Query Keys Summary

**IssuedAsset parser with Hasura→domain field mapping (address→issuerAddress, universalProfile→issuerProfile, issuedAsset→digitalAsset) + fetchIssuedAssets service with 7 filters, 6 sorts, nested include builder, and query key factory**

## Performance

- **Duration:** 5 min
- **Started:** 2026-02-23T20:08:56Z
- **Completed:** 2026-02-23T20:14:08Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Parser maps all 7 fields with correct Hasura→domain name translations, including numeric→number conversion for array_index
- Service function translates all 7 filter fields (3 address/interfaceId with \_ilike + escapeLike, 2 nested name filters via universalProfile/issuedAsset, 2 timestamp with normalizeTimestamp)
- All 6 sort fields use orderDir() with nested sorts defaulting to nulls: 'last' for name sorts
- buildIssuedAssetIncludeVars reuses buildProfileIncludeVars (IssuerProfile prefix) and buildDigitalAssetIncludeVars (DigitalAsset prefix) via hasActiveIncludes
- issuedAssetKeys query key factory with list/infinite namespaces

## Task Commits

Each task was committed atomically:

1. **Task 1: Create query key factory and parser** - `c364251` (feat)
2. **Task 2: Create service function and wire barrel exports** - `733449b` (feat)

## Files Created/Modified

- `packages/node/src/keys/issued-assets.ts` - Query key factory with list/infinite namespaces for cache invalidation
- `packages/node/src/parsers/issued-assets.ts` - Hasura lsp12_issued_asset → IssuedAsset parser with 3-overload generic pattern and stripExcluded
- `packages/node/src/services/issued-assets.ts` - fetchIssuedAssets with filter/sort/include builders, buildIssuedAssetIncludeVars exported
- `packages/node/src/index.ts` - Added all issued asset barrel exports (keys, parsers, services, documents)

## Decisions Made

None - followed plan as specified. All Hasura field name mappings confirmed against codegen types.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Service layer complete — ready for Plan 03 (hooks + server actions + build validation)
- All exports available from `@lsp-indexer/node` for hook/action consumption
- fetchIssuedAssets serves both useIssuedAssets and useInfiniteIssuedAssets hooks
- buildIssuedAssetIncludeVars exported for potential direct use by consumers

## Self-Check: PASSED

---

_Phase: 09.8-issued-assets_
_Completed: 2026-02-23_
