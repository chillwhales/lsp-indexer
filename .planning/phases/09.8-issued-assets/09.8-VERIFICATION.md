---
phase: 09.8-issued-assets
verified: 2026-02-23T20:38:16Z
status: passed
score: 10/10 must-haves verified
---

# Phase 9.8: Issued Assets Verification Report

**Phase Goal:** Developer can use `useIssuedAssets`, `useInfiniteIssuedAssets` for LSP12 issued asset data — querying which digital assets were issued by which addresses/profiles.
**Verified:** 2026-02-23T20:38:16Z
**Status:** ✅ PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                | Status     | Evidence                                                                                                                                                                                                                              |
| --- | -------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | IssuedAsset types exist and are importable from @lsp-indexer/types   | ✓ VERIFIED | `packages/types/src/issued-assets.ts` (233 lines) exports 16 named items; barrel re-export at `packages/types/src/index.ts:7`                                                                                                         |
| 2   | `IssuedAssetResult<I>` conditional type narrowing works (DX-04)      | ✓ VERIFIED | Lines 217-227: `IssuedAssetResult<I>` — undefined→full, `{}`→base only, with `ResolveIssuerProfile<I>` + `ResolveIssuedAssetDigitalAsset<I>` intersection types for nested relations                                                  |
| 3   | All 7 filter fields present                                          | ✓ VERIFIED | `IssuedAssetFilterSchema` (lines 57-72): issuerAddress, assetAddress, interfaceId, issuerName, digitalAssetName, timestampFrom, timestampTo                                                                                           |
| 4   | All 6 sort fields present                                            | ✓ VERIFIED | `IssuedAssetSortFieldSchema` (lines 85-92): timestamp, issuerAddress, assetAddress, arrayIndex, issuerName, digitalAssetName                                                                                                          |
| 5   | Parser maps Hasura fields correctly                                  | ✓ VERIFIED | `parseIssuedAsset` (lines 61-71): `raw.address`→`issuerAddress`, `raw.asset_address`→`assetAddress`, `raw.universalProfile`→`issuerProfile` (via parseProfile), `raw.issuedAsset`→`digitalAsset` (via parseDigitalAsset)              |
| 6   | 2 React hooks + 1 server action + 2 Next.js hooks exist              | ✓ VERIFIED | React: `useIssuedAssets` + `useInfiniteIssuedAssets` (203 lines); Next hooks: same 2 hooks (195 lines) routing through server action; Server action: `getIssuedAssets` (57 lines) with `'use server'` directive                       |
| 7   | All use 3-overload `<const I>` pattern (DX-05)                       | ✓ VERIFIED | Every public function has 3 overload signatures + 1 implementation (4 declarations): react hooks (4 each), next hooks (4 each), server action (4), fetchIssuedAssets (4), parseIssuedAsset (4), parseIssuedAssets (3 — batch wrapper) |
| 8   | Playground page at /issued-assets works with 2 tabs                  | ✓ VERIFIED | `apps/test/src/app/issued-assets/page.tsx` (389 lines) — `ListTab` + `InfiniteTab` with `PlaygroundPageLayout`; `next build` shows `/issued-assets` in route table                                                                    |
| 9   | IssuedAssetCard uses shared components (not inline render functions) | ✓ VERIFIED | `issued-asset-card.tsx` imports `CollapsibleProfileSection` + `CollapsibleDigitalAssetSection` from `@/components/collapsible-sections` (97-line shared component file)                                                               |
| 10  | All 4 packages build and typecheck                                   | ✓ VERIFIED | `@lsp-indexer/types` ✅ `@lsp-indexer/node` ✅ `@lsp-indexer/react` ✅ `@lsp-indexer/next` ✅ — all build with tsup zero errors; `next build` (test app) succeeds with 0 errors                                                       |

**Score:** 10/10 truths verified

### Required Artifacts

| Artifact                                         | Expected                                           | Status     | Details                                                                        |
| ------------------------------------------------ | -------------------------------------------------- | ---------- | ------------------------------------------------------------------------------ |
| `packages/types/src/issued-assets.ts`            | Zod schemas + TS types + conditional include types | ✓ VERIFIED | 233 lines, 16 exports, no stubs, barrel-exported                               |
| `packages/node/src/documents/issued-assets.ts`   | GraphQL document with @include directives          | ✓ VERIFIED | 201 lines, `GetIssuedAssetsDocument` with 35 boolean variables                 |
| `packages/node/src/parsers/issued-assets.ts`     | Parser with Hasura→domain field mapping            | ✓ VERIFIED | 100 lines, 3-overload pattern, correct field mappings                          |
| `packages/node/src/keys/issued-assets.ts`        | TkDodo query key factory                           | ✓ VERIFIED | 59 lines, `all`/`lists`/`list`/`infinites`/`infinite` hierarchy                |
| `packages/node/src/services/issued-assets.ts`    | Service with filter/sort/include builders          | ✓ VERIFIED | 299 lines, all 7 filter fields, all 6 sort fields, include var builder         |
| `packages/react/src/hooks/issued-assets.ts`      | 2 TanStack Query hooks (browser→Hasura)            | ✓ VERIFIED | 203 lines, `useIssuedAssets` + `useInfiniteIssuedAssets`                       |
| `packages/next/src/hooks/issued-assets.ts`       | 2 TanStack Query hooks (via server action)         | ✓ VERIFIED | 195 lines, routes through `getIssuedAssets` action                             |
| `packages/next/src/actions/issued-assets.ts`     | Server action with 'use server'                    | ✓ VERIFIED | 57 lines, `'use server'` directive, `getServerUrl()`                           |
| `apps/test/src/app/issued-assets/page.tsx`       | Playground page with 2 tabs                        | ✓ VERIFIED | 389 lines, ListTab + InfiniteTab, all 7 filters exposed                        |
| `apps/test/src/components/issued-asset-card.tsx` | Card using shared collapsible sections             | ✓ VERIFIED | 102 lines, uses `CollapsibleProfileSection` + `CollapsibleDigitalAssetSection` |

### Key Link Verification

| From                            | To                                         | Via                                       | Status  | Details                                                                                          |
| ------------------------------- | ------------------------------------------ | ----------------------------------------- | ------- | ------------------------------------------------------------------------------------------------ |
| `react/hooks/issued-assets.ts`  | `node/services/issued-assets.ts`           | `fetchIssuedAssets` import                | ✓ WIRED | Direct import + call in queryFn                                                                  |
| `react/hooks/issued-assets.ts`  | `node/keys/issued-assets.ts`               | `issuedAssetKeys` import                  | ✓ WIRED | Used for queryKey in both hooks                                                                  |
| `next/hooks/issued-assets.ts`   | `next/actions/issued-assets.ts`            | `getIssuedAssets` import                  | ✓ WIRED | Server action call in queryFn                                                                    |
| `next/actions/issued-assets.ts` | `node/services/issued-assets.ts`           | `fetchIssuedAssets` import                | ✓ WIRED | Server-side fetch with `getServerUrl()`                                                          |
| `services/issued-assets.ts`     | `documents/issued-assets.ts`               | `GetIssuedAssetsDocument` import          | ✓ WIRED | Used in `execute()` call                                                                         |
| `services/issued-assets.ts`     | `parsers/issued-assets.ts`                 | `parseIssuedAssets` import                | ✓ WIRED | Called on query result                                                                           |
| `services/issued-assets.ts`     | `graphql/graphql.ts`                       | `Lsp12_Issued_Asset_Bool_Exp` type import | ✓ WIRED | Codegen types used in buildWhere/buildOrderBy                                                    |
| `page.tsx`                      | `@lsp-indexer/react` + `@lsp-indexer/next` | Hook imports                              | ✓ WIRED | Both hook packages used with mode switching                                                      |
| `page.tsx`                      | `issued-asset-card.tsx`                    | `<IssuedAssetCard>` component             | ✓ WIRED | Rendered inside ResultsList renderItem                                                           |
| `nav.tsx`                       | `/issued-assets`                           | Navigation link                           | ✓ WIRED | Line 49: `{ href: '/issued-assets', label: 'Issued Assets', icon: FileOutput, available: true }` |

### Requirements Coverage

| Requirement                                                            | Status      | Blocking Issue                                     |
| ---------------------------------------------------------------------- | ----------- | -------------------------------------------------- |
| QUERY-07 — `useIssuedAssets`, `useInfiniteIssuedAssets` for LSP12 data | ✓ SATISFIED | —                                                  |
| PAGE-01 — `useInfinite*` hooks for offset-based infinite scroll        | ✓ SATISFIED | `useInfiniteIssuedAssets` with offset pagination   |
| DX-04 — `IssuedAssetResult<I>` conditional type narrowing              | ✓ SATISFIED | Full implementation with nested relation resolvers |
| DX-05 — 3-overload `<const I>` pattern                                 | ✓ SATISFIED | All hooks, actions, services, parsers use pattern  |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact                 |
| ---- | ---- | ------- | -------- | ---------------------- |
| —    | —    | —       | —        | No anti-patterns found |

Scanned all 10 artifacts for TODO/FIXME/placeholder/stub/empty-return patterns. Only hits were `placeholder` in the playground page's input field placeholder attributes (valid UI usage, not stub indicators).

### Human Verification Required

### 1. Visual Playground Functionality

**Test:** Navigate to `/issued-assets`, enter an issuer address in the filter, verify results appear
**Expected:** Issued asset cards render with issuer→asset arrows, collapsible profile/DA sections expand
**Why human:** Visual rendering and interactive behavior can't be verified structurally

### 2. Infinite Scroll Behavior

**Test:** Switch to Infinite tab, scroll down or click "Load more"
**Expected:** Additional issued assets load and append below existing ones
**Why human:** Infinite scroll pagination behavior requires runtime interaction

### 3. Include Toggle Narrowing

**Test:** Toggle off all include fields except "Timestamp", verify cards show only base fields + timestamp
**Expected:** arrayIndex, interfaceId, issuerProfile, digitalAsset sections disappear; timestamp remains
**Why human:** Runtime type narrowing + conditional rendering requires live observation

### Gaps Summary

No gaps found. All 10 must-haves verified across 3 levels (existence, substantive, wired). All 4 packages build with zero errors. `next build` succeeds with `/issued-assets` route generated. The full vertical slice from types→codegen→parser→service→hooks→server action→playground page is complete and correctly wired.

---

_Verified: 2026-02-23T20:38:16Z_
_Verifier: Claude (gsd-verifier)_
