---
phase: 09.8-issued-assets
plan: 01
subsystem: types
tags: [zod, typescript, graphql, codegen, lsp12, issued-assets, conditional-include]

# Dependency graph
requires:
  - phase: 09.7-creators
    provides: Creator domain pattern (closest analog — same 2 base + 3 scalar + 2 nested relations structure)
  - phase: 09.4-conditional-include-types
    provides: IncludeResult utility type, PartialExcept, ResolveX pattern
provides:
  - IssuedAsset Zod schemas + inferred types with conditional include support
  - GetIssuedAssetsDocument GraphQL document with 31 include variables
  - Codegen output with Lsp12_Issued_Asset_Bool_Exp Hasura types
affects: [09.8-02-parsers-services-keys, 09.8-03-hooks-actions, 09.8-04-playground]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created:
    - packages/types/src/issued-assets.ts
    - packages/node/src/documents/issued-assets.ts
  modified:
    - packages/types/src/index.ts
    - packages/node/src/graphql/graphql.ts
    - packages/node/src/graphql/gql.ts

key-decisions:
  - 'IssuedAsset base fields are issuerAddress and assetAddress (composite natural key)'
  - 'Hasura field universalProfile → domain issuerProfile, Hasura field issuedAsset → domain digitalAsset'
  - '31 include variables (3 scalar + 10 issuerProfile + 18 digitalAsset) matching Creator document pattern'
  - "followed_aggregate used for following count (not following_aggregate which doesn't exist in schema)"

patterns-established:
  - 'IssuedAsset follows exact Creator pattern: 2 base + 3 scalar + 2 nested relation sub-includes'

# Metrics
duration: 5min
completed: 2026-02-23
---

# Phase 9.8 Plan 01: Issued Asset Types + GraphQL Documents + Codegen Summary

**IssuedAsset Zod schemas with conditional include types + GetIssuedAssetsDocument GraphQL document with 31 @include variables for LSP12 issued asset queries**

## Performance

- **Duration:** 5 min
- **Started:** 2026-02-23T19:58:53Z
- **Completed:** 2026-02-23T20:03:24Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- IssuedAssetSchema with 7 fields (2 base + 3 scalar + 2 relations) following Creator pattern
- IssuedAssetResult<I> conditional include type with ResolveIssuerProfile and ResolveIssuedAssetDigitalAsset resolver types
- GetIssuedAssetsDocument with 31 Boolean! include variables (all defaulting to true)
- Codegen output updated with GetIssuedAssetsQuery, Lsp12_Issued_Asset_Bool_Exp, Lsp12_Issued_Asset_Order_By types

## Task Commits

Each task was committed atomically:

1. **Task 1: Create Issued Asset domain types with conditional include types** - `3c50ce6` (feat)
2. **Task 2: Create GraphQL document and run codegen** - `7c467d6` (feat)

## Files Created/Modified

- `packages/types/src/issued-assets.ts` - All IssuedAsset Zod schemas, inferred types, and conditional include types (IssuedAssetResult<I>, ResolveIssuerProfile, ResolveIssuedAssetDigitalAsset)
- `packages/types/src/index.ts` - Added barrel export for issued-assets (alphabetical order)
- `packages/node/src/documents/issued-assets.ts` - GetIssuedAssetsDocument GraphQL document with 31 include variables
- `packages/node/src/graphql/graphql.ts` - Codegen output with GetIssuedAssetsQuery types
- `packages/node/src/graphql/gql.ts` - Codegen document registry updated

## Decisions Made

- **Base fields:** `issuerAddress` and `assetAddress` — composite natural key per CONTEXT.md locked decision
- **Hasura field mapping:** `universalProfile` → `issuerProfile`, `issuedAsset` → `digitalAsset` — parser layer will handle this mapping
- **Profile aggregate field:** Uses `followed_aggregate` for following count (not `following_aggregate` which doesn't exist in Hasura schema) — matching established profile document pattern
- **31 include variables:** Same count as Creator (3 scalar + 10 profile + 18 DA) with `$includeIssuerProfile*` prefix replacing `$includeCreatorProfile*`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed incorrect profile aggregate field name in GraphQL document**

- **Found during:** Task 2 (GraphQL document creation)
- **Issue:** Initial implementation used `following_aggregate` for following count, but Hasura schema uses `followed_aggregate` (the `following_aggregate` field doesn't exist)
- **Fix:** Changed to `followed_aggregate @include(if: $includeIssuerProfileFollowingCount)` matching the established profile document pattern
- **Files modified:** packages/node/src/documents/issued-assets.ts
- **Verification:** Codegen completed successfully, node typecheck passed
- **Committed in:** 7c467d6 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Fixed before commit — codegen wouldn't have succeeded with wrong field name.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Foundation layer complete — types and documents ready for Plan 02 (parsers + services + keys)
- All 16 exports specified in plan's must_haves are available from `@lsp-indexer/types`
- GetIssuedAssetsDocument ready for parser and service consumption
- Codegen types available for type-safe parser implementation

## Self-Check: PASSED

---

_Phase: 09.8-issued-assets_
_Completed: 2026-02-23_
