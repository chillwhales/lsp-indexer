---
phase: 09.1-digital-assets
verified: 2026-03-08T16:12:00Z
status: passed
score: 17/17 must-haves verified
---

# Phase 9.1: Digital Assets Verification Report

**Phase Goal:** Developer can use `useDigitalAsset`, `useDigitalAssets`, `useInfiniteDigitalAssets` for LSP7/LSP8 digital asset metadata.
**Verified:** 2026-03-08
**Status:** ✅ PASSED
**Re-verification:** No — initial verification (gap closure for v1.1 audit)

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | DigitalAsset, DigitalAssetFilter, DigitalAssetSort, DigitalAssetInclude types exist with clean camelCase fields | ✓ VERIFIED | `packages/types/src/digital-assets.ts` lines 26-54 (DigitalAssetSchema with 18 fields), lines 68-95 (DigitalAssetFilterSchema), lines 97 (DigitalAssetSortSchema), lines 99-133 (DigitalAssetIncludeSchema); all fields camelCase: `totalSupply`, `tokenType`, `holderCount`, `creatorCount` |
| 2  | DigitalAssetResult<I> narrows return type based on include parameter with ResolveStandard | ✓ VERIFIED | `packages/types/src/digital-assets.ts` lines 232-236 — uses `IncludeResult<DigitalAsset, 'address', DigitalAssetIncludeFieldMap, I> & ResolveStandard<NonNullable<I>>` for standard↔decimals derived field handling |
| 3  | ResolveStandard<I> type conditionally includes `standard` when `decimals` is included | ✓ VERIFIED | `packages/types/src/digital-assets.ts` line 211 — `type ResolveStandard<I> = I extends { decimals: true } ? { standard: Standard | null } : {}` |
| 4  | Digital asset types are re-exported from @lsp-indexer/types via barrel export | ✓ VERIFIED | `packages/types/src/index.ts` line 4 — `export * from './digital-assets'` |
| 5  | GraphQL documents for digital asset queries exist with 17 @include directives | ✓ VERIFIED | `packages/node/src/documents/digital-assets.ts` — `GetDigitalAssetDocument` (lines 4-25, 17 Boolean include variables), `GetDigitalAssetsDocument` (302 lines total) |
| 6  | parseDigitalAsset derives `standard` field from `decimals` (3-state logic) | ✓ VERIFIED | `packages/node/src/parsers/digital-assets.ts` lines 35-38 — `undefined` = not included → null, `null` = LSP8, value present = LSP7 |
| 7  | parseDigitalAsset supports include parameter with stripExcluded and derivedFields map | ✓ VERIFIED | `packages/node/src/parsers/digital-assets.ts` lines 22-26 (overloads) + line 73 — calls `stripExcluded(result, include, ['address'], { standard: 'decimals' })` |
| 8  | parseDigitalAsset maps Hasura tokenType strings to TOKEN/NFT/COLLECTION enum | ✓ VERIFIED | `packages/node/src/parsers/digital-assets.ts` lines 16-19 — `mapTokenType` validates raw string → `TokenType | null` |
| 9  | fetchDigitalAsset (detail) service function exists with 3-overload generic pattern | ✓ VERIFIED | `packages/node/src/services/digital-assets.ts` lines 186-213 — 3 overloads: no-include → `DigitalAsset | null`, `<const I>` → `DigitalAssetResult<I> | null`, catch-all → `PartialDigitalAsset | null` |
| 10 | fetchDigitalAssets (list) service function exists with pagination and FetchDigitalAssetsResult | ✓ VERIFIED | `packages/node/src/services/digital-assets.ts` lines 221-282 — accepts filter, sort, limit, offset; returns `FetchDigitalAssetsResult<P>` with digitalAssets + totalCount |
| 11 | digitalAssetKeys cache key factory uses hierarchical TkDodo pattern | ✓ VERIFIED | `packages/node/src/keys/digital-assets.ts` lines 15-37 — `all`, `details()`, `detail(addr, include)`, `lists()`, `list(f,s,l,o,i)`, `infinites()`, `infinite(f,s,i)` |
| 12 | useDigitalAsset, useDigitalAssets, useInfiniteDigitalAssets hooks exist in @lsp-indexer/react | ✓ VERIFIED | `packages/react/src/hooks/digital-assets/use-digital-asset.ts` (7 lines), `use-digital-assets.ts` (7 lines), `use-infinite-digital-assets.ts` (7 lines); all exported via `packages/react/src/hooks/digital-assets/index.ts` |
| 13 | React hooks use factory pattern with fetchDigitalAsset/fetchDigitalAssets service functions | ✓ VERIFIED | `packages/react/src/hooks/digital-assets/use-digital-asset.ts` line 5 — `createUseDigitalAsset((params) => fetchDigitalAsset(getClientUrl(), params))`; same pattern for useDigitalAssets and useInfiniteDigitalAssets |
| 14 | Server actions with 'use server' directive exist for digital assets | ✓ VERIFIED | `packages/next/src/actions/digital-assets.ts` line 1 — `'use server'`; exports `getDigitalAsset` (lines 22-37) and `getDigitalAssets` (lines 40-69) with Zod input validation |
| 15 | Next.js hooks exist routing through server actions | ✓ VERIFIED | `packages/next/src/hooks/digital-assets/` — 5 files: use-digital-asset.ts, use-digital-assets.ts, use-infinite-digital-assets.ts, use-digital-asset-subscription.ts, index.ts |
| 16 | Digital assets playground page exists at apps/test | ✓ VERIFIED | `apps/test/src/app/digital-assets/page.tsx` — 451 lines |
| 17 | All address filters use _ilike + escapeLike for case-insensitive matching | ✓ VERIFIED | `packages/node/src/services/digital-assets.ts` line 205 — `address: { _ilike: escapeLike(params.address) }` in fetchDigitalAsset; filter builders use `_ilike` with `escapeLike` throughout (lines 36, 44, 60, 69, 78) |

**Score:** 17/17 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/types/src/digital-assets.ts` | DigitalAsset domain types + conditional include types | ✓ VERIFIED | 244 lines, 11 types exported (DigitalAsset, DigitalAssetFilter, DigitalAssetSort, DigitalAssetSortField, DigitalAssetInclude, Standard, TokenType, DigitalAssetOwner, + 3 param types), DigitalAssetResult<I> with ResolveStandard |
| `packages/node/src/documents/digital-assets.ts` | GraphQL documents with @include directives | ✓ VERIFIED | 302 lines, 3 documents (GetDigitalAsset, GetDigitalAssets, DigitalAssetSubscription), 17 @include directives each |
| `packages/node/src/parsers/digital-assets.ts` | Parser with camelCase transform + standard derivation + stripExcluded | ✓ VERIFIED | 88 lines, overloaded parseDigitalAsset + parseDigitalAssets, standard 3-state derivation |
| `packages/node/src/services/digital-assets.ts` | Service functions + internal builders | ✓ VERIFIED | 282 lines, fetchDigitalAsset/fetchDigitalAssets + buildDigitalAssetWhere/OrderBy/IncludeVars |
| `packages/node/src/keys/digital-assets.ts` | Cache key factory with hierarchical keys | ✓ VERIFIED | 37 lines, 7 key methods (all, details, detail, lists, list, infinites, infinite) |
| `packages/react/src/hooks/digital-assets/` | 3 query hooks + subscription hook | ✓ VERIFIED | 5 files: use-digital-asset.ts, use-digital-assets.ts, use-infinite-digital-assets.ts, use-digital-asset-subscription.ts, index.ts |
| `packages/next/src/actions/digital-assets.ts` | Server actions with 'use server' | ✓ VERIFIED | 69 lines, 2 functions (getDigitalAsset, getDigitalAssets), 'use server' directive, Zod input validation |
| `packages/next/src/hooks/digital-assets/` | Next.js hooks via server actions | ✓ VERIFIED | 5 files: use-digital-asset.ts, use-digital-assets.ts, use-infinite-digital-assets.ts, use-digital-asset-subscription.ts, index.ts |
| `apps/test/src/app/digital-assets/page.tsx` | Playground page with query tabs | ✓ VERIFIED | 451 lines, imports from both @lsp-indexer/react and @lsp-indexer/next |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| React hooks | Node services | `import { fetchDigitalAsset, getClientUrl } from '@lsp-indexer/node'` | ✓ WIRED | All 3 query hooks import fetch functions via factory pattern |
| Next.js hooks | Server actions | `import { getDigitalAsset } from '../../actions/digital-assets'` | ✓ WIRED | All 3 Next.js hooks route through server actions via factory |
| Server actions | Node services | `import { fetchDigitalAsset, fetchDigitalAssets, getServerUrl } from '@lsp-indexer/node'` | ✓ WIRED | Both server actions delegate to Node service functions |
| Services | Documents | `import { GetDigitalAssetDocument, GetDigitalAssetsDocument } from '../documents/digital-assets'` | ✓ WIRED | Both service functions use correct documents via `execute()` |
| Services | Parsers | `import { parseDigitalAsset, parseDigitalAssets } from '../parsers/digital-assets'` | ✓ WIRED | fetchDigitalAsset calls parseDigitalAsset, fetchDigitalAssets calls parseDigitalAssets |
| Parsers | stripExcluded | `import { stripExcluded } from './strip'` | ✓ WIRED | parseDigitalAsset calls stripExcluded with derivedFields map `{ standard: 'decimals' }` |
| Parser | Standard derivation | `raw.decimals !== undefined ? (raw.decimals !== null ? 'LSP7' : 'LSP8') : null` | ✓ WIRED | Standard derived from decimals presence — decimals undefined → null, null → LSP8, value → LSP7 |
| Playground page | React hooks | `import { useDigitalAsset, useDigitalAssets, useInfiniteDigitalAssets } from '@lsp-indexer/react'` | ✓ WIRED | All hooks imported and used in tab components |
| Playground page | Next.js hooks | `import { useDigitalAsset as useDigitalAssetNext, ... } from '@lsp-indexer/next'` | ✓ WIRED | All hooks imported with aliases for server mode |
| Node index | Keys export | `export * from './keys/digital-assets'` | ✓ WIRED | digitalAssetKeys exported from @lsp-indexer/node (line 39) |

### Requirements Coverage

| Requirement | Status | Notes |
|-------------|--------|-------|
| QUERY-02: useDigitalAsset, useDigitalAssets, useInfiniteDigitalAssets for Digital Asset data | ✓ SATISFIED | All 3 hooks exist in both @lsp-indexer/react and @lsp-indexer/next; full end-to-end architecture with standard derivation and 17-field conditional include |
| PAGE-01: useInfinite* hooks for offset-based infinite scroll (contribution) | ✓ SATISFIED | useInfiniteDigitalAssets exists in both @lsp-indexer/react and @lsp-indexer/next |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | No stub patterns, TODOs, FIXMEs, or placeholder content found | — | — |

### Human Verification Required

### 1. Playground Page Visual Correctness

**Test:** Navigate to `/digital-assets`, enter a token contract address, verify that digital asset metadata renders correctly
**Expected:** Token name, symbol, standard (LSP7/LSP8), total supply, and metadata display correctly
**Why human:** Visual rendering + real API data cannot be verified programmatically

### 2. Infinite Scroll Pagination

**Test:** In the "Infinite Digital Assets" tab, search for digital assets and click "Load more"
**Expected:** Additional pages load and append to the list
**Why human:** Requires real Hasura data + interactive scroll behavior

### 3. Standard Derivation Correctness

**Test:** Look up both an LSP7 (fungible) and LSP8 (non-fungible) token
**Expected:** LSP7 shows decimals field with a value, LSP8 shows decimals as null; standard field correctly shows LSP7 or LSP8
**Why human:** Requires real token data to verify derivation logic

---

_Verified: 2026-03-08_
_Verifier: Claude (gsd-executor)_
