---
phase: 09.9-encrypted-assets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/types/src/encrypted-assets.ts
  - packages/types/src/index.ts
  - packages/node/src/documents/encrypted-assets.ts
  - packages/node/src/graphql/graphql.ts
  - packages/node/src/graphql/gql.ts
autonomous: true

must_haves:
  truths:
    - 'EncryptedAsset, EncryptedAssetFilter, EncryptedAssetSort, EncryptedAssetInclude types exist and are importable from @lsp-indexer/types'
    - 'EncryptedAssetResult<I> narrows return type based on include parameter — base fields (address, contentId, revision) always present, all other fields conditional'
    - 'EncryptedAssetInclude has encryption nested include: boolean OR { accessControlConditions?: boolean }'
    - 'EncryptedAssetInclude has universalProfile (ProfileIncludeSchema) sub-include'
    - 'Sub-types exist: EncryptedAssetEncryption, AccessControlCondition, EncryptedAssetFile, EncryptedAssetChunks, EncryptedAssetImage'
    - 'All 8 filter fields from CONTEXT.md are present: address, universalProfileName, contentId, revision, encryptionMethod, fileType, fileSize, timestamp'
    - 'GraphQL document for encrypted asset list exists with @include directives for all togglable fields and relations'
    - 'Codegen output includes Lsp29_Encrypted_Asset_Bool_Exp Hasura type'
  artifacts:
    - path: 'packages/types/src/encrypted-assets.ts'
      provides: 'All EncryptedAsset domain Zod schemas + inferred types + conditional include types + sub-type schemas'
      exports:
        [
          'AccessControlConditionSchema',
          'AccessControlCondition',
          'EncryptedAssetEncryptionSchema',
          'EncryptedAssetEncryption',
          'EncryptedAssetEncryptionIncludeSchema',
          'EncryptedAssetEncryptionInclude',
          'EncryptedAssetFileSchema',
          'EncryptedAssetFile',
          'EncryptedAssetChunksSchema',
          'EncryptedAssetChunks',
          'EncryptedAssetImageSchema',
          'EncryptedAssetImage',
          'EncryptedAssetSchema',
          'EncryptedAsset',
          'EncryptedAssetFilterSchema',
          'EncryptedAssetFilter',
          'EncryptedAssetSortSchema',
          'EncryptedAssetSort',
          'EncryptedAssetSortFieldSchema',
          'EncryptedAssetSortField',
          'EncryptedAssetIncludeSchema',
          'EncryptedAssetInclude',
          'UseEncryptedAssetsParamsSchema',
          'UseEncryptedAssetsParams',
          'UseInfiniteEncryptedAssetsParamsSchema',
          'UseInfiniteEncryptedAssetsParams',
          'EncryptedAssetResult',
          'PartialEncryptedAsset',
        ]
    - path: 'packages/node/src/documents/encrypted-assets.ts'
      provides: 'GraphQL document string for encrypted asset list query'
      exports: ['GetEncryptedAssetsDocument']
    - path: 'packages/node/src/graphql/graphql.ts'
      provides: 'Codegen output with encrypted asset Hasura types'
      contains: 'Lsp29_Encrypted_Asset_Bool_Exp'
  key_links:
    - from: 'packages/node/src/documents/encrypted-assets.ts'
      to: 'packages/node/src/graphql/gql.ts'
      via: 'graphql() tagged template function'
      pattern: "graphql\\("
    - from: 'packages/types/src/index.ts'
      to: 'packages/types/src/encrypted-assets.ts'
      via: 'barrel re-export'
      pattern: "from './encrypted-assets'"
    - from: 'packages/types/src/encrypted-assets.ts'
      to: 'packages/types/src/profiles.ts'
      via: 'ProfileIncludeSchema import for nested universalProfile includes'
      pattern: "from './profiles'"
    - from: 'packages/types/src/encrypted-assets.ts'
      to: 'packages/types/src/include-types.ts'
      via: 'IncludeResult and PartialExcept imports'
      pattern: "from './include-types'"
---

<objective>
Create Encrypted Asset domain types (Zod schemas + sub-type schemas + inferred TS types + conditional include types) and GraphQL document — the foundation layer for Phase 9.9.

Purpose: The `lsp29_encrypted_asset` Hasura table represents LSP29 encrypted asset metadata with rich nested structure (encryption with access control conditions, file metadata, chunks, image arrays, title/description wrappers). This domain has 2 hooks (useEncryptedAssets, useInfiniteEncryptedAssets). No singular hook — no reliable natural key exists (user-introduced elements can share address + contentId + revision). One nested profile relation (universalProfile) plus 6 relation-typed includes (title, description, encryption, file, chunks, images). Encryption has a sub-include for accessControlConditions.

Output: `packages/types/src/encrypted-assets.ts` with all Zod schemas, sub-type schemas, inferred types, and conditional include types. `packages/node/src/documents/encrypted-assets.ts` with GraphQL document string. Regenerated codegen output.
</objective>

<execution_context>
@/home/coder/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.9-encrypted-assets/09.9-CONTEXT.md
@packages/types/src/common.ts
@packages/types/src/creators.ts
@packages/types/src/issued-assets.ts
@packages/types/src/profiles.ts
@packages/types/src/include-types.ts
@packages/types/src/index.ts
@packages/node/src/documents/creators.ts
@packages/node/src/documents/issued-assets.ts
@packages/node/src/documents/profiles.ts
@packages/node/src/graphql/gql.ts
@packages/node/codegen.ts
@packages/node/schema.graphql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Encrypted Asset sub-type schemas and domain types with conditional include types</name>
  <files>
    packages/types/src/encrypted-assets.ts
    packages/types/src/index.ts
  </files>
  <action>
**Create `packages/types/src/encrypted-assets.ts`** — this domain is significantly more complex than creators/issued-assets due to 6 relation-typed includes, encryption sub-include, and wrapper flattening.

**Hasura schema reference for `lsp29_encrypted_asset` (schema.graphql lines 3884–3956):**

```
address: String!               → address (UP address, base field)
array_index: numeric           → arrayIndex (nullable, includable scalar)
chunks: ...chunks              → chunks (object relation, boolean include)
content_id: String             → contentId (nullable, base field)
description: ...description    → description (object relation with .value, flattened to string|null)
encryption: ...encryption      → encryption (object relation, nested include)
file: ...file                  → file (object relation, boolean include)
images: [...image!]!           → images (array relation, boolean include)
revision: Int                  → revision (nullable, base field)
timestamp: timestamptz!        → timestamp (includable scalar)
title: ...title                → title (object relation with .value, flattened to string|null)
universalProfile: universal_profile → universalProfile (ProfileInclude sub-narrowing)
```

Fields NOT exposed per CONTEXT.md: `createdAt`, `rawValue`, `url`, `version`, `isDataFetched`, `retryCount`, `decodeError`, `fetchErrorCode`, `fetchErrorMessage`, `fetchErrorStatus`, `universalProfileId`, `id`.

---

**Part 1: Sub-type schemas** — define nested object schemas first.

```typescript
import { z } from 'zod';

import { SortDirectionSchema, SortNullsSchema } from './common';
import type { IncludeResult, PartialExcept } from './include-types';
import {
  ProfileIncludeSchema,
  ProfileSchema,
  type ProfileInclude,
  type ProfileResult,
} from './profiles';
```

**AccessControlConditionSchema** — from `lsp29_access_control_condition` (schema.graphql lines 3537–3553):

```typescript
export const AccessControlConditionSchema = z.object({
  /** Blockchain chain identifier */
  chain: z.string().nullable(),
  /** Comparison operator */
  comparator: z.string().nullable(),
  /** Position in conditions array */
  conditionIndex: z.number(),
  /** Contract address for the condition */
  contractAddress: z.string().nullable(),
  /** Follower address (for social conditions) */
  followerAddress: z.string().nullable(),
  /** Access control method */
  method: z.string().nullable(),
  /** Raw condition JSON string */
  rawCondition: z.string(),
  /** Standard contract type (ERC20, ERC721, etc.) */
  standardContractType: z.string().nullable(),
  /** Token ID for NFT conditions */
  tokenId: z.string().nullable(),
  /** Threshold value */
  value: z.string().nullable(),
});
```

**EncryptedAssetEncryptionSchema** — from `lsp29_encrypted_asset_encryption` (schema.graphql lines 4344–4390):

```typescript
export const EncryptedAssetEncryptionSchema = z.object({
  /** Encrypted ciphertext */
  ciphertext: z.string().nullable(),
  /** Hash of original data */
  dataToEncryptHash: z.string().nullable(),
  /** Decryption code */
  decryptionCode: z.string().nullable(),
  /** Decryption parameters (JSON string) */
  decryptionParams: z.string().nullable(),
  /** Encryption method identifier */
  method: z.string().nullable(),
  /** Access control conditions array (null = not included or empty) */
  accessControlConditions: z.array(AccessControlConditionSchema).nullable(),
});
```

**EncryptedAssetFileSchema** — from `lsp29_encrypted_asset_file` (schema.graphql lines 4797–4808):

```typescript
export const EncryptedAssetFileSchema = z.object({
  /** File hash */
  hash: z.string().nullable(),
  /** Last modified timestamp (numeric → number) */
  lastModified: z.number().nullable(),
  /** File name */
  name: z.string().nullable(),
  /** File size in bytes (numeric → number) */
  size: z.number().nullable(),
  /** MIME type */
  type: z.string().nullable(),
});
```

**EncryptedAssetChunksSchema** — from `lsp29_encrypted_asset_chunks` (schema.graphql lines 4082–4091):

```typescript
export const EncryptedAssetChunksSchema = z.object({
  /** IPFS content identifiers for each chunk */
  cids: z.array(z.string()).nullable(),
  /** Initialization vector */
  iv: z.string().nullable(),
  /** Total size across all chunks (numeric → number) */
  totalSize: z.number().nullable(),
});
```

**EncryptedAssetImageSchema** — from `lsp29_encrypted_asset_image` (schema.graphql lines 4987–5000). **NOTE:** LSP29 images differ from LSP4 images — they have `verificationSource` and `imageIndex` which LSP4 images don't have:

```typescript
export const EncryptedAssetImageSchema = z.object({
  /** Position in images array */
  imageIndex: z.number(),
  /** Image URL */
  url: z.string().nullable(),
  /** Image width in pixels */
  width: z.number().nullable(),
  /** Image height in pixels */
  height: z.number().nullable(),
  /** Verification data hash */
  verificationData: z.string().nullable(),
  /** Verification method */
  verificationMethod: z.string().nullable(),
  /** Verification source URL */
  verificationSource: z.string().nullable(),
});
```

---

**Part 2: Core domain schema**

```typescript
export const EncryptedAssetSchema = z.object({
  /** Universal Profile address that owns this encrypted asset (always present) */
  address: z.string(),
  /** Content identifier (always present, nullable — may not be set) */
  contentId: z.string().nullable(),
  /** Revision number (always present, nullable — may not be set) */
  revision: z.number().nullable(),
  /** Position in array (null = not included or not set) */
  arrayIndex: z.number().nullable(),
  /** Timestamp when indexed (ISO string) */
  timestamp: z.string(),
  /** Title text, flattened from title.value wrapper (null = not included or not set) */
  title: z.string().nullable(),
  /** Description text, flattened from description.value wrapper (null = not included or not set) */
  description: z.string().nullable(),
  /** Encryption details with optional access control conditions (null = not included) */
  encryption: EncryptedAssetEncryptionSchema.nullable(),
  /** File metadata (null = not included) */
  file: EncryptedAssetFileSchema.nullable(),
  /** Chunk information (null = not included) */
  chunks: EncryptedAssetChunksSchema.nullable(),
  /** Image array (null = not included, [] = included but empty) */
  images: z.array(EncryptedAssetImageSchema).nullable(),
  /** Universal Profile of the owner (null = not included in query) */
  universalProfile: ProfileSchema.nullable(),
});
```

---

**Part 3: Filter schema** — all 8 filter fields from CONTEXT.md:

```typescript
export const EncryptedAssetFilterSchema = z.object({
  /** Case-insensitive match on UP address (uses _ilike) */
  address: z.string().optional(),
  /** Case-insensitive search on profile name (nested universalProfile → lsp3Profile → name) */
  universalProfileName: z.string().optional(),
  /** Case-insensitive match on content ID */
  contentId: z.string().optional(),
  /** Exact match on revision number */
  revision: z.number().optional(),
  /** Case-insensitive match on encryption method (nested encryption.method) */
  encryptionMethod: z.string().optional(),
  /** Case-insensitive match on file type (nested file.type) */
  fileType: z.string().optional(),
  /** Numeric filter on file size (nested file.size) — entries with file.size >= this value */
  fileSize: z.number().optional(),
  /** ISO timestamp or unix seconds — entries with timestamp >= this value */
  timestamp: z.union([z.string(), z.number()]).optional(),
});
```

---

**Part 4: Sort schema** — Claude's discretion on sort fields per CONTEXT.md. Reasonable sort fields:

```typescript
export const EncryptedAssetSortFieldSchema = z.enum([
  'timestamp',
  'address',
  'contentId',
  'revision',
  'arrayIndex',
]);
```

```typescript
export const EncryptedAssetSortSchema = z.object({
  field: EncryptedAssetSortFieldSchema,
  direction: SortDirectionSchema,
  nulls: SortNullsSchema.optional(),
});
```

---

**Part 5: Include schema** — the most complex include of any domain due to encryption sub-include:

```typescript
/** Sub-include for encryption — controls whether access control conditions array is fetched */
export const EncryptedAssetEncryptionIncludeSchema = z.object({
  accessControlConditions: z.boolean().optional(),
});

export const EncryptedAssetIncludeSchema = z.object({
  /** Include array index */
  arrayIndex: z.boolean().optional(),
  /** Include timestamp */
  timestamp: z.boolean().optional(),
  /** Include title text (flattened from title.value) */
  title: z.boolean().optional(),
  /** Include description text (flattened from description.value) */
  description: z.boolean().optional(),
  /**
   * Include encryption details.
   * - `true` → fetch all encryption fields INCLUDING accessControlConditions
   * - `{ accessControlConditions: true }` → same as true
   * - `{ accessControlConditions: false }` or `{}` → fetch encryption scalars only, NO access control conditions
   * - `false` / omitted → don't fetch encryption at all
   */
  encryption: z.union([z.boolean(), EncryptedAssetEncryptionIncludeSchema]).optional(),
  /** Include file metadata */
  file: z.boolean().optional(),
  /** Include chunks data */
  chunks: z.boolean().optional(),
  /** Include images array */
  images: z.boolean().optional(),
  /** Include Universal Profile — sub-fields control which profile attributes to fetch */
  universalProfile: ProfileIncludeSchema.optional(),
});
```

---

**Part 6: Hook param schemas** — standard pattern:

```typescript
export const UseEncryptedAssetsParamsSchema = z.object({
  filter: EncryptedAssetFilterSchema.optional(),
  sort: EncryptedAssetSortSchema.optional(),
  limit: z.number().optional(),
  offset: z.number().optional(),
  include: EncryptedAssetIncludeSchema.optional(),
});

export const UseInfiniteEncryptedAssetsParamsSchema = z.object({
  filter: EncryptedAssetFilterSchema.optional(),
  sort: EncryptedAssetSortSchema.optional(),
  pageSize: z.number().optional(),
  include: EncryptedAssetIncludeSchema.optional(),
});
```

---

**Part 7: Inferred types:**

```typescript
export type AccessControlCondition = z.infer<typeof AccessControlConditionSchema>;
export type EncryptedAssetEncryption = z.infer<typeof EncryptedAssetEncryptionSchema>;
export type EncryptedAssetEncryptionInclude = z.infer<typeof EncryptedAssetEncryptionIncludeSchema>;
export type EncryptedAssetFile = z.infer<typeof EncryptedAssetFileSchema>;
export type EncryptedAssetChunks = z.infer<typeof EncryptedAssetChunksSchema>;
export type EncryptedAssetImage = z.infer<typeof EncryptedAssetImageSchema>;
export type EncryptedAsset = z.infer<typeof EncryptedAssetSchema>;
export type EncryptedAssetFilter = z.infer<typeof EncryptedAssetFilterSchema>;
export type EncryptedAssetSortField = z.infer<typeof EncryptedAssetSortFieldSchema>;
export type EncryptedAssetSort = z.infer<typeof EncryptedAssetSortSchema>;
export type EncryptedAssetInclude = z.infer<typeof EncryptedAssetIncludeSchema>;
export type UseEncryptedAssetsParams = z.infer<typeof UseEncryptedAssetsParamsSchema>;
export type UseInfiniteEncryptedAssetsParams = z.infer<
  typeof UseInfiniteEncryptedAssetsParamsSchema
>;
```

---

**Part 8: Conditional include types (DX-04)** — this domain has a unique pattern because:

1. Simple boolean includes (arrayIndex, timestamp, title, description, file, chunks, images) → go in scalar field map
2. Encryption include (boolean OR object) → resolver type that handles both forms
3. universalProfile → ProfileResult resolver type (standard)

```typescript
/**
 * Scalar/boolean include fields: include key → EncryptedAsset field name.
 * Relations with sub-includes (encryption, universalProfile) handled by resolver types.
 */
type EncryptedAssetScalarIncludeFieldMap = {
  arrayIndex: 'arrayIndex';
  timestamp: 'timestamp';
  title: 'title';
  description: 'description';
  file: 'file';
  chunks: 'chunks';
  images: 'images';
};
```

**ResolveEncryption** — handles `encryption: true` (full) vs `encryption: { accessControlConditions?: boolean }` (object):

```typescript
/**
 * Base encryption type WITHOUT accessControlConditions.
 * Used when encryption is included as an object without accessControlConditions: true.
 */
type EncryptedAssetEncryptionBase = Omit<EncryptedAssetEncryption, 'accessControlConditions'>;

/**
 * Resolve encryption based on include parameter.
 * - true → full EncryptedAssetEncryption (with accessControlConditions)
 * - { accessControlConditions: true } → full EncryptedAssetEncryption
 * - { accessControlConditions: false } or {} → base only (no accessControlConditions)
 * - false/omitted → field absent from type
 */
type ResolveEncryption<I> = I extends { encryption: infer E }
  ? E extends true
    ? { encryption: EncryptedAssetEncryption | null }
    : E extends { accessControlConditions: true }
      ? { encryption: EncryptedAssetEncryption | null }
      : E extends Record<string, unknown>
        ? { encryption: EncryptedAssetEncryptionBase | null }
        : {}
  : {};
```

**ResolveUniversalProfile** — standard pattern from other domains:

```typescript
type ResolveUniversalProfile<I> = I extends { universalProfile: infer P }
  ? P extends ProfileInclude
    ? { universalProfile: ProfileResult<P> | null }
    : {}
  : {};
```

**EncryptedAssetResult** — intersection of IncludeResult + resolver types:

```typescript
export type EncryptedAssetResult<I extends EncryptedAssetInclude | undefined = undefined> =
  I extends undefined
    ? EncryptedAsset
    : IncludeResult<
        EncryptedAsset,
        'address' | 'contentId' | 'revision',
        EncryptedAssetScalarIncludeFieldMap,
        I
      > &
        ResolveEncryption<NonNullable<I>> &
        ResolveUniversalProfile<NonNullable<I>>;

export type PartialEncryptedAsset = PartialExcept<
  EncryptedAsset,
  'address' | 'contentId' | 'revision'
>;
```

---

**Update `packages/types/src/index.ts`** — add barrel export in alphabetical order (before `errors`):

```typescript
export * from './encrypted-assets';
```

  </action>
  <verify>

1. `pnpm --filter @lsp-indexer/types typecheck` passes
2. `pnpm --filter @lsp-indexer/types build` passes
3. All 5 sub-type schemas exist (AccessControlCondition, Encryption, File, Chunks, Image)
4. All 8 filter fields from CONTEXT.md present in EncryptedAssetFilterSchema
5. EncryptedAssetInclude has `encryption: z.union([z.boolean(), EncryptedAssetEncryptionIncludeSchema]).optional()`
6. EncryptedAssetInclude has `universalProfile: ProfileIncludeSchema.optional()`
7. EncryptedAssetResult uses IncludeResult with base fields 'address' | 'contentId' | 'revision'
8. ResolveEncryption handles all 4 cases (true, {acc:true}, {acc:false}/{}, absent)
9. ResolveUniversalProfile follows standard pattern
10. All schemas and types exported from index.ts
    </verify>
    <done>Encrypted Asset domain types exist with all schemas, 5 sub-type schemas, inferred types, and conditional include types. EncryptedAssetSchema has 12 fields (3 base + 2 scalar + 7 relations). EncryptedAssetFilterSchema has 8 fields. EncryptedAssetIncludeSchema has 2 scalar + 5 boolean relation + 1 nested encryption + 1 ProfileInclude sub-narrowing. EncryptedAssetResult narrows by include param with ResolveEncryption and ResolveUniversalProfile. All exported from index.ts.</done>
    </task>

<task type="auto">
  <name>Task 2: Create GraphQL document and run codegen</name>
  <files>
    packages/node/src/documents/encrypted-assets.ts
    packages/node/src/graphql/graphql.ts
    packages/node/src/graphql/gql.ts
  </files>
  <action>
Create the GraphQL document using the `graphql()` tag from `../graphql`, following established patterns.

**Document: `GetEncryptedAssetsDocument`** — list with filtering, sorting, pagination + total count.

This document is more complex than Creator/IssuedAsset documents because it has more relations with @include directives and nested sub-selections.

**Variables:**

```graphql
# Pagination + filtering
$where: lsp29_encrypted_asset_bool_exp
$order_by: [lsp29_encrypted_asset_order_by!]
$limit: Int
$offset: Int

# Scalar includes (2)
$includeArrayIndex: Boolean! = true
$includeTimestamp: Boolean! = true

# Relation boolean includes (5 — title, description, file, chunks, images)
$includeTitle: Boolean! = true
$includeDescription: Boolean! = true
$includeFile: Boolean! = true
$includeChunks: Boolean! = true
$includeImages: Boolean! = true

# Encryption includes (2 — top-level + access control conditions sub-toggle)
$includeEncryption: Boolean! = true
$includeEncryptionAccessControlConditions: Boolean! = true

# Universal Profile includes (10 = 1 top-level + 9 sub-fields)
$includeUniversalProfile: Boolean! = true
$includeUniversalProfileName: Boolean! = true
$includeUniversalProfileDescription: Boolean! = true
$includeUniversalProfileTags: Boolean! = true
$includeUniversalProfileLinks: Boolean! = true
$includeUniversalProfileAvatar: Boolean! = true
$includeUniversalProfileImage: Boolean! = true
$includeUniversalProfileBackgroundImage: Boolean! = true
$includeUniversalProfileFollowerCount: Boolean! = true
$includeUniversalProfileFollowingCount: Boolean! = true
```

Total: 4 + 2 + 5 + 2 + 10 = **23 variables**

**Query body:**

```graphql
query GetEncryptedAssets(
  $where: lsp29_encrypted_asset_bool_exp
  $order_by: [lsp29_encrypted_asset_order_by!]
  $limit: Int
  $offset: Int
) # ... all include variables above
{
  lsp29_encrypted_asset(where: $where, order_by: $order_by, limit: $limit, offset: $offset) {
    # Base fields — always fetched
    address
    content_id
    revision

    # Include-toggled scalars
    array_index @include(if: $includeArrayIndex)
    timestamp @include(if: $includeTimestamp)

    # Title — object relation, select only .value
    title @include(if: $includeTitle) {
      value
    }

    # Description — object relation, select only .value
    description @include(if: $includeDescription) {
      value
    }

    # Encryption — object relation with sub-include for access control conditions
    encryption @include(if: $includeEncryption) {
      ciphertext
      data_to_encrypt_hash
      decryption_code
      decryption_params
      method
      accessControlConditions @include(if: $includeEncryptionAccessControlConditions) {
        chain
        comparator
        condition_index
        contract_address
        follower_address
        method
        raw_condition
        standard_contract_type
        token_id
        value
      }
    }

    # File — object relation, all fields
    file @include(if: $includeFile) {
      hash
      last_modified
      name
      size
      type
    }

    # Chunks — object relation, all fields
    chunks @include(if: $includeChunks) {
      cids
      iv
      total_size
    }

    # Images — array relation, all fields
    images @include(if: $includeImages) {
      image_index
      url
      width
      height
      verification_data
      verification_method
      verification_source
    }

    # Universal Profile — same sub-field pattern as other domains
    universalProfile @include(if: $includeUniversalProfile) {
      address
      lsp3Profile {
        name @include(if: $includeUniversalProfileName) {
          value
        }
        description @include(if: $includeUniversalProfileDescription) {
          value
        }
        tags @include(if: $includeUniversalProfileTags)
        links @include(if: $includeUniversalProfileLinks) {
          title
          url
        }
        avatar @include(if: $includeUniversalProfileAvatar) {
          url
          file_type
          verification_method
          verification_data
        }
        profileImage @include(if: $includeUniversalProfileImage) {
          url
          width
          height
          verification_method
          verification_data
        }
        backgroundImage @include(if: $includeUniversalProfileBackgroundImage) {
          url
          width
          height
          verification_method
          verification_data
        }
      }
      followedBy_aggregate @include(if: $includeUniversalProfileFollowerCount) {
        aggregate {
          count
        }
      }
      following_aggregate @include(if: $includeUniversalProfileFollowingCount) {
        aggregate {
          count
        }
      }
    }
  }

  lsp29_encrypted_asset_aggregate(where: $where) {
    aggregate {
      count
    }
  }
}
```

**NOTE:** The profile sub-field variable prefix is `$includeUniversalProfile*` (NOT `$includeProfile*`) because the Hasura relation name is `universalProfile`. Copy profile sub-field selections from `documents/issued-assets.ts` or `documents/creators.ts` and replace `$includeIssuerProfile*` / `$includeCreatorProfile*` with `$includeUniversalProfile*`.

**NOTE:** Do NOT select `id` fields from sub-types (encryption, file, chunks, images, accessControlConditions) — those are opaque Hasura IDs not needed by the parser.

**NOTE:** The `following_aggregate` field name for universalProfile — check if this profile uses `following_aggregate` or `followed_aggregate` for the "following count". Look at the profiles document for reference. Use the standard pattern from `documents/profiles.ts`.

---

**Run codegen:**

```bash
cd packages/node && pnpm codegen
```

Verify codegen output contains `GetEncryptedAssetsQuery`, `Lsp29_Encrypted_Asset_Bool_Exp`, `Lsp29_Encrypted_Asset_Order_By` types.
</action>
<verify>

1. `pnpm codegen` completes without errors (in packages/node)
2. `packages/node/src/graphql/graphql.ts` contains `GetEncryptedAssetsQuery` type
3. `pnpm --filter @lsp-indexer/node typecheck` passes
4. GetEncryptedAssetsDocument has @include directives with Boolean! = true defaults for all 19 include variables (excluding 4 pagination vars)
5. Title and description sub-selections only fetch `.value` (not `.id`)
6. Encryption has nested @include for accessControlConditions
7. Access control conditions select all 10 user-facing fields (no id, no encryption_id, no lsp29_encrypted_asset_id)
8. File, chunks, images sub-selections exclude `id` and back-reference fields
9. Universal Profile sub-fields use `$includeUniversalProfile*` prefix
10. Both `lsp29_encrypted_asset` query and `lsp29_encrypted_asset_aggregate` present
    </verify>
    <done>GraphQL document exists: GetEncryptedAssetsDocument (list with 23 variables including 19 include toggles) using the lsp29_encrypted_asset Hasura table. All 7 relation sub-selections present with correct @include gating. Encryption has nested sub-include for accessControlConditions. Codegen output reflects new document.</done>
    </task>

</tasks>

<verification>
1. `pnpm --filter @lsp-indexer/types typecheck` passes
2. `pnpm --filter @lsp-indexer/types build` passes
3. `pnpm codegen` succeeds in packages/node
4. `pnpm --filter @lsp-indexer/node typecheck` passes
5. `packages/types/src/encrypted-assets.ts` exports all schemas, sub-types, types, and conditional include types
6. `packages/types/src/index.ts` re-exports all encrypted asset schemas/types
7. `packages/node/src/documents/encrypted-assets.ts` exports GetEncryptedAssetsDocument
8. Codegen graphql.ts contains generated types for document
9. EncryptedAssetInclude supports encryption as boolean OR nested object
10. EncryptedAssetResult narrowing uses ResolveEncryption and ResolveUniversalProfile intersection types
</verification>

<success_criteria>

- Encrypted Asset types file follows Zod-first pattern with conditional include types from the start (DX-04)
- Base fields are address, contentId, revision (3 base fields per CONTEXT.md locked decision)
- All 5 sub-type schemas defined (AccessControlCondition, Encryption, File, Chunks, Image)
- EncryptedAssetInclude has encryption as union type (boolean | EncryptionInclude)
- EncryptedAssetResult narrows correctly: base always present, all other fields conditional
- All 8 filter fields from CONTEXT.md present
- Domain name is "EncryptedAsset" everywhere per CONTEXT.md locked decision
- No singular hook — per CONTEXT.md locked decision
- GraphQL document uses lsp29_encrypted_asset root query
- Codegen runs successfully
  </success_criteria>

<output>
After completion, create `.planning/phases/09.9-encrypted-assets/09.9-01-SUMMARY.md`
</output>
