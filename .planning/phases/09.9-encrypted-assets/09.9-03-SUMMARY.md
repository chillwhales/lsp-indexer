---
phase: 09.9-encrypted-assets
plan: 03
subsystem: api
tags: [hooks, server-action, tanstack-query, encrypted-assets, lsp29, react, next, use-server]

# Dependency graph
requires:
  - phase: 09.9-02
    provides: fetchEncryptedAssets service, encryptedAssetKeys factory, FetchEncryptedAssetsResult type
  - phase: 09.6
    provides: 3-overload <const I> generic pattern (DX-05)
provides:
  - useEncryptedAssets React hook (paginated list, browser → Hasura)
  - useInfiniteEncryptedAssets React hook (infinite scroll, browser → Hasura)
  - getEncryptedAssets Next.js server action (browser → server → Hasura)
  - useEncryptedAssets Next.js hook (routes through server action)
  - useInfiniteEncryptedAssets Next.js hook (routes through server action)
affects: [09.9-04 playground]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Encrypted asset hooks follow exact same pattern as creators/issued assets (2 hooks, no singular, no count)

key-files:
  created:
    - packages/react/src/hooks/encrypted-assets.ts
    - packages/next/src/actions/encrypted-assets.ts
    - packages/next/src/hooks/encrypted-assets.ts
  modified:
    - packages/react/src/index.ts
    - packages/next/src/index.ts

key-decisions:
  - 'No singular hook — encrypted assets have no reliable natural key (same pattern as creators/issued assets)'
  - 'Server action uses fetchEncryptedAssets(url, params) passthrough — no branching needed'

patterns-established:
  - 'Encrypted asset hooks follow established 2-hook pattern (list + infinite) with 3-overload generics'

# Metrics
duration: 3min
completed: 2026-02-24
---

# Phase 09.9 Plan 03: Encrypted Asset Hooks + Server Action + Build Validation Summary

**2 React hooks + 1 server action + 2 Next.js hooks for LSP29 encrypted asset domain with 3-overload `<const I>` generic pattern, encryption sub-include propagation, and all 4 packages building cleanly**

## Performance

- **Duration:** 3 min
- **Started:** 2026-02-24T16:10:17Z
- **Completed:** 2026-02-24T16:14:05Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Created 2 React hooks (useEncryptedAssets, useInfiniteEncryptedAssets) querying Hasura directly via getClientUrl()
- Created 1 Next.js server action (getEncryptedAssets) with 'use server' directive routing through getServerUrl()
- Created 2 Next.js hooks routing through server action for client-safe GraphQL endpoint hiding
- All hooks use 3-overload `<const I>` generic pattern (DX-05) supporting include parameter with encryption sub-include
- All 4 packages build and typecheck with zero errors

## Task Commits

Each task was committed atomically:

1. **Task 1: Create React hooks and Next.js server action + hooks** - `9bf3d1d` (feat)
2. **Task 2: Wire barrel exports and validate builds** - `1af9f69` (feat)

## Files Created/Modified

- `packages/react/src/hooks/encrypted-assets.ts` — 2 TanStack Query hooks for encrypted asset domain (browser → Hasura directly)
- `packages/next/src/actions/encrypted-assets.ts` — Server action wrapping fetchEncryptedAssets with getServerUrl()
- `packages/next/src/hooks/encrypted-assets.ts` — 2 TanStack Query hooks routing through server action
- `packages/react/src/index.ts` — Added encrypted-assets barrel export
- `packages/next/src/index.ts` — Added encrypted-assets action + hooks barrel exports

## Decisions Made

- **No singular hook:** Encrypted assets have no reliable natural key (address + contentId + revision can be shared) — same pattern as creators and issued assets. Developers query by filter.
- **Server action passthrough:** getEncryptedAssets delegates directly to fetchEncryptedAssets(url, params) — no branching needed, overloads handle type narrowing.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Hooks and server action complete — ready for 09.9-04-PLAN.md (playground page + E2E verification)
- All exports available from @lsp-indexer/react and @lsp-indexer/next
- No blockers or concerns

## Self-Check: PASSED

---

_Phase: 09.9-encrypted-assets_
_Completed: 2026-02-24_
