---
phase: 13-indexer-v1-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/indexer/ (DELETE v1)
  - packages/indexer-v2/ (RENAME to packages/indexer/)
  - packages/indexer/package.json
  - docker/v1/ (DELETE)
  - docker/v2/docker-compose.yml (MOVE to docker/)
  - docker/v2/Dockerfile (MOVE to docker/)
  - docker/v2/docker-v2.sh (MOVE+RENAME to docker/manage.sh)
  - docker/v2/entrypoint.sh (MOVE to docker/)
  - docker/README.md
  - package.json
  - eslint.config.ts
  - .github/workflows/ci.yml
  - .env.example
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03, CLEAN-04]

must_haves:
  truths:
    - 'Only one packages/indexer/ directory exists — no packages/indexer-v2/'
    - 'No docker/v1/ directory exists — only one Docker setup at docker/'
    - '`pnpm start` runs the indexer (no start:v2 script exists)'
    - 'pnpm --filter=@chillwhales/indexer build succeeds'
    - "No 'indexer-v2' string appears in operational config files"
  artifacts:
    - path: 'packages/indexer/package.json'
      provides: 'Canonical indexer package'
      contains: '@chillwhales/indexer'
    - path: 'docker/docker-compose.yml'
      provides: 'Docker compose for indexer'
      contains: 'indexer:'
    - path: 'docker/Dockerfile'
      provides: 'Multi-stage Docker build'
      contains: 'packages/indexer/'
    - path: 'docker/manage.sh'
      provides: 'Docker management script'
    - path: 'docker/entrypoint.sh'
      provides: 'Container entrypoint'
      contains: 'packages/indexer'
  key_links:
    - from: 'package.json'
      to: 'packages/indexer/package.json'
      via: 'pnpm --filter=@chillwhales/indexer start'
      pattern: '@chillwhales/indexer'
    - from: 'docker/Dockerfile'
      to: 'packages/indexer/'
      via: 'COPY and build commands'
      pattern: 'packages/indexer'
    - from: 'eslint.config.ts'
      to: 'packages/indexer/tsconfig.eslint.json'
      via: 'parserOptions.project'
      pattern: 'packages/indexer/tsconfig.eslint.json'
    - from: '.github/workflows/ci.yml'
      to: 'packages/indexer/package.json'
      via: 'pnpm --filter=@chillwhales/indexer build'
      pattern: '@chillwhales/indexer'
---

<objective>
Delete all v1 indexer code, rename v2 to canonical, promote Docker files, and update all configs so the repo has a single indexer with zero "v2" suffixes.

Purpose: Eliminate all v1 remnants and "v2" naming so the repo reads as if v1 never existed. This is the structural cleanup that all subsequent documentation updates depend on.
Output: Single packages/indexer/ with @chillwhales/indexer name, flat docker/ directory, updated root configs and CI.
</objective>

<execution_context>
@/home/coder/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-indexer-v1-cleanup/13-CONTEXT.md

<interfaces>
<!-- Current state of files that will be modified. Executor uses these directly. -->

Root package.json scripts (lines 17-20 change):

```json
"migration:generate": ". ./env.sh && pnpm --filter=@chillwhales/typeorm migration:generate",
"migration:apply": ". ./env.sh && pnpm --filter=@chillwhales/typeorm migration:apply",
"start": ". ./env.sh && pnpm --filter=@chillwhales/indexer start",
"start:v2": ". ./env.sh && pnpm --filter=@chillwhales/indexer-v2 start",
```

Note: `. ./env.sh` sourcing is dead — env.sh only existed at docker/v1/env.sh (not root).
Indexer-v2 uses dotenv internally (src/constants/index.ts). Drop all `. ./env.sh &&` prefixes.

eslint.config.ts ignores + parserOptions (lines 19-20, 48):

```typescript
// Legacy v1 indexer — read-only reference, not actively maintained
'packages/indexer/',
// ...
'./packages/indexer-v2/tsconfig.eslint.json',
```

.github/workflows/ci.yml (lines 5, 7, 63, 89-90):

```yaml
branches: [main, refactor/indexer-v2]
# ...
# (v1 indexer is legacy/read-only and excluded)
# ...
- name: Build indexer-v2
  run: pnpm --filter=@chillwhales/indexer-v2 build
```

packages/indexer-v2/package.json (line 2, 4):

```json
"name": "@chillwhales/indexer-v2",
"description": "LUKSO blockchain indexer v2 — plugin architecture",
```

.env.example (lines 62, 125-129):

```
# --- Logging Configuration (Indexer V2) ---
# ...
# Legacy v1 compatibility (not needed for v2)
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete v1 package, rename v2 → canonical indexer</name>
  <files>
    packages/indexer/ (DELETE entire v1 directory)
    packages/indexer-v2/ (RENAME to packages/indexer/)
    packages/indexer/package.json (EDIT after rename)
  </files>
  <action>
    1. Delete the entire v1 indexer package:
       ```bash
       rm -rf packages/indexer/
       ```

    2. Rename the v2 package directory to take the canonical name. Use `git mv` to preserve git history:
       ```bash
       git mv packages/indexer-v2/ packages/indexer/
       ```

    3. Edit `packages/indexer/package.json`:
       - Change `"name": "@chillwhales/indexer-v2"` → `"name": "@chillwhales/indexer"`
       - Change `"description": "LUKSO blockchain indexer v2 — plugin architecture"` → `"description": "LUKSO blockchain indexer — plugin architecture"`

    4. Search inside `packages/indexer/src/` for any "v2" references in source code (class names, logger labels, error messages). The researcher found only 1 occurrence: `v2.archive.subsquid.io` in constants — this is Subsquid's URL, NOT our versioning. Leave it as-is.

    NOTE: Do NOT rename any env var names — per CONTEXT.md, env vars stay unchanged to avoid breaking deployments. Only package names, directory names, and internal code labels change.

  </action>
  <verify>
    <automated>ls packages/ | grep -E "^indexer$" && ! ls packages/ | grep "indexer-v2" && grep '"name": "@chillwhales/indexer"' packages/indexer/package.json</automated>
  </verify>
  <done>
    - packages/indexer/ contains the v2 indexer code (plugin architecture, pino logging, vitest tests)
    - No packages/indexer-v2/ directory exists
    - package.json name is @chillwhales/indexer
    - No "v2" in package description
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete v1 Docker, promote v2 Docker to flat docker/</name>
  <files>
    docker/v1/ (DELETE entire directory)
    docker/v2/docker-compose.yml (MOVE to docker/docker-compose.yml)
    docker/v2/Dockerfile (MOVE to docker/Dockerfile)
    docker/v2/docker-v2.sh (MOVE+RENAME to docker/manage.sh)
    docker/v2/entrypoint.sh (MOVE to docker/entrypoint.sh)
    docker/README.md (REWRITE)
  </files>
  <action>
    1. Delete the v1 Docker directory:
       ```bash
       rm -rf docker/v1/
       ```

    2. Move v2 files up to docker/ root, renaming the management script:
       ```bash
       git mv docker/v2/docker-compose.yml docker/docker-compose.yml
       git mv docker/v2/Dockerfile docker/Dockerfile
       git mv docker/v2/docker-v2.sh docker/manage.sh
       git mv docker/v2/entrypoint.sh docker/entrypoint.sh
       rmdir docker/v2/
       ```

    3. Update `docker/docker-compose.yml` — replace ALL v2 references (10 occurrences):
       - Comments: "Docker Compose for indexer-v2" → "Docker Compose for indexer"
       - Comments: `logs -f indexer-v2` → `logs -f indexer`
       - Comments: `cd docker/v2` → `cd docker`
       - Service name: `indexer-v2:` → `indexer:`
       - Build context: `dockerfile: ./docker/v2/Dockerfile` → `dockerfile: ./docker/Dockerfile`
       - Cache image: `lsp-indexer-v2:latest` → `lsp-indexer:latest`
       - Image: `image: lsp-indexer-v2:latest` → `image: lsp-indexer:latest`
       - Container name: `container_name: lsp-indexer-v2` → `container_name: lsp-indexer`
       - Volume mount: `indexer-logs:/app/packages/indexer-v2/logs` → `indexer-logs:/app/packages/indexer/logs`
       - Log dir env: `LOG_DIR: /app/packages/indexer-v2/logs` → `LOG_DIR: /app/packages/indexer/logs`

    4. Update `docker/Dockerfile` — replace ALL v2 references (14 occurrences):
       - Comment: "Multi-stage Dockerfile for indexer-v2" → "Multi-stage Dockerfile for indexer"
       - Remove the line that copies `packages/indexer/package.json` for v1 (line ~29) — only copy the canonical indexer package.json
       - Remove the line that copies `packages/indexer/node_modules` for v1 (line ~49) — only copy canonical
       - All `packages/indexer-v2/` paths → `packages/indexer/`
       - Build command: `pnpm --filter=@chillwhales/indexer-v2 build` → `pnpm --filter=@chillwhales/indexer build`
       - Log dir: `packages/indexer-v2/logs` → `packages/indexer/logs`

    5. Update `docker/manage.sh` (formerly docker-v2.sh) — replace ALL 36 v2 references:
       - All `docker-v2.sh` self-references → `manage.sh`
       - Default service: `SERVICE="${1:-indexer-v2}"` → `SERVICE="${1:-indexer}"`
       - Container names: `lsp-indexer-v2` → `lsp-indexer`
       - Compose file references: `docker/v2/docker-compose.yml` → `docker/docker-compose.yml`
       - Help text: update all examples and descriptions
       - Any path references from `docker/v2/` → `docker/`

    6. Update `docker/entrypoint.sh` — replace 2 v2 references:
       - Comment: "Navigate to indexer-v2 and start" → "Navigate to indexer and start"
       - Path: `cd /app/packages/indexer-v2` → `cd /app/packages/indexer`

    7. Rewrite `docker/README.md`:
       - Remove ALL v1/v2 comparison content
       - Document only the current Docker setup (compose, manage.sh, Dockerfile, entrypoint)
       - Keep it concise — quick start and reference, no version history
       - Drop "v2" from all service names and descriptions

    Per CONTEXT.md: "Update all internal references (build contexts, volume mounts, env file paths) in the same pass as the file promotion — no broken intermediate state."

  </action>
  <verify>
    <automated>! test -d docker/v1 && ! test -d docker/v2 && test -f docker/docker-compose.yml && test -f docker/Dockerfile && test -f docker/manage.sh && test -f docker/entrypoint.sh && ! grep -r "indexer-v2" docker/ --include="*.yml" --include="*.sh" --include="Dockerfile" | grep -v ".planning"</automated>
  </verify>
  <done>
    - No docker/v1/ or docker/v2/ directories exist
    - docker/docker-compose.yml, docker/Dockerfile, docker/manage.sh, docker/entrypoint.sh exist at docker/ root
    - Zero occurrences of "indexer-v2" in any Docker file
    - Service name is "indexer", image is "lsp-indexer:latest", container is "lsp-indexer"
    - Paths reference packages/indexer/ (not packages/indexer-v2/)
  </done>
</task>

<task type="auto">
  <name>Task 3: Update root configs, CI, and build validation</name>
  <files>
    package.json
    eslint.config.ts
    .github/workflows/ci.yml
    .env.example
  </files>
  <action>
    1. Update root `package.json` scripts:
       - Remove line 19 (`"start"` pointing to v1 `@chillwhales/indexer`)
       - Change line 20: `"start:v2": ". ./env.sh && pnpm --filter=@chillwhales/indexer-v2 start"` → `"start": "pnpm --filter=@chillwhales/indexer start"`
       - Drop the `. ./env.sh &&` prefix entirely — the v2 indexer loads env vars via `dotenv` in `src/constants/index.ts` (`dotenvSetup({ path: path.resolve(__dirname, '../../../../.env') })`), so shell-level env sourcing is unnecessary. The `env.sh` file only existed at `docker/v1/env.sh` (deleted in Task 2) and was never at root.
       - Also drop `. ./env.sh &&` from the `migration:generate` and `migration:apply` scripts for the same reason — env.sh doesn't exist at root.
       - Result: `"start"` points to canonical `@chillwhales/indexer`, no `"start:v2"` exists, no broken env.sh sourcing
       - Check for any other v2-suffixed scripts (dev:v2, build:v2, test:v2) and promote similarly. Based on research, only `start:v2` exists.

    2. Update `eslint.config.ts`:
       - Remove lines 19-20 (the v1 ignore entry):
         ```typescript
         // Legacy v1 indexer — read-only reference, not actively maintained
         'packages/indexer/',
         ```
         IMPORTANT: After rename, `packages/indexer/` is the ACTIVE v2 code that SHOULD be linted. Removing this ignore entry is correct — the active indexer should be linted.
       - Update line 48: `'./packages/indexer-v2/tsconfig.eslint.json'` → `'./packages/indexer/tsconfig.eslint.json'`

    3. Update `.github/workflows/ci.yml`:
       - Lines 5, 7: Remove `refactor/indexer-v2` from branch triggers (or keep if branch still active — check git branch). If the branch is the current working branch for v1.1 work, keep it. Otherwise remove.
       - Line 63: Remove comment `# (v1 indexer is legacy/read-only and excluded)` — there is no v1 anymore
       - Lines 89-90: Change step name `"Build indexer-v2"` → `"Build indexer"` and command `pnpm --filter=@chillwhales/indexer-v2 build` → `pnpm --filter=@chillwhales/indexer build`

    4. Update `.env.example`:
       - Line 62: `# --- Logging Configuration (Indexer V2) ---` → `# --- Logging Configuration ---`
       - Lines 125-129: Remove the `# Legacy v1 compatibility (not needed for v2)` comment. Keep the env vars themselves (PG_DATABASE_URL, HASURA_GRAPHQL_DATABASE_URL, etc.) — they may still be needed by Hasura. Just remove the v1/v2 commentary.

    5. Build validation:
       ```bash
       pnpm --filter=@chillwhales/indexer build
       ```
       This proves the renamed package builds correctly with the new name.

  </action>
  <verify>
    <automated>grep '"start":' package.json | grep "@chillwhales/indexer" && ! grep "start:v2" package.json && ! grep "indexer-v2" eslint.config.ts && ! grep "indexer-v2" .github/workflows/ci.yml && pnpm --filter=@chillwhales/indexer build</automated>
  </verify>
  <done>
    - `pnpm start` filter points to @chillwhales/indexer (not @chillwhales/indexer-v2)
    - No start:v2 script exists in root package.json
    - eslint.config.ts references packages/indexer/tsconfig.eslint.json and no longer ignores packages/indexer/
    - CI workflow builds @chillwhales/indexer
    - .env.example has no v1/v2 commentary
    - `pnpm --filter=@chillwhales/indexer build` succeeds
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
1. `ls packages/` shows: abi, comparison-tool, indexer, next, node, react, typeorm, types — no indexer-v2
2. `ls docker/` shows: docker-compose.yml, Dockerfile, entrypoint.sh, manage.sh, README.md — no v1/ or v2/ subdirectories
3. `grep -r "indexer-v2" package.json eslint.config.ts .github/workflows/ci.yml .env.example` returns zero matches
4. `pnpm --filter=@chillwhales/indexer build` succeeds
5. `grep -r "indexer-v2" docker/ --include="*.yml" --include="*.sh" --include="Dockerfile"` returns zero matches
</verification>

<success_criteria>

- packages/indexer/ contains the v2 indexer code — no packages/indexer-v2/ exists
- docker/ has flat structure with promoted v2 files — no v1/ or v2/ subdirectories
- pnpm start runs the canonical indexer via @chillwhales/indexer filter
- pnpm build for the indexer succeeds with the new package name
- Zero "indexer-v2" references in operational config files (package.json, eslint, CI, docker, .env.example)
  </success_criteria>

<output>
After completion, create `.planning/phases/13-indexer-v1-cleanup/13-01-SUMMARY.md`
</output>
