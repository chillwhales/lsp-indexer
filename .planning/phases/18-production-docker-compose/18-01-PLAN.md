---
phase: 18-production-docker-compose
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/docker-compose.prod.yml
  - docker/.env.prod.example
  - docker/README.md
autonomous: true
requirements:
  - DOCK-01
  - DOCK-02
  - DOCK-03

must_haves:
  truths:
    - "Production compose file uses ghcr.io/chillwhales/lsp-indexer image (not build)"
    - "All 4 services present in prod compose: postgres, indexer, hasura, data-connector-agent"
    - "Required vars (POSTGRES_PASSWORD, HASURA_GRAPHQL_ADMIN_SECRET, RPC_URL) fail loudly if missing"
    - "Dev compose file (docker/docker-compose.yml) is not modified"
    - ".env.prod.example documents all required and optional variables"
  artifacts:
    - path: "docker/docker-compose.prod.yml"
      provides: "Production compose with released Docker image"
      contains: "ghcr.io/chillwhales/lsp-indexer"
    - path: "docker/.env.prod.example"
      provides: "Production environment variable template"
      contains: "POSTGRES_PASSWORD"
  key_links:
    - from: "docker/docker-compose.prod.yml"
      to: "ghcr.io/chillwhales/lsp-indexer"
      via: "image directive on indexer service"
      pattern: "image:.*ghcr.io/chillwhales/lsp-indexer"
    - from: "docker/docker-compose.prod.yml"
      to: "docker/.env.prod.example"
      via: "env var references match template"
      pattern: "POSTGRES_PASSWORD.*:?"
---

<objective>
Create a production Docker Compose file that pulls the released indexer image from ghcr.io and runs the full stack (PostgreSQL + Hasura + indexer) with production-hardened defaults.

Purpose: Enable anyone to run the indexer in production without building from source — just configure env vars and `docker compose up`.
Output: `docker/docker-compose.prod.yml`, `docker/.env.prod.example`, updated `docker/README.md`
</objective>

<execution_context>
@/home/coder/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-production-docker-compose/18-RESEARCH.md

<!-- Key existing files the executor needs to reference -->
@docker/docker-compose.yml
@docker/entrypoint.sh
@docker/.env.prod.example
@.env.example

<interfaces>
<!-- Existing docker-compose.yml structure — prod compose mirrors this with targeted changes -->

From docker/docker-compose.yml:
- 4 services: postgres, indexer, hasura, data-connector-agent
- postgres: image postgres:17-alpine, health check with pg_isready, port 5432 exposed, resource limits 2G
- indexer: build from source (context: .., dockerfile: ./docker/Dockerfile), depends_on postgres healthy, health check with pgrep, resource limits 4G
- hasura: image hasura/graphql-engine:v2.46.0, depends_on postgres + data-connector healthy, port 8080, DEV_MODE=true, ENABLE_CONSOLE=true
- data-connector-agent: image hasura/graphql-data-connector:v2.46.0, health check on /api/v1/athena/health
- 2 volumes: postgres-data, indexer-logs
- All env vars use ${VAR:-default} syntax (dev defaults)

From docker/entrypoint.sh:
- Runs migrations (pnpm migration:generate + migration:apply)
- Waits for Hasura /healthz (up to 60s)
- Applies Hasura metadata (pnpm hasura:generate + hasura:apply)
- Starts indexer (pnpm start:simple)
- This is baked into the Docker image — no changes needed for prod
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create production compose file and env template</name>
  <files>docker/docker-compose.prod.yml, docker/.env.prod.example</files>
  <action>
Create `docker/docker-compose.prod.yml` by adapting the existing `docker/docker-compose.yml` with these specific changes:

**indexer service — replace build with image:**
- Remove `build:` block entirely (context, dockerfile, cache_from)
- Replace `image: lsp-indexer:latest` with `image: ghcr.io/chillwhales/lsp-indexer:${INDEXER_VERSION:-latest}`
- Keep everything else identical (depends_on, healthcheck, volumes, logging, deploy)

**Production hardening — change defaults:**
- PostgreSQL `POSTGRES_PASSWORD`: change `${POSTGRES_PASSWORD:-postgres}` to `${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}` — required in prod, no default
- PostgreSQL: REMOVE the `ports:` section entirely — DB should NOT be exposed in production
- Indexer `RPC_URL`: change `${RPC_URL:-https://rpc.lukso.sigmacore.io}` to `${RPC_URL:?Set RPC_URL in .env}` — required in prod
- Indexer `DB_URL`: update password interpolation to match (no default for password)
- Hasura `HASURA_GRAPHQL_DEV_MODE`: change default from `true` to `false`
- Hasura `HASURA_GRAPHQL_ENABLE_CONSOLE`: change default from `true` to `false`
- Hasura database URLs: update password interpolation to match (no default for password)
- Hasura `HASURA_GRAPHQL_METADATA_DEFAULTS`: hardcode the JSON value directly (single-quoted YAML string) rather than using `${VAR:-default}` — the data connector URIs reference internal service names that won't change

**Keep identical to dev:**
- All service names, container names, restart policies
- All health checks (test commands, intervals, timeouts)
- All `command:` blocks (PostgreSQL tuning)
- All volume definitions
- All resource limits (deploy.resources)
- All logging config on indexer
- data-connector-agent service (copy verbatim)
- All optional env vars keep their `${VAR:-default}` syntax

**Header comment:** Update the usage section to show production commands:
```
# Usage:
#   cd docker
#   cp .env.prod.example ../.env.prod
#   nano ../.env.prod  # Set REQUIRED variables
#   docker compose -f docker-compose.prod.yml --env-file ../.env.prod up -d
```

**Create `docker/.env.prod.example`** with these sections:

REQUIRED (no defaults — prod compose will fail if unset):
```
POSTGRES_PASSWORD=           # Strong password for PostgreSQL
HASURA_GRAPHQL_ADMIN_SECRET= # Strong secret for Hasura admin access
RPC_URL=                     # LUKSO RPC endpoint (e.g., https://rpc.lukso.sigmacore.io)
```

OPTIONAL (have sane defaults in compose):
```
POSTGRES_USER=postgres
POSTGRES_DB=postgres
INDEXER_VERSION=latest        # Pin to version: v0.1.0, sha-abc1234
SQD_GATEWAY=https://v2.archive.subsquid.io/network/lukso-mainnet
RPC_RATE_LIMIT=10
FINALITY_CONFIRMATION=75
IPFS_GATEWAY=https://api.universalprofile.cloud/ipfs/
FETCH_LIMIT=10000
FETCH_BATCH_SIZE=1000
FETCH_RETRY_COUNT=5
METADATA_WORKER_POOL_SIZE=4
LOG_LEVEL=info
HASURA_GRAPHQL_PORT=8080
HASURA_GRAPHQL_ENABLE_CONSOLE=false
HASURA_GRAPHQL_DEV_MODE=false
HASURA_GRAPHQL_UNAUTHORIZED_ROLE=public
```

Include comments explaining each variable's purpose, matching the style of the existing `.env.example`.
  </action>
  <verify>
    <automated>cd docker && docker compose -f docker-compose.prod.yml config --quiet 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <done>
    - `docker/docker-compose.prod.yml` exists with ghcr.io image for indexer
    - `docker/.env.prod.example` exists with all 3 required vars documented
    - `docker compose -f docker-compose.prod.yml config` parses without errors (when required vars are set)
    - Dev compose (`docker/docker-compose.yml`) is NOT modified
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Docker README with production instructions</name>
  <files>docker/README.md</files>
  <action>
Update `docker/README.md` to add a Production section. Keep all existing content. Add after the existing "Quick Start" section:

```markdown
## Production Deployment

The production compose file uses the pre-built Docker image from GitHub Container Registry instead of building from source.

### Setup

```bash
cd docker

# Copy and configure production environment
cp .env.prod.example ../.env.prod
nano ../.env.prod  # Set REQUIRED: POSTGRES_PASSWORD, HASURA_GRAPHQL_ADMIN_SECRET, RPC_URL

# Start production stack
docker compose -f docker-compose.prod.yml --env-file ../.env.prod up -d

# Monitor logs
docker compose -f docker-compose.prod.yml --env-file ../.env.prod logs -f indexer

# Stop
docker compose -f docker-compose.prod.yml --env-file ../.env.prod down
```

### Production vs Development

| Aspect | Development | Production |
|--------|-------------|------------|
| Indexer | Built from source | `ghcr.io/chillwhales/lsp-indexer:latest` |
| PostgreSQL port | Exposed (5432) | Not exposed |
| Hasura console | Enabled | Disabled |
| Hasura dev mode | Enabled | Disabled |
| Secrets | Optional defaults | Required (no defaults) |
```

Also update the "Files" section to include the new files:
- **`docker-compose.prod.yml`** — Production orchestration with released Docker image
- **`.env.prod.example`** — Production environment template

Do NOT modify any other content in the README.
  </action>
  <verify>
    <automated>grep -q "docker-compose.prod.yml" docker/README.md && grep -q "Production Deployment" docker/README.md && echo "README updated" || echo "README missing production content"</automated>
  </verify>
  <done>
    - README.md contains "Production Deployment" section with setup instructions
    - README.md "Files" section lists docker-compose.prod.yml and .env.prod.example
    - All existing README content is preserved unchanged
  </done>
</task>

</tasks>

<verification>
1. `docker compose -f docker/docker-compose.prod.yml config --quiet` — prod compose parses (with required vars set)
2. `docker compose -f docker/docker-compose.yml config --quiet` — dev compose still parses unchanged
3. `grep "ghcr.io/chillwhales/lsp-indexer" docker/docker-compose.prod.yml` — confirms released image
4. `grep "build:" docker/docker-compose.prod.yml` — should return nothing (no build directive)
5. `grep ":?" docker/docker-compose.prod.yml` — confirms required vars use error syntax
6. `git diff docker/docker-compose.yml` — should show no changes (DOCK-02)
</verification>

<success_criteria>
- Production compose exists and validates with `docker compose config`
- Production compose pulls `ghcr.io/chillwhales/lsp-indexer` image (not build from source)
- All 4 services present: postgres, indexer, hasura, data-connector-agent
- Required variables (POSTGRES_PASSWORD, HASURA_GRAPHQL_ADMIN_SECRET, RPC_URL) fail with error message if unset
- PostgreSQL port is NOT exposed in production
- Hasura console and dev mode default to disabled
- Dev compose (`docker-compose.yml`) has zero changes
- `.env.prod.example` documents all required and optional variables
- `docker/README.md` includes production setup instructions
</success_criteria>

<output>
After completion, create `.planning/phases/18-production-docker-compose/18-01-SUMMARY.md`
</output>
