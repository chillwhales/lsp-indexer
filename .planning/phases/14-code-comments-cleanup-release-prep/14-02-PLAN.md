---
phase: 14-code-comments-cleanup-release-prep
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - apps/test/src/app/page.tsx
  - apps/test/src/app/profiles/page.tsx
  - apps/test/src/app/digital-assets/page.tsx
  - apps/test/src/app/nfts/page.tsx
  - apps/test/src/app/owned-assets/page.tsx
  - apps/test/src/app/owned-tokens/page.tsx
  - apps/test/src/app/follows/page.tsx
  - apps/test/src/app/creators/page.tsx
  - apps/test/src/app/issued-assets/page.tsx
  - apps/test/src/app/encrypted-assets/page.tsx
  - apps/test/src/app/data-changed-events/page.tsx
  - apps/test/src/app/token-id-data-changed-events/page.tsx
  - apps/test/src/app/universal-receiver-events/page.tsx
  - apps/test/src/app/subscriptions/page.tsx
  - apps/test/src/app/layout.tsx
  - apps/test/src/app/providers.tsx
  - apps/test/src/components/profile-card.tsx
  - apps/test/src/components/digital-asset-card.tsx
  - apps/test/src/components/nft-card.tsx
  - apps/test/src/components/owned-asset-card.tsx
  - apps/test/src/components/owned-token-card.tsx
  - apps/test/src/components/follower-card.tsx
  - apps/test/src/components/creator-card.tsx
  - apps/test/src/components/issued-asset-card.tsx
  - apps/test/src/components/encrypted-asset-card.tsx
  - apps/test/src/components/data-changed-event-card.tsx
  - apps/test/src/components/token-id-data-changed-event-card.tsx
  - apps/test/src/components/universal-receiver-event-card.tsx
  - apps/test/src/components/connection-status.tsx
  - apps/test/src/components/image-list.tsx
  - apps/test/src/components/expandable-hex.tsx
  - apps/test/src/components/collapsible-sections.tsx
  - apps/test/src/components/nav.tsx
  - apps/test/src/components/playground/filter-fields.tsx
  - apps/test/src/components/playground/sort-controls.tsx
  - apps/test/src/components/playground/results-list.tsx
  - apps/test/src/components/playground/error-alert.tsx
  - apps/test/src/components/playground/raw-json-toggle.tsx
autonomous: true
requirements:
  - RELEASE-03
  - RELEASE-04

must_haves:
  truths:
    - 'Every test app domain page has a header comment explaining what hooks it demonstrates'
    - 'A new developer can read a page header comment and understand the usage patterns within minutes'
    - 'Every card component has JSDoc explaining its props and rendering behavior'
    - 'pnpm build succeeds for all 4 publishable packages'
    - 'pnpm validate:publish passes with zero errors'
  artifacts:
    - path: 'apps/test/src/app/profiles/page.tsx'
      provides: 'Profiles playground page with documentation'
      contains: '/**'
    - path: 'apps/test/src/app/digital-assets/page.tsx'
      provides: 'Digital Assets playground page with documentation'
      contains: '/**'
    - path: 'apps/test/src/components/profile-card.tsx'
      provides: 'ProfileCard component with JSDoc'
      contains: '/**'
    - path: 'apps/test/src/components/playground/filter-fields.tsx'
      provides: 'Shared FilterFields component with JSDoc'
      contains: '/**'
  key_links:
    - from: 'apps/test/src/app/*/page.tsx'
      to: '@lsp-indexer/react and @lsp-indexer/next hooks'
      via: 'import and usage demonstrations'
      pattern: 'import.*@lsp-indexer'
    - from: 'pnpm validate:publish'
      to: 'packages/*/dist/'
      via: 'publint + attw check exports and types'
      pattern: 'validate:publish'
---

<objective>
Add clear, consumer-oriented documentation to every test app page and component, then run final build + publish validation to confirm all 4 packages are ready for npm release.

Purpose: The test app is the primary reference implementation for hook consumers — clear documentation makes the difference between "useful reference" and "opaque demo code." Final validation confirms packages are publish-ready.

Output: Documented test app + validated, publish-ready packages.
</objective>

<execution_context>
@/home/coder/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-code-comments-cleanup-release-prep/14-01-SUMMARY.md
@apps/test/src/app/page.tsx
@apps/test/src/app/profiles/page.tsx
@apps/test/src/components/profile-card.tsx
@apps/test/src/components/playground/filter-fields.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test app page-level and component documentation</name>
  <files>
    apps/test/src/app/page.tsx
    apps/test/src/app/profiles/page.tsx
    apps/test/src/app/digital-assets/page.tsx
    apps/test/src/app/nfts/page.tsx
    apps/test/src/app/owned-assets/page.tsx
    apps/test/src/app/owned-tokens/page.tsx
    apps/test/src/app/follows/page.tsx
    apps/test/src/app/creators/page.tsx
    apps/test/src/app/issued-assets/page.tsx
    apps/test/src/app/encrypted-assets/page.tsx
    apps/test/src/app/data-changed-events/page.tsx
    apps/test/src/app/token-id-data-changed-events/page.tsx
    apps/test/src/app/universal-receiver-events/page.tsx
    apps/test/src/app/subscriptions/page.tsx
    apps/test/src/app/layout.tsx
    apps/test/src/app/providers.tsx
    apps/test/src/components/profile-card.tsx
    apps/test/src/components/digital-asset-card.tsx
    apps/test/src/components/nft-card.tsx
    apps/test/src/components/owned-asset-card.tsx
    apps/test/src/components/owned-token-card.tsx
    apps/test/src/components/follower-card.tsx
    apps/test/src/components/creator-card.tsx
    apps/test/src/components/issued-asset-card.tsx
    apps/test/src/components/encrypted-asset-card.tsx
    apps/test/src/components/data-changed-event-card.tsx
    apps/test/src/components/token-id-data-changed-event-card.tsx
    apps/test/src/components/universal-receiver-event-card.tsx
    apps/test/src/components/connection-status.tsx
    apps/test/src/components/image-list.tsx
    apps/test/src/components/expandable-hex.tsx
    apps/test/src/components/collapsible-sections.tsx
    apps/test/src/components/nav.tsx
    apps/test/src/components/playground/filter-fields.tsx
    apps/test/src/components/playground/sort-controls.tsx
    apps/test/src/components/playground/results-list.tsx
    apps/test/src/components/playground/error-alert.tsx
    apps/test/src/components/playground/raw-json-toggle.tsx
  </files>
  <action>
    **Part A: Page-level header comments for all domain pages**

    Add a `/** */` block comment at the top of each page file (after 'use client' directive and before imports) explaining:
    1. What domain this page demonstrates
    2. Which hooks from which packages are used (both @lsp-indexer/react and @lsp-indexer/next variants)
    3. What interaction patterns are shown (single lookup, filtered list, infinite scroll, subscription)
    4. Any notable usage patterns a hook consumer should learn from

    **Template for domain pages (adapt per domain):**
    ```typescript
    /**
     * {Domain} Playground — demonstrates @lsp-indexer hook usage for {domain description}.
     *
     * **Hooks demonstrated:**
     * - `use{X}` / `use{X}` (next) — Single {domain} lookup by {key}
     * - `use{X}s` / `use{X}s` (next) — Filtered, sorted, paginated list
     * - `useInfinite{X}s` / `useInfinite{X}s` (next) — Infinite scroll with fetchNextPage
     * - `use{X}Subscription` / `use{X}Subscription` (next) — Real-time WebSocket updates
     *
     * **Patterns shown:**
     * - Filter fields with debounced search inputs
     * - Sort controls with field/direction/nulls configuration
     * - Include toggles for conditional field inclusion (DX-04 type narrowing)
     * - Package toggle (react vs next) to compare client-direct vs server-action routing
     *
     * @see {@link https://github.com/chillwhales/lsp-indexer} for package documentation
     */
    ```

    **Pages to document (14 pages):**
    1. `apps/test/src/app/page.tsx` — Home page (package entry point validation + environment status)
    2. `apps/test/src/app/profiles/page.tsx` — Universal Profiles (4 tabs: Single, List, Infinite, Subscription)
    3. `apps/test/src/app/digital-assets/page.tsx` — Digital Assets (LSP7/LSP8 metadata)
    4. `apps/test/src/app/nfts/page.tsx` — NFTs (LSP8 individual tokens with collection relation)
    5. `apps/test/src/app/owned-assets/page.tsx` — Owned Assets (LSP7 fungible ownership)
    6. `apps/test/src/app/owned-tokens/page.tsx` — Owned Tokens (LSP8 NFT ownership with 4 nested relations)
    7. `apps/test/src/app/follows/page.tsx` — Followers/Following (LSP26 social graph, bidirectional)
    8. `apps/test/src/app/creators/page.tsx` — Creators (LSP4 creator attestations)
    9. `apps/test/src/app/issued-assets/page.tsx` — Issued Assets (LSP12 asset issuance)
    10. `apps/test/src/app/encrypted-assets/page.tsx` — Encrypted Assets (LSP29 encrypted metadata with encryption sub-include)
    11. `apps/test/src/app/data-changed-events/page.tsx` — Data Changed Events (ERC725Y, 3 tabs: Latest, List, Infinite)
    12. `apps/test/src/app/token-id-data-changed-events/page.tsx` — Token ID Data Changed Events
    13. `apps/test/src/app/universal-receiver-events/page.tsx` — Universal Receiver Events (3 relations)
    14. `apps/test/src/app/subscriptions/page.tsx` — Subscriptions overview page (if exists)

    Also add header comments to infrastructure files:
    - `layout.tsx` — Root layout with nav sidebar
    - `providers.tsx` — QueryClient + SubscriptionProvider setup

    **Part B: Component-level JSDoc for card and utility components**

    Add `/** */` JSDoc to each card component and shared utility component. Cards use the `Record<string, unknown>` props pattern with `'key' in obj` field-presence guards.

    **Card components (12 files in `apps/test/src/components/`):**
    Each card renders a single domain entity. JSDoc should explain:
    - What domain entity it renders
    - What the props shape is (Record<string, unknown> with specific expected fields)
    - Which collapsible sections it contains
    - How field-presence guards determine which sections render

    ```typescript
    /**
     * Renders a single {Domain} entity card with collapsible detail sections.
     *
     * Accepts a generic `Record<string, unknown>` props shape — field presence is
     * checked at runtime via `'fieldName' in obj` guards, so the card gracefully
     * handles any subset of the full {Domain} type (works with include-narrowed results).
     *
     * **Sections:**
     * - Header: {always-visible fields}
     * - {Section 1}: {conditional fields}
     * - {Section 2}: {conditional fields}
     * - Raw JSON toggle
     */
    ```

    **Shared playground components (5+ files in `apps/test/src/components/playground/`):**
    - `FilterFieldsRow` / filter-fields — Renders dynamic filter inputs from a config array
    - `SortControls` — Field/direction/nulls dropdowns with hideDirectionAndNulls support
    - `ResultsList<T>` — Generic paginated results display
    - `ErrorAlert` — Error message display from IndexerError
    - `RawJsonToggle` — Collapsible raw JSON viewer

    **Shared utility components:**
    - `ImageList` — Reusable image grid for profile/DA/NFT images
    - `ExpandableHex` — Truncated hex display with expand toggle
    - `CollapsibleSections` — Shared collapsible section wrapper
    - `ConnectionStatus` — Client-side import validation component
    - `Nav` — Sidebar navigation component

    Also sweep test app files for dead comments (`.planning`, `TODO`, `FIXME`) — same patterns as Plan 01.

  </action>
  <verify>
    ```bash
    # Every domain page has a /** block
    for page in apps/test/src/app/*/page.tsx; do
      if ! head -20 "$page" | grep -q '/\*\*'; then
        echo "MISSING header comment: $page"
      fi
    done
    # Should produce no output

    # Every card component has a /** block
    for card in apps/test/src/components/*-card.tsx; do
      if ! head -20 "$card" | grep -q '/\*\*'; then
        echo "MISSING JSDoc: $card"
      fi
    done
    # Should produce no output

    # Zero dead comments in test app
    grep -rn 'TODO\|FIXME\|\.planning\|PLAN\.md' apps/test/src/ --include="*.ts" --include="*.tsx" | wc -l
    # Should output: 0
    ```

  </verify>
  <done>
    All 14 test app pages have header comments explaining demonstrated hooks and patterns. All 12 card components have JSDoc explaining props shape and sections. All shared playground/utility components have JSDoc. Zero dead comments in test app.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build + publish validation + comprehensive verification</name>
  <files>
    (no files modified — validation only)
  </files>
  <action>
    **Part A: Build all 4 publishable packages**

    Run builds sequentially (types first since node depends on it, then node since react/next depend on it):
    ```bash
    pnpm --filter @lsp-indexer/types build
    pnpm --filter @lsp-indexer/node build
    pnpm --filter @lsp-indexer/react build
    pnpm --filter @lsp-indexer/next build
    ```

    All must pass with zero errors. If any fail, fix the issue (likely a comment syntax error from Plan 01/Task 1 edits) and retry.

    **Part B: Run publish validation**

    ```bash
    pnpm validate:publish
    ```

    This runs `publint` (validates package.json exports, entry points, ESM/CJS compatibility) and `arethetypeswrong` (validates .d.ts type resolution) on all 4 packages.

    All must pass with zero errors. If any fail:
    - `publint` errors → fix package.json exports map or missing files
    - `attw` errors → fix typesVersions or missing .d.cts files

    **Part C: Run ESLint**

    ```bash
    pnpm lint
    ```

    Should pass with zero warnings related to unused code or dead comments. If there are pre-existing warnings unrelated to Phase 14 work, note them but don't fix (out of scope).

    **Part D: Comprehensive verification grep**

    Run the complete dead-comment verification across all packages AND test app:
    ```bash
    # Must return 0 matches
    grep -rn '\.planning\|PLAN\.md\|TODO\|FIXME' \
      packages/types/src/ \
      packages/node/src/ \
      packages/react/src/ \
      packages/next/src/ \
      apps/test/src/ \
      --include="*.ts" --include="*.tsx" \
      | grep -v 'node_modules' \
      | grep -v '/graphql/graphql.ts'
    ```

    **Part E: Spot-check JSDoc on hover**

    Read 2-3 representative files from EACH package to confirm JSDoc is present and consumer-oriented:
    - `packages/types/src/profiles.ts` — ProfileSchema, Profile type
    - `packages/node/src/services/nfts.ts` — fetchNft, fetchNfts
    - `packages/node/src/keys/digital-assets.ts` — digitalAssetKeys
    - `packages/react/src/hooks/owned-assets/use-owned-assets.ts` — useOwnedAssets
    - `packages/next/src/actions/followers.ts` — getFollowers
    - `apps/test/src/app/encrypted-assets/page.tsx` — page header comment

    Confirm each has `/** */` with appropriate content (not placeholder or empty JSDoc).

  </action>
  <verify>
    ```bash
    # Build all 4 packages
    pnpm --filter @lsp-indexer/types build && \
    pnpm --filter @lsp-indexer/node build && \
    pnpm --filter @lsp-indexer/react build && \
    pnpm --filter @lsp-indexer/next build
    # Must exit 0

    # Publish validation
    pnpm validate:publish
    # Must exit 0

    # Final dead-comment check
    grep -rn '\.planning\|PLAN\.md\|TODO\|FIXME' \
      packages/types/src/ packages/node/src/ packages/react/src/ packages/next/src/ apps/test/src/ \
      --include="*.ts" --include="*.tsx" \
      | grep -v 'node_modules' | grep -v '/graphql/graphql.ts' | wc -l
    # Must output: 0
    ```

  </verify>
  <done>
    All 4 packages build successfully. publint and arethetypeswrong pass with zero errors on all 4 packages. ESLint passes (or only has pre-existing unrelated warnings). Zero dead comments confirmed via comprehensive grep. Spot-check confirms JSDoc is present and consumer-oriented on representative exports across all packages and test app.
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @lsp-indexer/types build && pnpm --filter @lsp-indexer/node build && pnpm --filter @lsp-indexer/react build && pnpm --filter @lsp-indexer/next build` — zero errors
2. `pnpm validate:publish` — zero errors
3. Comprehensive grep for dead comments — zero matches across all packages + test app
4. Every test app domain page has a `/** */` header comment
5. Spot-check 2-3 representative files per package confirms JSDoc with @param/@returns
</verification>

<success_criteria>

- All 4 packages build and pass publish validation (publint + attw)
- Test app pages have clear header comments explaining demonstrated hooks and patterns
- Card and playground components have JSDoc explaining their purpose
- A new developer reading the test app can understand hook usage patterns within minutes
- Ready for `npm publish`
  </success_criteria>

<output>
After completion, create `.planning/phases/14-code-comments-cleanup-release-prep/14-02-SUMMARY.md`
</output>
