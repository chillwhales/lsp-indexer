---
phase: 01-handler-migration
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - packages/indexer-v2/src/core/handlerHelpers.ts
  - packages/indexer-v2/src/core/populateHelpers.ts
  - packages/indexer-v2/src/core/persistHelpers.ts
  - packages/indexer-v2/src/core/index.ts
autonomous: true
must_haves:
  truths:
    - 'No legacy code remains — DataKeyPlugin interface, populate helpers, handler helpers all deleted'
    - 'Zero references to deleted files remain in the codebase'
    - 'core/index.ts barrel export updated to remove deleted modules'
  artifacts:
    - packages/indexer-v2/src/core/index.ts
  key_links:
    - .planning/phases/01-handler-migration/01-RESEARCH.md
---

<objective>
Delete all legacy code that has been superseded by the V2 EntityHandler pattern and enrichment queue pipeline (HMIG-05). This includes handlerHelpers.ts (dead code replaced by standalone handlers in Plans 02-03), populateHelpers.ts (populate pattern replaced by enrichment queue), and persistHelpers.ts (persist pattern replaced by pipeline Steps 2/4). Update the barrel export and verify zero dangling references.
</objective>

<context>
@packages/indexer-v2/src/core/handlerHelpers.ts — Dead code: updateTotalSupply, updateOwnedAssets (replaced by handlers in Plan 02)
@packages/indexer-v2/src/core/populateHelpers.ts — Legacy: populateByUP, populateByDA, enrichEntityFk, populateByUPAndDA, populateNFTs (replaced by enrichment queue)
@packages/indexer-v2/src/core/persistHelpers.ts — Legacy: insertEntities, upsertEntities, insertNewEntities, mergeUpsertEntities (replaced by pipeline Steps 2/4)
@packages/indexer-v2/src/core/index.ts — Barrel export that re-exports from all three files
</context>

<tasks>
<task type="auto">
  <name>Task 1: Verify zero imports, delete legacy files, update barrel export</name>
  <files>
    packages/indexer-v2/src/core/handlerHelpers.ts
    packages/indexer-v2/src/core/populateHelpers.ts
    packages/indexer-v2/src/core/persistHelpers.ts
    packages/indexer-v2/src/core/index.ts
  </files>
  <action>
  1. **Verify zero remaining imports** before deleting anything.

     Run these grep searches across the ENTIRE `packages/indexer-v2/src/` directory:
     ```
     grep -r "handlerHelpers" --include="*.ts" packages/indexer-v2/src/ | grep -v "core/handlerHelpers.ts" | grep -v "core/index.ts"
     grep -r "populateHelpers" --include="*.ts" packages/indexer-v2/src/ | grep -v "core/populateHelpers.ts" | grep -v "core/index.ts"
     grep -r "persistHelpers" --include="*.ts" packages/indexer-v2/src/ | grep -v "core/persistHelpers.ts" | grep -v "core/index.ts"
     ```

     Also check for imports of the FUNCTIONS exported by these files:
     ```
     grep -r "updateTotalSupply\|updateOwnedAssets" --include="*.ts" packages/indexer-v2/src/ | grep -v "core/handlerHelpers.ts"
     grep -r "populateByUP\|populateByDA\|populateByUPAndDA\|enrichEntityFk\|populateNFTs" --include="*.ts" packages/indexer-v2/src/ | grep -v "core/populateHelpers.ts"
     grep -r "insertEntities\|upsertEntities\|insertNewEntities\|mergeUpsertEntities" --include="*.ts" packages/indexer-v2/src/ | grep -v "core/persistHelpers.ts" | grep -v "core/pipeline.ts"
     ```

     NOTE: `mergeUpsertEntities` exists in BOTH `persistHelpers.ts` AND `pipeline.ts` — the pipeline has its own implementation. Only delete the one in persistHelpers.ts.

     If ANY non-barrel imports are found, DO NOT DELETE yet. Instead, update the importing files to remove the unused imports first.

2. **Delete the three legacy files:**

   ```
   rm packages/indexer-v2/src/core/handlerHelpers.ts
   rm packages/indexer-v2/src/core/populateHelpers.ts
   rm packages/indexer-v2/src/core/persistHelpers.ts
   ```

3. **Update `core/index.ts`** barrel export — remove the three re-export lines:
   Remove:

   ```typescript
   export * from './handlerHelpers';
   export * from './persistHelpers';
   export * from './populateHelpers';
   ```

   The resulting core/index.ts should have:

   ```typescript
   // Core pipeline infrastructure
   export * from './batchContext';
   export * from './metadataWorkerPool';
   export * from './multicall';
   export * from './pipeline';
   export * from './registry';
   export * from './types';
   export * from './verification';
   ```

4. **Final verification** — build and search:
   ```
   pnpm --filter=@chillwhales/indexer-v2 build
   grep -r "handlerHelpers\|populateHelpers\|persistHelpers" --include="*.ts" packages/indexer-v2/src/
   grep -r "populateByUP\|populateByDA\|populateByUPAndDA\|enrichEntityFk\|populateNFTs\|insertEntities\|upsertEntities\|insertNewEntities\|updateTotalSupply\|updateOwnedAssets" --include="*.ts" packages/indexer-v2/src/
   ```
   Both greps should return zero results.
   </action>
   <verify>

- `pnpm --filter=@chillwhales/indexer-v2 build` compiles with zero errors
- `ls packages/indexer-v2/src/core/handlerHelpers.ts` returns "No such file"
- `ls packages/indexer-v2/src/core/populateHelpers.ts` returns "No such file"
- `ls packages/indexer-v2/src/core/persistHelpers.ts` returns "No such file"
- Grep for file names and exported function names returns zero results
- core/index.ts does NOT re-export from any deleted module
  </verify>
  <done>
- handlerHelpers.ts deleted (updateTotalSupply, updateOwnedAssets — replaced by standalone handlers)
- populateHelpers.ts deleted (populateByUP, populateByDA, etc. — replaced by enrichment queue)
- persistHelpers.ts deleted (insertEntities, upsertEntities, etc. — replaced by pipeline)
- core/index.ts barrel export updated
- Zero dangling references in codebase
- Build passes with zero errors
  </done>
  </task>
  </tasks>

<verification>
1. Run `pnpm --filter=@chillwhales/indexer-v2 build` — must compile with zero errors
2. Verify the three files are physically deleted from the filesystem
3. Verify zero references to the deleted files or their exported functions exist anywhere in `packages/indexer-v2/src/`
4. Verify `core/index.ts` has exactly 7 re-export lines (batchContext, metadataWorkerPool, multicall, pipeline, registry, types, verification)
</verification>

<success_criteria>

- `pnpm --filter=@chillwhales/indexer-v2 build` passes with zero type errors
- handlerHelpers.ts, populateHelpers.ts, persistHelpers.ts are deleted
- core/index.ts barrel export is clean — no references to deleted modules
- Zero dangling imports or references to legacy functions anywhere in the codebase
- All Phase 1 requirements (HMIG-01 through HMIG-05) are complete
  </success_criteria>
