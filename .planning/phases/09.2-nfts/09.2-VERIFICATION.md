---
phase: 09.2-nfts
verified: 2026-03-08T16:10:00Z
status: passed
score: 18/18 must-haves verified
---

# Phase 9.2: NFTs Verification Report

**Phase Goal:** Developer can use `useNft`, `useNfts`, `useInfiniteNfts` for LSP8 individual NFT token data.
**Verified:** 2026-03-08
**Status:** ✅ PASSED
**Re-verification:** No — initial verification (gap closure for v1.1 milestone audit)

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                               | Status     | Evidence                                                                                                                                                                                                                                               |
| --- | ------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Nft, NftFilter, NftSort, NftInclude, NftResult types exist and are importable from @lsp-indexer/types               | ✓ VERIFIED | `packages/types/src/nfts.ts` exports all types (lines 181-189); re-exported via `packages/types/src/index.ts` line 10                                                                                                                                  |
| 2   | NFT composite key uses (address, tokenId) pair — not single address                                                 | ✓ VERIFIED | `NftSchema` (line 38-67) has `address` and `tokenId` as required fields; `nftKeys.detail(address, tokenId?, formattedTokenId?, include?)` in keys (line 20-21)                                                                                         |
| 3   | NftResult<I> narrows return type based on include parameter with ResolveNftCollection and ResolveNftHolder           | ✓ VERIFIED | `packages/types/src/nfts.ts` lines 257-266 — uses `IncludeResult` + `ResolveNftCollection<NonNullable<I>>` + `ResolveNftHolder<NonNullable<I>>` conditional types                                                                                     |
| 4   | NftInclude has collection sub-include (DigitalAssetIncludeSchema) and holder sub-include (ProfileIncludeSchema)      | ✓ VERIFIED | Lines 108-129 — `collection: z.union([z.boolean(), DigitalAssetIncludeSchema]).optional()` and `holder: z.union([z.boolean(), ProfileIncludeSchema]).optional()`                                                                                         |
| 5   | NftHolder = Profile + timestamp (merged flat via NftHolderSchema)                                                    | ✓ VERIFIED | Lines 30-35 — `NftHolderSchema = z.object({ timestamp: z.string() }).merge(ProfileSchema)`                                                                                                                                                             |
| 6   | GraphQL documents for NFT (detail, list, subscription) exist with @include directives                                | ✓ VERIFIED | `packages/node/src/documents/nfts.ts` — 752 lines, 3 documents: `GetNftDocument`, `GetNftsDocument`, `NftSubscriptionDocument` with 129 `@include` directives total                                                                                    |
| 7   | NFT documents fetch both lsp4Metadata and lsp4MetadataBaseUri with identical @include conditionals                   | ✓ VERIFIED | `GetNftDocument` lines 175-244 — `lsp4Metadata` block + `lsp4MetadataBaseUri` block with matching `@include(if: $includeName/Description/Category/Icons/Images/Links/Attributes)` on both                                                              |
| 8   | parseNft handles lsp4Metadata + lsp4MetadataBaseUri dual source (direct first, baseUri fallback)                     | ✓ VERIFIED | `packages/node/src/parsers/nfts.ts` lines 15-16 — `const direct = raw.lsp4Metadata; const baseUri = raw.lsp4MetadataBaseUri;` then line 26: `name: direct?.name?.value ?? baseUri?.name?.value ?? null`                                                 |
| 9   | parseNft supports include parameter for conditional type narrowing via stripExcluded                                 | ✓ VERIFIED | Lines 12-13 (overloads) + lines 69-73 — calls `stripExcluded(result, include, ['address', 'tokenId', 'isBurned', 'isMinted'], undefined, { collection: ..., holder: ... })`                                                                             |
| 10  | parseNft parses collection via parseDigitalAsset reuse and holder via parseProfile reuse                             | ✓ VERIFIED | Line 30: `collection: raw.digitalAsset ? parseDigitalAsset(raw.digitalAsset) : null`; line 40: `parseProfile(raw.ownedToken.universalProfile)` for holder                                                                                               |
| 11  | fetchNft (detail) and fetchNfts (list) service functions exist with 3-overload pattern                               | ✓ VERIFIED | `packages/node/src/services/nfts.ts` — `fetchNft` lines 199-242 (3 overloads, requires address + tokenId OR formattedTokenId), `fetchNfts` lines 250-305 (3 overloads with filter/sort/pagination)                                                     |
| 12  | NFT name filter uses _or search on both lsp4Metadata.name and lsp4MetadataBaseUri.name                               | ✓ VERIFIED | `buildNftWhere` lines 46-54 — `_or: [{ lsp4Metadata: { name: { value: { _ilike: namePattern } } } }, { lsp4MetadataBaseUri: { name: { value: { _ilike: namePattern } } } }]`                                                                            |
| 13  | All address/tokenId filters use _ilike + escapeLike for case-insensitive matching                                     | ✓ VERIFIED | Lines 29-43 — `collectionAddress`, `tokenId`, `formattedTokenId` all use `{ _ilike: escapeLike(...) }`; `holderAddress` at line 58 same pattern                                                                                                        |
| 14  | nftKeys provides hierarchical cache key factory with composite detail key                                             | ✓ VERIFIED | `packages/node/src/keys/nfts.ts` — 37 lines, `nftKeys.detail(address, tokenId?, formattedTokenId?, include?)` at line 20-21 (composite key), plus `all`, `details()`, `lists()`, `list()`, `infinites()`, `infinite()`                                 |
| 15  | 3 React hooks exported from @lsp-indexer/react: useNft, useNfts, useInfiniteNfts                                     | ✓ VERIFIED | `packages/react/src/hooks/nfts/` — 5 files: `use-nft.ts`, `use-nfts.ts`, `use-infinite-nfts.ts`, `use-nft-subscription.ts`, `index.ts`; re-exported via `packages/react/src/hooks/index.ts` line 11                                                    |
| 16  | 2 server actions exported from @lsp-indexer/next with 'use server' + Zod validation                                  | ✓ VERIFIED | `packages/next/src/actions/nfts.ts` — 74 lines, `'use server'` directive line 1, `getNft` with `validateInput(UseNftParamsSchema)` line 40, `getNfts` with `validateInput(UseNftsParamsSchema)` line 72                                                 |
| 17  | 3 Next.js hooks exported from @lsp-indexer/next: useNft, useNfts, useInfiniteNfts                                    | ✓ VERIFIED | `packages/next/src/hooks/nfts/` — 5 files: `use-nft.ts`, `use-nfts.ts`, `use-infinite-nfts.ts`, `use-nft-subscription.ts`, `index.ts`; routed through server actions                                                                                   |
| 18  | NFTs playground page at /nfts with nav link                                                                           | ✓ VERIFIED | `apps/test/src/app/nfts/page.tsx` — 17,712 bytes; `apps/test/src/components/nav.tsx` line 47: `{ href: '/nfts', label: 'NFTs', icon: Layers, available: true }`                                                                                         |

**Score:** 18/18 truths verified

### Required Artifacts

| Artifact                                       | Expected                                          | Status     | Details                                                                                |
| ---------------------------------------------- | ------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------- |
| `packages/types/src/nfts.ts`                   | NFT domain types + conditional include types      | ✓ VERIFIED | 274 lines, 12+ types exported, NftResult<I> conditional narrowing with 2 relation resolvers |
| `packages/node/src/documents/nfts.ts`          | GraphQL documents with @include directives        | ✓ VERIFIED | 752 lines, 3 documents (query detail, query list, subscription), 129 @include directives   |
| `packages/node/src/parsers/nfts.ts`            | Parser with dual metadata source + stripExcluded  | ✓ VERIFIED | 82 lines, overloaded parseNft + parseNfts, nested relation stripping                        |
| `packages/node/src/services/nfts.ts`           | 2 service functions + builders + subscription cfg | ✓ VERIFIED | 306 lines, fetchNft/fetchNfts + buildNftWhere/OrderBy/IncludeVars + buildNftSubscriptionConfig |
| `packages/node/src/keys/nfts.ts`               | Cache key factory with composite detail key       | ✓ VERIFIED | 37 lines, 7 key methods including composite detail(address, tokenId)                        |
| `packages/react/src/hooks/nfts/`               | 3 query hooks + 1 subscription hook               | ✓ VERIFIED | 5 files: useNft, useNfts, useInfiniteNfts, useNftSubscription, index                       |
| `packages/next/src/actions/nfts.ts`            | 2 server actions with 'use server'                | ✓ VERIFIED | 74 lines, getNft + getNfts with Zod validation                                              |
| `packages/next/src/hooks/nfts/`                | 3 query hooks + 1 subscription hook (via actions) | ✓ VERIFIED | 5 files matching React hooks structure, routed through server actions                        |
| `apps/test/src/app/nfts/page.tsx`              | Playground page                                   | ✓ VERIFIED | 17,712 bytes                                                                                |

### Key Link Verification

| From            | To             | Via                                                                                    | Status  | Details                                                    |
| --------------- | -------------- | -------------------------------------------------------------------------------------- | ------- | ---------------------------------------------------------- |
| React hooks     | Node services  | `import { fetchNft, fetchNfts } from '@lsp-indexer/node'` via factory                  | ✓ WIRED | All 3 query hooks call corresponding service functions     |
| Next.js hooks   | Server actions | `import { getNft, getNfts } from '../actions/nfts'` via factory                         | ✓ WIRED | All 3 Next.js hooks call corresponding server actions      |
| Server actions  | Node services  | `import { fetchNft, fetchNfts } from '@lsp-indexer/node'`                               | ✓ WIRED | Both server actions delegate to Node services              |
| Services        | Documents      | `import { GetNftDocument, GetNftsDocument } from '../documents/nfts'`                   | ✓ WIRED | All service functions use correct documents via `execute()` |
| Services        | Parsers        | `import { parseNft, parseNfts } from '../parsers/nfts'`                                 | ✓ WIRED | fetchNft/fetchNfts call parseNft/parseNfts on response     |
| Parsers         | stripExcluded  | `import { stripExcluded } from './strip'`                                                | ✓ WIRED | parseNft calls stripExcluded with nested relation config   |
| Parsers         | parseDigitalAsset | `import { parseDigitalAsset } from './digital-assets'`                                | ✓ WIRED | Collection relation parsed via reused DA parser            |
| Parsers         | parseProfile   | `import { parseProfile } from './profiles'`                                              | ✓ WIRED | Holder relation parsed via reused Profile parser           |
| Nav             | NFTs route     | `{ href: '/nfts', label: 'NFTs', available: true }`                                     | ✓ WIRED | Active state via pathname matching                         |

### Requirements Coverage

| Requirement                                                                | Status      | Notes                                                                   |
| -------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------- |
| QUERY-03: useNft, useNfts, useNftsByCollection for NFT data                | ✓ SATISFIED | useNft, useNfts, useInfiniteNfts in both @lsp-indexer/react and @lsp-indexer/next; useNftsByCollection via collectionAddress filter |
| PAGE-01: useInfinite* hooks for offset-based infinite scroll               | ✓ SATISFIED | useInfiniteNfts in both packages                                         |

### Anti-Patterns Found

| File | Line | Pattern                                                       | Severity | Impact |
| ---- | ---- | ------------------------------------------------------------- | -------- | ------ |
| —    | —    | No stub patterns, TODOs, FIXMEs, or placeholder content found | —        | —      |

### Human Verification Required

### 1. Playground Page Visual Correctness

**Test:** Navigate to `/nfts`, enter a collection address or search by NFT name, verify that results render correctly
**Expected:** NFT list loads with cards showing address, tokenId, name, collection info, holder info
**Why human:** Visual rendering + real API data cannot be verified programmatically

### 2. Single NFT Detail Lookup

**Test:** In the single tab, enter a collection address and tokenId, verify the detail view renders
**Expected:** Single NFT data with dual metadata source (direct + baseUri fallback), collection as full DigitalAsset, holder profile
**Why human:** Requires real Hasura data with NFT composite key lookup

### 3. Infinite Scroll Pagination

**Test:** In the "Infinite" tab, search for a collection with many NFTs, click "Load more"
**Expected:** Additional pages load and append to the list
**Why human:** Requires real Hasura data + interactive scroll behavior

---

_Verified: 2026-03-08_
_Verifier: Claude (gsd-executor)_
