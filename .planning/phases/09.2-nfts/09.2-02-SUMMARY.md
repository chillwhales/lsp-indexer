---
phase: 09.2-nfts
plan: 02
subsystem: api
tags: [graphql, hasura, parser, service-layer, query-keys, tanstack-query, nfts]

# Dependency graph
requires:
  - phase: 09.2-01
    provides: NFT types (Nft, NftFilter, NftSort, NftInclude) and GraphQL documents (GetNftDocument, GetNftsDocument)
  - phase: 09.1-02
    provides: Service pattern (buildWhere/buildOrderBy/buildIncludeVars + execute + parse pipeline), escapeLike, orderDir, numericToString utilities
provides:
  - Query key factory (nftKeys) for NFT domain with detail/list/infinite namespaces
  - Parser (parseNft, parseNfts) transforming Hasura nft responses to clean Nft type
  - Service functions (fetchNft, fetchNfts) translating flat params to Hasura variables
  - Shared parseImage helper extracted to parsers/utils.ts for cross-domain reuse
affects: [09.2-03 (React hooks), 09.2-04 (playground UI), future domains using parseImage]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 'Shared parseImage in parsers/utils.ts — cross-domain image parsing for LSP4 metadata'
    - 'Composite detail key (address + tokenId) — NFTs identified by pair, not single address'
    - 'Nullable lsp4Metadata handling — optional chaining on entire metadata object'
    - 'ownedToken → owner mapping — raw ownedToken.owner/timestamp → clean { address, timestamp }'

key-files:
  created:
    - packages/node/src/keys/nfts.ts
    - packages/node/src/parsers/nfts.ts
    - packages/node/src/services/nfts.ts
  modified:
    - packages/node/src/parsers/utils.ts
    - packages/node/src/parsers/digital-assets.ts
    - packages/node/src/index.ts

key-decisions:
  - 'Extracted parseImage to parsers/utils.ts for cross-domain reuse (digital-assets + nfts share identical LSP4 image parsing)'
  - 'parseImage returns Lsp4Image type (structurally identical to DigitalAssetImage alias)'
  - 'Boolean filter fields (isBurned, isMinted) use !== undefined check to allow filtering by false values'

patterns-established:
  - 'Shared parseImage in parsers/utils.ts: future domains (owned-assets, etc.) import from utils instead of duplicating'

# Metrics
duration: 4min
completed: 2026-02-20
---

# Phase 9.2 Plan 02: NFT Service Layer Summary

**NFT query keys with composite (address, tokenId) detail keys, parser handling nullable lsp4Metadata + ownedToken→owner mapping, and service functions translating all 5 filter fields to Hasura variables with shared parseImage extracted to utils**

## Performance

- **Duration:** 4 min
- **Started:** 2026-02-20T13:58:34Z
- **Completed:** 2026-02-20T14:02:40Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- Extracted `parseImage` to shared `parsers/utils.ts` for cross-domain reuse (digital-assets parser updated to import)
- Created NFT query key factory with composite `(address, tokenId)` detail keys and separate list/infinite namespaces
- Created NFT parser handling nullable `lsp4Metadata` gracefully and mapping `ownedToken` to clean `owner` object
- Created NFT service functions translating all 5 filter fields + 2 sort fields + inverted-default include to Hasura variables
- Updated package barrel exports with all NFT modules

## Task Commits

Each task was committed atomically:

1. **Task 1: Extract shared parseImage to utils and create query key factory** - `f185e86` (feat)
2. **Task 2: Create parser and service functions** - `212f41f` (feat)

## Files Created/Modified

- `packages/node/src/parsers/utils.ts` - Added shared `parseImage` helper for LSP4 metadata images
- `packages/node/src/parsers/digital-assets.ts` - Updated to import `parseImage` from utils (removed local copy)
- `packages/node/src/keys/nfts.ts` - Query key factory with composite detail keys and list/infinite separation
- `packages/node/src/parsers/nfts.ts` - Hasura nft → clean Nft type transformation with nullable metadata handling
- `packages/node/src/services/nfts.ts` - Service functions with filter/sort/include translation and execute+parse pipeline
- `packages/node/src/index.ts` - Added NFT barrel exports (services, parsers, keys, documents)

## Decisions Made

- Extracted `parseImage` to `parsers/utils.ts` returning `Lsp4Image` type (structurally identical to `DigitalAssetImage` alias) for cross-domain reuse
- Boolean filter fields (`isBurned`, `isMinted`) use `!== undefined` guard rather than truthiness check, allowing explicit filtering by `false` values
- `formattedTokenId` sort defaults to nulls-last since this field can be null

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- NFT service layer complete — keys, parser, services all passing typecheck and build
- Ready for 09.2-03-PLAN.md (React hooks wrapping these service functions)
- `parseImage` shared utility available for future domains

## Self-Check: PASSED

---

_Phase: 09.2-nfts_
_Completed: 2026-02-20_
