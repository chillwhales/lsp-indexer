---
phase: 09.6-generic-type-propagation
plan: 01
subsystem: api
tags: [typescript, generics, overloads, type-safety, const-type-parameters]

# Dependency graph
requires:
  - phase: 09.4-conditional-include-types
    provides: XResult<I> conditional types for all 5 domains
provides:
  - 3-overload generic <const I> pattern on all 5 parser pairs (parseX/parseXs)
  - 3-overload generic <const I> pattern on all 5 service pairs (fetchX/fetchXs)
  - Type-safe return narrowing — XResult<I> flows through parser → service
affects:
  - 09.6-02 (React hooks — needs service generic propagation)
  - 09.6-03 (Next.js hooks — needs service generic propagation)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - '3-overload <const I> generic pattern for type-safe include narrowing'
    - 'Generic overload positioned between no-include and fallback overloads'

key-files:
  created: []
  modified:
    - packages/node/src/parsers/profiles.ts
    - packages/node/src/parsers/digital-assets.ts
    - packages/node/src/parsers/nfts.ts
    - packages/node/src/parsers/owned-assets.ts
    - packages/node/src/parsers/owned-tokens.ts
    - packages/node/src/services/profiles.ts
    - packages/node/src/services/digital-assets.ts
    - packages/node/src/services/nfts.ts
    - packages/node/src/services/owned-assets.ts
    - packages/node/src/services/owned-tokens.ts

key-decisions:
  - 'No implementation body changes — only overload declarations modified'
  - 'Generic overload always position 2 of 3 (between no-include and fallback)'

patterns-established:
  - '3-overload <const I> pattern: (1) no include → Full type, (2) <const I> → XResult<I>, (3) fallback → PartialX'

# Metrics
duration: 11min
completed: 2026-02-23
---

# Phase 9.6 Plan 01: Parsers + Services — Generic Overloads Summary

**Added 3-overload `<const I>` generic pattern to all 10 parser/service function pairs across 5 domains in `@lsp-indexer/node`, enabling type-safe `XResult<I>` return narrowing from service through parser layer**

## Performance

- **Duration:** 11 min
- **Started:** 2026-02-23T12:02:49Z
- **Completed:** 2026-02-23T12:13:50Z
- **Tasks:** 11 (10 code tasks + 1 validation)
- **Files modified:** 10

## Accomplishments

- All 5 parser files (profiles, digital-assets, nfts, owned-assets, owned-tokens) upgraded to 3-overload pattern with `<const I>` generic
- All 5 service files upgraded to 3-overload pattern on both detail (`fetchX`) and list (`fetchXs`) functions
- `tsc --noEmit` passes with zero errors
- `pnpm build` succeeds with full codegen + tsup pipeline
- No `as` type assertions added — implementation bodies completely unchanged

## Task Commits

Each task was committed atomically:

1. **Task 1: Profile parsers** - `81d8848` (feat)
2. **Task 2: Digital asset parsers** - `c62ad06` (feat)
3. **Task 3: NFT parsers** - `4ba7420` (feat)
4. **Task 4: Owned asset parsers** - `4a715e8` (feat)
5. **Task 5: Owned token parsers** - `cfad219` (feat)
6. **Task 6: Profile services** - `a4eeb74` (feat)
7. **Task 7: Digital asset services** - `7a0b9f3` (feat)
8. **Task 8: NFT services** - `db8875e` (feat)
9. **Task 9: Owned asset services** - `b2d22b1` (feat)
10. **Task 10: Owned token services** - `5052e56` (feat)
11. **Task 11: Validation** - no commit (verification-only)

## Files Created/Modified

- `packages/node/src/parsers/profiles.ts` — Added `ProfileResult` import + generic overloads for `parseProfile`/`parseProfiles`
- `packages/node/src/parsers/digital-assets.ts` — Added `DigitalAssetResult` import + generic overloads for `parseDigitalAsset`/`parseDigitalAssets`
- `packages/node/src/parsers/nfts.ts` — Added `NftResult` import + generic overloads for `parseNft`/`parseNfts`
- `packages/node/src/parsers/owned-assets.ts` — Added `OwnedAssetResult` import + generic overloads for `parseOwnedAsset`/`parseOwnedAssets`
- `packages/node/src/parsers/owned-tokens.ts` — Added `OwnedTokenResult` import + generic overloads for `parseOwnedToken`/`parseOwnedTokens`
- `packages/node/src/services/profiles.ts` — Added `ProfileResult` import + generic overloads for `fetchProfile`/`fetchProfiles`
- `packages/node/src/services/digital-assets.ts` — Added `DigitalAssetResult` import + generic overloads for `fetchDigitalAsset`/`fetchDigitalAssets`
- `packages/node/src/services/nfts.ts` — Added `NftResult` import + generic overloads for `fetchNft`/`fetchNfts`
- `packages/node/src/services/owned-assets.ts` — Added `OwnedAssetResult` import + generic overloads for `fetchOwnedAsset`/`fetchOwnedAssets`
- `packages/node/src/services/owned-tokens.ts` — Added `OwnedTokenResult` import + generic overloads for `fetchOwnedToken`/`fetchOwnedTokens`

## Decisions Made

None - followed plan as specified.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Node package parsers + services fully upgraded to generic `<const I>` overload pattern
- Ready for 09.6-02-PLAN.md (React hooks — propagating generic types through hook layer)
- All `XResult<I>` types available from `@lsp-indexer/types` (established in Phase 9.4)

---

## Self-Check: PASSED

---

_Phase: 09.6-generic-type-propagation_
_Completed: 2026-02-23_
