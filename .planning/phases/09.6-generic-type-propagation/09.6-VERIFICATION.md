---
phase: 09.6-generic-type-propagation
verified: 2026-02-23T13:15:00Z
status: passed
score: 5/5 must-haves verified
must_haves:
  truths:
    - 'All 5 domains have 3-overload generic <const I> pattern at every layer (parser, service, action, react hook, next hook)'
    - 'All hooks return XResult<I> when const include param is provided — TypeScript narrows the return type'
    - 'All hooks have explicit return type aliases with UseQueryResult<T, Error> — error typed as Error everywhere'
    - 'Zero type assertions in the entire parser-to-consumer chain (25 files)'
    - 'All 4 packages + test app build/type-check with zero errors'
  artifacts:
    - path: 'packages/node/src/parsers/{profiles,digital-assets,nfts,owned-assets,owned-tokens}.ts'
      provides: '3-overload parseX/parseXs with <const I> → XResult<I>'
    - path: 'packages/node/src/services/{profiles,digital-assets,nfts,owned-assets,owned-tokens}.ts'
      provides: '3-overload fetchX/fetchXs with <const I> → XResult<I>'
    - path: 'packages/next/src/actions/{profiles,digital-assets,nfts,owned-assets,owned-tokens}.ts'
      provides: '3-overload getX/getXs with <const I> → XResult<I>'
    - path: 'packages/react/src/hooks/{profiles,digital-assets,nfts,owned-assets,owned-tokens}.ts'
      provides: '3-overload useX/useXs/useInfiniteXs with <const I> → UseXReturn<XResult<I>>'
    - path: 'packages/next/src/hooks/{profiles,digital-assets,nfts,owned-assets,owned-tokens}.ts'
      provides: '3-overload useX/useXs/useInfiniteXs with <const I> → UseXReturn<XResult<I>>'
  key_links:
    - from: 'parsers'
      to: 'services'
      via: 'services call parseX/parseXs with include param, propagating XResult<I>'
    - from: 'services'
      to: 'actions'
      via: 'actions call fetchX/fetchXs with include param'
    - from: 'actions'
      to: 'next hooks'
      via: 'next hooks call getX/getXs in queryFn'
    - from: 'services'
      to: 'react hooks'
      via: 'react hooks call fetchX/fetchXs in queryFn'
    - from: 'return type aliases'
      to: 'hook overloads'
      via: 'UseXReturn<XResult<I>> used as return type on generic overload'
---

# Phase 9.6: Generic Type Propagation Verification Report

**Phase Goal:** All 5 existing domain hooks, services, actions, and parsers use the 3-overload generic `<const I>` pattern established in followers — propagating `XResult<I>` narrowed types from parser → service → action → hook → consumer with zero type assertions. Fixes `error: unknown` → `error: Error` on all hook returns.

**Verified:** 2026-02-23T13:15:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                       | Status     | Evidence                                                                                                                                                                                                                                   |
| --- | --------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | All 5 domains have 3-overload `<const I>` pattern at every layer            | ✓ VERIFIED | All 25 files confirmed: parsers (2 `<const I>` each), services (2 each), actions (2 each), react hooks (3 each), next hooks (3 each). Pattern matches followers gold standard exactly.                                                     |
| 2   | All hooks return `XResult<I>` when const include param provided             | ✓ VERIFIED | grep confirms `XResult<I>` return types on generic overloads in all 10 hook files + 5 action files. e.g., `UseProfileReturn<ProfileResult<I>>`, `UseDigitalAssetReturn<DigitalAssetResult<I>>`, etc.                                       |
| 3   | All hooks have explicit return type aliases with `UseQueryResult<T, Error>` | ✓ VERIFIED | All 10 hook files have 3 return type aliases each (`UseXReturn<F>`, `UseXsReturn<F>`, `UseInfiniteXsReturn<F>`), all parameterized with `Error` (not `unknown`). 3 `Error>` references per file, 0 `unknown` references.                   |
| 4   | Zero type assertions in parser-to-consumer chain (25 files)                 | ✓ VERIFIED | grep for `as [A-Z]` across all 25 files found 0 matches. No `as` type assertions anywhere in the chain.                                                                                                                                    |
| 5   | All 4 packages + test app build with zero errors                            | ✓ VERIFIED | `@lsp-indexer/types` build success (tsup DTS). `@lsp-indexer/node` build success (tsup DTS). `@lsp-indexer/react` build success (tsup DTS). `@lsp-indexer/next` build success (tsup DTS). Test app `tsc --noEmit` passes with zero errors. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact                                       | Expected                                                                          | Status     | Details                                                                                       |
| ---------------------------------------------- | --------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------- |
| `packages/node/src/parsers/profiles.ts`        | 3-overload `parseProfile`/`parseProfiles`                                         | ✓ VERIFIED | 84 lines, 6 export function declarations, 2 `<const I>`, `ProfileResult<I>` return            |
| `packages/node/src/parsers/digital-assets.ts`  | 3-overload `parseDigitalAsset`/`parseDigitalAssets`                               | ✓ VERIFIED | 135 lines, 6 export function declarations, 2 `<const I>`, `DigitalAssetResult<I>` return      |
| `packages/node/src/parsers/nfts.ts`            | 3-overload `parseNft`/`parseNfts`                                                 | ✓ VERIFIED | 126 lines, 6 export function declarations, 2 `<const I>`, `NftResult<I>` return               |
| `packages/node/src/parsers/owned-assets.ts`    | 3-overload `parseOwnedAsset`/`parseOwnedAssets`                                   | ✓ VERIFIED | 91 lines, 6 export function declarations, 2 `<const I>`, `OwnedAssetResult<I>` return         |
| `packages/node/src/parsers/owned-tokens.ts`    | 3-overload `parseOwnedToken`/`parseOwnedTokens`                                   | ✓ VERIFIED | 103 lines, 6 export function declarations, 2 `<const I>`, `OwnedTokenResult<I>` return        |
| `packages/node/src/services/profiles.ts`       | 3-overload `fetchProfile`/`fetchProfiles`                                         | ✓ VERIFIED | 309 lines, 8 export async function declarations, 2 `<const I>`, `ProfileResult<I>`            |
| `packages/node/src/services/digital-assets.ts` | 3-overload `fetchDigitalAsset`/`fetchDigitalAssets`                               | ✓ VERIFIED | 307 lines, 8 export async function declarations, 2 `<const I>`, `DigitalAssetResult<I>`       |
| `packages/node/src/services/nfts.ts`           | 3-overload `fetchNft`/`fetchNfts`                                                 | ✓ VERIFIED | 350 lines, 8 export async function declarations, 2 `<const I>`, `NftResult<I>`                |
| `packages/node/src/services/owned-assets.ts`   | 3-overload `fetchOwnedAsset`/`fetchOwnedAssets`                                   | ✓ VERIFIED | 317 lines, 8 export async function declarations, 2 `<const I>`, `OwnedAssetResult<I>`         |
| `packages/node/src/services/owned-tokens.ts`   | 3-overload `fetchOwnedToken`/`fetchOwnedTokens`                                   | ✓ VERIFIED | 323 lines, 8 export async function declarations, 2 `<const I>`, `OwnedTokenResult<I>`         |
| `packages/next/src/actions/profiles.ts`        | 3-overload `getProfile`/`getProfiles`                                             | ✓ VERIFIED | 84 lines, 8 export async function declarations, 2 `<const I>`, `ProfileResult<I>`             |
| `packages/next/src/actions/digital-assets.ts`  | 3-overload `getDigitalAsset`/`getDigitalAssets`                                   | ✓ VERIFIED | 84 lines, 8 export async function declarations, 2 `<const I>`, `DigitalAssetResult<I>`        |
| `packages/next/src/actions/nfts.ts`            | 3-overload `getNft`/`getNfts`                                                     | ✓ VERIFIED | 98 lines, 8 export async function declarations, 2 `<const I>`, `NftResult<I>`                 |
| `packages/next/src/actions/owned-assets.ts`    | 3-overload `getOwnedAsset`/`getOwnedAssets`                                       | ✓ VERIFIED | 84 lines, 8 export async function declarations, 2 `<const I>`, `OwnedAssetResult<I>`          |
| `packages/next/src/actions/owned-tokens.ts`    | 3-overload `getOwnedToken`/`getOwnedTokens`                                       | ✓ VERIFIED | 84 lines, 8 export async function declarations, 2 `<const I>`, `OwnedTokenResult<I>`          |
| `packages/react/src/hooks/profiles.ts`         | 3-overload `useProfile`/`useProfiles`/`useInfiniteProfiles` + return type aliases | ✓ VERIFIED | 245 lines, 12 export function declarations, 3 `<const I>`, 3 return type aliases with `Error` |
| `packages/react/src/hooks/digital-assets.ts`   | 3-overload hooks + return type aliases                                            | ✓ VERIFIED | 268 lines, 12 export function declarations, 3 `<const I>`, 3 return type aliases with `Error` |
| `packages/react/src/hooks/nfts.ts`             | 3-overload hooks + return type aliases                                            | ✓ VERIFIED | 261 lines, 12 export function declarations, 3 `<const I>`, 3 return type aliases with `Error` |
| `packages/react/src/hooks/owned-assets.ts`     | 3-overload hooks + return type aliases                                            | ✓ VERIFIED | 262 lines, 12 export function declarations, 3 `<const I>`, 3 return type aliases with `Error` |
| `packages/react/src/hooks/owned-tokens.ts`     | 3-overload hooks + return type aliases                                            | ✓ VERIFIED | 263 lines, 12 export function declarations, 3 `<const I>`, 3 return type aliases with `Error` |
| `packages/next/src/hooks/profiles.ts`          | 3-overload hooks + return type aliases (via server actions)                       | ✓ VERIFIED | 238 lines, 12 export function declarations, 3 `<const I>`, 3 return type aliases with `Error` |
| `packages/next/src/hooks/digital-assets.ts`    | 3-overload hooks + return type aliases                                            | ✓ VERIFIED | 245 lines, 12 export function declarations, 3 `<const I>`, 3 return type aliases with `Error` |
| `packages/next/src/hooks/nfts.ts`              | 3-overload hooks + return type aliases                                            | ✓ VERIFIED | 255 lines, 12 export function declarations, 3 `<const I>`, 3 return type aliases with `Error` |
| `packages/next/src/hooks/owned-assets.ts`      | 3-overload hooks + return type aliases                                            | ✓ VERIFIED | 245 lines, 12 export function declarations, 3 `<const I>`, 3 return type aliases with `Error` |
| `packages/next/src/hooks/owned-tokens.ts`      | 3-overload hooks + return type aliases                                            | ✓ VERIFIED | 246 lines, 12 export function declarations, 3 `<const I>`, 3 return type aliases with `Error` |

### Key Link Verification

| From                | To             | Via                                                           | Status  | Details                                                                                                                               |
| ------------------- | -------------- | ------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| parsers             | services       | Services call `parseX(raw, include)` passing generic include  | ✓ WIRED | All 5 service files import and call their corresponding parser functions with include parameter                                       |
| services            | actions        | Actions call `fetchX(url, { ...params, include })`            | ✓ WIRED | All 5 action files import and call corresponding service functions, branching on `include` presence                                   |
| services            | react hooks    | Hooks call `fetchX(url, { ...params, include })` in `queryFn` | ✓ WIRED | All 5 react hook files import service functions and call them in useQuery/useInfiniteQuery queryFn                                    |
| actions             | next hooks     | Hooks call `getX(params)` server actions in `queryFn`         | ✓ WIRED | All 5 next hook files import from `../actions/` and call server actions in queryFn                                                    |
| return type aliases | hook overloads | `UseXReturn<XResult<I>>` used on generic overload             | ✓ WIRED | All 10 hook files define return type aliases and use them as return types on all 3 overloads                                          |
| gold standard match | all domains    | Same overload structure as followers                          | ✓ WIRED | All 5 domains match followers pattern: same overload order (generic → no-include → dynamic), same `<const I extends XInclude>` syntax |

### Requirements Coverage

| Requirement                                                                                                                              | Status      | Blocking Issue |
| ---------------------------------------------------------------------------------------------------------------------------------------- | ----------- | -------------- |
| DX-05: All domain hooks/services/actions use 3-overload generic `<const I>` pattern with `XResult<I>` narrowing and zero type assertions | ✓ SATISFIED | None           |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact                                                                                                                                                                         |
| ---- | ---- | ------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| —    | —    | —       | —        | No anti-patterns found across all 25 files. Zero TODO/FIXME/placeholder/stub patterns. Empty returns in services are legitimate (Hasura where-clause builders returning `{}`). |

### Human Verification Required

### 1. Type Narrowing End-to-End in IDE

**Test:** In a consuming component, call `useProfile({ address, include: { name: true, tags: true } as const })` and hover over the destructured result — verify that the return type shows `ProfileResult<{ name: true; tags: true }>` with only `address`, `name`, `tags` fields, not all `Profile` fields.

**Expected:** IDE shows narrowed type with only included fields + base fields.

**Why human:** Requires TypeScript language server evaluation of conditional types — can't verify via grep.

### 2. Error Type in IDE

**Test:** In a component using `useProfile`, hover over the destructured `error` variable.

**Expected:** Type shows `Error | null`, not `unknown | null` or `Error | unknown`.

**Why human:** Requires TypeScript language server to resolve the `Omit<UseQueryResult<..., Error>, 'data'>` expansion.

### Gaps Summary

No gaps found. All 5 must-haves verified. The phase goal is fully achieved:

- **25 files** across 5 domains × 5 layers all have the 3-overload `<const I>` generic pattern
- **XResult<I>** narrowed return types propagate from parser → service → action → hook → consumer
- **Error** (not `unknown`) typed on all hook returns via explicit `UseQueryResult<T, Error>` aliases
- **Zero type assertions** in the entire chain
- **All 4 packages + test app** build and type-check with zero errors

The pattern is consistent with the followers gold standard and ready for future domains (9.7+) to follow.

---

_Verified: 2026-02-23T13:15:00Z_
_Verifier: Claude (gsd-verifier)_
