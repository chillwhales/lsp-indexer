---
phase: 09.10-data-changed-events
plan: 03
subsystem: react, next
tags:
  [
    hooks,
    server-actions,
    tanstack-query,
    data-changed-events,
    token-id-data-changed-events,
    dx-04,
    dx-05,
  ]

# Dependency graph
requires:
  - phase: 09.10-02
    provides: fetchDataChangedEvents, fetchTokenIdDataChangedEvents, dataChangedEventKeys, tokenIdDataChangedEventKeys
  - phase: 09.10-01
    provides: DataChangedEvent + TokenIdDataChangedEvent types, Zod schemas, include types
provides:
  - useDataChangedEvents and useInfiniteDataChangedEvents React hooks (browser → Hasura)
  - useTokenIdDataChangedEvents and useInfiniteTokenIdDataChangedEvents React hooks (browser → Hasura)
  - getDataChangedEvents and getTokenIdDataChangedEvents Next.js server actions (server → Hasura)
  - useDataChangedEvents and useInfiniteDataChangedEvents Next.js hooks (browser → server action → Hasura)
  - useTokenIdDataChangedEvents and useInfiniteTokenIdDataChangedEvents Next.js hooks (browser → server action → Hasura)
  - Barrel exports in both react and next package index.ts
affects: [09.10-04 playground]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 3-overload <const I> generic pattern (DX-05) for include-based type narrowing
    - flat return shape with destructured TanStack Query state
    - server action routing pattern for Next.js hooks

key-files:
  created:
    - packages/react/src/hooks/data-changed-events.ts
    - packages/react/src/hooks/token-id-data-changed-events.ts
    - packages/next/src/actions/data-changed-events.ts
    - packages/next/src/actions/token-id-data-changed-events.ts
    - packages/next/src/hooks/data-changed-events.ts
    - packages/next/src/hooks/token-id-data-changed-events.ts
  modified:
    - packages/react/src/index.ts
    - packages/next/src/index.ts

key-decisions:
  - 'All hooks follow encrypted-assets pattern exactly — consistent API across all domains'
  - 'React hooks use getClientUrl() for direct browser queries, Next.js hooks route through server actions'
  - 'Both domains use same flat return shape: { items, totalCount, ...queryState } for useQuery, { items, hasNextPage, fetchNextPage, isFetchingNextPage, ...queryState } for useInfiniteQuery'

patterns-established: []

# Metrics
duration: 4min
completed: 2026-02-25
---

# Phase 09.10 Plan 03: React Hooks, Next.js Server Actions, and Next.js Hooks for Data Changed Events Summary

**4 React hooks + 2 server actions + 4 Next.js hooks delivering the complete developer-facing API for both DataChangedEvent and TokenIdDataChangedEvent domains with include-based type narrowing**

## Performance

- **Duration:** 4 min
- **Started:** 2026-02-25T07:54:55Z
- **Completed:** 2026-02-25T07:59:07Z
- **Tasks:** 2
- **Files modified:** 8

## Accomplishments

- Created 2 React hooks for DataChangedEvent domain: `useDataChangedEvents` (paginated list) and `useInfiniteDataChangedEvents` (infinite scroll) — query Hasura directly via `getClientUrl()`
- Created 2 React hooks for TokenIdDataChangedEvent domain: `useTokenIdDataChangedEvents` and `useInfiniteTokenIdDataChangedEvents` — same direct browser pattern
- Created 2 Next.js server actions: `getDataChangedEvents` and `getTokenIdDataChangedEvents` — wrap fetch functions with `getServerUrl()` and `'use server'` directive
- Created 2 Next.js hooks per domain (4 total) — route through server actions instead of querying Hasura directly, keeping GraphQL endpoint hidden from client
- All 10 entry points use 3-overload `<const I>` generic pattern (DX-05) for precise type narrowing based on `include` parameter
- All hooks support `include` parameter for conditional field narrowing (DX-04)
- Wired barrel exports in react/src/index.ts (4 new hooks) and next/src/index.ts (2 actions + 4 hooks) in alphabetical order
- All 4 packages (types, node, react, next) build and typecheck successfully

## Task Commits

Each task was committed atomically:

1. **Task 1: Create React hooks, Next.js server actions, and Next.js hooks for both domains** - `1b98206` (feat)
2. **Task 2: Wire barrel exports and validate builds across all packages** - `ae63966` (feat)

## Files Created/Modified

- `packages/react/src/hooks/data-changed-events.ts` — 2 TanStack Query hooks for DataChangedEvent (196 lines)
- `packages/react/src/hooks/token-id-data-changed-events.ts` — 2 TanStack Query hooks for TokenIdDataChangedEvent (207 lines)
- `packages/next/src/actions/data-changed-events.ts` — Server action wrapping fetchDataChangedEvents (53 lines)
- `packages/next/src/actions/token-id-data-changed-events.ts` — Server action wrapping fetchTokenIdDataChangedEvents (57 lines)
- `packages/next/src/hooks/data-changed-events.ts` — 2 Next.js hooks routing through getDataChangedEvents server action (189 lines)
- `packages/next/src/hooks/token-id-data-changed-events.ts` — 2 Next.js hooks routing through getTokenIdDataChangedEvents server action (200 lines)
- `packages/react/src/index.ts` — Added barrel exports for data-changed-events and token-id-data-changed-events hooks
- `packages/next/src/index.ts` — Added barrel exports for data-changed-events and token-id-data-changed-events actions + hooks

## Decisions Made

- **Exact pattern replication:** All files follow the encrypted-assets hooks/actions pattern exactly — consistency across all domains
- **Flat return shape:** `{ items, totalCount, ...rest }` for useQuery hooks, `{ items, hasNextPage, fetchNextPage, isFetchingNextPage, ...rest }` for infinite hooks — prevents TanStack Query data wrapper exposure
- **Memoized page flattening:** Infinite hooks memoize `flatMap` to avoid re-flattening on every render

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- All 10 consumer-facing entry points delivered for both data changed event domains
- Ready for 09.10-04-PLAN.md (playground / integration verification)
- No blockers or concerns

## Self-Check: PASSED
