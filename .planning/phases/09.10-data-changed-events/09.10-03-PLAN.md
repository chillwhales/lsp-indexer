---
phase: 09.10-data-changed-events
plan: 03
type: execute
wave: 3
depends_on: ['09.10-02']
files_modified:
  - packages/react/src/hooks/data-changed-events.ts
  - packages/react/src/hooks/token-id-data-changed-events.ts
  - packages/react/src/index.ts
  - packages/next/src/actions/data-changed-events.ts
  - packages/next/src/actions/token-id-data-changed-events.ts
  - packages/next/src/hooks/data-changed-events.ts
  - packages/next/src/hooks/token-id-data-changed-events.ts
  - packages/next/src/index.ts
autonomous: true

must_haves:
  truths:
    - 'useDataChangedEvents returns typed data changed events list with loading/error states and totalCount'
    - 'useInfiniteDataChangedEvents returns flattened array with fetchNextPage/hasNextPage infinite scroll controls'
    - 'useTokenIdDataChangedEvents returns typed token-level data changed events list'
    - 'useInfiniteTokenIdDataChangedEvents returns infinite scroll for token-level events'
    - 'All 4 hooks support include parameter for conditional field narrowing'
    - 'Server actions getDataChangedEvents and getTokenIdDataChangedEvents wrap fetch functions for server-side execution'
    - 'Next.js hooks route through server actions via getServerUrl()'
    - 'React hooks use getClientUrl() for direct browser → Hasura queries'
    - 'All hooks use 3-overload generic <const I> pattern (DX-05)'
    - 'All exports wired through react and next package index.ts files'
    - 'pnpm build succeeds across all 4 packages'
  artifacts:
    - path: 'packages/react/src/hooks/data-changed-events.ts'
      provides: '2 TanStack Query hooks for DataChangedEvent domain (browser → Hasura directly)'
      exports: ['useDataChangedEvents', 'useInfiniteDataChangedEvents']
    - path: 'packages/react/src/hooks/token-id-data-changed-events.ts'
      provides: '2 TanStack Query hooks for TokenIdDataChangedEvent domain'
      exports: ['useTokenIdDataChangedEvents', 'useInfiniteTokenIdDataChangedEvents']
    - path: 'packages/next/src/actions/data-changed-events.ts'
      provides: 'Server action for DataChangedEvent domain'
      exports: ['getDataChangedEvents']
    - path: 'packages/next/src/actions/token-id-data-changed-events.ts'
      provides: 'Server action for TokenIdDataChangedEvent domain'
      exports: ['getTokenIdDataChangedEvents']
    - path: 'packages/next/src/hooks/data-changed-events.ts'
      provides: '2 TanStack Query hooks routing through server action'
      exports: ['useDataChangedEvents', 'useInfiniteDataChangedEvents']
    - path: 'packages/next/src/hooks/token-id-data-changed-events.ts'
      provides: '2 TanStack Query hooks routing through server action'
      exports: ['useTokenIdDataChangedEvents', 'useInfiniteTokenIdDataChangedEvents']
    - path: 'packages/react/src/index.ts'
      provides: 'Barrel re-export of data changed event hooks'
    - path: 'packages/next/src/index.ts'
      provides: 'Barrel re-export of data changed event actions and hooks'
  key_links:
    - from: 'packages/react/src/hooks/data-changed-events.ts'
      to: '@lsp-indexer/node'
      via: 'fetchDataChangedEvents, dataChangedEventKeys, getClientUrl imports'
      pattern: "from '@lsp-indexer/node'"
    - from: 'packages/next/src/actions/data-changed-events.ts'
      to: '@lsp-indexer/node'
      via: 'fetchDataChangedEvents, getServerUrl imports'
      pattern: "from '@lsp-indexer/node'"
    - from: 'packages/next/src/hooks/data-changed-events.ts'
      to: 'packages/next/src/actions/data-changed-events.ts'
      via: 'Server action import for hook routing'
      pattern: "from '../actions/data-changed-events'"
---

<objective>
Create React hooks (4), Next.js server actions (2), and Next.js hooks (4) for BOTH the DataChangedEvent and TokenIdDataChangedEvent domains — the consumer-facing API layer.

Purpose: This plan delivers the complete developer-facing API for data change event queries. 4 React hooks query Hasura directly from the browser. 2 server actions route through the server. 4 Next.js hooks use the server actions. All support `include` for conditional field narrowing (DX-04) and use the 3-overload `<const I>` generic pattern (DX-05).

Output: 6 new hook/action files (3 per domain), 2 updated index.ts barrel exports. All 4 packages build successfully.
</objective>

<execution_context>
@/home/coder/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.10-data-changed-events/09.10-CONTEXT.md
@.planning/phases/09.10-data-changed-events/09.10-02-SUMMARY.md
@packages/react/src/hooks/issued-assets.ts
@packages/react/src/hooks/creators.ts
@packages/react/src/hooks/encrypted-assets.ts
@packages/react/src/index.ts
@packages/next/src/actions/issued-assets.ts
@packages/next/src/actions/creators.ts
@packages/next/src/hooks/issued-assets.ts
@packages/next/src/hooks/creators.ts
@packages/next/src/index.ts
@packages/types/src/data-changed-events.ts
@packages/types/src/token-id-data-changed-events.ts
@packages/node/src/services/data-changed-events.ts
@packages/node/src/services/token-id-data-changed-events.ts
@packages/node/src/keys/data-changed-events.ts
@packages/node/src/keys/token-id-data-changed-events.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create React hooks, Next.js server actions, and Next.js hooks for both domains</name>
  <files>
    packages/react/src/hooks/data-changed-events.ts
    packages/react/src/hooks/token-id-data-changed-events.ts
    packages/next/src/actions/data-changed-events.ts
    packages/next/src/actions/token-id-data-changed-events.ts
    packages/next/src/hooks/data-changed-events.ts
    packages/next/src/hooks/token-id-data-changed-events.ts
  </files>
  <action>
**All 6 files follow the EXACT same patterns as the encrypted-assets or issued-assets domains.** The only differences are type names and import paths. This is pure replication work — 6 files, ~50-100 lines each.

---

**DataChangedEvent domain (3 files):**

**1. `packages/react/src/hooks/data-changed-events.ts`** — 2 TanStack Query hooks. Follow `hooks/encrypted-assets.ts` exactly (same structure: 2 hooks, no singular hook, no direction param, no count hook).

- `useDataChangedEvents` — uses `useQuery` with `dataChangedEventKeys.list(...)` and `fetchDataChangedEvents(getClientUrl(), params)`
- `useInfiniteDataChangedEvents` — uses `useInfiniteQuery` with `dataChangedEventKeys.infinite(...)` and `fetchDataChangedEvents(getClientUrl(), { ...params, offset })` with `getNextPageParam` based on `totalCount`

Both hooks use 3-overload `<const I>` generic pattern with return type aliases:

```typescript
type UseDataChangedEventsReturn<F> = UseQueryResult<FetchDataChangedEventsResult<F>, Error>;
type UseInfiniteDataChangedEventsReturn<F> = UseInfiniteQueryResult<
  InfiniteData<FetchDataChangedEventsResult<F>>,
  Error
>;
```

**Imports from `@lsp-indexer/node`:** `fetchDataChangedEvents`, `dataChangedEventKeys`, `getClientUrl`
**Imports from `@lsp-indexer/types`:** `DataChangedEventInclude`, `DataChangedEventResult`, `DataChangedEvent`, `PartialDataChangedEvent`, `UseDataChangedEventsParams`, `UseInfiniteDataChangedEventsParams`, `FetchDataChangedEventsResult`

**NOTE:** `FetchDataChangedEventsResult` is a TYPE export from `@lsp-indexer/node` — import with `type` keyword.

**2. `packages/next/src/actions/data-changed-events.ts`** — server action. Follow `actions/encrypted-assets.ts` exactly.

```typescript
'use server';
```

- `getDataChangedEvents` — wraps `fetchDataChangedEvents(getServerUrl(), params)`
- 3-overload `<const I>` generic pattern matching the service function

**3. `packages/next/src/hooks/data-changed-events.ts`** — 2 hooks routing through server action. Follow `hooks/encrypted-assets.ts` in the next package.

- Same structure as react hooks but calls `getDataChangedEvents(params)` instead of `fetchDataChangedEvents(getClientUrl(), params)`
- Uses `dataChangedEventKeys` from `@lsp-indexer/node` for cache keys

---

**TokenIdDataChangedEvent domain (3 files):**

Identical pattern, different type names. Copy data-changed-events files and replace:

- `DataChangedEvent` → `TokenIdDataChangedEvent`
- `dataChangedEvent` → `tokenIdDataChangedEvent`
- `dataChangedEventKeys` → `tokenIdDataChangedEventKeys`
- `fetchDataChangedEvents` → `fetchTokenIdDataChangedEvents`
- `getDataChangedEvents` → `getTokenIdDataChangedEvents`
- `useDataChangedEvents` → `useTokenIdDataChangedEvents`
- `useInfiniteDataChangedEvents` → `useInfiniteTokenIdDataChangedEvents`
- `UseDataChangedEventsParams` → `UseTokenIdDataChangedEventsParams`
- `UseInfiniteDataChangedEventsParams` → `UseInfiniteTokenIdDataChangedEventsParams`
- `FetchDataChangedEventsResult` → `FetchTokenIdDataChangedEventsResult`

**Import paths change:**

- `'./data-changed-events'` → `'./token-id-data-changed-events'`
- `'../actions/data-changed-events'` → `'../actions/token-id-data-changed-events'`

**VERIFY after each file:** Check that all import references are correct and no leftover "DataChangedEvent" references appear in token-id files.
</action>
<verify>

1. `pnpm --filter @lsp-indexer/react typecheck` passes
2. `pnpm --filter @lsp-indexer/next typecheck` passes
3. useDataChangedEvents returns typed list with loading/error states
4. useInfiniteDataChangedEvents returns flattened array with fetchNextPage/hasNextPage
5. useTokenIdDataChangedEvents returns typed list with loading/error states
6. useInfiniteTokenIdDataChangedEvents returns flattened array with fetchNextPage/hasNextPage
7. Server actions have `'use server'` directive
8. Next.js hooks import from `../actions/` (NOT from `@lsp-indexer/node` for fetch)
9. React hooks import `getClientUrl` from `@lsp-indexer/node`
10. All hooks use 3-overload `<const I>` generic pattern
11. No cross-contamination between DataChangedEvent and TokenIdDataChangedEvent types
    </verify>
    <done>6 hook/action files created (3 per domain). All hooks support include parameter for conditional field narrowing. Server actions route through server. React hooks query Hasura directly. All use 3-overload generic pattern.</done>
    </task>

<task type="auto">
  <name>Task 2: Wire barrel exports and validate builds across all packages</name>
  <files>
    packages/react/src/index.ts
    packages/next/src/index.ts
  </files>
  <action>
**Update `packages/react/src/index.ts`** — add barrel exports for both domains. Place in alphabetical order among existing exports:

```typescript
export { useDataChangedEvents, useInfiniteDataChangedEvents } from './hooks/data-changed-events';
// ... existing exports ...
export {
  useTokenIdDataChangedEvents,
  useInfiniteTokenIdDataChangedEvents,
} from './hooks/token-id-data-changed-events';
```

**Update `packages/next/src/index.ts`** — add barrel exports for both domains:

```typescript
// Data changed events
export { getDataChangedEvents } from './actions/data-changed-events';
export { useDataChangedEvents, useInfiniteDataChangedEvents } from './hooks/data-changed-events';

// Token ID data changed events
export { getTokenIdDataChangedEvents } from './actions/token-id-data-changed-events';
export {
  useTokenIdDataChangedEvents,
  useInfiniteTokenIdDataChangedEvents,
} from './hooks/token-id-data-changed-events';
```

---

**Build validation across ALL 4 packages:**

```bash
pnpm --filter @lsp-indexer/types build
pnpm --filter @lsp-indexer/node build
pnpm --filter @lsp-indexer/react build
pnpm --filter @lsp-indexer/next build
```

All 4 must pass. If any build fails, fix the issue before proceeding.

**Typecheck all 4 packages:**

```bash
pnpm --filter @lsp-indexer/types typecheck
pnpm --filter @lsp-indexer/node typecheck
pnpm --filter @lsp-indexer/react typecheck
pnpm --filter @lsp-indexer/next typecheck
```

  </action>
  <verify>

1. `pnpm --filter @lsp-indexer/types build` passes
2. `pnpm --filter @lsp-indexer/node build` passes
3. `pnpm --filter @lsp-indexer/react build` passes
4. `pnpm --filter @lsp-indexer/next build` passes
5. `pnpm --filter @lsp-indexer/types typecheck` passes
6. `pnpm --filter @lsp-indexer/node typecheck` passes
7. `pnpm --filter @lsp-indexer/react typecheck` passes
8. `pnpm --filter @lsp-indexer/next typecheck` passes
9. react/src/index.ts exports 4 new hooks
10. next/src/index.ts exports 2 new actions + 4 new hooks
    </verify>
    <done>All barrel exports wired. All 4 packages build and typecheck successfully. React exports 4 new hooks. Next exports 2 new actions + 4 new hooks. Complete developer-facing API ready for both data changed event domains.</done>
    </task>

</tasks>

<verification>
1. All 4 packages build successfully
2. All 4 packages typecheck successfully
3. React package exports: useDataChangedEvents, useInfiniteDataChangedEvents, useTokenIdDataChangedEvents, useInfiniteTokenIdDataChangedEvents
4. Next package exports: getDataChangedEvents, getTokenIdDataChangedEvents + 4 hooks
5. All hooks use 3-overload generic `<const I>` pattern (DX-05)
6. Server actions have 'use server' directive
7. React hooks use getClientUrl(), Next hooks route through server action
</verification>

<success_criteria>

- 4 React hooks + 4 Next.js hooks + 2 server actions = 10 consumer-facing entry points
- All hooks support include for conditional field narrowing (DX-04)
- All hooks use 3-overload generic pattern (DX-05)
- All 4 packages build without errors
- Barrel exports are complete and in alphabetical order
  </success_criteria>

<output>
After completion, create `.planning/phases/09.10-data-changed-events/09.10-03-SUMMARY.md`
</output>
