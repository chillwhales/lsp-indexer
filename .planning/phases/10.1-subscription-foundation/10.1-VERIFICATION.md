---
phase: 10.1-subscription-foundation
verified: 2026-03-01T22:15:00Z
status: gaps_found
score: 3/5 must-haves verified
gaps:
  - truth: 'IndexerSubscriptionProvider wraps the test app inside QueryClientProvider'
    status: failed
    reason: "IndexerSubscriptionProvider is used in providers.tsx but never imported — TS2304: Cannot find name 'IndexerSubscriptionProvider'"
    artifacts:
      - path: 'apps/test/src/app/providers.tsx'
        issue: "Missing import: `import { IndexerSubscriptionProvider } from '@lsp-indexer/react';`"
    missing:
      - "Add import { IndexerSubscriptionProvider } from '@lsp-indexer/react' to providers.tsx"
  - truth: 'All 4 packages + test app build with zero type errors'
    status: failed
    reason: 'Test app has 3 TypeScript errors — 2 missing import statements prevent compilation'
    artifacts:
      - path: 'apps/test/src/app/providers.tsx'
        issue: "TS2304: Cannot find name 'IndexerSubscriptionProvider' (lines 24, 26)"
      - path: 'apps/test/src/components/nav.tsx'
        issue: "TS2552: Cannot find name 'Radio' (line 66) — Radio icon not imported from lucide-react"
    missing:
      - "Add import { IndexerSubscriptionProvider } from '@lsp-indexer/react' to providers.tsx"
      - 'Add Radio to the lucide-react import in nav.tsx (lines 3-22)'
---

# Phase 10.1: Subscription Foundation Verification Report

**Phase Goal:** Type-safe subscription infrastructure with zero type assertions, exported where-clause builders, and test app wired with subscription provider — the foundation all 12 domain sub-phases build on.
**Verified:** 2026-03-01T22:15:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                    | Status     | Evidence                                                                                                                                                                                                                                                                                                                |
| --- | -------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | SubscriptionConfig uses 4 generic params with typed extract function — zero type assertions in data path | ✓ VERIFIED | `packages/node/src/subscriptions/types.ts` defines `SubscriptionConfig<TResult, TVariables, TRaw, TParsed>` with `extract: (result: TResult) => TRaw[]`. No `as any`, `as unknown`, or `: any` in data path. `config.extract(result.data)` replaces `result.data[this.config.dataKey]`.                                 |
| 2   | buildXWhere functions are importable from @lsp-indexer/node for all 12 domains                           | ✓ VERIFIED | All 12 service files have `export function buildXWhere`: profiles, digital-assets, nfts, owned-assets, owned-tokens, followers, creators, issued-assets, encrypted-assets, data-changed-events, token-id-data-changed-events, universal-receiver-events. `packages/node/src/index.ts` uses `export *` for all services. |
| 3   | IndexerSubscriptionProvider wraps the test app inside QueryClientProvider                                | ✗ FAILED   | `IndexerSubscriptionProvider` is used in JSX on lines 24-26 of `providers.tsx` but **the import statement is missing**. TypeScript confirms: `TS2304: Cannot find name 'IndexerSubscriptionProvider'`.                                                                                                                  |
| 4   | Subscriptions playground page skeleton exists at /subscriptions                                          | ✓ VERIFIED | `apps/test/src/app/subscriptions/page.tsx` exists with proper 'use client' directive, title "Subscriptions Playground", description, 12 domain placeholder cards, shadcn Card components.                                                                                                                               |
| 5   | All 4 packages + test app build with zero type errors                                                    | ✗ FAILED   | 4 packages (types, node, react, next) build cleanly. Test app has 3 TypeScript errors: (1) TS2304 for `IndexerSubscriptionProvider` in providers.tsx (×2), (2) TS2552 for `Radio` in nav.tsx. NOTE: The Next.js `use client` build failure is pre-existing and not caused by this phase.                                |

**Score:** 3/5 truths verified

### Required Artifacts

| Artifact                                   | Expected                                                                | Status     | Details                                                                                                                                                  |
| ------------------------------------------ | ----------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/node/src/subscriptions/types.ts` | Type-safe SubscriptionConfig with 4 generic params and extract function | ✓ VERIFIED | 31 lines, imports TypedDocumentString, defines `SubscriptionConfig<TResult, TVariables, TRaw, TParsed>` with document, variables, extract, parser fields |
| `apps/test/src/app/subscriptions/page.tsx` | Subscriptions playground page skeleton                                  | ✓ VERIFIED | 58 lines, 'use client', Radio icon, 12 domain placeholder cards, shadcn Card layout                                                                      |
| `apps/test/src/app/providers.tsx`          | Provider tree with IndexerSubscriptionProvider                          | ✗ STUB     | IndexerSubscriptionProvider is used in JSX but never imported — will crash at runtime and fail TypeScript compilation                                    |

### Key Link Verification

| From                                                       | To                                         | Via                                       | Status      | Details                                                                                                                                         |
| ---------------------------------------------------------- | ------------------------------------------ | ----------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/node/src/subscriptions/types.ts`                 | `packages/node/src/graphql/graphql.ts`     | TypedDocumentString import                | ✓ WIRED     | Line 1: `import type { TypedDocumentString } from '../graphql/graphql'`; Line 21: `document: TypedDocumentString<TResult, TVariables>`          |
| `packages/node/src/subscriptions/subscription-instance.ts` | `packages/node/src/subscriptions/types.ts` | extract function replacing string dataKey | ✓ WIRED     | Line 3: `import type { SubscriptionConfig } from './types'`; Line 131: `const rawData = this.config.extract(result.data)` — no dataKey anywhere |
| `apps/test/src/app/providers.tsx`                          | `@lsp-indexer/react`                       | IndexerSubscriptionProvider import        | ✗ NOT_WIRED | No import statement exists. `IndexerSubscriptionProvider` used on lines 24-26 without being imported.                                           |

### Requirements Coverage

| Requirement | Source Plan  | Description                                                                                           | Status      | Evidence                                                                                                                                                                                   |
| ----------- | ------------ | ----------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| SUB-01      | 10.1-01-PLAN | Developer can establish WebSocket connection to Hasura with `graphql-ws`, with automatic reconnection | ✓ SATISFIED | `SubscriptionClient` in `client.ts` uses `createClient` from `graphql-ws` with `retryAttempts: Infinity`, `shouldRetry: () => true`, connection state tracking, and reconnection callbacks |

### Anti-Patterns Found

| File                                        | Line  | Pattern                                                | Severity   | Impact                                                                                                                                                                                                             |
| ------------------------------------------- | ----- | ------------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `packages/node/src/subscriptions/client.ts` | 57    | `Set<GenericSubscriptionInstance<any, any, any, any>>` | ⚠️ Warning | Internal bookkeeping Set uses `any` for type erasure. Not in the data path — tracked subscriptions are only added/deleted, never read with type info. Acceptable tradeoff for heterogeneous subscription tracking. |
| `apps/test/src/app/providers.tsx`           | 24-26 | Missing import for `IndexerSubscriptionProvider`       | 🛑 Blocker | TypeScript error TS2304 prevents compilation                                                                                                                                                                       |
| `apps/test/src/components/nav.tsx`          | 66    | Missing import for `Radio` icon                        | 🛑 Blocker | TypeScript error TS2552 prevents compilation                                                                                                                                                                       |

### Human Verification Required

### 1. WebSocket Subscription Provider Initialization

**Test:** After fixing the missing imports, start the test app and check browser console for WebSocket connection errors
**Expected:** No errors on page load; WebSocket should only connect lazily when a subscription is created (not on provider mount)
**Why human:** Provider initialization behavior requires runtime browser verification

### 2. Subscriptions Playground Navigation

**Test:** After fixes, navigate to /subscriptions via the sidebar link
**Expected:** Page renders with "Subscriptions Playground" title, 12 domain placeholder cards in a grid
**Why human:** Visual layout and navigation routing require browser verification

### Gaps Summary

Two missing import statements prevent the test app from compiling:

1. **`providers.tsx` missing `IndexerSubscriptionProvider` import** — The component is used in JSX but never imported from `@lsp-indexer/react`. This is a simple one-line fix: add `import { IndexerSubscriptionProvider } from '@lsp-indexer/react';` to the imports.

2. **`nav.tsx` missing `Radio` import** — The `Radio` icon is used on line 66 but not included in the lucide-react import block (lines 3-22). Fix: add `Radio,` to the destructured import.

Both gaps share the same root cause: **missing import statements after adding component/icon usage**. The underlying implementations are correct — the type-safe infrastructure, buildXWhere exports, and playground page are all properly built. Only the wiring in the test app has these two import omissions.

The 4 library packages (types, node, react, next) all build cleanly with zero errors.

---

_Verified: 2026-03-01T22:15:00Z_
_Verifier: Claude (gsd-verifier)_
