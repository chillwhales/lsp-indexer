---
phase: 10.6-owned-tokens-subscription
plan: 01
subsystem: subscriptions
tags: [react, next, websocket, graphql-ws, subscription, owned-tokens, factory-pattern]

# Dependency graph
requires:
  - phase: 10.5-owned-assets-subscription
    provides: Factory pattern template, domain subscription replication pattern
  - phase: 10.1-subscription-foundation
    provides: useSubscription hook, SubscriptionConfig<TResult,TVariables,TRaw,TParsed>, IndexerSubscriptionProvider
provides:
  - createUseOwnedTokenSubscription factory for react + next
  - useOwnedTokenSubscription hook in both @lsp-indexer/react and @lsp-indexer/next
  - Owned token hooks refactored from monolith to directory pattern (queries + subscription)
  - OwnedTokenSubscriptionDocument + buildOwnedTokenSubscriptionConfig in node service layer
  - Subscription tab in /owned-tokens playground page
affects: [10.7-creators-subscription]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Owned token domain directory refactoring pattern (single file → directory with queries + subscription)
    - buildOwnedTokenSubscriptionConfig in node service layer

key-files:
  created:
    - packages/react/src/hooks/factories/owned-tokens/create-use-owned-token-subscription.ts
    - packages/react/src/hooks/factories/owned-tokens/index.ts
    - packages/react/src/hooks/owned-tokens/use-owned-token-subscription.ts
    - packages/react/src/hooks/owned-tokens/index.ts
    - packages/react/src/hooks/types/owned-tokens.ts
    - packages/next/src/hooks/owned-tokens/use-owned-token-subscription.ts
    - packages/next/src/hooks/owned-tokens/index.ts
  modified:
    - packages/node/src/documents/owned-tokens.ts
    - packages/node/src/services/owned-tokens.ts
    - packages/node/src/graphql/gql.ts
    - packages/node/src/graphql/graphql.ts
    - packages/react/src/hooks/factories/index.ts
    - packages/react/src/hooks/types/index.ts
    - packages/react/src/hooks/owned-tokens/queries.ts (renamed from owned-tokens.ts)
    - packages/next/src/hooks/owned-tokens/queries.ts (renamed from owned-tokens.ts, fixed import path)
    - apps/test/src/app/owned-tokens/page.tsx

key-decisions:
  - 'Converted owned-tokens single files to directory modules in both react and next packages'
  - 'Only subscription factory needed — existing query hooks do not have factories in this directory'
  - 'Fixed relative import paths after file-to-directory conversion (../actions/ → ../../actions/)'

patterns-established:
  - 'Domain subscription replication confirmed for fifth domain (profiles → digital assets → NFTs → owned assets → owned tokens)'

requirements-completed: [SUB-02, SUB-03]

# Metrics
duration: 8min
completed: 2026-03-04
---

# Phase 10.6 Plan 01: Owned Tokens Subscription Summary

**useOwnedTokenSubscription with 3-overload type narrowing + directory refactoring of owned-tokens hooks in react/next (most complex domain: 46 variables, 4 nested relations)**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-03-04T18:50:34Z
- **Completed:** 2026-03-04T18:59:16Z
- **Tasks:** 1
- **Files modified:** 16

## Accomplishments

- Added `OwnedTokenSubscriptionDocument` with 46 variables (43 include + 3 pagination) — most complex subscription document in the project (4 nested relations: DA + NFT + OA + Profile)
- Ran codegen to generate `OwnedTokenSubscriptionSubscription` and `OwnedTokenSubscriptionSubscriptionVariables` types
- Added `buildOwnedTokenSubscriptionConfig` to node service layer
- Created `createUseOwnedTokenSubscription` factory with 3-overload type narrowing
- Added `useOwnedTokenSubscription` thin wrappers in both `@lsp-indexer/react` and `@lsp-indexer/next`
- Converted owned-tokens single files to directory modules (queries.ts + use-owned-token-subscription.ts + index.ts)
- Fixed relative import paths after file-to-directory conversion in next package
- Added subscription tab to `/owned-tokens` playground with connection status badges, filter/sort/include/invalidate controls
- Added `UseOwnedTokenSubscriptionParams` type with proper exports
- All 4 packages + test app build with zero errors

## Task Commits

Each task was committed atomically:

1. **Task 1: Subscription document + codegen + config builder + factory + hooks + directory conversion + playground** - `353444b` (feat)

## Files Created/Modified

- `packages/node/src/documents/owned-tokens.ts` - Added OwnedTokenSubscriptionDocument (46 variables)
- `packages/node/src/services/owned-tokens.ts` - Added buildOwnedTokenSubscriptionConfig + imports
- `packages/node/src/graphql/gql.ts` - Codegen output (subscription document mapping)
- `packages/node/src/graphql/graphql.ts` - Codegen output (OwnedTokenSubscriptionSubscription type)
- `packages/react/src/hooks/factories/owned-tokens/` - Factory + barrel export
- `packages/react/src/hooks/types/owned-tokens.ts` - UseOwnedTokenSubscriptionParams interface + query return types
- `packages/react/src/hooks/owned-tokens/` - queries.ts (renamed) + subscription wrapper + barrel
- `packages/next/src/hooks/owned-tokens/` - queries.ts (renamed, fixed import) + subscription wrapper + barrel
- `packages/react/src/hooks/factories/index.ts` - Added owned-tokens barrel export
- `packages/react/src/hooks/types/index.ts` - Added owned-tokens type export
- `apps/test/src/app/owned-tokens/page.tsx` - Added SubscriptionTab with full controls

## Decisions Made

- Converted owned-tokens single files to directory modules (consistent with owned-assets pattern from 10.5)
- Only created subscription factory — owned-tokens does not have query factories in the factories directory
- Fixed relative import paths after file-to-directory conversion (`../actions/` → `../../actions/` in next package)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- Initial import edits to the playground page partially failed due to edit tool string matching — required separate edits for lucide icons, UI component imports, and package imports
- Pre-existing build failures in `packages/indexer` (unrelated typeorm/abi issues) — worked around by building individual packages

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Owned tokens subscription pattern confirmed — fifth domain replicated from established template
- Ready for 10.7 (Creators Subscription) and remaining domain subscription phases
- All packages build cleanly

## Self-Check: PASSED

- All 7 created files verified present on disk
- Commit `353444b` verified in git log
- node, react, next packages + test app all build successfully
- `useOwnedTokenSubscription` exported in react dist
- `useOwnedTokenSubscription` exported in next dist
- `buildOwnedTokenSubscriptionConfig` exported in node dist
- `OwnedTokenSubscriptionDocument` exported in node dist

---

_Phase: 10.6-owned-tokens-subscription_
_Completed: 2026-03-04_
