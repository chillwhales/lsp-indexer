---
phase: 10.6-owned-tokens-subscription
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/react/src/subscriptions/owned-tokens.ts
  - packages/react/src/index.ts
  - apps/test/src/app/subscriptions/page.tsx
autonomous: true
requirements: [SUB-02, SUB-03]

must_haves:
  truths:
    - 'Developer can call useOwnedTokenSubscription() and get { data, isConnected, isSubscribed, error }'
    - 'All type params inferred — zero explicit type arguments at call sites'
    - 'Owned token subscription data appears in the playground page'
    - 'Cache invalidation works when invalidate: true'
  artifacts:
    - path: 'packages/react/src/subscriptions/owned-tokens.ts'
      provides: 'useOwnedTokenSubscription hook'
      contains: 'useOwnedTokenSubscription'
    - path: 'apps/test/src/app/subscriptions/page.tsx'
      provides: 'Owned tokens subscription demo section'
      contains: 'useOwnedTokenSubscription'
  key_links:
    - from: 'packages/react/src/subscriptions/owned-tokens.ts'
      to: '@lsp-indexer/node'
      via: 'buildOwnedTokenWhere + ownedTokenKeys + parseOwnedTokens + OwnedTokenSubscriptionDocument'
      pattern: 'buildOwnedTokenWhere|ownedTokenKeys|parseOwnedTokens|OwnedTokenSubscriptionDocument'
    - from: 'packages/react/src/subscriptions/owned-tokens.ts'
      to: 'packages/react/src/subscriptions/use-subscription.ts'
      via: 'useSubscription call'
      pattern: 'useSubscription'
---

<objective>
Create the `useOwnedTokenSubscription` hook and add a working owned tokens subscription demo to the playground page.

Purpose: Domain subscription hook for owned tokens. Follows the exact pattern established by profiles (10.2).

Output: `useOwnedTokenSubscription` exported from `@lsp-indexer/react`, owned tokens subscription demo visible in the test app.
</objective>

<execution_context>
@/home/coder/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-subscriptions/10-CONTEXT.md
@.planning/phases/10.1-subscription-foundation/10.1-01-SUMMARY.md
@.planning/phases/10.2-profiles-subscription/10.2-01-SUMMARY.md
@packages/node/src/services/owned-tokens.ts
@packages/node/src/keys/owned-tokens.ts
@packages/node/src/documents/owned-tokens.ts
@packages/node/src/parsers/owned-tokens.ts
@packages/react/src/index.ts
@apps/test/src/app/subscriptions/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useOwnedTokenSubscription hook + playground demo + export</name>
  <files>
    packages/react/src/subscriptions/owned-tokens.ts
    packages/react/src/index.ts
    apps/test/src/app/subscriptions/page.tsx
  </files>
  <action>
    **Create `packages/react/src/subscriptions/owned-tokens.ts`:**

    Follow the exact pattern from the 10.2 profiles SUMMARY:
    - Import `OwnedToken`, `OwnedTokenFilter` from `@lsp-indexer/types`
    - Import `buildOwnedTokenWhere`, `ownedTokenKeys`, `parseOwnedTokens`, `OwnedTokenSubscriptionDocument` from `@lsp-indexer/node`
    - `extract: (result) => result.owned_token` — Hasura table extraction
    - `parser: (raw) => parseOwnedTokens(raw)`
    - `order_by: undefined` — entity domain, Hasura default sort
    - Params interface: `UseOwnedTokenSubscriptionParams` with `{ filter?, limit?, enabled?, invalidate?, onData?, onReconnect? }`
    - `useQueryClient()` called unconditionally (Rules of Hooks), only passed when `invalidate: true`

    **Update `packages/react/src/index.ts`:**

    Add export:
    ```typescript
    export * from './subscriptions/owned-tokens';
    ```

    **Add owned tokens demo to playground page:**

    Update `apps/test/src/app/subscriptions/page.tsx` — add an owned tokens subscription section showing:
    1. Connection/subscription status badges
    2. Data count and list of owned token IDs/addresses
    3. Error display
    4. Filter input if relevant
    5. Limit selector

    **Build validation:**
    ```bash
    pnpm build
    ```

    Commit: `feat(10.6-owned-tokens): add useOwnedTokenSubscription hook + playground demo`

  </action>
  <verify>
    `pnpm build` — all packages + test app build.
    `grep "useOwnedTokenSubscription" packages/react/dist/index.js` — exported.
    `grep "useOwnedTokenSubscription" apps/test/src/app/subscriptions/page.tsx` — used in playground.
  </verify>
  <done>useOwnedTokenSubscription hook exported from @lsp-indexer/react with full type inference. Owned tokens subscription demo visible in test app playground at /subscriptions.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes for all packages + test app
- `useOwnedTokenSubscription` is importable from `@lsp-indexer/react`
- Hook accepts { filter?, limit?, enabled?, invalidate?, onData?, onReconnect? }
- Hook returns { data: OwnedToken[] | null, isConnected, isSubscribed, error: Error | null }
- All types inferred — no explicit type arguments
- Playground shows owned tokens subscription demo
</verification>

<success_criteria>

1. useOwnedTokenSubscription created with type-safe SubscriptionConfig (extract + parser, zero assertions)
2. Hook exported from @lsp-indexer/react
3. Owned tokens subscription demo works in playground
4. All packages build with zero type errors
   </success_criteria>

<output>
After completion, create `.planning/phases/10.6-owned-tokens-subscription/10.6-01-SUMMARY.md`
</output>
