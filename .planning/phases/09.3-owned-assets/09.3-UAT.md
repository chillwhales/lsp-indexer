---
status: diagnosed
phase: 09.3-owned-assets
source:
  [
    09.3-01-SUMMARY.md,
    09.3-02-SUMMARY.md,
    09.3-03-SUMMARY.md,
    09.3-04-SUMMARY.md,
    cross-domain-audit,
  ]
started: 2026-02-21T00:00:00Z
updated: 2026-02-21T00:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. All 4 SDK packages build cleanly

expected: Running `pnpm build` for all 4 packages produces ESM + CJS + DTS output with zero errors.
result: pass

### 2. Owned Assets page loads with 3 tabs

expected: Navigating to /owned-assets shows a playground page with Single, List, and Infinite tabs. Single tab is selected by default.
result: pass

### 3. Owned Assets single lookup with preset

expected: Clicking a preset button fills in holder address + asset address fields. Results show an OwnedAssetCard with formatted BigInt balance, holder address, digital asset address, block, and timestamp.
result: pass

### 4. Owned Assets list with filters

expected: On the List tab, entering a holder address filter returns filtered results. Name search filters (asset name, holder name) also work. Two rows of inputs — first row for addresses, second row for name search.
result: pass

### 5. Owned Assets infinite scroll

expected: On the Infinite tab, results load. Scrolling down or clicking "Load More" fetches additional pages. hasNextPage indicator works correctly.
result: pass

### 6. Owned Assets include toggles

expected: Toggling Balance, Block, Timestamp, Token ID Count on/off correctly shows/hides those fields in the card. Digital Asset and Holder Profile have sub-include sections with individual field toggles.
result: pass

### 3. Owned Assets single lookup with preset

expected: Clicking a preset button fills in holder address + asset address fields. Results show an OwnedAssetCard with formatted BigInt balance, holder address, digital asset address, block, and timestamp.
result: [pending]

### 4. Owned Assets list with filters

expected: On the List tab, entering a holder address filter returns filtered results. Name search filters (asset name, holder name) also work. Two rows of inputs — first row for addresses, second row for name search.
result: [pending]

### 5. Owned Assets infinite scroll

expected: On the Infinite tab, results load. Scrolling down or clicking "Load More" fetches additional pages. hasNextPage indicator works correctly.
result: [pending]

### 6. Owned Assets include toggles

expected: Toggling Balance, Block, Timestamp, Token ID Count on/off correctly shows/hides those fields in the card. Digital Asset and Holder Profile have sub-include sections with individual field toggles.
result: [pending]

### 7. Owned Tokens page loads with 3 tabs

expected: Navigating to /owned-tokens shows a playground page with Single, List, and Infinite tabs. Single tab is selected by default.
result: pass

### 8. Owned Tokens single lookup with preset

expected: Clicking a preset button fills in holder + asset address + tokenId fields. Results show an OwnedTokenCard with holder address, digital asset address, tokenId, and timestamp.
result: pass

### 9. Owned Tokens list with filters

expected: On the List tab, entering a holder address filter returns filtered results. Name search filters (asset name, holder name, token name) work. Two rows of inputs — addresses then names.
result: pass

### 10. Owned Tokens infinite scroll

expected: On the Infinite tab, results load and additional pages can be fetched.
result: pass

### 11. Owned Tokens include toggles with sub-includes

expected: Block and Timestamp can be toggled. Digital Asset, NFT, Owned Asset, and Holder Profile each have sub-include sections with individual field toggles. Toggling a parent off hides the entire nested section.
result: pass

### 8. Owned Tokens single lookup with preset

expected: Clicking a preset button fills in holder + asset address + tokenId fields. Results show an OwnedTokenCard with holder address, digital asset address, tokenId, and timestamp.
result: [pending]

### 9. Owned Tokens list with filters

expected: On the List tab, entering a holder address filter returns filtered results. Name search filters (asset name, holder name, token name) work. Two rows of inputs — addresses then names.
result: [pending]

### 10. Owned Tokens infinite scroll

expected: On the Infinite tab, results load and additional pages can be fetched.
result: [pending]

### 11. Owned Tokens include toggles with sub-includes

expected: Block and Timestamp can be toggled. Digital Asset, NFT, Owned Asset, and Holder Profile each have sub-include sections with individual field toggles. Toggling a parent off hides the entire nested section.
result: [pending]

### 12. OwnedTokenCard nested sections

expected: OwnedTokenCard shows collapsible sections for Holder (with ProfileCard), NFT metadata, Digital Asset, and Owned Asset. All use ghost Button trigger style with icon + text + ChevronDown.
result: pass

### 13. OwnedAssetCard BigInt balance formatting

expected: OwnedAssetCard shows balance formatted with token decimals (e.g., "1,000.5 TOKEN") when digitalAsset with decimals is included. Shows raw BigInt string when DA not included.
result: pass

### 14. Field naming uses developer-friendly names

expected: Cards show "Holder Address" (not "Owner"), "Digital Asset Address" (not "Address"). All UI labels say "Holder" not "Owner". Filters use holderAddress/digitalAssetAddress naming.
result: pass

### 15. Full addresses displayed without truncation

expected: All hex addresses in cards are displayed in full (not truncated with "...") and wrap correctly with break-all styling.
result: pass

### 16. RawJsonToggle handles BigInt

expected: Clicking "Raw JSON" on an OwnedAssetCard with a BigInt balance shows valid JSON (balance as string) instead of crashing.
result: pass

### 13. OwnedAssetCard BigInt balance formatting

expected: OwnedAssetCard shows balance formatted with token decimals (e.g., "1,000.5 TOKEN") when digitalAsset with decimals is included. Shows raw BigInt string when DA not included.
result: [pending]

### 14. Field naming uses developer-friendly names

expected: Cards show "Holder Address" (not "Owner"), "Digital Asset Address" (not "Address"). All UI labels say "Holder" not "Owner". Filters use holderAddress/digitalAssetAddress naming.
result: [pending]

### 15. Full addresses displayed without truncation

expected: All hex addresses in cards are displayed in full (not truncated with "...") and wrap correctly with break-all styling.
result: [pending]

### 16. RawJsonToggle handles BigInt

expected: Clicking "Raw JSON" on an OwnedAssetCard with a BigInt balance shows valid JSON (balance as string) instead of crashing.
result: [pending]

### 17. React/Next.js mode toggle works

expected: Switching between React and Next.js mode in the playground re-fetches data through the appropriate hook path (direct Hasura vs server action). Both modes return the same data.
result: pass

### 18. Navigation sidebar shows both domains

expected: Sidebar navigation shows "Owned Assets" and "Owned Tokens" as separate clickable links. Both are highlighted when active.
result: pass

### 19. Cross-domain: formatRelativeTime not duplicated

expected: The `formatRelativeTime` utility function should exist in one shared location, not copy-pasted in both owned-asset-card.tsx and owned-token-card.tsx.
result: issue
reported: "formatRelativeTime is duplicated character-for-character in owned-asset-card.tsx:20 and owned-token-card.tsx:20. Should be extracted to apps/test/src/lib/utils.ts."
severity: minor

### 20. Cross-domain: buildOwnedAssetIncludeVars barrel export

expected: `buildOwnedAssetIncludeVars` should be re-exported from `packages/node/src/index.ts` barrel, matching the pattern of `buildProfileIncludeVars`, `buildDigitalAssetIncludeVars`, and `buildNftIncludeVars`.
result: issue
reported: "buildOwnedAssetIncludeVars is exported from services/owned-assets.ts but missing from packages/node/src/index.ts barrel. Other 3 cross-domain include var builders are all exported."
severity: minor

### 21. Cross-domain: Consistent private function naming in services

expected: Private service functions (`buildWhere`, `buildOrderBy`) should either all be domain-prefixed or all unprefixed. Currently profiles/digital-assets/nfts prefix them but owned-assets/owned-tokens don't.
result: issue
reported: "owned-assets.ts and owned-tokens.ts use unprefixed buildWhere/buildOrderBy while profiles, digital-assets, and nfts use domain-prefixed names (buildProfileWhere, buildDigitalAssetWhere, buildNftWhere). Module-private so no external impact."
severity: cosmetic

### 18. Navigation sidebar shows both domains

expected: Sidebar navigation shows "Owned Assets" and "Owned Tokens" as separate clickable links. Both are highlighted when active.
result: [pending]

### 19. Cross-domain: formatRelativeTime not duplicated

expected: The `formatRelativeTime` utility function should exist in one shared location, not copy-pasted in both owned-asset-card.tsx and owned-token-card.tsx.
result: [pending]

### 20. Cross-domain: buildOwnedAssetIncludeVars barrel export

expected: `buildOwnedAssetIncludeVars` should be re-exported from `packages/node/src/index.ts` barrel, matching the pattern of `buildProfileIncludeVars`, `buildDigitalAssetIncludeVars`, and `buildNftIncludeVars`.
result: [pending]

### 21. Cross-domain: Consistent private function naming in services

expected: Private service functions (`buildWhere`, `buildOrderBy`) should either all be domain-prefixed or all unprefixed. Currently profiles/digital-assets/nfts prefix them but owned-assets/owned-tokens don't.
result: [pending]

### 22. Sub-include default value fix works correctly

expected: When all sub-include toggles are ON (default state), toggling OFF a sibling sub-include should NOT cause other sub-includes to disappear. Each sub-include section operates independently.
result: pass

## Summary

total: 22
passed: 19
issues: 3
pending: 0
skipped: 0

## Gaps

- truth: "formatRelativeTime utility should exist in one shared location, not duplicated"
  status: failed
  reason: "formatRelativeTime is duplicated character-for-character in owned-asset-card.tsx:20 and owned-token-card.tsx:20. Should be extracted to apps/test/src/lib/utils.ts."
  severity: minor
  test: 19
  root_cause: "Copy-paste during card component creation. Function was written inline in owned-asset-card.tsx and duplicated when creating owned-token-card.tsx."
  artifacts:

  - path: "apps/test/src/components/owned-asset-card.tsx"
    issue: "Contains duplicate formatRelativeTime at line 20"
  - path: "apps/test/src/components/owned-token-card.tsx"
    issue: "Contains duplicate formatRelativeTime at line 20"
    missing:
  - "Extract formatRelativeTime to apps/test/src/lib/utils.ts"
  - "Import from both card files instead of defining inline"
    debug_session: ""

- truth: "buildOwnedAssetIncludeVars should be re-exported from packages/node/src/index.ts barrel"
  status: failed
  reason: "buildOwnedAssetIncludeVars is exported from services/owned-assets.ts but missing from packages/node/src/index.ts barrel. Other 3 cross-domain include var builders are all exported."
  severity: minor
  test: 20
  root_cause: "Barrel export was not updated when buildOwnedAssetIncludeVars was made public. The other 3 builders were added to index.ts during plan 04 post-refinements but this one was missed."
  artifacts:

  - path: "packages/node/src/index.ts"
    issue: "Missing buildOwnedAssetIncludeVars in owned-assets export group"
  - path: "packages/node/src/services/owned-assets.ts"
    issue: "Function is exported (line 173) but not barrel-exported"
    missing:
  - "Add buildOwnedAssetIncludeVars to the owned-assets export line in packages/node/src/index.ts"
    debug_session: ""

- truth: "Private service functions should follow consistent naming convention"
  status: failed
  reason: "owned-assets.ts and owned-tokens.ts use unprefixed buildWhere/buildOrderBy while profiles, digital-assets, and nfts use domain-prefixed names. Module-private so no external impact."
  severity: cosmetic
  test: 21
  root_cause: "Style inconsistency introduced during plan 02 implementation. The first 3 domains established a domain-prefixed convention for private functions but the owned-assets/owned-tokens services did not follow it."
  artifacts:
  - path: "packages/node/src/services/owned-assets.ts"
    issue: "Uses buildWhere/buildOrderBy (unprefixed) at lines 35, 88"
  - path: "packages/node/src/services/owned-tokens.ts"
    issue: "Uses buildWhere/buildOrderBy (unprefixed) at lines 38, 105"
    missing:
  - "Rename buildWhere to buildOwnedAssetWhere and buildOrderBy to buildOwnedAssetOrderBy in owned-assets.ts"
  - "Rename buildWhere to buildOwnedTokenWhere and buildOrderBy to buildOwnedTokenOrderBy in owned-tokens.ts"
  - "Update internal call sites within each file"
    debug_session: ""
