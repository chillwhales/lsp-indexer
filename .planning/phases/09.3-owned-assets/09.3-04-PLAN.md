---
phase: 09.3-owned-assets
plan: 04
type: execute
wave: 4
depends_on: ['09.3-03']
files_modified:
  - apps/test/src/app/owned-assets/page.tsx
  - apps/test/src/app/owned-assets/layout.tsx
  - apps/test/src/app/owned-tokens/page.tsx
  - apps/test/src/app/owned-tokens/layout.tsx
  - apps/test/src/components/owned-asset-card.tsx
  - apps/test/src/components/owned-token-card.tsx
  - apps/test/src/components/nav.tsx
autonomous: false

must_haves:
  truths:
    - 'Developer can visit /owned-assets and see live owned asset data from Hasura'
    - 'Developer can visit /owned-tokens and see live owned token data from Hasura'
    - 'Owned assets page has 3 tabs: single lookup, list with filters, infinite scroll'
    - 'Owned tokens page has 3 tabs: single lookup, list with filters, infinite scroll'
    - 'Owned asset cards show owner + asset address + balance + timestamp'
    - 'Owned token cards show owner + asset address + token ID + timestamp'
    - 'Include toggles for digital asset and universal profile work on owned assets page'
    - 'Include toggles for digital asset, NFT, owned asset, and universal profile work on owned tokens page'
    - 'Balance is formatted using formatTokenAmount when digital asset decimals are available, raw bigint otherwise'
    - 'Both pages are separate routes (not tabs on one page) per locked decision'
    - 'Client/server mode toggle works on both pages'
    - 'All filter fields are exposed as inputs on both pages'
    - 'Navigation sidebar shows both Owned Assets and Owned Tokens as available'
  artifacts:
    - path: 'apps/test/src/app/owned-assets/page.tsx'
      provides: 'Owned assets playground page with 3 tabs'
      min_lines: 100
    - path: 'apps/test/src/app/owned-tokens/page.tsx'
      provides: 'Owned tokens playground page with 3 tabs'
      min_lines: 100
    - path: 'apps/test/src/components/owned-asset-card.tsx'
      provides: 'Card component for owned asset rendering'
      min_lines: 30
    - path: 'apps/test/src/components/owned-token-card.tsx'
      provides: 'Card component for owned token rendering'
      min_lines: 30
    - path: 'apps/test/src/components/nav.tsx'
      provides: 'Updated navigation with owned assets + owned tokens links enabled'
      contains: 'available: true'
  key_links:
    - from: 'apps/test/src/app/owned-assets/page.tsx'
      to: '@lsp-indexer/react'
      via: 'useOwnedAsset, useOwnedAssets, useInfiniteOwnedAssets imports'
      pattern: 'useOwnedAsset|useInfiniteOwnedAssets'
    - from: 'apps/test/src/app/owned-tokens/page.tsx'
      to: '@lsp-indexer/react'
      via: 'useOwnedToken, useOwnedTokens, useInfiniteOwnedTokens imports'
      pattern: 'useOwnedToken|useInfiniteOwnedTokens'
    - from: 'apps/test/src/components/owned-asset-card.tsx'
      to: '@lsp-indexer/types'
      via: 'OwnedAsset type import'
      pattern: 'OwnedAsset'
    - from: 'apps/test/src/app/owned-assets/page.tsx'
      to: 'apps/test/src/lib/utils.ts'
      via: 'formatTokenAmount for balance formatting'
      pattern: 'formatTokenAmount'
---

<objective>
Build two separate playground pages (/owned-assets and /owned-tokens) that demonstrate all hooks working end-to-end against live Hasura data, with card components, filters, sorts, include toggles, and client/server mode switching.

Purpose: End-to-end validation that the complete vertical slice works — from Zod types through GraphQL documents through parsers through services through hooks to rendered UI. Both pages independently verify their respective domain's full stack. This delivers the final confirmation of QUERY-04 and PAGE-01 for the owned assets/tokens domains.

Output: 2 playground pages, 2 card components, 2 layout files, updated nav — all using shared playground components (FilterFieldsRow, SortControls, ResultsList, etc.).
</objective>

<execution_context>
@/home/coder/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.2-nfts/09.2-04-SUMMARY.md
@.planning/phases/09.3-owned-assets/09.3-CONTEXT.md
@apps/test/src/app/digital-assets/page.tsx
@apps/test/src/app/nfts/page.tsx
@apps/test/src/app/nfts/layout.tsx
@apps/test/src/components/digital-asset-card.tsx
@apps/test/src/components/nft-card.tsx
@apps/test/src/components/nav.tsx
@apps/test/src/components/playground/
@apps/test/src/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build both playground pages, card components, and update navigation</name>
  <files>
    apps/test/src/app/owned-assets/page.tsx
    apps/test/src/app/owned-assets/layout.tsx
    apps/test/src/app/owned-tokens/page.tsx
    apps/test/src/app/owned-tokens/layout.tsx
    apps/test/src/components/owned-asset-card.tsx
    apps/test/src/components/owned-token-card.tsx
    apps/test/src/components/nav.tsx
  </files>
  <action>
**Create `apps/test/src/components/owned-asset-card.tsx`** — card component for rendering OwnedAsset data:

Follow the pattern from `digital-asset-card.tsx` and `nft-card.tsx`. Import shadcn Card, Badge components.

Display:

- **Owner address** (truncated, monospace)
- **Asset address** (truncated, monospace)
- **Balance** — use `formatTokenAmount` from `@/lib/utils` when `ownedAsset.digitalAsset?.decimals` is available; otherwise display raw `ownedAsset.balance.toString()`. Per locked decision: parser returns bigint, consumer formats.
- **Timestamp** as relative time (e.g., "2 days ago") — use `formatDistanceToNow` from `date-fns` or implement a simple relative time helper
- **Digital asset name/symbol** if included (from nested digitalAsset)
- **Universal profile name** if included (from nested universalProfile)
- **Token ID count** if included (badge showing count of NFT token IDs)
- Use shadcn Card component with consistent styling

**Create `apps/test/src/components/owned-token-card.tsx`** — card component for OwnedToken:

Display:

- **Owner address** (truncated, monospace)
- **Asset address** (truncated, monospace)
- **Token ID** (monospace)
- **Timestamp** as relative time
- **Digital asset name/symbol** if included
- **NFT info** if included (name, collection, burned/minted status)
- **Owned asset balance** if included
- **Universal profile name** if included

---

**Create `apps/test/src/app/owned-assets/layout.tsx`** — force-dynamic layout:

```typescript
export const dynamic = 'force-dynamic';
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
```

**Create `apps/test/src/app/owned-assets/page.tsx`** — owned assets playground:

Use shared playground components from `components/playground/` (FilterFieldsRow, SortControls, ResultsList, useFilterFields, ErrorAlert, RawJsonToggle).

**3 tabs:** Single Lookup, Owned Asset List, Infinite Scroll (shadcn Tabs)

**Tab 1 — Single Lookup:**

- ID input field for owned asset lookup
- Include toggles: digitalAsset (checkbox), universalProfile (checkbox), tokenIdCount (checkbox)
- When digitalAsset is checked, show sub-include checkboxes for the 17 DigitalAssetInclude fields
- Client/server mode toggle (switches between @lsp-indexer/react and @lsp-indexer/next hooks)
- Renders single OwnedAssetCard

**Tab 2 — Owned Asset List:**

- Filter fields (per CONTEXT.md): owner, address, digitalAssetId, universalProfileId
  - All as text inputs with labels on top, 300ms debounce
  - Address fields use \_ilike pattern (handled by service layer)
- Sort controls: field dropdown (balance, timestamp, address, owner, block, digitalAssetName, tokenIdCount) + direction + nulls
- Limit/offset pagination controls
- Include toggles same as Tab 1
- Client/server mode toggle
- ResultsList with OwnedAssetCard items

**Tab 3 — Infinite Scroll:**

- Same filters and sorts as Tab 2
- Page size control
- Include toggles
- Client/server mode toggle
- Infinite scroll with "Load more" button
- ResultsList with OwnedAssetCard items

---

**Create `apps/test/src/app/owned-tokens/layout.tsx`** — same force-dynamic pattern.

**Create `apps/test/src/app/owned-tokens/page.tsx`** — owned tokens playground:

**3 tabs:** Single Lookup, Owned Token List, Infinite Scroll

**Tab 1 — Single Lookup:**

- ID input field
- Include toggles: digitalAsset (with 17 sub-includes), nft (checkbox), ownedAsset (checkbox), universalProfile (checkbox)
- Client/server mode toggle
- Renders single OwnedTokenCard

**Tab 2 — Owned Token List:**

- Filter fields (per CONTEXT.md): owner, address, tokenId, digitalAssetId, nftId, ownedAssetId, universalProfileId
  - All text inputs, labels on top, 300ms debounce
- Sort controls: field dropdown (address, block, owner, timestamp, tokenId) + direction + nulls
- Limit/offset pagination
- Include toggles same as Tab 1
- Client/server mode toggle
- ResultsList with OwnedTokenCard items

**Tab 3 — Infinite Scroll:**

- Same filters/sorts as Tab 2
- Infinite scroll with "Load more" button

---

**Update `apps/test/src/components/nav.tsx`:**

The current nav has `{ href: '/owned', label: 'Owned Assets', icon: Wallet, available: false }`. Replace this single entry with TWO entries:

```typescript
{ href: '/owned-assets', label: 'Owned Assets', icon: Wallet, available: true },
{ href: '/owned-tokens', label: 'Owned Tokens', icon: Wallet, available: true },
```

Use the `Wallet` icon for owned assets and consider `Coins` or `Layers` for owned tokens to differentiate (use Claude's discretion for icon choice). Import any new icon from `lucide-react`.

**IMPORTANT:** Use shadcn/ui components for all UI. No custom components when a shadcn equivalent exists (per locked project decision).
</action>
<verify>

1. `pnpm --filter test build` succeeds (or `next build` in apps/test)
2. Both pages render at /owned-assets and /owned-tokens without errors
3. OwnedAsset balance displays correctly (formatted with decimals or raw bigint)
4. Nav shows both Owned Assets and Owned Tokens as available
5. Include toggles work (expanding/collapsing nested data sections)
6. Filters produce correct results (address fields case-insensitive)
   </verify>
   <done>Two playground pages exist at /owned-assets and /owned-tokens (per locked decision: separate pages, not tabs). OwnedAssetCard shows owner + address + balance (formatted) + timestamp (relative). OwnedTokenCard shows owner + address + tokenId + timestamp with nested NFT/DA/profile data. Both pages have 3 tabs with all filter/sort/include controls. Navigation updated with both routes enabled.</done>
   </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify owned assets and owned tokens playground pages</name>
  <what-built>
Complete owned assets and owned tokens vertical slices: types → documents → codegen → parsers → services → keys → hooks → actions → playground pages. Two separate playground pages at /owned-assets and /owned-tokens with filters, sorts, include toggles, and client/server mode switching.
  </what-built>
  <how-to-verify>
1. Start the dev server: `pnpm dev` in apps/test (or from workspace root if configured)
2. Navigate to `/owned-assets`:
   - **Single tab:** Enter an owned asset ID and verify data loads (owner, address, balance displayed)
   - **List tab:** Verify list loads with data. Try filtering by owner address. Toggle digital asset include and verify nested data appears/disappears. Verify balance shows formatted number when decimals available, raw bigint string otherwise.
   - **Infinite tab:** Verify initial page loads. Click "Load more" and verify additional results appear.
   - **Sort:** Try sorting by balance, timestamp, digitalAssetName. Verify order changes.
   - **Client/Server toggle:** Switch to server mode and verify data still loads (routes through server actions).
3. Navigate to `/owned-tokens`:
   - **Single tab:** Enter an owned token ID and verify data loads (owner, address, tokenId displayed)
   - **List tab:** Verify list loads. Try filtering by owner address and tokenId. Toggle NFT include and verify nested NFT data appears. Toggle digitalAsset include and verify nested asset data appears.
   - **Infinite tab:** Verify pagination works.
   - **Client/Server toggle:** Verify both modes work.
4. Check navigation sidebar: Both "Owned Assets" and "Owned Tokens" should appear as active links (not grayed out with "Soon" badge).
5. Spot check: Open browser DevTools Network tab and verify GraphQL requests are being made to Hasura with correct variables.
  </how-to-verify>
  <resume-signal>Type "approved" if both pages work correctly, or describe any issues you see (missing data, broken filters, layout problems, etc.)</resume-signal>
</task>

</tasks>

<verification>
1. `/owned-assets` page renders with live Hasura data
2. `/owned-tokens` page renders with live Hasura data
3. Both pages have 3 functional tabs (single, list, infinite)
4. OwnedAsset balance formatted correctly (formatTokenAmount when decimals available)
5. OwnedToken shows tokenId, nested NFT/DA data when included
6. Include toggles expand/collapse nested data sections on both pages
7. All filter fields from CONTEXT.md exposed as inputs
8. All sort fields functional
9. Client/server mode toggle works on both pages
10. Navigation sidebar updated with both routes as available
11. `next build` succeeds for apps/test
</verification>

<success_criteria>

- Two fully independent playground pages (per LOCKED decision: separate, not tabs)
- OwnedAsset cards show owner + address + balance (formatted) + timestamp (relative time)
- OwnedToken cards show owner + address + tokenId + timestamp + nested relations
- Balance is bigint → formatted display (per LOCKED decision)
- All filter fields from CONTEXT.md present and functional
- All sort fields from CONTEXT.md present and functional
- Include toggles for digitalAsset (with sub-includes), universalProfile, nft, ownedAsset
- Shared playground components reused (not custom rebuilds)
- End-to-end data flow validated against live Hasura
- QUERY-04 delivered (useOwnedAssets, useOwnedTokens)
- PAGE-01 delivered (useInfiniteOwnedAssets, useInfiniteOwnedTokens)
  </success_criteria>

<output>
After completion, create `.planning/phases/09.3-owned-assets/09.3-04-SUMMARY.md`
</output>
