---
phase: 21-sorting-consumer-package-release
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - .changeset/sorting-support.md
  - packages/types/package.json
  - packages/node/package.json
  - packages/react/package.json
  - packages/next/package.json
  - packages/types/CHANGELOG.md
  - packages/node/CHANGELOG.md
  - packages/react/CHANGELOG.md
  - packages/next/CHANGELOG.md
autonomous: true
requirements: [RELP-01]

must_haves:
  truths:
    - "All 4 consumer packages build successfully with sorting changes"
    - "Changeset created with minor bump describing sorting support"
    - "All 4 packages version-bumped to same new version"
  artifacts:
    - path: ".changeset/*.md"
      provides: "Changeset describing sorting support for coordinated release"
      contains: "minor"
  key_links:
    - from: ".changeset/config.json fixed group"
      to: "All 4 packages versioned together"
      via: "changesets fixed group config"
      pattern: "fixed.*@lsp-indexer"
---

<objective>
Build-verify all 4 consumer packages and create a changeset for coordinated release with sorting support.

Purpose: RELP-01 requires all 4 packages (`@lsp-indexer/types`, `@lsp-indexer/node`, `@lsp-indexer/react`, `@lsp-indexer/next`) released with sorting support. The changeset config has a `fixed` group ensuring all 4 packages are versioned and released together.

Output: Verified builds + changeset file ready for version bump and publish
</objective>

<execution_context>
@/home/coder/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-sorting-consumer-package-release/21-CONTEXT.md
@.planning/phases/21-sorting-consumer-package-release/21-01-SUMMARY.md
</context>

<interfaces>
<!-- Changeset config — fixed group means ONE changeset bumps ALL 4 packages together -->

From .changeset/config.json:
```json
{
  "fixed": [["@lsp-indexer/types", "@lsp-indexer/node", "@lsp-indexer/react", "@lsp-indexer/next"]],
  "access": "public",
  "commit": false,
  "baseBranch": "main"
}
```

<!-- Per user decision: "Version goes from 1.0.0 to 1.1.0 (minor bump)" -->
<!-- Per user decision: "Coordinated release — all 4 packages released together" -->
<!-- Per user decision: "Use existing changesets workflow for versioning and publishing" -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Full build verification across all 4 consumer packages</name>
  <files></files>
  <action>
Build all 4 consumer packages to verify the sorting changes from Plan 21-01 compile correctly through the full dependency chain:

```bash
pnpm --filter=@lsp-indexer/types build
pnpm --filter=@lsp-indexer/node build
pnpm --filter=@lsp-indexer/react build
pnpm --filter=@lsp-indexer/next build
```

Or use `pnpm build` if it builds all packages.

This verifies:
- SORT-02: Subscription hooks compile with updated sort types (react package)
- SORT-03: React hooks compile with updated sort types (react package)
- SORT-04: Server actions compile with updated sort types (next package)

If any build fails, diagnose and fix the issue in the affected file. Common issues:
- Missing sort field case in switch statement (TypeScript exhaustiveness check)
- Import path issue for buildBlockOrderSort
- Type mismatch between SortFieldSchema and buildOrderBy switch cases
  </action>
  <verify>pnpm --filter=@lsp-indexer/types build && pnpm --filter=@lsp-indexer/node build && pnpm --filter=@lsp-indexer/react build && pnpm --filter=@lsp-indexer/next build</verify>
  <done>All 4 consumer packages build successfully with zero errors. Sort types flow correctly from types → node → react → next.</done>
</task>

<task type="auto">
  <name>Task 2: Create changeset for coordinated package release</name>
  <files>.changeset/sorting-support.md</files>
  <action>
Create a changeset file for a coordinated minor release of all 4 consumer packages. The changeset config's `fixed` group ensures all 4 packages are versioned together — only ONE package needs to be listed in the changeset.

Write `.changeset/sorting-support.md` (the filename can be any kebab-case name):

```markdown
---
'@lsp-indexer/types': minor
---

Add newest/oldest block-order sorting across all 12 query domains. All domain services now default to newest-first when no sort parameter is provided. Non-block sort fields include block-order as a deterministic pagination tiebreaker. Removed individual block/timestamp sort fields in favor of consistent newest/oldest.
```

Per user decision: "Version goes from 1.0.0 to 1.1.0 (minor bump) — removals are not breaking since no external consumers yet."

Note: The `fixed` group in `.changeset/config.json` means this single changeset will bump all 4 packages (`types`, `node`, `react`, `next`) to the same new version.

After creating the changeset, run `pnpm changeset version` to apply the version bumps. Then run `pnpm build` one more time to verify everything still compiles with the bumped versions.

If the existing CI/CD pipeline handles publishing on merge to main, do NOT run `pnpm changeset publish` — that will happen automatically. If no CI pipeline exists for publishing, note this in the SUMMARY for the user to handle.
  </action>
  <verify>ls .changeset/sorting-support.md && cat .changeset/sorting-support.md</verify>
  <done>Changeset created with minor bump. Version bumps applied. All 4 packages show new version in package.json. Ready for publish (via CI/CD on merge or manual `pnpm changeset publish`).</done>
</task>

</tasks>

<verification>
1. `pnpm build` — all packages compile cleanly
2. `.changeset/sorting-support.md` exists with minor bump
3. After `pnpm changeset version`: all 4 package.json files show bumped version
4. `git diff --stat` shows only expected changes (package.json versions + CHANGELOG.md)
</verification>

<success_criteria>
- All 4 consumer packages build with zero errors
- Changeset created describing sorting support
- Version bumps applied to all 4 packages
- Ready for coordinated publish
</success_criteria>

<output>
After completion, create `.planning/phases/21-sorting-consumer-package-release/21-02-SUMMARY.md`
</output>
