---
phase: 03.1-improve-debug-logging-strategy
verified: 2026-02-11T14:15:00Z
status: gaps_found
score: 1/4 must-haves verified
gaps:
  - truth: 'User can set LOG_LEVEL=debug and see debug-level logs from all components'
    status: failed
    reason: 'createComponentLogger function used but never imported in any file'
    artifacts:
      - path: 'packages/indexer-v2/src/core/metadataWorkerPool.ts'
        issue: 'Uses createComponentLogger and getFileLogger without importing them (lines 113, 124-125)'
      - path: 'packages/indexer-v2/src/handlers/lsp3ProfileFetch.handler.ts'
        issue: 'Uses createComponentLogger without importing it (line 212)'
      - path: 'packages/indexer-v2/src/handlers/lsp4MetadataFetch.handler.ts'
        issue: 'Uses createComponentLogger without importing it (line 346)'
      - path: 'packages/indexer-v2/src/handlers/lsp29EncryptedAssetFetch.handler.ts'
        issue: 'Uses createComponentLogger without importing it (line 326)'
      - path: 'packages/indexer-v2/src/core/__tests__/logger.test.ts'
        issue: 'Tests createComponentLogger but does not import it (lines 155-183)'
    missing:
      - "Add import: import { createComponentLogger } from '../logger'; to metadataWorkerPool.ts"
      - "Add import: import { getFileLogger } from '../logger'; to metadataWorkerPool.ts"
      - "Add import: import { createComponentLogger } from '../core/logger'; to all three handler files"
      - 'Add createComponentLogger to imports in logger.test.ts'
  - truth: 'User can set DEBUG_COMPONENTS=worker_pool and see only worker pool logs'
    status: failed
    reason: 'Cannot test filtering when imports are missing - code will not compile/run'
    artifacts:
      - path: 'packages/indexer-v2/src/core/logger.ts'
        issue: 'createComponentLogger exists and adds component field, but cannot be used due to missing imports'
    missing:
      - 'Fix imports first before DEBUG_COMPONENTS can be verified'
  - truth: 'User can trace worker pool operations without modifying code'
    status: failed
    reason: 'Worker pool debug logging present but not functional due to missing imports'
    artifacts:
      - path: 'packages/indexer-v2/src/core/metadataWorkerPool.ts'
        issue: 'All debug log calls will cause ReferenceError: createComponentLogger is not defined'
    missing:
      - 'Add missing imports to make worker pool logger functional'
  - truth: 'User sees structured log output with timestamp, level, component, message, context fields'
    status: failed
    reason: 'Logger infrastructure exists but is non-functional due to missing imports throughout codebase'
    artifacts:
      - path: 'packages/indexer-v2/src/core/logger.ts'
        issue: 'Function is exported correctly but never imported by consumers'
    missing:
      - 'Add imports to all 4 files that use createComponentLogger'
---

# Phase 3.1: Improve Debug Logging Strategy Verification Report

**Phase Goal:** Create structured debug logging infrastructure with configurable log levels and component-specific debug flags, enabling faster debugging of worker pool, metadata fetch, and pipeline issues.

**Verified:** 2026-02-11T14:15:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                              | Status   | Evidence                                                                 |
| --- | ---------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------ |
| 1   | User can set LOG_LEVEL=debug and see debug-level logs from all components          | ✗ FAILED | createComponentLogger never imported - ReferenceError on execution       |
| 2   | User can set DEBUG_COMPONENTS=worker_pool and see only worker pool logs            | ✗ FAILED | Cannot test filtering - code will not run due to missing imports         |
| 3   | User can trace worker pool operations without modifying code                       | ✗ FAILED | Debug logs exist but non-functional (createComponentLogger not imported) |
| 4   | User sees structured log output with timestamp, level, component, message, context | ✗ FAILED | Logger infrastructure exists but consumers cannot access it              |

**Score:** 0/4 truths verified

### Required Artifacts

| Artifact                                                               | Expected                                                     | Status        | Details                                                                                    |
| ---------------------------------------------------------------------- | ------------------------------------------------------------ | ------------- | ------------------------------------------------------------------------------------------ |
| `packages/indexer-v2/src/core/logger.ts`                               | createComponentLogger function with proper exports           | ✓ SUBSTANTIVE | Function exists, well-documented, exports correct (84 lines)                               |
| `packages/indexer-v2/src/core/__tests__/logger.test.ts`                | Unit tests for createComponentLogger                         | ✗ STUB        | Tests exist but missing import - will fail with ReferenceError                             |
| `packages/indexer-v2/src/core/metadataWorkerPool.ts`                   | Worker pool debug logging with component='worker_pool'       | ✗ STUB        | 10+ debug log points exist but createComponentLogger/getFileLogger not imported (line 113) |
| `packages/indexer-v2/src/handlers/lsp3ProfileFetch.handler.ts`         | Metadata fetch debug logging with component='metadata_fetch' | ✗ STUB        | Debug logging present (lines 212-240) but createComponentLogger not imported               |
| `packages/indexer-v2/src/handlers/lsp4MetadataFetch.handler.ts`        | Metadata fetch debug logging with component='metadata_fetch' | ✗ STUB        | Debug logging present (lines 346-375) but createComponentLogger not imported               |
| `packages/indexer-v2/src/handlers/lsp29EncryptedAssetFetch.handler.ts` | Metadata fetch debug logging with component='metadata_fetch' | ✗ STUB        | Debug logging present (lines 326-352) but createComponentLogger not imported               |

### Key Link Verification

| From                                                                 | To                                     | Via                                   | Status      | Details                                                             |
| -------------------------------------------------------------------- | -------------------------------------- | ------------------------------------- | ----------- | ------------------------------------------------------------------- |
| packages/indexer-v2/src/core/logger.ts                               | pino.child()                           | createComponentLogger wraps component | ✓ WIRED     | Implementation correct: `baseLogger.child({ component })` (line 88) |
| packages/indexer-v2/src/core/metadataWorkerPool.ts                   | packages/indexer-v2/src/core/logger.ts | imports createComponentLogger         | ✗ NOT_WIRED | **MISSING IMPORT** - uses createComponentLogger without importing   |
| packages/indexer-v2/src/handlers/lsp3ProfileFetch.handler.ts         | ../core/logger                         | imports createComponentLogger         | ✗ NOT_WIRED | **MISSING IMPORT** - uses createComponentLogger without importing   |
| packages/indexer-v2/src/handlers/lsp4MetadataFetch.handler.ts        | ../core/logger                         | imports createComponentLogger         | ✗ NOT_WIRED | **MISSING IMPORT** - uses createComponentLogger without importing   |
| packages/indexer-v2/src/handlers/lsp29EncryptedAssetFetch.handler.ts | ../core/logger                         | imports createComponentLogger         | ✗ NOT_WIRED | **MISSING IMPORT** - uses createComponentLogger without importing   |
| DEBUG_COMPONENTS env var                                             | pino log level check                   | runtime filtering in logger           | ? UNCERTAIN | Cannot verify - logger never actually instantiated due to imports   |

### Requirements Coverage

| Requirement | Description                                                              | Status      | Blocking Issue                                  |
| ----------- | ------------------------------------------------------------------------ | ----------- | ----------------------------------------------- |
| LOG-01      | Logger utility module with ERROR, WARN, INFO, DEBUG, TRACE levels        | ✓ SATISFIED | Logger module complete and functional           |
| LOG-02      | Component-specific debug flags (DEBUG_WORKER_POOL, DEBUG_METADATA_FETCH) | ✗ BLOCKED   | createComponentLogger exists but not wired      |
| LOG-03      | Environment variable control (LOG_LEVEL, DEBUG_COMPONENTS)               | ⚠️ PARTIAL  | LOG_LEVEL works, DEBUG_COMPONENTS advisory only |
| LOG-04      | Apply logger to worker pool, metadata fetch handlers, and pipeline       | ✗ BLOCKED   | Logger usage present but imports missing        |

### Anti-Patterns Found

| File                                | Line | Pattern                  | Severity   | Impact                                                       |
| ----------------------------------- | ---- | ------------------------ | ---------- | ------------------------------------------------------------ |
| metadataWorkerPool.ts               | 113  | Undefined function usage | 🛑 Blocker | ReferenceError at runtime: createComponentLogger not defined |
| metadataWorkerPool.ts               | 124  | Undefined function usage | 🛑 Blocker | ReferenceError at runtime: getFileLogger not defined         |
| lsp3ProfileFetch.handler.ts         | 212  | Undefined function usage | 🛑 Blocker | ReferenceError at runtime: createComponentLogger not defined |
| lsp4MetadataFetch.handler.ts        | 346  | Undefined function usage | 🛑 Blocker | ReferenceError at runtime: createComponentLogger not defined |
| lsp29EncryptedAssetFetch.handler.ts | 326  | Undefined function usage | 🛑 Blocker | ReferenceError at runtime: createComponentLogger not defined |
| logger.test.ts                      | 159  | Undefined function usage | 🛑 Blocker | Test will fail: createComponentLogger not imported           |

### Human Verification Required

Cannot proceed with human verification until imports are fixed. Once fixed, these tests are needed:

#### 1. Debug logging visibility test

**Test:** Set `LOG_LEVEL=debug` and run indexer, check log files for component field
**Expected:** Log files contain JSON entries with `component: "worker_pool"` and `component: "metadata_fetch"` fields
**Why human:** Need to run application and inspect actual log output

#### 2. Component filtering test

**Test:** Filter logs with `cat logs/indexer*.log | jq 'select(.component == "worker_pool")'`
**Expected:** Output shows only worker pool logs (batch scheduling, worker distribution, retries, shutdown)
**Why human:** Need to verify post-hoc filtering works with real data

#### 3. Worker pool tracing test

**Test:** Process a batch with debug logging enabled and verify worker operations are traceable
**Expected:** Log shows batch start → worker assignment → completion → duration without code modifications
**Why human:** Need to verify debug logs provide sufficient detail for debugging

### Gaps Summary

**Root cause:** Implementation complete but wiring completely missing.

The phase accomplished significant implementation work:

- ✅ createComponentLogger function implemented correctly in logger.ts
- ✅ 10+ debug log points added to MetadataWorkerPool
- ✅ Debug logging added to all three metadata fetch handlers
- ✅ Unit tests written for createComponentLogger

However, **zero imports were added**, making the entire implementation non-functional. Every file that uses `createComponentLogger` or `getFileLogger` will throw a ReferenceError at runtime.

**Impact:** Phase goal NOT achieved. User cannot enable debug logging because the code will crash on execution.

**Required fixes:**

1. Add `import { createComponentLogger, getFileLogger } from './logger';` to `metadataWorkerPool.ts` (relative path - both in core/)
2. Add `import { createComponentLogger } from '../core/logger';` to all three handler files
3. Add `createComponentLogger` to imports in `logger.test.ts`

**Verification after fixes:** All 4 truths can then be verified with manual testing (run indexer with LOG_LEVEL=debug and inspect logs).

---

_Verified: 2026-02-11T14:15:00Z_
_Verifier: Claude (gsd-verifier)_
