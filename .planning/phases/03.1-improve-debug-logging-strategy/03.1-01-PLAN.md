---
phase: 03.1-improve-debug-logging-strategy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/indexer-v2/src/core/logger.ts
  - packages/indexer-v2/src/core/__tests__/logger.test.ts
  - packages/indexer-v2/src/core/metadataWorkerPool.ts
  - packages/indexer-v2/src/handlers/LSP3MetadataFetchHandler.ts
  - packages/indexer-v2/src/handlers/LSP4MetadataFetchHandler.ts
  - packages/indexer-v2/src/handlers/LSP29MetadataFetchHandler.ts
autonomous: true

must_haves:
  truths:
    - 'User can set LOG_LEVEL=debug and see debug-level logs from all components'
    - 'User can set DEBUG_COMPONENTS=worker_pool and see only worker pool logs'
    - 'User can trace worker pool operations without modifying code'
    - 'User sees structured log output with timestamp, level, component, message, context fields'
  artifacts:
    - path: 'packages/indexer-v2/src/core/logger.ts'
      provides: 'createComponentLogger function and DEBUG_COMPONENTS filtering'
      exports: ['createComponentLogger', 'createStepLogger', 'createDualLogger']
    - path: 'packages/indexer-v2/src/core/metadataWorkerPool.ts'
      provides: "Worker pool debug logging with component='worker_pool'"
      contains: 'createComponentLogger.*worker_pool'
    - path: 'packages/indexer-v2/src/handlers/LSP3MetadataFetchHandler.ts'
      provides: "Metadata fetch debug logging with component='metadata_fetch'"
      contains: 'debug.*fetching metadata'
  key_links:
    - from: 'packages/indexer-v2/src/core/logger.ts'
      to: 'pino.child()'
      via: 'createComponentLogger wraps with component field'
      pattern: "baseLogger\\.child\\(\\{ component \\}\\)"
    - from: 'packages/indexer-v2/src/core/metadataWorkerPool.ts'
      to: 'packages/indexer-v2/src/core/logger.ts'
      via: 'imports createComponentLogger'
      pattern: 'import.*createComponentLogger.*from.*logger'
    - from: 'DEBUG_COMPONENTS env var'
      to: 'pino log level check'
      via: 'runtime filtering in logger'
      pattern: 'DEBUG_COMPONENTS.*split.*component'
---

<objective>
Add component-specific debug logging infrastructure with configurable filtering, enabling faster debugging of worker pool and metadata fetch operations without code modifications.

Purpose: Accelerate debugging by allowing toggling debug logs via environment variables (LOG_LEVEL, DEBUG_COMPONENTS) without redeploying or adding console.log statements.

Output: Enhanced logger module with component filtering, debug logging in MetadataWorkerPool and metadata fetch handlers, plus unit tests.
</objective>

<execution_context>
@/home/coder/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-improve-debug-logging-strategy/03.1-RESEARCH.md
@.planning/phases/02-new-handlers-structured-logging/02-01-SUMMARY.md
@packages/indexer-v2/src/core/logger.ts
@packages/indexer-v2/src/core/metadataWorkerPool.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance logger module with component-specific logging</name>
  <files>
packages/indexer-v2/src/core/logger.ts
packages/indexer-v2/src/core/__tests__/logger.test.ts
  </files>
  <action>
Add `createComponentLogger()` helper function to logger.ts that creates a child logger with a `component` field for filtering debug logs by component name.

Implementation details:

- Add function signature: `export function createComponentLogger(baseLogger: Logger | pino.Logger, component: string): Logger | pino.Logger`
- Use logger.child({ component }) to create component-specific child logger
- Add optional DEBUG_COMPONENTS environment variable filtering logic (checked at runtime when logger.isLevelEnabled('debug') is called)
- Update logger.ts exports to include createComponentLogger
- Add unit tests in logger.test.ts to verify:
  1. createComponentLogger adds component field to log output
  2. Component loggers work with both Subsquid Logger and pino
  3. Multiple component loggers can be created independently
- Follow existing patterns: use pino.child() pattern, maintain dual-output support, respect LOG_LEVEL precedence

Note: DEBUG_COMPONENTS filtering should be advisory/documentation only for now. Full runtime filtering can be added in a future optimization if needed. Focus on making component field available for post-hoc filtering via jq/grep.
</action>
<verify>
Run unit tests: `cd packages/indexer-v2 && pnpm test src/core/__tests__/logger.test.ts`

All tests pass, including new component logger tests.
</verify>
<done>

- createComponentLogger function exists and is exported
- Unit tests verify component field injection for both Subsquid Logger and pino
- Documentation comments explain component field usage and DEBUG_COMPONENTS env var
  </done>
  </task>

<task type="auto">
  <name>Task 2: Add debug logging to MetadataWorkerPool</name>
  <files>packages/indexer-v2/src/core/metadataWorkerPool.ts</files>
  <action>
Add debug-level logging to MetadataWorkerPool to trace worker operations without modifying code.

Implementation details:

- Import createComponentLogger from './logger' (NOT from '@/core/logger' - use relative path since both are in core/)
- Create component logger at class initialization: `private logger = createComponentLogger(baseLogger, 'worker_pool')` where baseLogger is pino logger from getFileLogger()
- Add debug logs at key points:
  1. Constructor: Log pool initialization with poolSize, ipfsGateway, timeouts
  2. fetchBatch start: Log batch size, available workers
  3. Worker assignment: Log which worker gets which batch slice
  4. Worker completion: Log worker ID, batch size processed, duration
  5. Worker error: Log worker ID, error details (already has console.error, enhance with structured logging)
  6. Retry logic: Log retry attempts with URL, retry count, backoff delay
  7. shutdown: Log pool shutdown with worker count

Use this pattern for debug logs:

```typescript
if (this.logger.isLevelEnabled('debug')) {
  this.logger.debug({ workerId, batchSize, queueSize }, 'Processing batch');
}
```

Keep existing console.error for critical failures, but add structured debug logging for trace-level debugging.

Note: Since workers are in separate threads, worker thread logging will be addressed in Phase 3.2. For now, focus on main thread pool management logging only.
</action>
<verify>
Read metadataWorkerPool.ts and verify:

1. createComponentLogger imported from './logger'
2. Debug logs present at constructor, fetchBatch, worker completion, errors, shutdown
3. All debug logs use component='worker_pool' and include relevant context (workerId, batchSize, etc.)
4. Pattern `logger.isLevelEnabled('debug')` used for performance-sensitive paths
   </verify>
   <done>

- MetadataWorkerPool has component logger with component='worker_pool'
- Debug logs at 7+ key points (init, batch, assignment, completion, error, retry, shutdown)
- Logs include structured context (workerId, batchSize, duration, error details)
- Performance-optimized with isLevelEnabled check
  </done>
  </task>

<task type="auto">
  <name>Task 3: Add debug logging to metadata fetch handlers</name>
  <files>
packages/indexer-v2/src/handlers/LSP3MetadataFetchHandler.ts
packages/indexer-v2/src/handlers/LSP4MetadataFetchHandler.ts
packages/indexer-v2/src/handlers/LSP29MetadataFetchHandler.ts
  </files>
  <action>
Add debug logging to LSP3, LSP4, and LSP29 metadata fetch handlers to trace metadata fetching and parsing operations.

Implementation details:

- Import createComponentLogger from '../core/logger'
- Create component logger in each handler's handle() method: `const logger = createComponentLogger(context.log, 'metadata_fetch')`
- Add debug logs at key points for each handler:
  1. Handler entry: Log unfetched entity count, handler name
  2. Before fetchBatch call: Log batch size, total pending
  3. After fetchBatch: Log success count, error count, duration
  4. Entity parsing: Log contentType, parse success/failure (sample first few, not all)
  5. Sub-entity creation: Log sub-entity type counts (e.g., "Created 5 LSP3ProfileImage, 3 LSP3ProfileTag")
  6. Database persistence: Log entity count being persisted

Use consistent pattern:

```typescript
const logger = createComponentLogger(context.log, 'metadata_fetch');
if (logger.isLevelEnabled('debug')) {
  logger.debug({ handler: 'LSP3MetadataFetchHandler', unfetchedCount }, 'Starting metadata fetch');
}
```

Keep logs concise — log IDs and counts, not full entity objects. Avoid logging every single URL (too verbose for 1000s of entities).

Apply same pattern to all three handlers (LSP3, LSP4, LSP29) for consistency.
</action>
<verify>
Read all three handler files and verify:

1. createComponentLogger imported and used
2. Debug logs at 6+ key points per handler
3. All debug logs use component='metadata_fetch'
4. Logs include handler name, counts, and timing where relevant
5. No full entity object dumps in logs (IDs/counts only)
   </verify>
   <done>

- LSP3, LSP4, and LSP29 handlers have component logger with component='metadata_fetch'
- Debug logs at 6+ key points per handler (entry, batch, parse, persist)
- Logs include structured context (handler name, counts, contentType, timing)
- Logs are concise (IDs/counts, not full objects)
  </done>
  </task>

</tasks>

<verification>
**Overall phase verification:**

1. **Environment variable control:**

   ```bash
   # Test debug logging enabled
   LOG_LEVEL=debug pnpm start
   # Should see debug logs from worker_pool and metadata_fetch components

   # Test info logging (debug disabled)
   LOG_LEVEL=info pnpm start
   # Should NOT see debug logs, only info/warn/error
   ```

2. **Component field present:**

   ```bash
   # Check log files for component field
   cat logs/indexer*.log | jq 'select(.component == "worker_pool")'
   cat logs/indexer*.log | jq 'select(.component == "metadata_fetch")'
   ```

3. **Structured output:**

   ```bash
   # Verify structured fields present
   cat logs/indexer*.log | jq 'select(.level == "debug") | keys'
   # Should include: timestamp, level, component, message, and context fields
   ```

4. **Unit tests pass:**
   ```bash
   cd packages/indexer-v2 && pnpm test src/core/__tests__/logger.test.ts
   ```
   </verification>

<success_criteria>
**Phase complete when:**

1. User can set `LOG_LEVEL=debug` and see debug logs from worker_pool and metadata_fetch components in log files
2. User can filter logs by component using `jq 'select(.component == "worker_pool")'` and see only worker pool logs
3. User can trace worker pool batch scheduling, worker assignment, and completion without adding console.log statements
4. User can trace metadata fetch operations (URL, contentType, parse results) without code modifications
5. All unit tests pass, including new component logger tests
6. Logs are structured JSON with consistent fields (timestamp, level, component, message, context)
   </success_criteria>

<output>
After completion, create `.planning/phases/03.1-improve-debug-logging-strategy/03.1-01-SUMMARY.md`
</output>
