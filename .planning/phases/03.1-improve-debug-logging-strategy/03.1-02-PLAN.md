---
phase: 03.1-improve-debug-logging-strategy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/indexer-v2/src/core/metadataWorkerPool.ts
  - packages/indexer-v2/src/handlers/lsp3ProfileFetch.handler.ts
  - packages/indexer-v2/src/handlers/lsp4MetadataFetch.handler.ts
  - packages/indexer-v2/src/handlers/lsp29EncryptedAssetFetch.handler.ts
  - packages/indexer-v2/src/core/__tests__/logger.test.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - 'User can set LOG_LEVEL=debug and see debug-level logs from all components'
    - 'User can trace worker pool operations without modifying code'
    - 'User sees structured log output with timestamp, level, component, message, context fields'
  artifacts:
    - path: 'packages/indexer-v2/src/core/metadataWorkerPool.ts'
      provides: 'Imports createComponentLogger and getFileLogger from logger module'
      contains: 'import.*createComponentLogger.*getFileLogger.*from.*logger'
    - path: 'packages/indexer-v2/src/handlers/lsp3ProfileFetch.handler.ts'
      provides: 'Imports createComponentLogger from logger module'
      contains: 'import.*createComponentLogger.*from.*core/logger'
    - path: 'packages/indexer-v2/src/handlers/lsp4MetadataFetch.handler.ts'
      provides: 'Imports createComponentLogger from logger module'
      contains: 'import.*createComponentLogger.*from.*core/logger'
    - path: 'packages/indexer-v2/src/handlers/lsp29EncryptedAssetFetch.handler.ts'
      provides: 'Imports createComponentLogger from logger module'
      contains: 'import.*createComponentLogger.*from.*core/logger'
    - path: 'packages/indexer-v2/src/core/__tests__/logger.test.ts'
      provides: 'Imports createComponentLogger for unit tests'
      contains: 'import.*createComponentLogger'
  key_links:
    - from: 'packages/indexer-v2/src/core/metadataWorkerPool.ts'
      to: 'packages/indexer-v2/src/core/logger.ts'
      via: 'import statement'
      pattern: "import.*createComponentLogger.*getFileLogger.*from.*['\"]\\./logger['\"]"
    - from: 'packages/indexer-v2/src/handlers/lsp3ProfileFetch.handler.ts'
      to: 'packages/indexer-v2/src/core/logger.ts'
      via: 'import statement'
      pattern: "import.*createComponentLogger.*from.*['\"]\\.\\.\/core\/logger['\"]"
---

<objective>
Fix missing imports for createComponentLogger and getFileLogger across all files that use these functions, making the debug logging infrastructure from Plan 01 fully functional.

Purpose: Complete the wiring for Phase 3.1's debug logging implementation by adding the import statements that were omitted in Plan 01. Without these imports, all debug logging code throws ReferenceError at runtime.

Output: All 5 files that use createComponentLogger/getFileLogger will have proper import statements, enabling debug logging to work when LOG_LEVEL=debug is set.
</objective>

<execution_context>
@/home/coder/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Relevant summary from Plan 01

@.planning/phases/03.1-improve-debug-logging-strategy/03.1-01-SUMMARY.md

# Verification report showing gaps

@.planning/phases/03.1-improve-debug-logging-strategy/03.1-01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Add missing imports to all files using createComponentLogger/getFileLogger</name>
  <files>
    packages/indexer-v2/src/core/metadataWorkerPool.ts
    packages/indexer-v2/src/handlers/lsp3ProfileFetch.handler.ts
    packages/indexer-v2/src/handlers/lsp4MetadataFetch.handler.ts
    packages/indexer-v2/src/handlers/lsp29EncryptedAssetFetch.handler.ts
    packages/indexer-v2/src/core/__tests__/logger.test.ts
  </files>
  <action>
Add import statements for createComponentLogger and getFileLogger to all files that use these functions:

**metadataWorkerPool.ts** (lines 113, 124-125 use createComponentLogger and getFileLogger):

- Add: `import { createComponentLogger, getFileLogger } from './logger';`
- Location: Near top of file with other imports (both functions are in same file - core/logger.ts)
- Relative path is `./logger` because metadataWorkerPool.ts is in core/ directory

**lsp3ProfileFetch.handler.ts** (line 212 uses createComponentLogger):

- Add: `import { createComponentLogger } from '../core/logger';`
- Location: Near top of file with other imports
- Relative path is `../core/logger` because handler files are in handlers/ directory

**lsp4MetadataFetch.handler.ts** (line 346 uses createComponentLogger):

- Add: `import { createComponentLogger } from '../core/logger';`
- Location: Near top of file with other imports
- Relative path is `../core/logger` because handler files are in handlers/ directory

**lsp29EncryptedAssetFetch.handler.ts** (line 326 uses createComponentLogger):

- Add: `import { createComponentLogger } from '../core/logger';`
- Location: Near top of file with other imports
- Relative path is `../core/logger` because handler files are in handlers/ directory

**logger.test.ts** (lines 155-183 use createComponentLogger in tests):

- Add: `createComponentLogger` to the existing import from '../logger'
- Location: Update existing import statement that already imports other logger functions
- File already has import from '../logger' - just add createComponentLogger to the destructured imports

**Why this fixes the gaps:**

- Gap 1: "createComponentLogger function used but never imported" - directly addressed by adding imports
- Gap 2: "Cannot test filtering when imports are missing" - fixed when imports allow code to run
- Gap 3: "Worker pool debug logging present but not functional" - becomes functional with imports
- Gap 4: "Logger infrastructure exists but non-functional" - becomes functional when wired properly

Reference VERIFICATION.md lines 22-25 for exact import strings needed.
</action>
<verify>
Verify imports are syntactically correct and match file structure:

```bash
# Verify metadataWorkerPool.ts has both imports
grep "import.*createComponentLogger.*getFileLogger.*from.*'\./logger'" packages/indexer-v2/src/core/metadataWorkerPool.ts

# Verify all three handler files have createComponentLogger import
grep "import.*createComponentLogger.*from.*'\.\./core/logger'" packages/indexer-v2/src/handlers/lsp3ProfileFetch.handler.ts
grep "import.*createComponentLogger.*from.*'\.\./core/logger'" packages/indexer-v2/src/handlers/lsp4MetadataFetch.handler.ts
grep "import.*createComponentLogger.*from.*'\.\./core/logger'" packages/indexer-v2/src/handlers/lsp29EncryptedAssetFetch.handler.ts

# Verify logger.test.ts imports createComponentLogger
grep "createComponentLogger" packages/indexer-v2/src/core/__tests__/logger.test.ts | head -1
```

All commands should return matching import statements.
</verify>
<done>
All 5 files have correct import statements added:

- metadataWorkerPool.ts imports both createComponentLogger and getFileLogger from './logger'
- Three handler files (lsp3, lsp4, lsp29) import createComponentLogger from '../core/logger'
- logger.test.ts includes createComponentLogger in its imports from '../logger'
- No ReferenceError will occur when debug logging code executes
  </done>
  </task>

</tasks>

<verification>
After adding imports, verify the code is now functional:

1. **Import syntax check:** All grep commands in verify section return matches
2. **No duplicate imports:** Each file has exactly one import statement for createComponentLogger (not multiple)
3. **Correct relative paths:** metadataWorkerPool uses `./logger`, handlers use `../core/logger`, test uses `../logger`
4. **All gaps addressed:** Every missing import listed in VERIFICATION.md lines 22-25 is now present

Manual verification (post-execution):

- Run indexer with LOG_LEVEL=debug and verify no ReferenceError occurs
- Check log files contain component field (component='worker_pool', component='metadata_fetch')
- Filter logs with jq: `cat logs/*.log | jq 'select(.component == "worker_pool")'`
  </verification>

<success_criteria>
Gap closure complete when:

1. All 5 files have correct import statements for createComponentLogger/getFileLogger
2. Imports use correct relative paths based on file location
3. No duplicate imports exist in any file
4. All 4 observable truths from VERIFICATION.md become achievable:
   - User can set LOG_LEVEL=debug without ReferenceError
   - User can trace worker pool operations (logger now instantiates)
   - User sees structured log output with component field
   - User can filter by DEBUG_COMPONENTS (post-hoc via jq)
5. Phase 3.1 requirements LOG-02 and LOG-04 unblocked
   </success_criteria>

<output>
After completion, create `.planning/phases/03.1-improve-debug-logging-strategy/03.1-02-SUMMARY.md` documenting the import additions and gap closure.
</output>
