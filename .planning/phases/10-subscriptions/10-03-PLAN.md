---
phase: 10-subscriptions
plan: 03
type: execute
wave: 3
depends_on: ['10-02']
files_modified:
  - apps/test/src/app/subscriptions/page.tsx
  - apps/test/src/app/layout.tsx
  - apps/test/src/components/navigation.tsx
autonomous: false
requirements: [SUB-01, SUB-02, SUB-03]

must_haves:
  truths:
    - 'Developer can see a WebSocket connection established to Hasura when subscription hook mounts'
    - 'Developer can call useProfileSubscription and see the component re-render when data changes'
    - 'Developer can observe that when a subscription fires with invalidate:true, TanStack Query cache entries are invalidated'
    - 'Developer can see subscription hooks working for multiple domains in the playground'
  artifacts:
    - path: 'apps/test/src/app/subscriptions/page.tsx'
      provides: 'Subscription playground page'
      contains: 'useProfileSubscription'
    - path: 'apps/test/src/app/layout.tsx'
      provides: 'IndexerSubscriptionProvider wrapping the app'
      contains: 'IndexerSubscriptionProvider'
  key_links:
    - from: 'apps/test/src/app/subscriptions/page.tsx'
      to: '@lsp-indexer/react'
      via: 'subscription hook imports'
      pattern: 'useProfileSubscription|useEncryptedAssetSubscription'
    - from: 'apps/test/src/app/layout.tsx'
      to: '@lsp-indexer/react'
      via: 'IndexerSubscriptionProvider import'
      pattern: 'IndexerSubscriptionProvider'
---

<objective>
Add subscription playground to the test app, wire up IndexerSubscriptionProvider, and verify end-to-end subscription functionality against live Hasura.

Purpose: This validates that the entire subscription stack works end-to-end: WebSocket connection to Hasura, subscription data flowing through graphql-ws, data parsed by existing parsers, state reflected in React hooks, and optional cache invalidation triggering TanStack Query refetches.

Output: Working subscription playground at apps/test/src/app/subscriptions/page.tsx demonstrating real-time data for multiple domains.
</objective>

<execution_context>
@/home/coder/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-subscriptions/10-CONTEXT.md
@.planning/phases/10-subscriptions/10-01-SUMMARY.md
@.planning/phases/10-subscriptions/10-02-SUMMARY.md
@apps/test/src/app/layout.tsx
@apps/test/src/components/navigation.tsx
@apps/test/src/app/profiles/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire provider + create subscription playground page</name>
  <files>
    apps/test/src/app/layout.tsx
    apps/test/src/app/subscriptions/page.tsx
    apps/test/src/components/navigation.tsx
  </files>
  <action>
    **Update layout.tsx — Add IndexerSubscriptionProvider:**

    Read the existing `apps/test/src/app/layout.tsx`. Add `IndexerSubscriptionProvider` from `@lsp-indexer/react` INSIDE the existing `QueryClientProvider` (or IndexerProvider — whatever wraps the app currently). The subscription provider must be nested inside the query client provider for cache invalidation to work.

    ```tsx
    import { IndexerSubscriptionProvider } from '@lsp-indexer/react';

    // In the layout component's return:
    <QueryClientProvider client={queryClient}>
      <IndexerSubscriptionProvider>
        {children}
      </IndexerSubscriptionProvider>
    </QueryClientProvider>
    ```

    The subscription provider doesn't need explicit `wsUrl` — it derives from `NEXT_PUBLIC_INDEXER_URL` via `getClientWsUrl()` inside the SubscriptionClient constructor.

    **Update navigation — Add subscriptions link:**

    Read `apps/test/src/components/navigation.tsx`. Add a link to `/subscriptions` alongside existing domain links.

    **Create subscription playground page:**

    Create `apps/test/src/app/subscriptions/page.tsx` — a `'use client'` component that demonstrates subscription hooks for 2-3 representative domains.

    Use shadcn/ui components (consistent with existing playground pages — reference `apps/test/src/app/profiles/page.tsx` for patterns).

    Structure:
    - Title: "Subscriptions Playground"
    - Description: "Real-time data via WebSocket subscriptions"
    - Connection status indicator (green dot for connected, red for disconnected)
    - Tabs or sections for 2-3 domains: **Profiles**, **Encrypted Assets**, **Universal Receiver Events** (covering entity domain + primary use case from CONTEXT.md + event domain)

    **Profiles section:**
    - Simple list showing live profile data (names, addresses)
    - Optional address filter input
    - Limit selector (5, 10, 20)
    - Toggle for `invalidate` (shows cache invalidation)
    - Display `data?.length` items, `isConnected`, `isSubscribed` status

    **Encrypted Assets section (primary use case from CONTEXT.md):**
    - Shows live feed of newest encrypted assets
    - No filter (global firehose — the primary use case)
    - Limit: 20
    - Demonstrates: `useEncryptedAssetSubscription({ limit: 20 })`

    **Universal Receiver Events section (event domain):**
    - Shows live event feed with block-order desc sorting
    - Optional address filter
    - Limit: 10

    Each section should show:
    1. Connection status badge (`isConnected`)
    2. Subscription status badge (`isSubscribed`)
    3. Data count
    4. Error display (if any)
    5. Data rendered as simple cards or list items

    Use the following hooks:
    ```typescript
    import {
      useProfileSubscription,
      useEncryptedAssetSubscription,
      useUniversalReceiverEventSubscription,
    } from '@lsp-indexer/react';
    ```

    Keep it simple — this is a verification playground, not a production UI. Focus on demonstrating that:
    1. WebSocket connection works
    2. Subscription data flows
    3. Data is parsed correctly (camelCase, not snake_case)
    4. Cache invalidation works when enabled

    **Build validation:**

    Run `pnpm build` in apps/test to verify the Next.js build succeeds.

    Commit:
    `feat(10-subscriptions): add subscription playground page with provider wiring`

  </action>
  <verify>
    Run `pnpm build` in apps/test — Next.js build succeeds with subscription page.
    Files exist: `apps/test/src/app/subscriptions/page.tsx`, layout has `IndexerSubscriptionProvider`.
  </verify>
  <done>Subscription playground at /subscriptions shows real-time data for profiles, encrypted assets, and universal receiver events. IndexerSubscriptionProvider wraps the app. Navigation updated with subscriptions link.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete subscription system for all 12 domains:
    - Type-safe subscription infrastructure with 4 generic params and `extract` function
    - SubscriptionClient (in @lsp-indexer/node) + IndexerSubscriptionProvider (in @lsp-indexer/react)
    - Generic useSubscription hook with useSyncExternalStore for connection state
    - 12 domain subscription hooks (useProfileSubscription, useDigitalAssetSubscription, etc.)
    - Codegen-typed subscription documents for all 12 Hasura tables
    - Optional TanStack Query cache invalidation via domain key factories
    - Next.js WebSocket proxy for production deployments
    - Subscription playground page in test app
  </what-built>
  <how-to-verify>
    1. Run `pnpm dev` in apps/test
    2. Navigate to http://localhost:3000/subscriptions (or the appropriate dev server URL)
    3. **Verify WebSocket connection:**
       - Check browser devtools Network → WS tab — should see a WebSocket connection to the Hasura endpoint
       - Connection status indicator on the page should show "connected" (green)
    4. **Verify subscription data:**
       - Profiles section should show live profile data
       - Encrypted Assets section should show a feed of encrypted assets (primary use case)
       - Universal Receiver Events section should show events with block-order sorting
    5. **Verify reconnection (optional):**
       - Disconnect WiFi/network briefly → status should show "disconnected"
       - Reconnect → status should return to "connected" and data should reappear
    6. **Verify cache invalidation:**
       - Enable the "invalidate" toggle on the Profiles section
       - If React Query devtools are available, observe that profile queries get invalidated when subscription data arrives
    7. **Verify type safety:**
       - Open any subscription hook file in editor — hover over `extract` function call to verify TypeScript knows the exact type
       - Confirm no `as` casts, no `any`, no `unknown` in the subscription data path
  </how-to-verify>
  <resume-signal>Type "approved" if subscriptions work, or describe specific issues</resume-signal>
</task>

</tasks>

<verification>
- Subscription playground page loads and shows real-time data from Hasura
- WebSocket connection visible in browser devtools
- Connection/subscription status indicators work
- Data is correctly parsed (camelCase, not snake_case)
- Cache invalidation triggers when enabled
- Reconnection works (subscription resumes after network drop)
</verification>

<success_criteria>

1. Developer can see WebSocket connection to Hasura established when subscription page loads
2. Developer can see real-time data flowing in the subscription playground for multiple domains
3. Developer can toggle cache invalidation and observe TanStack Query cache entries being invalidated
4. Data is correctly parsed by existing domain parsers (clean camelCase types)
5. Connection status and subscription status correctly reflect the WebSocket state
   </success_criteria>

<output>
After completion, create `.planning/phases/10-subscriptions/10-03-SUMMARY.md`
</output>
