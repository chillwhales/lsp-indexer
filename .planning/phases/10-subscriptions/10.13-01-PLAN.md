---
phase: 10.13-universal-receiver-events-subscription
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/node/src/documents/universal-receiver-events.ts
  - packages/node/src/graphql/gql.ts
  - packages/node/src/graphql/graphql.ts
  - packages/node/src/services/universal-receiver-events.ts
  - packages/react/src/hooks/types/universal-receiver-events.ts
  - packages/react/src/hooks/types/index.ts
  - packages/react/src/hooks/factories/universal-receiver-events/create-use-universal-receiver-event-subscription.ts
  - packages/react/src/hooks/factories/universal-receiver-events/create-use-universal-receiver-events.ts
  - packages/react/src/hooks/factories/universal-receiver-events/create-use-infinite-universal-receiver-events.ts
  - packages/react/src/hooks/factories/universal-receiver-events/index.ts
  - packages/react/src/hooks/factories/index.ts
  - packages/react/src/hooks/universal-receiver-events/use-universal-receiver-events.ts
  - packages/react/src/hooks/universal-receiver-events/use-infinite-universal-receiver-events.ts
  - packages/react/src/hooks/universal-receiver-events/use-universal-receiver-event-subscription.ts
  - packages/react/src/hooks/universal-receiver-events/index.ts
  - packages/next/src/hooks/universal-receiver-events/use-universal-receiver-events.ts
  - packages/next/src/hooks/universal-receiver-events/use-infinite-universal-receiver-events.ts
  - packages/next/src/hooks/universal-receiver-events/use-universal-receiver-event-subscription.ts
  - packages/next/src/hooks/universal-receiver-events/index.ts
  - apps/test/src/app/universal-receiver-events/page.tsx
autonomous: true
requirements: [SUB-02, SUB-03]

must_haves:
  truths:
    - 'Developer can call useUniversalReceiverEventSubscription() and receive live universal receiver event data via WebSocket'
    - 'Developer can pass filter/sort/limit/include to useUniversalReceiverEventSubscription and see narrowed typed results (UniversalReceiverEventResult<I>)'
    - 'Developer can import useUniversalReceiverEventSubscription from both @lsp-indexer/react and @lsp-indexer/next'
    - 'Developer can see a Subscription tab in the universal-receiver-events playground with connection status, filter/sort/include controls, and live data'
    - 'When invalidate: true, TanStack Query cache for universal receiver event queries is automatically invalidated on subscription data'
    - 'Subscription defaults to block-order desc sort (newest first) when no sort is provided — EVENT domain pattern'
  artifacts:
    - path: 'packages/node/src/documents/universal-receiver-events.ts'
      provides: 'UniversalReceiverEventSubscriptionDocument GraphQL subscription'
      contains: 'UniversalReceiverEventSubscriptionDocument'
    - path: 'packages/node/src/services/universal-receiver-events.ts'
      provides: 'buildUniversalReceiverEventSubscriptionConfig function'
      contains: 'buildUniversalReceiverEventSubscriptionConfig'
    - path: 'packages/react/src/hooks/factories/universal-receiver-events/create-use-universal-receiver-event-subscription.ts'
      provides: 'Factory with 3-overload type narrowing'
      contains: 'createUseUniversalReceiverEventSubscription'
    - path: 'packages/react/src/hooks/universal-receiver-events/use-universal-receiver-event-subscription.ts'
      provides: 'React thin wrapper'
      contains: 'useUniversalReceiverEventSubscription'
    - path: 'packages/next/src/hooks/universal-receiver-events/use-universal-receiver-event-subscription.ts'
      provides: "Next.js thin wrapper with 'use client'"
      contains: 'useUniversalReceiverEventSubscription'
    - path: 'packages/react/src/hooks/types/universal-receiver-events.ts'
      provides: 'UseUniversalReceiverEventSubscriptionParams + query return type aliases'
      contains: 'UseUniversalReceiverEventSubscriptionParams'
    - path: 'packages/react/src/hooks/factories/universal-receiver-events/create-use-universal-receiver-events.ts'
      provides: 'List factory using createUseList'
      contains: 'createUseUniversalReceiverEvents'
    - path: 'packages/react/src/hooks/factories/universal-receiver-events/create-use-infinite-universal-receiver-events.ts'
      provides: 'Infinite factory using createUseInfinite'
      contains: 'createUseInfiniteUniversalReceiverEvents'
  key_links:
    - from: 'packages/react/src/hooks/factories/universal-receiver-events/create-use-universal-receiver-event-subscription.ts'
      to: 'packages/node/src/services/universal-receiver-events.ts'
      via: 'import buildUniversalReceiverEventSubscriptionConfig'
      pattern: 'import.*buildUniversalReceiverEventSubscriptionConfig.*from.*@lsp-indexer/node'
    - from: 'packages/react/src/hooks/universal-receiver-events/use-universal-receiver-event-subscription.ts'
      to: 'packages/react/src/hooks/factories/universal-receiver-events/create-use-universal-receiver-event-subscription.ts'
      via: 'import createUseUniversalReceiverEventSubscription'
      pattern: "createUseUniversalReceiverEventSubscription\\(useSubscription\\)"
    - from: 'packages/next/src/hooks/universal-receiver-events/use-universal-receiver-event-subscription.ts'
      to: 'packages/react/src/hooks/factories/universal-receiver-events/create-use-universal-receiver-event-subscription.ts'
      via: 'import from @lsp-indexer/react'
      pattern: 'import.*createUseUniversalReceiverEventSubscription.*from.*@lsp-indexer/react'
    - from: 'packages/node/src/services/universal-receiver-events.ts'
      to: 'packages/node/src/documents/universal-receiver-events.ts'
      via: 'import UniversalReceiverEventSubscriptionDocument'
      pattern: 'import.*UniversalReceiverEventSubscriptionDocument.*from.*documents/universal-receiver-events'
    - from: 'packages/react/src/hooks/universal-receiver-events/use-universal-receiver-events.ts'
      to: 'packages/react/src/hooks/factories/universal-receiver-events/create-use-universal-receiver-events.ts'
      via: 'import createUseUniversalReceiverEvents'
      pattern: 'createUseUniversalReceiverEvents\('
---

<objective>
Add `useUniversalReceiverEventSubscription` hook for live universal receiver event data via WebSocket — with full `include`/`sort` support, 3-overload type narrowing, dual-package export (`@lsp-indexer/react` + `@lsp-indexer/next`), and playground integration. This is the **last domain subscription** in Phase 10.

Purpose: Completes real-time subscription coverage for all 12 domains (Phase 10.13 of 13 subscription sub-phases). Follows the factory-based architecture established in Phase 10.2 (Profiles) and replicated across 10.3–10.12. Additionally refactors monolithic query hooks into factory pattern. Universal receiver events is an EVENT domain with block-order desc default sort and the highest include variable count in the project (46+ GraphQL variables, 3 nested relations: universalProfile, fromProfile, fromAsset).

Output: Subscription document + codegen types, node service config builder, subscription factory with 3-overload narrowing, query factories (list + infinite), React + Next.js thin wrappers, subscription types, directory refactoring of monolithic hooks, and Subscription tab in universal-receiver-events playground.
</objective>

<execution_context>
@/home/coder/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-subscriptions/10-DOMAIN-SUBSCRIPTION-PRECEDENT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From packages/node/src/services/universal-receiver-events.ts (EXISTING — to modify):

```typescript
// ALREADY EXPORTED:
export function buildUniversalReceiverEventWhere(
  filter?: UniversalReceiverEventFilter,
): Universal_Receiver_Bool_Exp;
export function buildUniversalReceiverEventIncludeVars(
  include?: UniversalReceiverEventInclude,
): Record<string, boolean>;
export interface FetchUniversalReceiverEventsResult<P = UniversalReceiverEvent> {
  universalReceiverEvents: P[];
  totalCount: number;
}
export async function fetchUniversalReceiverEvents(
  url: string,
  params?: { filter?; sort?; limit?; offset?; include? },
): Promise<FetchUniversalReceiverEventsResult<PartialUniversalReceiverEvent>>;

// NOT EXPORTED — must add `export` keyword (line 158):
function buildUniversalReceiverEventOrderBy(
  sort?: UniversalReceiverEventSort,
): Universal_Receiver_Order_By[] | undefined;
```

From packages/node/src/documents/universal-receiver-events.ts (EXISTING — to append):

```typescript
// GetUniversalReceiverEventsDocument already exists with 45 @include variables + 4 pagination vars.
// Variable groups:
//   3 data-value: $includeValue, $includeReceivedData, $includeReturnedValue
//   4 block/time: $includeBlockNumber, $includeTimestamp, $includeLogIndex, $includeTransactionIndex
//   10 universalProfile: $includeUniversalProfile + 9 sub-fields (name, desc, tags, links, avatar, image, bgImage, followerCount, followingCount)
//   10 fromProfile: $includeFromProfile + 9 sub-fields (same as UP)
//   18 fromAsset: $includeFromAsset + 17 sub-fields (name, symbol, tokenType, decimals, totalSupply, description, category, icons, images, links, attributes, owner, holderCount, creatorCount, referenceContract, tokenIdFormat, baseUri)
// Base fields (always): address, from, type_id
// Hasura table: universal_receiver
// Also has universal_receiver_aggregate for totalCount
// Subscription document mirrors it: same fields, same @include directives,
// operation type `subscription`, minus $offset and _aggregate block.
```

From packages/react/src/hooks/types/subscription.ts (EXISTING):

```typescript
export type UseSubscriptionFn = <TResult, TVariables, TRaw, TParsed>(
  config: SubscriptionConfig<TResult, TVariables, TRaw, TParsed>,
  options: UseSubscriptionOptions<TParsed>,
) => UseSubscriptionReturn<TParsed>;
```

From packages/react/src/hooks/factories/data-changed-events/ (CLOSEST EVENT DOMAIN TEMPLATE — data-changed-events has 2 relations vs URE's 3):

```typescript
// create-use-data-changed-events.ts — list factory using createUseList
export function createUseDataChangedEvents(
  queryFn: (
    params: DataChangedEventsListParams,
  ) => Promise<FetchDataChangedEventsResult<PartialDataChangedEvent>>,
) {
  const impl = createUseList<
    DataChangedEventsListParams,
    PartialDataChangedEvent,
    FetchDataChangedEventsResult<PartialDataChangedEvent>
  >({
    queryKey: (p) => dataChangedEventKeys.list(p.filter, p.sort, p.limit, p.offset, p.include),
    queryFn,
    extractItems: (r) => r.dataChangedEvents,
  });
  // 3 overloads: include I → DataChangedEventResult<I>, no include → DataChangedEvent, widest → PartialDataChangedEvent
  // Implementation: const { items, ...rest } = impl(params); return { dataChangedEvents: items, ...rest };
}

// create-use-data-changed-event-subscription.ts — subscription factory
export function createUseDataChangedEventSubscription(useSubscription: UseSubscriptionFn) {
  // 3 overloads + implementation with any[] for onData contravariance
  // Uses buildDataChangedEventSubscriptionConfig, dataChangedEventKeys.all, DEFAULT_SUBSCRIPTION_LIMIT
}
```

From packages/react/src/hooks/types/data-changed-events.ts (TYPE PATTERN TO FOLLOW):

```typescript
export type UseDataChangedEventsReturn<F> = { dataChangedEvents: F[]; totalCount: number } & Omit<
  UseQueryResult<FetchDataChangedEventsResult<F>, Error>,
  'data'
>;
export type UseInfiniteDataChangedEventsReturn<F> = {
  dataChangedEvents: F[];
  hasNextPage: boolean;
  fetchNextPage: UseInfiniteQueryResult['fetchNextPage'];
  isFetchingNextPage: boolean;
} & Omit<
  UseInfiniteQueryResult<InfiniteData<FetchDataChangedEventsResult<F>>, Error>,
  'data' | 'hasNextPage' | 'fetchNextPage' | 'isFetchingNextPage'
>;
export interface UseDataChangedEventSubscriptionParams {
  filter?: DataChangedEventFilter;
  sort?: DataChangedEventSort;
  limit?: number;
  include?: DataChangedEventInclude;
  enabled?: boolean;
  invalidate?: boolean;
  onReconnect?: () => void;
}
```

From packages/react/src/hooks/universal-receiver-events.ts (EXISTING MONOLITH — will be split + deleted):

```typescript
// Contains useUniversalReceiverEvents + useInfiniteUniversalReceiverEvents with 3-overload patterns
// Local type aliases UseUniversalReceiverEventsReturn<F> (lines 24-27) and
// UseInfiniteUniversalReceiverEventsReturn<F> (lines 30-38) defined inline — must be MOVED to hooks/types/
// Imports: fetchUniversalReceiverEvents, getClientUrl, universalReceiverEventKeys from @lsp-indexer/node
// DEFAULT_PAGE_SIZE = 20
```

From packages/next/src/hooks/universal-receiver-events.ts (EXISTING MONOLITH — will be split + deleted):

```typescript
// Contains useUniversalReceiverEvents + useInfiniteUniversalReceiverEvents with 3-overload patterns
// Local type aliases duplicated (must be MOVED to types file)
// Has 'use client' directive at top
// Imports: getUniversalReceiverEvents from '../actions/universal-receiver-events' (server action)
// After directory conversion, server action path becomes '../../actions/universal-receiver-events'
```

From packages/next/src/actions/universal-receiver-events.ts (EXISTING — server action, unchanged):

```typescript
export async function getUniversalReceiverEvents(params?: {
  filter?;
  sort?;
  limit?;
  offset?;
  include?;
}): Promise<FetchUniversalReceiverEventsResult<PartialUniversalReceiverEvent>>;
// 3 overloads matching service pattern
```

From apps/test/src/app/universal-receiver-events/page.tsx (EXISTING — 2 tabs: list, infinite):

```typescript
// UniversalReceiverEventHooks type has: useUniversalReceiverEvents, useInfiniteUniversalReceiverEvents
// Mode-switching with aliased imports from @lsp-indexer/react and @lsp-indexer/next
// Filter config: 11 filter params in 3 rows (address/from/typeId/typeIdName, timestampFrom/To/blockNumberFrom/To, universalProfileName/fromProfileName/fromAssetName)
// Sort config: SORT_OPTIONS with 5 fields (newest, oldest, universalProfileName, fromProfileName, fromAssetName)
// Include config: UNIVERSAL_RECEIVER_EVENT_INCLUDE_FIELDS + 3 sub-include groups (universalProfile, fromProfile, fromAsset)
// UreIncludeSections component renders all include toggles + 3 SubIncludeSection components
// Card component: UniversalReceiverEventCard from @/components/universal-receiver-event-card
// useListState() hook already manages all filter/sort/include state including sub-includes
// Add Subscription tab with Radio icon, SubscriptionTab component
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Universal receiver event subscription — document + codegen + config builder + factories + types + hooks + directory conversion + playground</name>
  <files>
    packages/node/src/documents/universal-receiver-events.ts
    packages/node/src/services/universal-receiver-events.ts
    packages/react/src/hooks/types/universal-receiver-events.ts
    packages/react/src/hooks/types/index.ts
    packages/react/src/hooks/factories/universal-receiver-events/create-use-universal-receiver-event-subscription.ts
    packages/react/src/hooks/factories/universal-receiver-events/create-use-universal-receiver-events.ts
    packages/react/src/hooks/factories/universal-receiver-events/create-use-infinite-universal-receiver-events.ts
    packages/react/src/hooks/factories/universal-receiver-events/index.ts
    packages/react/src/hooks/factories/index.ts
    packages/react/src/hooks/universal-receiver-events/use-universal-receiver-events.ts
    packages/react/src/hooks/universal-receiver-events/use-infinite-universal-receiver-events.ts
    packages/react/src/hooks/universal-receiver-events/use-universal-receiver-event-subscription.ts
    packages/react/src/hooks/universal-receiver-events/index.ts
    packages/next/src/hooks/universal-receiver-events/use-universal-receiver-events.ts
    packages/next/src/hooks/universal-receiver-events/use-infinite-universal-receiver-events.ts
    packages/next/src/hooks/universal-receiver-events/use-universal-receiver-event-subscription.ts
    packages/next/src/hooks/universal-receiver-events/index.ts
    apps/test/src/app/universal-receiver-events/page.tsx
  </files>
  <action>
    Follow the DOMAIN-SUBSCRIPTION-PRECEDENT.md step-by-step, adapting for the universal-receiver-events domain. Universal receiver events is an **EVENT domain** — block-order desc default sort when no sort is provided. Use the **data-changed-events subscription (10.11)** as the direct template since both are event domains with nested relation includes. URE is the most complex domain (3 relations: universalProfile, fromProfile, fromAsset — 46+ GraphQL variables).

    **Step 1: Subscription document** — Append `UniversalReceiverEventSubscriptionDocument` to `packages/node/src/documents/universal-receiver-events.ts` using `graphql()` tag. Mirror `GetUniversalReceiverEventsDocument` exactly (same 45 `@include` variables, same field selection, same nested `universalProfile`, `fromProfile`, and `fromAsset` relations) but:
    - Change operation type from `query` to `subscription`
    - Change operation name from `GetUniversalReceiverEvents` to `UniversalReceiverEventSubscription`
    - Remove `$offset: Int` variable (subscriptions don't paginate)
    - Remove `offset: $offset` from the `universal_receiver(...)` args
    - Remove the entire `universal_receiver_aggregate(where: $where) { aggregate { count } }` block (no totalCount in subscriptions)
    - Keep all 45 `@include` directives and `$includeX: Boolean! = true` defaults
    - Keep `$where`, `$order_by`, `$limit` variables
    - Table is `universal_receiver` (same as query)
    - Add JSDoc: `/** GraphQL subscription for real-time universal receiver events. Mirrors GetUniversalReceiverEventsDocument minus offset and aggregate. Used by useUniversalReceiverEventSubscription via buildUniversalReceiverEventSubscriptionConfig. Variables: ~46 total (3 pagination/filter + 7 scalar + 10 UP + 10 fromProfile + 18 fromAsset include toggles). */`

    **Step 2: Run codegen** — Execute `pnpm --filter @lsp-indexer/node codegen` to generate `UniversalReceiverEventSubscriptionSubscription` type in `graphql/graphql.ts`. This type is needed for the `extract` function and `RawUniversalReceiverEventSubscriptionRow` type alias.

    **Step 3: Export `buildUniversalReceiverEventOrderBy`** — In `packages/node/src/services/universal-receiver-events.ts`, add `export` keyword to `function buildUniversalReceiverEventOrderBy(sort?: UniversalReceiverEventSort)` on line 158 (currently file-private). The subscription config builder needs it. Keep the JSDoc comment above it unchanged.

    **Step 4: Add `buildUniversalReceiverEventSubscriptionConfig`** — Append to `packages/node/src/services/universal-receiver-events.ts`:
    ```typescript
    // ---------------------------------------------------------------------------
    // Subscription config
    // ---------------------------------------------------------------------------

    type RawUniversalReceiverEventSubscriptionRow =
      UniversalReceiverEventSubscriptionSubscription['universal_receiver'][number];

    /**
     * Build a subscription config for real-time universal receiver event data.
     *
     * EVENT domain — defaults to block-order desc sort when no sort is provided
     * (block_number desc → transaction_index desc → log_index desc).
     */
    export function buildUniversalReceiverEventSubscriptionConfig(params: {
      filter?: UniversalReceiverEventFilter;
      sort?: UniversalReceiverEventSort;
      limit?: number;
      include?: UniversalReceiverEventInclude;
    }) {
      const where = buildUniversalReceiverEventWhere(params.filter);
      const orderBy = buildUniversalReceiverEventOrderBy(params.sort) ?? buildBlockOrderSort('desc');
      const includeVars = buildUniversalReceiverEventIncludeVars(params.include);

      return {
        document: UniversalReceiverEventSubscriptionDocument,
        variables: {
          where: Object.keys(where).length > 0 ? where : undefined,
          order_by: orderBy,
          limit: params.limit,
          ...includeVars,
        },
        extract: (result: UniversalReceiverEventSubscriptionSubscription) =>
          result.universal_receiver,
        parser: (raw: RawUniversalReceiverEventSubscriptionRow[]) =>
          params.include
            ? parseUniversalReceiverEvents(raw, params.include)
            : parseUniversalReceiverEvents(raw),
      };
    }
    ```
    Import `UniversalReceiverEventSubscriptionDocument` from `'../documents/universal-receiver-events'` and `UniversalReceiverEventSubscriptionSubscription` from `'../graphql/graphql'` at the top of the file. Note: EVENT domain — uses `?? buildBlockOrderSort('desc')` fallback (matching data-changed-events pattern).

    **Step 5: Create subscription types** — Create `packages/react/src/hooks/types/universal-receiver-events.ts`:
    - MOVE `UseUniversalReceiverEventsReturn<F>` type alias FROM the monolith `packages/react/src/hooks/universal-receiver-events.ts` (lines 24-27):
      ```typescript
      export type UseUniversalReceiverEventsReturn<F> = {
        universalReceiverEvents: F[];
        totalCount: number;
      } & Omit<UseQueryResult<FetchUniversalReceiverEventsResult<F>, Error>, 'data'>;
      ```
    - MOVE `UseInfiniteUniversalReceiverEventsReturn<F>` type alias FROM the monolith (lines 30-38):
      ```typescript
      export type UseInfiniteUniversalReceiverEventsReturn<F> = {
        universalReceiverEvents: F[];
        hasNextPage: boolean;
        fetchNextPage: UseInfiniteQueryResult['fetchNextPage'];
        isFetchingNextPage: boolean;
      } & Omit<
        UseInfiniteQueryResult<InfiniteData<FetchUniversalReceiverEventsResult<F>>, Error>,
        'data' | 'hasNextPage' | 'fetchNextPage' | 'isFetchingNextPage'
      >;
      ```
    - ADD `UseUniversalReceiverEventSubscriptionParams` interface:
      ```typescript
      /**
       * Base params for `useUniversalReceiverEventSubscription`.
       *
       * `onData` is intentionally omitted here — it is added per-overload in the
       * factory so the callback receives the correctly-narrowed type:
       * - No `include` → `onData(data: UniversalReceiverEvent[])`
       * - `include: I` → `onData(data: UniversalReceiverEventResult<I>[])`
       * - Widest   → `onData(data: PartialUniversalReceiverEvent[])`
       */
      export interface UseUniversalReceiverEventSubscriptionParams {
        /** Filter criteria (optional — omit for all events) */
        filter?: UniversalReceiverEventFilter;
        /** Sort order (optional — defaults to block-order desc) */
        sort?: UniversalReceiverEventSort;
        /** Maximum events in subscription result (default: 10) */
        limit?: number;
        /** Field inclusion config (optional — omit for all fields) */
        include?: UniversalReceiverEventInclude;
        /** Enable/disable subscription (default: true) */
        enabled?: boolean;
        /** Invalidate TanStack Query cache on subscription data (default: false) */
        invalidate?: boolean;
        /** Callback when WebSocket reconnects after a drop */
        onReconnect?: () => void;
        // NOTE: onData is NOT here — it's per-overload in the factory
      }
      ```
    - Import `FetchUniversalReceiverEventsResult` from `@lsp-indexer/node`
    - Import `UniversalReceiverEventFilter, UniversalReceiverEventInclude, UniversalReceiverEventSort` from `@lsp-indexer/types`
    - Import TanStack Query types: `InfiniteData, UseInfiniteQueryResult, UseQueryResult` from `@tanstack/react-query`

    **Step 6: Update types barrel** — Add `export * from './universal-receiver-events'` to `packages/react/src/hooks/types/index.ts` (alphabetically after `token-id-data-changed-events`).

    **Step 7: Create subscription factory** — Create `packages/react/src/hooks/factories/universal-receiver-events/create-use-universal-receiver-event-subscription.ts`:
    - Follow `create-use-data-changed-event-subscription.ts` exactly as template, swap DataChangedEvent→UniversalReceiverEvent naming
    - Import `buildUniversalReceiverEventSubscriptionConfig, universalReceiverEventKeys` from `@lsp-indexer/node`
    - Import `PartialUniversalReceiverEvent, UniversalReceiverEvent, UniversalReceiverEventInclude, UniversalReceiverEventResult, UseSubscriptionReturn` from `@lsp-indexer/types`
    - Import `useQueryClient` from `@tanstack/react-query`
    - Import `DEFAULT_SUBSCRIPTION_LIMIT` from `'../../../constants'`
    - Import `UseSubscriptionFn, UseUniversalReceiverEventSubscriptionParams` from `'../../types'`
    - 3 overloads:
      (1) `include: I` → `UseSubscriptionReturn<UniversalReceiverEventResult<I>>`
      (2) `include?: never` → `UseSubscriptionReturn<UniversalReceiverEvent>`
      (3) `include?: UniversalReceiverEventInclude` → `UseSubscriptionReturn<PartialUniversalReceiverEvent>`
    - Implementation: destructure params (filter, sort, limit=DEFAULT_SUBSCRIPTION_LIMIT, include, enabled=true, invalidate=false, onData, onReconnect), `const queryClient = useQueryClient()`, call `buildUniversalReceiverEventSubscriptionConfig({ filter, sort, limit, include })`, return `useSubscription(config, { enabled, invalidate, invalidateKeys: invalidate ? [universalReceiverEventKeys.all] : undefined, queryClient: invalidate ? queryClient : undefined, onData, onReconnect })`
    - Implementation param uses `any[]` for onData to avoid contravariance conflict
    - Add JSDoc: `/** Factory for useUniversalReceiverEventSubscription — shared between @lsp-indexer/react and @lsp-indexer/next. EVENT domain — defaults to block-order desc sort when no sort is provided. */`

    **Step 8: Create list factory** — Create `packages/react/src/hooks/factories/universal-receiver-events/create-use-universal-receiver-events.ts`:
    - Follow `create-use-data-changed-events.ts` exactly, swap DataChangedEvent→UniversalReceiverEvent naming
    - `type UniversalReceiverEventsListParams = UseUniversalReceiverEventsParams & { include?: UniversalReceiverEventInclude }`
    - Use `createUseList<UniversalReceiverEventsListParams, PartialUniversalReceiverEvent, FetchUniversalReceiverEventsResult<PartialUniversalReceiverEvent>>`
    - `queryKey: (p) => universalReceiverEventKeys.list(p.filter, p.sort, p.limit, p.offset, p.include)`
    - `extractItems: (r) => r.universalReceiverEvents`
    - 3 overloads returning `UseUniversalReceiverEventsReturn<UniversalReceiverEventResult<I>>`, `UseUniversalReceiverEventsReturn<UniversalReceiverEvent>`, `UseUniversalReceiverEventsReturn<PartialUniversalReceiverEvent>`
    - Implementation: `const { items, ...rest } = impl(params); return { universalReceiverEvents: items, ...rest };`
    - Import `UseUniversalReceiverEventsParams` from `@lsp-indexer/types` (not from hooks/types)

    **Step 9: Create infinite factory** — Create `packages/react/src/hooks/factories/universal-receiver-events/create-use-infinite-universal-receiver-events.ts`:
    - Follow `create-use-infinite-data-changed-events.ts` exactly, swap DataChangedEvent→UniversalReceiverEvent naming
    - `type UniversalReceiverEventsInfiniteParams = UseInfiniteUniversalReceiverEventsParams & { include?: UniversalReceiverEventInclude }`
    - Use `createUseInfinite<UniversalReceiverEventsInfiniteParams, PartialUniversalReceiverEvent, FetchUniversalReceiverEventsResult<PartialUniversalReceiverEvent>>`
    - `queryKey: (p) => universalReceiverEventKeys.infinite(p.filter, p.sort, p.include)`
    - `extractItems: (r) => r.universalReceiverEvents`
    - 3 overloads returning `UseInfiniteUniversalReceiverEventsReturn<...>` variants
    - Implementation: `const { items, ...rest } = impl(params); return { universalReceiverEvents: items, ...rest };`

    **Step 10: Create factory barrel** — Create `packages/react/src/hooks/factories/universal-receiver-events/index.ts`:
    ```typescript
    export * from './create-use-infinite-universal-receiver-events';
    export * from './create-use-universal-receiver-event-subscription';
    export * from './create-use-universal-receiver-events';
    ```

    **Step 11: Update factories barrel** — Add `export * from './universal-receiver-events'` to `packages/react/src/hooks/factories/index.ts` (in the "Domain factories" section, alphabetically after `'./token-id-data-changed-events'`).

    **Step 12: Convert React hooks to directory** —
    - Delete the monolithic `packages/react/src/hooks/universal-receiver-events.ts`
    - Create `packages/react/src/hooks/universal-receiver-events/use-universal-receiver-events.ts` — thin wrapper:
      ```typescript
      import { fetchUniversalReceiverEvents, getClientUrl } from '@lsp-indexer/node';
      import { createUseUniversalReceiverEvents } from '../factories';

      /**
       * Fetch a paginated list of universal receiver event records with filtering and sorting.
       *
       * Wraps `fetchUniversalReceiverEvents` in a TanStack `useQuery` hook via the shared list factory.
       */
      export const useUniversalReceiverEvents = createUseUniversalReceiverEvents((params) =>
        fetchUniversalReceiverEvents(getClientUrl(), params),
      );
      ```
    - Create `packages/react/src/hooks/universal-receiver-events/use-infinite-universal-receiver-events.ts` — thin wrapper:
      ```typescript
      import { fetchUniversalReceiverEvents, getClientUrl } from '@lsp-indexer/node';
      import { createUseInfiniteUniversalReceiverEvents } from '../factories';

      /**
       * Fetch universal receiver event records with infinite scroll pagination.
       *
       * Wraps `fetchUniversalReceiverEvents` in a TanStack `useInfiniteQuery` hook via the shared infinite factory.
       */
      export const useInfiniteUniversalReceiverEvents = createUseInfiniteUniversalReceiverEvents(
        (params) => fetchUniversalReceiverEvents(getClientUrl(), params),
      );
      ```
    - Create `packages/react/src/hooks/universal-receiver-events/use-universal-receiver-event-subscription.ts` (~12 lines):
      ```typescript
      import { createUseUniversalReceiverEventSubscription } from '../factories';
      import { useSubscription } from '../use-subscription';

      /**
       * Subscribe to real-time universal receiver event updates via WebSocket.
       *
       * Thin wrapper — injects the React-specific `useSubscription` into the shared factory.
       */
      export const useUniversalReceiverEventSubscription =
        createUseUniversalReceiverEventSubscription(useSubscription);
      ```
    - Create `packages/react/src/hooks/universal-receiver-events/index.ts`:
      ```typescript
      export * from './use-infinite-universal-receiver-events';
      export * from './use-universal-receiver-event-subscription';
      export * from './use-universal-receiver-events';
      ```
    - Note: `hooks/index.ts` already has `export * from './universal-receiver-events'` which resolves to the directory index — no change needed.

    **Step 13: Convert Next.js hooks to directory** —
    - Delete the monolithic `packages/next/src/hooks/universal-receiver-events.ts`
    - Create `packages/next/src/hooks/universal-receiver-events/use-universal-receiver-events.ts`:
      ```typescript
      'use client';

      import { createUseUniversalReceiverEvents } from '@lsp-indexer/react';
      import { getUniversalReceiverEvents } from '../../actions/universal-receiver-events';

      /**
       * Fetch universal receiver event records — Next.js version routing through server action.
       */
      export const useUniversalReceiverEvents = createUseUniversalReceiverEvents(getUniversalReceiverEvents);
      ```
    - Create `packages/next/src/hooks/universal-receiver-events/use-infinite-universal-receiver-events.ts`:
      ```typescript
      'use client';

      import { createUseInfiniteUniversalReceiverEvents } from '@lsp-indexer/react';
      import { getUniversalReceiverEvents } from '../../actions/universal-receiver-events';

      /**
       * Fetch universal receiver events with infinite scroll — Next.js version routing through server action.
       */
      export const useInfiniteUniversalReceiverEvents =
        createUseInfiniteUniversalReceiverEvents(getUniversalReceiverEvents);
      ```
    - Create `packages/next/src/hooks/universal-receiver-events/use-universal-receiver-event-subscription.ts` (~15 lines):
      ```typescript
      'use client';

      import { createUseUniversalReceiverEventSubscription } from '@lsp-indexer/react';
      import { useSubscription } from '../use-subscription';

      /**
       * Subscribe to real-time universal receiver event updates via WebSocket — Next.js version.
       */
      export const useUniversalReceiverEventSubscription =
        createUseUniversalReceiverEventSubscription(useSubscription);
      ```
      CRITICAL: `'use client'` directive at top. Import factory from `@lsp-indexer/react`, NOT relative path. Import `useSubscription` from `'../use-subscription'` (Next.js package-specific version).
    - Create `packages/next/src/hooks/universal-receiver-events/index.ts`:
      ```typescript
      export * from './use-infinite-universal-receiver-events';
      export * from './use-universal-receiver-event-subscription';
      export * from './use-universal-receiver-events';
      ```
    - Note: `hooks/index.ts` already has `export * from './universal-receiver-events'` — no change needed.

    **Step 14: Add Subscription tab to playground** — Modify `apps/test/src/app/universal-receiver-events/page.tsx`:
    - Add subscription hook imports:
      ```typescript
      import { useUniversalReceiverEventSubscription as useUniversalReceiverEventSubscriptionNext } from '@lsp-indexer/next';
      import { useUniversalReceiverEventSubscription as useUniversalReceiverEventSubscriptionReact } from '@lsp-indexer/react';
      ```
    - Add `Radio, Wifi, WifiOff` to the `lucide-react` import (alongside existing `Infinity, List`)
    - Add UI component imports: `Badge` from `@/components/ui/badge`, `Label` from `@/components/ui/label`, `Switch` from `@/components/ui/switch`
    - Extend the `UniversalReceiverEventHooks` type and `useUniversalReceiverEventHooks` function to include `useUniversalReceiverEventSubscription`:
      ```typescript
      type UniversalReceiverEventHooks = {
        useUniversalReceiverEvents: typeof useUniversalReceiverEventsReact;
        useInfiniteUniversalReceiverEvents: typeof useInfiniteUniversalReceiverEventsReact;
        useUniversalReceiverEventSubscription: typeof useUniversalReceiverEventSubscriptionReact;
      };
      ```
      Update both branches of `useUniversalReceiverEventHooks` to include the subscription hook.
    - Create `SubscriptionTab` component following the data-changed-events playground pattern:
      - `const { useUniversalReceiverEventSubscription } = useUniversalReceiverEventHooks(mode)`
      - State: `limit` (default 10), `invalidate` (default false)
      - Use shared `useListState()` for filter/sort/include controls (already manages all 11 filter params + 5 sort fields + 3 sub-include groups)
      - Call `useUniversalReceiverEventSubscription({ filter, sort, limit, include, invalidate })`
      - Destructure: `const { data, isConnected, isSubscribed, error } = ...`
      - Map data: `const universalReceiverEvents = data ?? []`
      - Derive loading: `const isLoading = data === null && isSubscribed`
      - Normalize error: `const normalizedError = error instanceof Error ? error : error != null ? new Error(String(error)) : null`
      - Display connection/subscription status badges (Wifi/WifiOff icon, Connected/Disconnected, Subscribed/Idle)
      - Display invalidate toggle switch (Switch + Label with `ml-auto`, id `sub-invalidate`)
      - Display filter controls (same 3 rows: ROW1, ROW2, ROW3)
      - Display sort controls with SORT_OPTIONS + `hideDirectionAndNulls` for newest/oldest
      - Display `UreIncludeSections` component (already extracted — includes 3 sub-include groups: universalProfile, fromProfile, fromAsset)
      - Display `ResultsList` with subscription data, `UniversalReceiverEventCard`, `isFetching={false}`, `totalCount={universalReceiverEvents.length}`
    - Add tab entry to the tabs array:
      ```typescript
      {
        value: 'subscription',
        label: 'Subscription',
        icon: <Radio className="size-4" />,
        render: (mode) => <SubscriptionTab mode={mode} />,
      }
      ```
    - Update page description to mention `useUniversalReceiverEventSubscription`

    **Step 15: Build validation** — Run individual package builds:
    ```bash
    pnpm --filter @lsp-indexer/node build && \
    pnpm --filter @lsp-indexer/types build && \
    pnpm --filter @lsp-indexer/react build && \
    pnpm --filter @lsp-indexer/next build && \
    pnpm --filter test build
    ```
    Do NOT run `pnpm build` (pre-existing indexer package errors).

  </action>
  <verify>
    All 5 package builds pass:
    ```bash
    pnpm --filter @lsp-indexer/node build && \
    pnpm --filter @lsp-indexer/types build && \
    pnpm --filter @lsp-indexer/react build && \
    pnpm --filter @lsp-indexer/next build && \
    pnpm --filter test build
    ```

    Grep checks:
    ```bash
    grep -r "useUniversalReceiverEventSubscription" packages/react/src/hooks/universal-receiver-events/use-universal-receiver-event-subscription.ts && \
    grep -r "useUniversalReceiverEventSubscription" packages/next/src/hooks/universal-receiver-events/use-universal-receiver-event-subscription.ts && \
    grep -r "buildUniversalReceiverEventSubscriptionConfig" packages/node/src/services/universal-receiver-events.ts && \
    grep -r "UniversalReceiverEventSubscriptionDocument" packages/node/src/documents/universal-receiver-events.ts && \
    grep "export.*buildUniversalReceiverEventOrderBy" packages/node/src/services/universal-receiver-events.ts && \
    grep "UseUniversalReceiverEventSubscriptionParams" packages/react/src/hooks/types/universal-receiver-events.ts && \
    grep "SubscriptionTab" apps/test/src/app/universal-receiver-events/page.tsx && \
    grep "createUseUniversalReceiverEvents" packages/react/src/hooks/factories/universal-receiver-events/create-use-universal-receiver-events.ts && \
    grep "createUseInfiniteUniversalReceiverEvents" packages/react/src/hooks/factories/universal-receiver-events/create-use-infinite-universal-receiver-events.ts
    ```

    Verify old monolith files are deleted:
    ```bash
    test ! -f packages/react/src/hooks/universal-receiver-events.ts && \
    test ! -f packages/next/src/hooks/universal-receiver-events.ts && \
    echo "Monolith files deleted"
    ```

    Verify event domain sort pattern:
    ```bash
    grep "buildBlockOrderSort" packages/node/src/services/universal-receiver-events.ts | grep -c "desc"
    ```
    Should return 2 (one in fetchUniversalReceiverEvents, one in buildUniversalReceiverEventSubscriptionConfig).

  </verify>
  <done>
    1. `UniversalReceiverEventSubscriptionDocument` exists in node documents with subscription operation type, 45 @include variables (7 scalar + 10 UP + 10 fromProfile + 18 fromAsset), no $offset, no aggregate block
    2. Codegen has run — `UniversalReceiverEventSubscriptionSubscription` type exists in graphql/graphql.ts
    3. `buildUniversalReceiverEventOrderBy` is exported from node services (was private)
    4. `buildUniversalReceiverEventSubscriptionConfig` exists in node services (EVENT domain — block-order desc fallback)
    5. `UseUniversalReceiverEventSubscriptionParams` interface exists in react hooks types (no onData — per-overload)
    6. `UseUniversalReceiverEventsReturn<F>` and `UseInfiniteUniversalReceiverEventsReturn<F>` type aliases moved from monolith to types file
    7. Subscription factory has 3 overloads with `UniversalReceiverEventResult<I>` narrowing
    8. List factory `createUseUniversalReceiverEvents` wraps `createUseList` with universalReceiverEventKeys + extractItems → universalReceiverEvents
    9. Infinite factory `createUseInfiniteUniversalReceiverEvents` wraps `createUseInfinite` with universalReceiverEventKeys + extractItems → universalReceiverEvents
    10. React thin wrappers: `use-universal-receiver-events.ts` calls `createUseUniversalReceiverEvents((p) => fetchUniversalReceiverEvents(getClientUrl(), p))`, `use-universal-receiver-event-subscription.ts` calls `createUseUniversalReceiverEventSubscription(useSubscription)`
    11. Next.js thin wrappers: `use-universal-receiver-events.ts` has `'use client'` + `createUseUniversalReceiverEvents(getUniversalReceiverEvents)`, `use-universal-receiver-event-subscription.ts` has `'use client'` + imports factory from `@lsp-indexer/react` + imports `useSubscription` from `../use-subscription`
    12. React hooks directory: `universal-receiver-events/use-universal-receiver-events.ts` + `use-infinite-universal-receiver-events.ts` + `use-universal-receiver-event-subscription.ts` + `index.ts`
    13. Next.js hooks directory: same structure with `'use client'` on all files
    14. Old monolithic `hooks/universal-receiver-events.ts` files deleted from both packages
    15. Factory barrel chain: `factories/universal-receiver-events/index.ts` → `factories/index.ts`
    16. Types barrel: `types/universal-receiver-events.ts` → `types/index.ts`
    17. Playground has Subscription tab with Radio icon, status badges, filter/sort/include controls (including all 3 sub-include groups: universalProfile, fromProfile, fromAsset), invalidate toggle, hideDirectionAndNulls for newest/oldest
    18. All 5 package builds pass
  </done>
</task>

</tasks>

<verification>
1. All 5 packages build successfully (`pnpm --filter @lsp-indexer/node build && pnpm --filter @lsp-indexer/types build && pnpm --filter @lsp-indexer/react build && pnpm --filter @lsp-indexer/next build && pnpm --filter test build`)
2. `useUniversalReceiverEventSubscription` is exported from both `@lsp-indexer/react` and `@lsp-indexer/next`
3. `buildUniversalReceiverEventSubscriptionConfig` is exported from `@lsp-indexer/node`
4. `createUseUniversalReceiverEvents` and `createUseInfiniteUniversalReceiverEvents` are exported from `@lsp-indexer/react`
5. Old monolithic `hooks/universal-receiver-events.ts` files no longer exist in react or next packages
6. Factory barrel chain: `factories/universal-receiver-events/index.ts` → `factories/index.ts` → accessible via `'../factories'` import
7. Hook barrel chain: `universal-receiver-events/index.ts` → `hooks/index.ts` → package index
8. No `as` casts, no `unknown`, no `any` in subscription data path (except `any[]` for onData implementation signature — standard pattern)
9. Subscription uses block-order desc default sort (event domain pattern) — 2 occurrences of `buildBlockOrderSort('desc')` in services file
</verification>

<success_criteria>

1. Developer can call `useUniversalReceiverEventSubscription()` from `@lsp-indexer/react` and receive `{ data, isConnected, isSubscribed, error }` with typed `UniversalReceiverEvent[]` data
2. Developer can call `useUniversalReceiverEventSubscription({ include: { universalProfile: true, fromAsset: true } })` and get `UniversalReceiverEventResult<I>` narrowed type
3. Developer can import `useUniversalReceiverEventSubscription` from `@lsp-indexer/next` with identical API
4. Playground shows Subscription tab with live universal receiver event data, connection status, and filter/sort/include controls (including 3 nested sub-include groups)
5. All 5 packages build without errors
   </success_criteria>

<output>
After completion, create `.planning/phases/10-subscriptions/10.13-01-SUMMARY.md`
</output>
