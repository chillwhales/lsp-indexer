---
phase: 17-version-normalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/abi/package.json
  - packages/typeorm/package.json
  - packages/indexer/package.json
  - pnpm-lock.yaml
autonomous: true
requirements:
  - VERS-01
  - VERS-02
  - VERS-03
  - VERS-04

must_haves:
  truths:
    - "@chillwhales/abi package.json shows version 0.1.0"
    - "@chillwhales/typeorm package.json shows version 0.1.0"
    - "@chillwhales/indexer package.json shows version 0.1.0"
    - "apps/test package.json shows version 0.1.0"
    - "All packages build successfully after version change"
  artifacts:
    - path: "packages/abi/package.json"
      provides: "Version field set to 0.1.0"
      contains: '"version": "0.1.0"'
    - path: "packages/typeorm/package.json"
      provides: "Version field set to 0.1.0"
      contains: '"version": "0.1.0"'
    - path: "packages/indexer/package.json"
      provides: "Version field at 0.1.0 and workspace refs updated"
      contains: '"version": "0.1.0"'
    - path: "apps/test/package.json"
      provides: "Version field at 0.1.0"
      contains: '"version": "0.1.0"'
  key_links:
    - from: "packages/indexer/package.json"
      to: "packages/abi/package.json"
      via: "workspace dependency reference"
      pattern: '"@chillwhales/abi": "workspace:0.1.0"'
    - from: "packages/indexer/package.json"
      to: "packages/typeorm/package.json"
      via: "workspace dependency reference"
      pattern: '"@chillwhales/typeorm": "workspace:0.1.0"'
---

<objective>
Normalize all private package versions to 0.1.0 across the monorepo.

Purpose: Establish clean, consistent versioning as the baseline for the v1.2 Production Readiness milestone. Private packages had accumulated arbitrary version numbers (1.0.4, 1.2.1) from development — resetting to 0.1.0 signals a fresh semantic versioning baseline.

Output: All 4 private packages at version 0.1.0 with consistent workspace references and passing builds.
</objective>

<execution_context>
@/home/coder/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package versions and workspace references to 0.1.0</name>
  <files>packages/abi/package.json, packages/typeorm/package.json, packages/indexer/package.json</files>
  <action>
Update version fields and workspace dependency references across the monorepo:

1. In `packages/abi/package.json`: Change `"version": "1.0.4"` to `"version": "0.1.0"`
2. In `packages/typeorm/package.json`: Change `"version": "1.2.1"` to `"version": "0.1.0"`
3. In `packages/indexer/package.json`: Update workspace dependency references to match new versions:
   - Change `"@chillwhales/abi": "workspace:1.0.4"` to `"@chillwhales/abi": "workspace:0.1.0"`
   - Change `"@chillwhales/typeorm": "workspace:1.2.1"` to `"@chillwhales/typeorm": "workspace:0.1.0"`
4. Confirm `packages/indexer/package.json` version is already `"0.1.0"` (no change needed — VERS-03)
5. Confirm `apps/test/package.json` version is already `"0.1.0"` (no change needed — VERS-04)
6. Run `pnpm install` to sync the lockfile with updated version references

Do NOT change any other fields in the package.json files. Only touch `version` fields and workspace dependency version references.
  </action>
  <verify>
    <automated>grep -E '"version"[[:space:]]*:[[:space:]]*"0\.1\.0"' packages/abi/package.json packages/typeorm/package.json packages/indexer/package.json apps/test/package.json | wc -l | grep -q '^4$' && grep '"@chillwhales/abi": "workspace:0.1.0"' packages/indexer/package.json && grep '"@chillwhales/typeorm": "workspace:0.1.0"' packages/indexer/package.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All 4 package.json files show version 0.1.0. Workspace references in packages/indexer/package.json point to 0.1.0. Lockfile updated.</done>
</task>

<task type="auto">
  <name>Task 2: Verify all packages build after version normalization</name>
  <files></files>
  <action>
Build each package individually (in dependency order) to confirm version changes introduce no regressions:

1. `pnpm --filter=@chillwhales/abi build`
2. `pnpm --filter=@chillwhales/typeorm build` (depends on abi output)
3. `pnpm --filter=@chillwhales/indexer build` (depends on abi + typeorm)

For `apps/test`: This is a Next.js app. A full `next build` is heavyweight. Skip building apps/test — verifying its version field (Task 1) satisfies VERS-04.
  </action>
  <verify>
    <automated>pnpm --filter=@chillwhales/abi build && pnpm --filter=@chillwhales/typeorm build && pnpm --filter=@chillwhales/indexer build && echo "ALL BUILDS PASS"</automated>
  </verify>
  <done>All 3 packages (abi, typeorm, indexer) build cleanly.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Run `grep '"version"' packages/*/package.json apps/*/package.json` — all 4 private packages show 0.1.0
2. Run `grep 'workspace:' packages/indexer/package.json` — workspace refs show 0.1.0
3. All packages build successfully
</verification>

<success_criteria>
- All 4 private packages (`@chillwhales/abi`, `@chillwhales/typeorm`, `@chillwhales/indexer`, `apps/test`) show version 0.1.0 in their package.json
- Workspace dependency references in `packages/indexer/package.json` updated to 0.1.0
- `pnpm install` completes and lockfile is consistent
- All packages build successfully
</success_criteria>

<output>
After completion, create `.planning/phases/17-version-normalization/17-01-SUMMARY.md`
</output>
