---
phase: 19.1-type-system-tightening
verified: 2026-03-10T19:35:00Z
status: passed
score: 13/13 must-haves verified
re_verification: false
---

# Phase 19.1: Type System Tightening — Verification Report

**Phase Goal:** BatchContext uses honest types — no unchecked generics on type-erased storage
**Verified:** 2026-03-10T19:35:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Every bag key is registered in EntityRegistry — typo = compile error | ✓ VERIFIED | `EntityRegistry` interface has 71 entries; `addEntity<K extends keyof EntityRegistry & string>` signature enforces valid keys |
| 2 | addEntity<K> enforces entity type matches bag key at compile time | ✓ VERIFIED | `addEntity<K>(type: K, id: string, entity: EntityRegistry[K]): void` in `batchContext.ts:113` |
| 3 | getEntities<K> returns `Map<string, EntityRegistry[K]>` — no cast needed at call sites | ✓ VERIFIED | `getEntities<K>(type: K): Map<string, EntityRegistry[K]>` in `batchContext.ts:149`; handler files (lsp4TokenName, nft, follower etc.) use `batchCtx.getEntities('Key')` with zero casts |
| 4 | addEntity validates with instanceof at runtime | ✓ VERIFIED | `batchContext.ts:127-133` — `instanceof ENTITY_CONSTRUCTORS[type]` check with descriptive error |
| 5 | getEntities validates with instanceof at runtime on non-empty maps | ✓ VERIFIED | `batchContext.ts:153-160` — spot-check first entity via `instanceof ctor` |
| 6 | Zero `as Map<string,` casts remain in handler files | ✓ VERIFIED | `grep "as Map<string," handlers/*.handler.ts` → 0 matches |
| 7 | Zero `as Map<string,` casts remain in handlerHelpers.ts | ✓ VERIFIED | `grep "as Map<string," handlerHelpers.ts` → 0 matches |
| 8 | getTypedEntities() is deleted — getEntities() IS the typed accessor | ✓ VERIFIED | `grep "getTypedEntities" src/` → 0 matches (only 2 in comments in entityRegistry.ts documenting replacement) |
| 9 | entityTypeMap.ts is deleted — entityRegistry.ts replaces it | ✓ VERIFIED | `ls entityTypeMap.ts` → file not found; `grep "entityTypeMap" src/` → 0 matches (only 2 in comments) |
| 10 | Pipeline and fkResolution use getEntitiesUntyped(string) for dynamic iteration | ✓ VERIFIED | `pipeline.ts` has 4 `getEntitiesUntyped` calls (lines 233, 345, 492, 547); `fkResolution.ts` has 3 (lines 219, 248, 269) |
| 11 | resolveEntity/resolveEntities no longer take entityClass — constructor comes from registry | ✓ VERIFIED | `handlerHelpers.ts:43` — `resolveEntity<K>(store, batchCtx, entityType: K, id: string)` (4 params, no entityClass); nft.handler.ts line 97 calls with 4 args |
| 12 | pnpm --filter=@chillwhales/indexer build exits with zero errors | ✓ VERIFIED | Build exits cleanly: `tsc` completes with no output (zero errors) |
| 13 | Full test suite passes | ✓ VERIFIED | 22 test files, 260 tests passed, 0 failures |

**Score:** 13/13 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/indexer/src/core/entityRegistry.ts` | EntityRegistry type map + ENTITY_CONSTRUCTORS runtime map + getEntityConstructor() | ✓ VERIFIED | 318 lines, 71 interface entries, 71 runtime entries, BagKey alias, getEntityConstructor<K>() helper |
| `packages/indexer/src/core/types/batchContext.ts` | IBatchContext with typed addEntity<K> and getEntities<K>, plus untyped getEntitiesUntyped(string) | ✓ VERIFIED | Typed methods (lines 120-127) + untyped methods (lines 130-131) |
| `packages/indexer/src/core/batchContext.ts` | BatchContext with instanceof validation on addEntity and getEntities | ✓ VERIFIED | instanceof validation on addEntity (line 129) and getEntities (line 156) |
| `packages/indexer/src/core/handlerHelpers.ts` | resolveEntity/resolveEntities using registry lookups, zero casts | ✓ VERIFIED | 4-param signatures, uses getEntityConstructor(), zero `as` casts |
| `packages/indexer/src/core/types/handler.ts` | EntityHandler with typed listensToBag and triggeredBy | ✓ VERIFIED | `listensToBag: (keyof EntityRegistry)[]` (line 55), `triggeredBy: keyof EntityRegistry` (line 96) |
| `packages/indexer/src/core/entityTypeMap.ts` | DELETED | ✓ VERIFIED | File does not exist |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| entityRegistry.ts | batchContext.ts (types) | `import type { EntityRegistry }` | ✓ WIRED | batchContext.ts:1 imports EntityRegistry |
| entityRegistry.ts | batchContext.ts (impl) | `ENTITY_CONSTRUCTORS` import | ✓ WIRED | batchContext.ts:1 imports ENTITY_CONSTRUCTORS |
| entityRegistry.ts | handlerHelpers.ts | `getEntityConstructor` import | ✓ WIRED | handlerHelpers.ts:16 imports both EntityRegistry and getEntityConstructor |
| entityRegistry.ts | types/handler.ts | `EntityRegistry` import | ✓ WIRED | handler.ts:2 imports EntityRegistry for listensToBag/triggeredBy types |
| handlers/*.handler.ts | batchContext.ts | `getEntities('BagKey')` calls | ✓ WIRED | Verified lsp4TokenName (1 call), nft (3 calls), follower (2 calls) — all use typed string literal keys |
| handlers/*.handler.ts | handlerHelpers.ts | `resolveEntity`/`resolveEntities` calls | ✓ WIRED | nft.handler.ts:97 calls resolveEntities with 4 params (no entityClass) |
| pipeline.ts | batchContext.ts | `getEntitiesUntyped()` calls | ✓ WIRED | 4 calls at lines 233, 345, 492, 547 |
| fkResolution.ts | batchContext.ts | `getEntitiesUntyped()` calls | ✓ WIRED | 3 calls at lines 219, 248, 269 |
| metadataFetch.ts | batchContext.ts | `getEntitiesUntyped()` + justified cast | ✓ WIRED | Line 221: one justified `as Map<string, TEntity>` cast (generic utility) |

### Requirements Coverage

No requirements mapped to this phase (pure refactor). No orphaned requirements found.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | No TODOs, FIXMEs, or placeholders found | — | — |

No anti-patterns detected in any modified core files or handler files.

### Human Verification Required

None required. All truths are verifiable via code inspection and automated build/test.

### Gaps Summary

No gaps found. Phase goal fully achieved:

- **EntityRegistry** is the single source of truth for all 71 bag keys (type-level + runtime)
- **Compile-time enforcement**: `addEntity<K>` and `getEntities<K>` use `keyof EntityRegistry` — typos and type mismatches are compile errors
- **Runtime validation**: `instanceof` checks on both write (addEntity) and read (getEntities)
- **Zero handler casts**: All handler files use `getEntities('Key')` which returns typed maps directly
- **Clean separation**: Typed methods for handlers, untyped methods for pipeline/fkResolution dynamic iteration
- **Full build and test suite pass**: 0 errors, 260/260 tests pass

---

_Verified: 2026-03-10T19:35:00Z_
_Verifier: Claude (gsd-verifier)_
