---
phase: 10.8-creators-subscription
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/react/src/subscriptions/creators.ts
  - packages/react/src/index.ts
  - apps/test/src/app/subscriptions/page.tsx
autonomous: true
requirements: [SUB-02, SUB-03]

must_haves:
  truths:
    - 'Developer can call useCreatorSubscription() and get { data, isConnected, isSubscribed, error }'
    - 'All type params inferred — zero explicit type arguments at call sites'
    - 'Creator subscription data appears in the playground page'
    - 'Cache invalidation works when invalidate: true'
  artifacts:
    - path: 'packages/react/src/subscriptions/creators.ts'
      provides: 'useCreatorSubscription hook'
      contains: 'useCreatorSubscription'
    - path: 'apps/test/src/app/subscriptions/page.tsx'
      provides: 'Creators subscription demo section'
      contains: 'useCreatorSubscription'
  key_links:
    - from: 'packages/react/src/subscriptions/creators.ts'
      to: '@lsp-indexer/node'
      via: 'buildCreatorWhere + creatorKeys + parseCreators + CreatorSubscriptionDocument'
      pattern: 'buildCreatorWhere|creatorKeys|parseCreators|CreatorSubscriptionDocument'
    - from: 'packages/react/src/subscriptions/creators.ts'
      to: 'packages/react/src/subscriptions/use-subscription.ts'
      via: 'useSubscription call'
      pattern: 'useSubscription'
---

<objective>
Create the `useCreatorSubscription` hook and add a working creators subscription demo to the playground page.

Purpose: Domain subscription hook for creators (LSP4). Follows the exact pattern established by profiles (10.2).

Output: `useCreatorSubscription` exported from `@lsp-indexer/react`, creators subscription demo visible in the test app.
</objective>

<execution_context>
@/home/coder/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-subscriptions/10-CONTEXT.md
@.planning/phases/10.1-subscription-foundation/10.1-01-SUMMARY.md
@.planning/phases/10.2-profiles-subscription/10.2-01-SUMMARY.md
@packages/node/src/services/creators.ts
@packages/node/src/keys/creators.ts
@packages/node/src/documents/creators.ts
@packages/node/src/parsers/creators.ts
@packages/react/src/index.ts
@apps/test/src/app/subscriptions/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useCreatorSubscription hook + playground demo + export</name>
  <files>
    packages/react/src/subscriptions/creators.ts
    packages/react/src/index.ts
    apps/test/src/app/subscriptions/page.tsx
  </files>
  <action>
    **Create `packages/react/src/subscriptions/creators.ts`:**

    Follow the exact pattern from the 10.2 profiles SUMMARY:
    - Import `Creator`, `CreatorFilter` from `@lsp-indexer/types`
    - Import `buildCreatorWhere`, `creatorKeys`, `parseCreators`, `CreatorSubscriptionDocument` from `@lsp-indexer/node`
    - `extract: (result) => result.lsp4_creator` — Hasura table extraction
    - `parser: (raw) => parseCreators(raw)`
    - `order_by: undefined` — entity domain, Hasura default sort
    - Params interface: `UseCreatorSubscriptionParams` with `{ filter?, limit?, enabled?, invalidate?, onData?, onReconnect? }`
    - `useQueryClient()` called unconditionally (Rules of Hooks), only passed when `invalidate: true`

    **Update `packages/react/src/index.ts`:**

    Add export:
    ```typescript
    export * from './subscriptions/creators';
    ```

    **Add creators demo to playground page:**

    Update `apps/test/src/app/subscriptions/page.tsx` — add a creators subscription section showing:
    1. Connection/subscription status badges
    2. Data count and list of creator addresses
    3. Error display
    4. Filter input if relevant
    5. Limit selector

    **Build validation:**
    ```bash
    pnpm build
    ```

    Commit: `feat(10.8-creators): add useCreatorSubscription hook + playground demo`

  </action>
  <verify>
    `pnpm build` — all packages + test app build.
    `grep "useCreatorSubscription" packages/react/dist/index.js` — exported.
    `grep "useCreatorSubscription" apps/test/src/app/subscriptions/page.tsx` — used in playground.
  </verify>
  <done>useCreatorSubscription hook exported from @lsp-indexer/react with full type inference. Creators subscription demo visible in test app playground at /subscriptions.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes for all packages + test app
- `useCreatorSubscription` is importable from `@lsp-indexer/react`
- Hook accepts { filter?, limit?, enabled?, invalidate?, onData?, onReconnect? }
- Hook returns { data: Creator[] | null, isConnected, isSubscribed, error: Error | null }
- All types inferred — no explicit type arguments
- Playground shows creators subscription demo
</verification>

<success_criteria>

1. useCreatorSubscription created with type-safe SubscriptionConfig (extract + parser, zero assertions)
2. Hook exported from @lsp-indexer/react
3. Creators subscription demo works in playground
4. All packages build with zero type errors
   </success_criteria>

<output>
After completion, create `.planning/phases/10.8-creators-subscription/10.8-01-SUMMARY.md`
</output>
