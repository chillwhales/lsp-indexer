---
phase: 04-integration-wiring
plan: 04
type: execute
wave: 4
depends_on: ['04-03']
files_modified:
  - packages/indexer-v2/test/fixtures/blocks/README.md
  - packages/indexer-v2/test/fixtures/blocks/transfer-lsp7.json
  - packages/indexer-v2/test/fixtures/blocks/transfer-lsp8.json
  - packages/indexer-v2/test/fixtures/blocks/multi-event.json
autonomous: true

must_haves:
  truths:
    - 'Real LUKSO block fixtures captured and stored as JSON'
    - 'Fixtures include critical path scenarios (transfers, mints)'
    - 'Fixtures include edge cases (zero-value, self-transfer, multi-event)'
    - 'Fixtures are deterministic and version-controlled'
  artifacts:
    - path: 'packages/indexer-v2/test/fixtures/blocks/README.md'
      provides: 'Documentation of fixture capture process and block selection'
      min_lines: 20
    - path: 'packages/indexer-v2/test/fixtures/blocks/transfer-lsp7.json'
      provides: 'LSP7 transfer event block fixture'
      contains: '"logs":'
    - path: 'packages/indexer-v2/test/fixtures/blocks/transfer-lsp8.json'
      provides: 'LSP8 transfer event block fixture'
      contains: '"logs":'
    - path: 'packages/indexer-v2/test/fixtures/blocks/multi-event.json'
      provides: 'Multi-event block fixture'
      contains: '"logs":'
  key_links: []
---

<objective>
Capture real LUKSO blockchain blocks as JSON fixtures for integration testing, covering critical path scenarios and edge cases.

Purpose: Create deterministic, version-controlled test fixtures that enable integration tests to verify all 6 pipeline steps without network dependency. Per 04-CONTEXT.md decisions: "Real LUKSO block data — Capture actual mainnet/testnet blocks, store as JSON fixtures in test/fixtures/blocks/". This satisfies INTG-03's requirement for real block fixtures.

Output: JSON fixtures capturing real LUKSO blocks with LSP7/LSP8 transfers and multi-event scenarios, ready for integration tests.
</objective>

<execution_context>
@/home/coder/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integration-wiring/04-CONTEXT.md
@.planning/phases/04-integration-wiring/04-03-SUMMARY.md
@packages/indexer-v2/src/plugins/events/lsp7Transfer.plugin.ts
@packages/indexer-v2/src/plugins/events/lsp8Transfer.plugin.ts
</context>

<tasks>

<task type="auto">
  <name>Create fixture directory structure and documentation</name>
  <files>packages/indexer-v2/test/fixtures/blocks/README.md</files>
  <action>
Create fixture directory structure and README documenting the fixture capture process.

**Directory structure:**

```
packages/indexer-v2/test/fixtures/blocks/
  README.md
  transfer-lsp7.json
  transfer-lsp8.json
  multi-event.json
```

**README.md content:**

# Block Fixtures for Integration Tests

Real LUKSO blockchain blocks captured as JSON for deterministic integration testing.

## Fixture Files

| File                 | Description                  | Block Height | Events           |
| -------------------- | ---------------------------- | ------------ | ---------------- |
| `transfer-lsp7.json` | LSP7 token transfer          | TBD          | LSP7.Transfer    |
| `transfer-lsp8.json` | LSP8 NFT transfer            | TBD          | LSP8.Transfer    |
| `multi-event.json`   | Multiple events in one block | TBD          | Mixed LSP events |

## Capture Process

Fixtures are captured using the Subsquid SDK's block format. Each fixture represents a single block with:

- Block header (height, hash, timestamp, parentHash)
- Logs array (address, topics, data, transactionHash, logIndex)
- Transactions (optional, included if needed for context)

## Selection Criteria

**Critical path (happy path):**

- LSP7 transfer: Standard token transfer between two addresses
- LSP8 transfer: Standard NFT transfer between two addresses

**Edge cases:**

- Multi-event: Block with multiple LSP events (DataChanged + Transfer, etc.)

## Adding New Fixtures

1. Identify target block height on LUKSO mainnet
2. Use Subsquid archive or RPC to fetch block data
3. Save as JSON with descriptive filename
4. Update this README with block height and event types
5. Add integration test in `test/integration/` that uses the fixture

## Notes

- Fixtures are version-controlled — no network dependency during tests
- Block format matches Subsquid's `Context.blocks` structure
- For privacy: Do NOT include blocks with sensitive data or PII

---

**Capture instructions:**

Use the Subsquid archive endpoint or LUKSO RPC to fetch blocks. Example using archive:

```bash
curl -X POST https://v2.archive.subsquid.io/network/lukso-mainnet \
  -H "Content-Type: application/json" \
  -d '{
    "type": "logs",
    "fromBlock": BLOCK_NUMBER,
    "toBlock": BLOCK_NUMBER,
    "logs": [{ "topic0": ["EVENT_TOPIC0"] }]
  }'
```

Then format as Subsquid block structure and save to this directory.

**End of README**

  </action>
  <verify>
Directory `packages/indexer-v2/test/fixtures/blocks/` exists with README.md explaining fixture purpose and capture process.
  </verify>
  <done>
Fixture directory created with comprehensive README documenting fixture files, capture process, and selection criteria.
  </done>
</task>

<task type="auto">
  <name>Capture real LUKSO block fixtures</name>
  <files>
    packages/indexer-v2/test/fixtures/blocks/transfer-lsp7.json
    packages/indexer-v2/test/fixtures/blocks/transfer-lsp8.json
    packages/indexer-v2/test/fixtures/blocks/multi-event.json
  </files>
  <action>
Capture real LUKSO blockchain blocks as JSON fixtures using the Subsquid archive or LUKSO RPC endpoint.

**Approach:**

1. **Identify target blocks on LUKSO mainnet:**

   - Query Subsquid archive for blocks containing LSP7.Transfer events
   - Query for blocks containing LSP8.Transfer events
   - Find a block with multiple LSP events for edge case coverage

2. **Fetch block data using LUKSO RPC:**
   Use `eth_getBlockByNumber` with full transaction details, then extract log data.

   Example RPC call (Node.js script approach):

   ```javascript
   const response = await fetch('https://rpc.lukso.gateway.fm', {
     method: 'POST',
     headers: { 'Content-Type': 'application/json' },
     body: JSON.stringify({
       jsonrpc: '2.0',
       method: 'eth_getBlockByNumber',
       params: ['0xHEX_BLOCK_NUMBER', true],
       id: 1,
     }),
   });
   ```

3. **Format as Subsquid block structure:**
   Match the structure expected by `Context.blocks` in pipeline:

   ```json
   {
     "header": {
       "height": NUMBER,
       "hash": "0x...",
       "timestamp": TIMESTAMP,
       "parentHash": "0x..."
     },
     "logs": [
       {
         "address": "0x...",
         "topics": ["0x...", "0x...", ...],
         "data": "0x...",
         "transactionHash": "0x...",
         "transactionIndex": NUMBER,
         "logIndex": NUMBER,
         "removed": false
       }
     ]
   }
   ```

4. **Specific fixtures to create:**

   **transfer-lsp7.json:**

   - Find a block with an LSP7 `Transfer` event using the correct LSP7 topic0 (see `LSP7DigitalAsset.events.Transfer.topic` in `packages/indexer-v2/src/plugins/events/lsp7Transfer.plugin.ts`; do not use the ERC-20 `0xddf252…` topic)
   - Example block: Search LUKSO explorer for recent LSP7 transfer
   - Capture block data and format as above
   - Update README.md with actual block height

   **transfer-lsp8.json:**

   - Find a block with LSP8.Transfer event
   - LSP8 Transfer has different event signature than LSP7
   - Capture block data and format as above
   - Update README.md with actual block height

   **multi-event.json:**

   - Find a block with multiple events (e.g., Transfer + DataChanged, or Mint + URIChanged)
   - Capture block data and format as above
   - Update README.md with actual block height and event list

5. **Validation:**
   - Each JSON file must be valid JSON (use `jq` to validate)
   - Block structure must match Subsquid's Context.blocks format
   - Logs array must include all required fields (address, topics, data)

**Alternative approach (if RPC access is limited):**

- Use Subsquid archive API to query specific block ranges with log filters
- Archive returns data in Subsquid-compatible format already
- Archive endpoint: `https://v2.archive.subsquid.io/network/lukso-mainnet`

**Practical execution:**
Create a temporary Node.js script `scripts/capture-fixtures.ts` that:

1. Connects to LUKSO RPC or archive
2. Queries for blocks with target events
3. Formats and writes JSON files to test/fixtures/blocks/
4. Updates README.md with block heights

Then run the script to generate fixtures, commit the JSON files, and delete the script.

**Important notes:**

- Use actual LUKSO mainnet blocks for authenticity
- Do NOT fabricate synthetic data — fixtures must be real blockchain data
- Ensure captured blocks include all log fields needed by EventPlugins (topics, data, address)
- Keep fixtures minimal (1-3 events per block) for test clarity
  </action>
  <verify>

1. Three JSON files exist in `test/fixtures/blocks/`
2. Each file is valid JSON (run `jq . < file.json`)
3. Each file has structure matching Subsquid block format (header + logs array)
4. README.md updated with actual block heights and event types
5. Logs in fixtures include required fields: address, topics, data, transactionHash, logIndex
   </verify>
   <done>
   Three JSON fixtures captured from real LUKSO mainnet blocks: transfer-lsp7.json (LSP7 transfer), transfer-lsp8.json (LSP8 transfer), multi-event.json (multiple events). README.md updated with block heights and event details. Fixtures are deterministic, version-controlled, and ready for integration tests.
   </done>
   </task>

</tasks>

<verification>
1. Directory `packages/indexer-v2/test/fixtures/blocks/` exists with README.md
2. Three JSON fixtures exist: transfer-lsp7.json, transfer-lsp8.json, multi-event.json
3. Each JSON file is valid JSON (validated with jq)
4. Fixtures match Subsquid block structure (header + logs)
5. README documents block heights, event types, and capture process
6. Fixtures contain real LUKSO blockchain data, not synthetic test data
</verification>

<success_criteria>
User can see:

- Fixture directory at `packages/indexer-v2/test/fixtures/blocks/` with comprehensive README
- Three JSON fixtures captured from real LUKSO mainnet blocks
- README documenting each fixture's block height, events, and capture process
- Valid JSON files with Subsquid-compatible block structure
- Deterministic, version-controlled fixtures ready for integration tests (no network dependency)
  </success_criteria>

<output>
After completion, create `.planning/phases/04-integration-wiring/04-04-SUMMARY.md`
</output>
