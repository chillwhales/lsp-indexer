---
phase: 04-integration-wiring
plan: 03
type: execute
wave: 3
depends_on: ['04-02']
files_modified:
  - packages/indexer-v2/src/app/index.ts
  - packages/indexer-v2/src/app/config.ts
autonomous: true

must_haves:
  truths:
    - 'Processor runs processBatch for each block batch'
    - 'processBatch receives registry, verifyAddresses, and workerPool'
    - 'All 6 pipeline steps execute in sequence per batch'
  artifacts:
    - path: 'packages/indexer-v2/src/app/config.ts'
      provides: 'PipelineConfig assembly with all dependencies'
      exports: ['createPipelineConfig']
      min_lines: 20
    - path: 'packages/indexer-v2/src/app/index.ts'
      provides: 'Full pipeline integration in processor.run()'
      contains: 'processBatch'
  key_links:
    - from: 'packages/indexer-v2/src/app/index.ts'
      to: 'packages/indexer-v2/src/core/pipeline.ts'
      via: 'processBatch import and call'
      pattern: 'processBatch'
    - from: 'packages/indexer-v2/src/app/config.ts'
      to: 'packages/indexer-v2/src/core/verification.ts'
      via: 'verifyAddresses function import'
      pattern: 'verifyAddresses'
    - from: 'packages/indexer-v2/src/app/config.ts'
      to: 'packages/indexer-v2/src/core/metadataWorkerPool.ts'
      via: 'MetadataWorkerPool instantiation'
      pattern: 'MetadataWorkerPool'
---

<objective>
Wire the processBatch pipeline into the processor's batch handler, connecting all EventPlugins and EntityHandlers to the 6-step enrichment queue flow.

Purpose: Complete the end-to-end integration so each block batch flows through EXTRACT → PERSIST RAW → HANDLE → PERSIST DERIVED → VERIFY → ENRICH. This satisfies the core of INTG-01 and INTG-02 by making the application actually process blocks through the full pipeline.

Output: Fully integrated application that processes real blockchain blocks through all 6 pipeline steps using discovered plugins and handlers.
</objective>

<execution_context>
@/home/coder/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integration-wiring/04-CONTEXT.md
@.planning/phases/04-integration-wiring/04-02-SUMMARY.md
@packages/indexer-v2/src/core/pipeline.ts
@packages/indexer-v2/src/core/verification.ts
@packages/indexer-v2/src/core/metadataWorkerPool.ts
@packages/indexer-v2/src/app/index.ts
@packages/indexer-v2/src/app/bootstrap.ts
</context>

<tasks>

<task type="auto">
  <name>Create pipeline configuration module</name>
  <files>packages/indexer-v2/src/app/config.ts</files>
  <action>
Create `packages/indexer-v2/src/app/config.ts` that assembles the `PipelineConfig` object required by processBatch.

**Function: `createPipelineConfig(registry: PluginRegistry): PipelineConfig`**

Per pipeline.ts lines 50-57, PipelineConfig requires:

- `registry: PluginRegistry` (passed in)
- `verifyAddresses: VerifyFn` (imported from verification module)
- `workerPool: IMetadataWorkerPool` (instantiate MetadataWorkerPool)

**Implementation:**

1. **Import dependencies:**

   - Import `PipelineConfig` type from `@/core/pipeline`
   - Import `PluginRegistry` from `@/core/registry`
   - Import `verifyAddresses` from `@/core/verification` (this is the V2 verification implementation)
   - Import `MetadataWorkerPool` from `@/core/metadataWorkerPool`

2. **Instantiate worker pool:**

   ```typescript
   // Read worker pool size from env var (default: 4)
   const poolSize = parseInt(process.env.METADATA_WORKER_POOL_SIZE || '4', 10);
   const workerPool = new MetadataWorkerPool(poolSize);
   ```

3. **Return config object:**
   ```typescript
   return {
     registry,
     verifyAddresses,
     workerPool,
   };
   ```

**Notes:**

- MetadataWorkerPool handles IPFS/HTTP fetching in worker threads (existing implementation)
- verifyAddresses is the batch verification function from verification.ts (existing implementation)
- Keep it simple — just assemble the config object, no complex logic
  </action>
  <verify>
  Build succeeds, config.ts exports createPipelineConfig function. Check that imports resolve correctly (verification and metadataWorkerPool modules exist).
  </verify>
  <done>
  Config module exists, exports createPipelineConfig() that assembles PipelineConfig with registry, verifyAddresses, and MetadataWorkerPool instance.
  </done>
  </task>

<task type="auto">
  <name>Wire processBatch into processor.run() handler</name>
  <files>packages/indexer-v2/src/app/index.ts</files>
  <action>
Update `packages/indexer-v2/src/app/index.ts` to call processBatch for each batch in the processor's handler.

**Changes:**

1. **Add imports:**

   - Import `processBatch` from `@/core/pipeline`
   - Import `createPipelineConfig` from `./config`

2. **Create pipeline config (after registry bootstrap, before processor.run()):**

   ```typescript
   // Create pipeline configuration
   const pipelineConfig = createPipelineConfig(registry);
   logger.info('Pipeline configuration created');
   ```

3. **Update processor.run() handler to call processBatch:**
   Replace the existing TODO/skeleton code with:

   ```typescript
   processor.run(new TypeormDatabase(), async (ctx) => {
     await processBatch(ctx, pipelineConfig);
   });
   ```

4. **Remove remaining TODO comments** related to pipeline wiring (now complete)

5. **Add final log before processor.run():**
   ```typescript
   logger.info('Starting processor — V2 indexer ready');
   ```

**Result:**

- Each batch of blocks from the processor is passed to processBatch
- processBatch executes all 6 steps: EXTRACT → PERSIST RAW → HANDLE → PERSIST DERIVED → VERIFY → ENRICH
- All EventPlugins and EntityHandlers discovered by registry are invoked as appropriate
- Structured logs from pipeline steps (created by createStepLogger in pipeline.ts) will appear during processing

**Important:**

- `ctx` passed to processBatch is the Subsquid `Context` object (has blocks, store, log, isHead)
- `pipelineConfig` provides registry, verification function, and worker pool
- Per pipeline.ts, errors propagate to Subsquid framework (no try/catch in processBatch)
  </action>
  <verify>

1. Build succeeds
2. Application structure is complete — entry point bootstraps registry, creates config, runs processor with processBatch
3. Code compiles with no TypeScript errors
4. (Optional) If test database available, can verify processor starts and attempts to process blocks (actual block processing verification happens in integration tests in Plan 04)
   </verify>
   <done>
   Main entry point creates pipeline config with createPipelineConfig(registry), processor.run() calls processBatch(ctx, pipelineConfig) for each batch. Full 6-step pipeline is wired and ready to process blocks.
   </done>
   </task>

</tasks>

<verification>
1. Build succeeds: `pnpm --filter=@chillwhales/indexer-v2 build`
2. Application structure is complete:
   - Entry point bootstraps registry
   - Creates pipeline config with registry, verifyAddresses, workerPool
   - Processor.run() calls processBatch for each batch
3. All imports resolve correctly (no missing modules)
4. Code compiles with no TypeScript errors
</verification>

<success_criteria>
User can see:

- Config module at `packages/indexer-v2/src/app/config.ts` assembling PipelineConfig
- Main entry point updated to create pipeline config and call processBatch in processor.run()
- Complete application flow: bootstrap → configure → process batches through 6-step pipeline
- Clean build with all dependencies wired correctly
- Application ready for integration testing with real block fixtures
  </success_criteria>

<output>
After completion, create `.planning/phases/04-integration-wiring/04-03-SUMMARY.md`
</output>
