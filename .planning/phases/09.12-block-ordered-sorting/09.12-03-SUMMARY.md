---
phase: 09.12-block-ordered-sorting
plan: 03
subsystem: ui, playground
tags: [sort-controls, playground, newest-oldest, conditional-hide, block-order, event-domains]

# Dependency graph
requires:
  - phase: 09.12-01
    provides: Updated sort field schemas with newest/oldest replacing timestamp/blockNumber
  - phase: 09.12-02
    provides: Default block-order sort in all 4 event domain services
provides:
  - SortControls with hideDirectionAndNulls conditional prop
  - All 4 event domain playgrounds with newest/oldest sort options
  - Default sort field 'newest' in all 4 event domain pages
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns: [hideDirectionAndNulls prop pattern for self-describing sort fields]

key-files:
  created: []
  modified:
    - apps/test/src/components/playground/sort-controls.tsx
    - apps/test/src/app/follows/page.tsx
    - apps/test/src/app/data-changed-events/page.tsx
    - apps/test/src/app/token-id-data-changed-events/page.tsx
    - apps/test/src/app/universal-receiver-events/page.tsx

key-decisions:
  - 'hideDirectionAndNulls is a simple boolean prop computed from sortField at usage site — keeps SortControls generic and reusable'
  - 'newest/oldest added at top of SORT_OPTIONS arrays — most common sort first for quick selection'

patterns-established:
  - 'hideDirectionAndNulls pattern: compute from sortField at SortControls usage site, not inside SortControls itself'

# Metrics
duration: 6min
completed: 2026-02-26
---

# Phase 9.12 Plan 03: Playground Sort Options + SortControls Conditional Hide/Show Summary

**All 4 event domain playgrounds updated with newest/oldest sort options, SortControls gains hideDirectionAndNulls prop for conditional Direction/Nulls visibility, full monorepo build passes**

## Performance

- **Duration:** 6 min
- **Started:** 2026-02-26T07:52:42Z
- **Completed:** 2026-02-26T07:59:05Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Added `hideDirectionAndNulls` optional boolean prop to SortControls component — hides Direction and Nulls dropdowns when true (used for self-describing sort fields like newest/oldest)
- Replaced `timestamp` and `blockNumber` sort options with `newest` (Newest First) and `oldest` (Oldest First) in all 4 event domain playground pages
- Changed default sort field from `'timestamp'` to `'newest'` in all 4 event domain pages
- Wired `hideDirectionAndNulls` to all 8 SortControls usages across 4 pages (2 per page: List + Infinite tabs)
- Full monorepo build passes (types, node, react, next, test app all compile)

## Task Commits

Each task was committed atomically:

1. **Task 1: Add conditional hide/show to SortControls component** - `160a53e` (feat)
2. **Task 2: Update all 4 playground pages with new sort options** - `7e46065` (feat)

## Files Created/Modified

- `apps/test/src/components/playground/sort-controls.tsx` — Added `hideDirectionAndNulls` prop, wrapped Direction and Nulls dropdowns with conditional rendering
- `apps/test/src/app/follows/page.tsx` — Replaced timestamp sort with newest/oldest, default to 'newest', added hideDirectionAndNulls to 2 SortControls
- `apps/test/src/app/data-changed-events/page.tsx` — Replaced timestamp/blockNumber sort with newest/oldest, default to 'newest', added hideDirectionAndNulls to 2 SortControls
- `apps/test/src/app/token-id-data-changed-events/page.tsx` — Replaced timestamp/blockNumber sort with newest/oldest, default to 'newest', added hideDirectionAndNulls to 2 SortControls
- `apps/test/src/app/universal-receiver-events/page.tsx` — Replaced timestamp/blockNumber sort with newest/oldest, default to 'newest', added hideDirectionAndNulls to 2 SortControls

## Decisions Made

- `hideDirectionAndNulls` computed at usage site (`state.sortField === 'newest' || state.sortField === 'oldest'`) rather than inside SortControls — keeps SortControls generic and reusable for non-event domains
- `newest`/`oldest` placed at top of SORT_OPTIONS arrays — most common sort first for quick selection in the playground UI

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 9.12 (Block-Ordered Sorting) is now complete — all 3 plans executed
- All event domains have deterministic block-order sorting (newest/oldest) throughout the full stack
- Ready for Phase 10 (Subscriptions) or Phase 11 (Server Actions & Publish Readiness)

## Self-Check: PASSED

---

_Phase: 09.12-block-ordered-sorting_
_Completed: 2026-02-26_
