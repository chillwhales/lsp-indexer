---
phase: 09.12-block-ordered-sorting
plan: 02
subsystem: services, documents, parsers
tags: [graphql, hasura, sorting, block-order, codegen, include-vars, event-domains]

# Dependency graph
requires:
  - phase: 09.12-01
    provides: buildBlockOrderSort utility, updated sort field schemas, follower block fields in types
provides:
  - Follower GraphQL document with 3 block field @include variables
  - Updated follower parser with proper typed codegen field access
  - Default block-order desc sort in all 4 event domain services
  - Follower include variables for blockNumber, transactionIndex, logIndex
affects: [09.12-03 (playground sort controls)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    [default sort fallback via ?? buildBlockOrderSort('desc'), per-service typed cast for order_by]

key-files:
  created: []
  modified:
    - packages/node/src/documents/followers.ts
    - packages/node/src/graphql/graphql.ts
    - packages/node/src/graphql/gql.ts
    - packages/node/src/parsers/followers.ts
    - packages/node/src/services/followers.ts
    - packages/node/src/services/data-changed-events.ts
    - packages/node/src/services/token-id-data-changed-events.ts
    - packages/node/src/services/universal-receiver-events.ts

key-decisions:
  - 'Default sort fallback uses ?? operator with typed cast: buildBlockOrderSort("desc") as X_Order_By[]'
  - 'Parser block fields use proper typed access after codegen (removed Record<string, unknown> workaround from Plan 01)'

patterns-established:
  - 'Default sort fallback pattern: buildXOrderBy(params.sort) ?? buildBlockOrderSort("desc") as X_Order_By[]'

# Metrics
duration: 4min
completed: 2026-02-26
---

# Phase 9.12 Plan 02: Follower Document/Parser + Service Default Sort Summary

**Follower GraphQL document with 3 block field @include directives, codegen regenerated, parser using typed access, all 4 event domain services defaulting to block-order desc when no sort specified**

## Performance

- **Duration:** 4 min
- **Started:** 2026-02-26T07:45:48Z
- **Completed:** 2026-02-26T07:49:53Z
- **Tasks:** 2
- **Files modified:** 8

## Accomplishments

- Updated `GetFollowersDocument` with 3 new `@include` variables (`includeBlockNumber`, `includeTransactionIndex`, `includeLogIndex`) and field selections (`block_number`, `transaction_index`, `log_index`)
- Ran codegen to regenerate `GetFollowersQuery`/`GetFollowersQueryVariables` types with new block fields
- Fixed follower parser to use proper typed codegen field access (replaced `Record<string, unknown>` cast workaround from Plan 01)
- Added `?? buildBlockOrderSort('desc')` default sort fallback to all 4 event domain `fetchX` implementations (followers, data-changed, token-id-data-changed, universal-receiver)
- Added 3 new include variables to `buildFollowerIncludeVars` (`includeBlockNumber`, `includeTransactionIndex`, `includeLogIndex`)
- Added 3 new disable lines to `fetchIsFollowing` for block field variables

## Task Commits

Each task was committed atomically:

1. **Task 1: Update Follower document + parser + codegen** - `dccb395` (feat)
2. **Task 2: Update all 4 service sort builders + include vars + default sort** - `89bd82a` (feat)

## Files Created/Modified

- `packages/node/src/documents/followers.ts` — Added 3 @include variables + field selections for block fields
- `packages/node/src/graphql/graphql.ts` — Regenerated codegen types with block fields in GetFollowersQuery
- `packages/node/src/graphql/gql.ts` — Regenerated codegen document string map
- `packages/node/src/parsers/followers.ts` — Fixed block field access to use proper typed codegen fields
- `packages/node/src/services/followers.ts` — Added default sort fallback, 3 new include vars, 3 fetchIsFollowing disables
- `packages/node/src/services/data-changed-events.ts` — Added default sort fallback
- `packages/node/src/services/token-id-data-changed-events.ts` — Added default sort fallback
- `packages/node/src/services/universal-receiver-events.ts` — Added default sort fallback

## Decisions Made

- Default sort fallback uses `??` operator with typed cast (`buildBlockOrderSort('desc') as X_Order_By[]`) — keeps `buildXOrderBy` return type as `undefined` for explicit "no sort requested" semantics, with the fallback providing deterministic block-order desc
- Parser block fields switched from `Record<string, unknown>` cast (Plan 01 workaround) to proper typed codegen field access after document update + codegen run

## Deviations from Plan

None — plan executed exactly as written. Plan 01 had already completed the sort builder cases, fetchLatest updates, and parser block fields as noted. This plan only needed the document/codegen update, parser typed access fix, default sort fallback, follower include vars, and fetchIsFollowing disables.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All 4 event domain services now default to block-order desc when no sort is specified
- Follower domain has full end-to-end block field support (document → codegen → parser → service include vars)
- Full monorepo build passes (types, node, react, next all compile)
- Playground pages still have TypeScript errors for sort field references — will be fixed in Plan 03

## Self-Check: PASSED

---

_Phase: 09.12-block-ordered-sorting_
_Completed: 2026-02-26_
