---
phase: 09.12-block-ordered-sorting
verified: 2026-02-26T08:10:00Z
status: passed
score: 10/10 must-haves verified
---

# Phase 9.12: Block-Ordered Sorting Verification Report

**Phase Goal:** All event domains sort by `blockNumber → transactionIndex → logIndex` instead of timestamp for deterministic ordering. Extract a shared sorting utility and apply across all 4 event domains.
**Verified:** 2026-02-26T08:10:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                    | Status     | Evidence                                                                                                                                                                                      |
| --- | ---------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `buildBlockOrderSort('desc')` returns 3-level Hasura order_by array in desc              | ✓ VERIFIED | `utils.ts:74-76` — returns `[{ block_number: direction }, { transaction_index: direction }, { log_index: direction }]`                                                                        |
| 2   | `buildBlockOrderSort('asc')` returns same structure in asc                               | ✓ VERIFIED | Same function, parameterized by direction                                                                                                                                                     |
| 3   | All 4 event domain sort field schemas include 'newest' and 'oldest'                      | ✓ VERIFIED | Confirmed in all 4 `*SortFieldSchema = z.enum([...])` — each starts with `'newest', 'oldest'`                                                                                                 |
| 4   | `timestamp` and `blockNumber` sort fields removed from all 4 schemas                     | ✓ VERIFIED | grep for `'timestamp'` in sort enums found zero matches; no `case 'timestamp'` or `case 'blockNumber'` in any of the 4 services                                                               |
| 5   | Follower schema includes blockNumber, transactionIndex, logIndex fields                  | ✓ VERIFIED | `followers.ts:34-38` — `blockNumber: z.number().nullable()`, `transactionIndex: z.number().nullable()`, `logIndex: z.number().nullable()`                                                     |
| 6   | Follower include schema has blockNumber, transactionIndex, logIndex toggles              | ✓ VERIFIED | `followers.ts:139-142` — all 3 boolean optional toggles present                                                                                                                               |
| 7   | All 4 services handle 'newest'/'oldest' via buildBlockOrderSort                          | ✓ VERIFIED | Each service's `buildXOrderBy` switch has `case 'newest': return buildBlockOrderSort('desc')` and `case 'oldest': return buildBlockOrderSort('asc')`                                          |
| 8   | All 4 services apply `buildBlockOrderSort('desc')` as default when no sort specified     | ✓ VERIFIED | Each `fetchX` impl uses `?? (buildBlockOrderSort('desc') as X_Order_By[])` fallback                                                                                                           |
| 9   | `fetchLatestDataChangedEvent` and `fetchLatestTokenIdDataChangedEvent` use 'newest' sort | ✓ VERIFIED | Both use `sort: { field: 'newest', direction: 'desc' }` in both branches                                                                                                                      |
| 10  | Playground sort controls hide Direction/Nulls when newest/oldest selected                | ✓ VERIFIED | `sort-controls.tsx:72,87` — `{!hideDirectionAndNulls && ...}` gates both dropdowns; all 4 pages pass `hideDirectionAndNulls={state.sortField === 'newest' \|\| state.sortField === 'oldest'}` |

**Score:** 10/10 truths verified

### Required Artifacts

| Artifact                                                     | Expected                                              | Status     | Details                                                                                                                                                         |
| ------------------------------------------------------------ | ----------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/node/src/services/utils.ts`                        | buildBlockOrderSort utility                           | ✓ VERIFIED | 76 lines, exported function, uses `Order_By` type, returns 3-element array                                                                                      |
| `packages/types/src/followers.ts`                            | Sort enum with newest/oldest + block fields           | ✓ VERIFIED | 279 lines, `FollowerSortFieldSchema` has newest/oldest, schema has blockNumber/transactionIndex/logIndex                                                        |
| `packages/types/src/data-changed-events.ts`                  | Sort enum with newest/oldest                          | ✓ VERIFIED | 281 lines, `DataChangedEventSortFieldSchema` has newest/oldest, no timestamp/blockNumber                                                                        |
| `packages/types/src/token-id-data-changed-events.ts`         | Sort enum with newest/oldest                          | ✓ VERIFIED | 295 lines, `TokenIdDataChangedEventSortFieldSchema` has newest/oldest, no timestamp/blockNumber                                                                 |
| `packages/types/src/universal-receiver-events.ts`            | Sort enum with newest/oldest                          | ✓ VERIFIED | 304 lines, `UniversalReceiverEventSortFieldSchema` has newest/oldest, no timestamp/blockNumber                                                                  |
| `packages/node/src/documents/followers.ts`                   | GraphQL document with block field @include directives | ✓ VERIFIED | Lines 31-33: `$includeBlockNumber`, `$includeTransactionIndex`, `$includeLogIndex`; Lines 60-62: field selections with `@include`                               |
| `packages/node/src/parsers/followers.ts`                     | Parser maps block_number → blockNumber etc.           | ✓ VERIFIED | Lines 59-61: `blockNumber: raw.block_number ?? null`, `transactionIndex: raw.transaction_index ?? null`, `logIndex: raw.log_index ?? null`                      |
| `packages/node/src/services/followers.ts`                    | Sort builder + include vars + default sort            | ✓ VERIFIED | 374 lines, imports buildBlockOrderSort, has newest/oldest cases, default `?? buildBlockOrderSort('desc')`, includeBlockNumber/TransactionIndex/LogIndex in vars |
| `packages/node/src/services/data-changed-events.ts`          | Sort builder with newest/oldest + default             | ✓ VERIFIED | 376 lines, imports buildBlockOrderSort, has newest/oldest cases, default fallback, fetchLatest uses 'newest'                                                    |
| `packages/node/src/services/token-id-data-changed-events.ts` | Sort builder with newest/oldest + default             | ✓ VERIFIED | 397 lines, imports buildBlockOrderSort, has newest/oldest cases, default fallback, fetchLatest uses 'newest'                                                    |
| `packages/node/src/services/universal-receiver-events.ts`    | Sort builder with newest/oldest + default             | ✓ VERIFIED | 362 lines, imports buildBlockOrderSort, has newest/oldest cases, default fallback                                                                               |
| `apps/test/src/components/playground/sort-controls.tsx`      | hideDirectionAndNulls prop                            | ✓ VERIFIED | 118 lines, prop in interface + destructured + used to gate Direction/Nulls                                                                                      |
| `apps/test/src/app/follows/page.tsx`                         | SORT_OPTIONS with newest/oldest, default 'newest'     | ✓ VERIFIED | SORT_OPTIONS has newest/oldest at top, `useState('newest')`, hideDirectionAndNulls on both SortControls                                                         |
| `apps/test/src/app/data-changed-events/page.tsx`             | SORT_OPTIONS with newest/oldest, default 'newest'     | ✓ VERIFIED | SORT_OPTIONS has newest/oldest, `useState('newest')`, hideDirectionAndNulls on both SortControls                                                                |
| `apps/test/src/app/token-id-data-changed-events/page.tsx`    | SORT_OPTIONS with newest/oldest, default 'newest'     | ✓ VERIFIED | SORT_OPTIONS has newest/oldest, `useState('newest')`, hideDirectionAndNulls on both SortControls                                                                |
| `apps/test/src/app/universal-receiver-events/page.tsx`       | SORT_OPTIONS with newest/oldest, default 'newest'     | ✓ VERIFIED | SORT_OPTIONS has newest/oldest, `useState('newest')`, hideDirectionAndNulls on both SortControls                                                                |

### Key Link Verification

| From                                      | To                     | Via                                           | Status  | Details                                                                                                                                                     |
| ----------------------------------------- | ---------------------- | --------------------------------------------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| followers.ts (service)                    | utils.ts               | `import buildBlockOrderSort`                  | ✓ WIRED | Line 16: imported, used in buildFollowerOrderBy (line 117-119) and fetchFollows default (line 270)                                                          |
| data-changed-events.ts (service)          | utils.ts               | `import buildBlockOrderSort`                  | ✓ WIRED | Line 17: imported, used in buildDataChangedEventOrderBy (line 149-152) and fetchDataChangedEvents default (line 309-310)                                    |
| token-id-data-changed-events.ts (service) | utils.ts               | `import buildBlockOrderSort`                  | ✓ WIRED | Line 20: imported, used in buildTokenIdDataChangedEventOrderBy (line 167-170) and fetch default (line 325-326)                                              |
| universal-receiver-events.ts (service)    | utils.ts               | `import buildBlockOrderSort`                  | ✓ WIRED | Line 16: imported, used in buildUniversalReceiverEventOrderBy (line 154-157) and fetch default (line 337-338)                                               |
| followers.ts (service)                    | documents/followers.ts | uses updated document with block include vars | ✓ WIRED | `includeBlockNumber`, `includeTransactionIndex`, `includeLogIndex` in buildFollowerIncludeVars (lines 170-172) and fetchIsFollowing disable (lines 348-350) |
| follows/page.tsx                          | sort-controls.tsx      | SortControls with hideDirectionAndNulls prop  | ✓ WIRED | Line 283, 346: `hideDirectionAndNulls={state.sortField === 'newest' \|\| state.sortField === 'oldest'}`                                                     |
| data-changed-events/page.tsx              | sort-controls.tsx      | SortControls with hideDirectionAndNulls prop  | ✓ WIRED | Line 391, 449: same pattern                                                                                                                                 |
| token-id-data-changed-events/page.tsx     | sort-controls.tsx      | SortControls with hideDirectionAndNulls prop  | ✓ WIRED | Line 409, 469: same pattern                                                                                                                                 |
| universal-receiver-events/page.tsx        | sort-controls.tsx      | SortControls with hideDirectionAndNulls prop  | ✓ WIRED | Line 313, 371: same pattern                                                                                                                                 |

### Requirements Coverage

No requirements in REQUIREMENTS.md are mapped to phase 9.12. Phase scope is defined in ROADMAP.md only.

### Anti-Patterns Found

| File | Line | Pattern    | Severity | Impact |
| ---- | ---- | ---------- | -------- | ------ |
| —    | —    | None found | —        | —      |

No TODO/FIXME/placeholder/stub patterns found in any of the 16 affected files.

### Human Verification Required

### 1. Playground Direction/Nulls Visibility Toggle

**Test:** Open any event domain playground (e.g., /follows), select "Newest First" sort → verify Direction and Nulls dropdowns are hidden. Then select "Follower Name" sort → verify Direction and Nulls dropdowns reappear.
**Expected:** Direction/Nulls controls hidden for newest/oldest, visible for name-based sorts.
**Why human:** CSS visibility is conditional on React state — needs browser to confirm rendering.

### 2. Sort Order Correctness

**Test:** Open any event domain playground, confirm results are ordered newest-first by default. Switch to "Oldest First" and confirm order reverses.
**Expected:** Events ordered by block_number desc → transaction_index desc → log_index desc for newest, asc for oldest.
**Why human:** Requires live Hasura data to confirm the 3-level sort produces correct ordering, especially for events sharing the same block.

### Gaps Summary

No gaps found. All 10 must-haves verified across all 3 levels (exists, substantive, wired):

1. **Shared utility** — `buildBlockOrderSort()` exported from `utils.ts`, returns correct 3-element Hasura order_by array
2. **Type schemas** — All 4 event domain sort field enums updated to `newest`/`oldest`, `timestamp`/`blockNumber` removed. Follower schema has block-level fields and include toggles
3. **Service layer** — All 4 services import and use `buildBlockOrderSort`, handle newest/oldest in switch statements, default to block-order desc when no sort specified. `fetchLatest*` functions use 'newest' sort
4. **Follower document + parser** — GraphQL document queries 3 new block fields with @include directives; parser maps snake_case → camelCase; service passes include variables
5. **Playground UI** — All 4 event domain pages have Newest First/Oldest First options, default to 'newest', pass `hideDirectionAndNulls` to SortControls. SortControls conditionally hides Direction/Nulls dropdowns

---

_Verified: 2026-02-26T08:10:00Z_
_Verifier: Claude (gsd-verifier)_
