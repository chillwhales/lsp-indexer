---
phase: 07-package-foundation
plan: 02
type: execute
wave: 2
depends_on: ['07-01']
files_modified:
  - pnpm-workspace.yaml
  - apps/test/package.json
  - apps/test/tsconfig.json
  - apps/test/next.config.ts
  - apps/test/.env.local.example
  - apps/test/src/app/layout.tsx
  - apps/test/src/app/page.tsx
  - apps/test/src/app/providers.tsx
  - apps/test/src/components/nav.tsx
  - apps/test/src/components/connection-status.tsx
autonomous: true

must_haves:
  truths:
    - 'Developer can run `next build` in apps/test with zero errors'
    - 'Developer can see imports from @lsp-indexer/react working in a client component'
    - 'Developer can see imports from @lsp-indexer/react/server working in a server component'
    - 'Developer can see type imports from @lsp-indexer/react/types resolving correctly'
    - 'Developer can see a landing page with navigation links for future domain playgrounds'
    - 'Developer can see connection status showing env var availability'
  artifacts:
    - path: 'pnpm-workspace.yaml'
      provides: 'Workspace config including apps/*'
      contains: 'apps/*'
    - path: 'apps/test/package.json'
      provides: 'Next.js test app package manifest'
      contains: '@lsp-indexer/react'
    - path: 'apps/test/next.config.ts'
      provides: 'Next.js config with transpilePackages'
      contains: 'transpilePackages'
    - path: 'apps/test/src/app/layout.tsx'
      provides: 'Root layout with providers wrapper'
      contains: 'Providers'
    - path: 'apps/test/src/app/providers.tsx'
      provides: 'Client-side QueryClientProvider wrapper'
      contains: 'QueryClientProvider'
    - path: 'apps/test/src/app/page.tsx'
      provides: 'Landing page with connection status and navigation'
      min_lines: 20
    - path: 'apps/test/.env.local.example'
      provides: 'Example env vars for developer setup'
      contains: 'NEXT_PUBLIC_INDEXER_URL'
  key_links:
    - from: 'apps/test/src/app/page.tsx'
      to: '@lsp-indexer/react'
      via: "import { IndexerError } from '@lsp-indexer/react'"
      pattern: 'from.*@lsp-indexer/react'
    - from: 'apps/test/src/app/page.tsx'
      to: '@lsp-indexer/react/server'
      via: 'import from server entry in server component'
      pattern: 'from.*@lsp-indexer/react/server'
    - from: 'apps/test/src/app/page.tsx'
      to: '@lsp-indexer/react/types'
      via: 'import type from types entry'
      pattern: 'from.*@lsp-indexer/react/types'
    - from: 'apps/test/src/app/layout.tsx'
      to: 'apps/test/src/app/providers.tsx'
      via: 'Providers wrapper around children'
      pattern: 'Providers'
    - from: 'apps/test/src/app/providers.tsx'
      to: '@tanstack/react-query'
      via: 'QueryClientProvider'
      pattern: 'QueryClientProvider'
    - from: 'pnpm-workspace.yaml'
      to: 'apps/*'
      via: 'workspace packages glob'
      pattern: "apps/\\*"
---

<objective>
Create a minimal Next.js test app (`apps/test`) that validates all 3 package entry points work correctly in a real framework context — catching bundle contamination, export resolution issues, and "use client" problems that unit tests cannot detect.

Purpose: The test app is the final validation layer for Phase 7. If `next build` succeeds with imports from all 3 entry points, the package foundation is solid. This app also serves as the dev playground for testing hooks in Phase 8+.

Output: A working Next.js 15 app that imports from `@lsp-indexer/react`, `@lsp-indexer/react/server`, and `@lsp-indexer/react/types` — validated by a successful `next build`.
</objective>

<execution_context>
@/home/coder/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-package-foundation/07-CONTEXT.md
@.planning/phases/07-package-foundation/07-RESEARCH.md
@.planning/phases/07-package-foundation/07-01-SUMMARY.md

Key codebase references:
@pnpm-workspace.yaml
@eslint.config.ts
@packages/react/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update workspace config, create Next.js test app shell</name>
  <files>
    pnpm-workspace.yaml
    apps/test/package.json
    apps/test/tsconfig.json
    apps/test/next.config.ts
    apps/test/.env.local.example
    apps/test/.gitignore
    apps/test/src/app/layout.tsx
    apps/test/src/app/providers.tsx
    apps/test/src/app/page.tsx
    apps/test/src/components/nav.tsx
  </files>
  <action>
    **pnpm-workspace.yaml** — Update to include apps:
    ```yaml
    packages:
      - 'packages/*'
      - 'apps/*'
    ```

    **Create `apps/test/` directory structure:**
    ```
    apps/test/
    ├── package.json
    ├── tsconfig.json
    ├── next.config.ts
    ├── .env.local.example
    ├── .gitignore
    └── src/
        ├── app/
        │   ├── layout.tsx
        │   ├── page.tsx
        │   └── providers.tsx
        └── components/
            └── nav.tsx
    ```

    **apps/test/package.json** — Create with:
    - `"name": "test"`, `"version": "0.1.0"`, `"private": true`
    - `"scripts"`: `"dev": "next dev --turbopack"`, `"build": "next build"`, `"start": "next start"`
    - `"dependencies"`: `next ^15.0.0`, `react ^19.0.0`, `react-dom ^19.0.0`, `@tanstack/react-query ^5.0.0`, `@lsp-indexer/react workspace:*`
    - `"devDependencies"`: `typescript ^5.9.2`, `@types/react ^19.0.0`, `@types/react-dom ^19.0.0`

    **apps/test/tsconfig.json** — Create for Next.js App Router:
    ```json
    {
      "compilerOptions": {
        "target": "ES2020",
        "lib": ["dom", "dom.iterable", "esnext"],
        "allowJs": true,
        "skipLibCheck": true,
        "strict": true,
        "noEmit": true,
        "esModuleInterop": true,
        "module": "esnext",
        "moduleResolution": "bundler",
        "resolveJsonModule": true,
        "isolatedModules": true,
        "jsx": "preserve",
        "incremental": true,
        "plugins": [{ "name": "next" }],
        "paths": { "@/*": ["./src/*"] }
      },
      "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
      "exclude": ["node_modules"]
    }
    ```

    **apps/test/next.config.ts** — Create:
    ```typescript
    import type { NextConfig } from 'next';

    const nextConfig: NextConfig = {
      transpilePackages: ['@lsp-indexer/react'],
    };

    export default nextConfig;
    ```

    **apps/test/.env.local.example** — Create with all env vars documented:
    ```bash
    # Required: Public Hasura GraphQL HTTP endpoint (exposed to browser)
    NEXT_PUBLIC_INDEXER_URL=https://your-hasura-instance.com/v1/graphql

    # Required: Private Hasura GraphQL HTTP endpoint (server-side only)
    INDEXER_URL=https://your-hasura-instance.com/v1/graphql

    # Optional: Public WebSocket endpoint (for subscriptions - Phase 10)
    # NEXT_PUBLIC_INDEXER_WS_URL=wss://your-hasura-instance.com/v1/graphql

    # Optional: Private WebSocket endpoint (server-side subscriptions - Phase 10)
    # INDEXER_WS_URL=wss://your-hasura-instance.com/v1/graphql
    ```

    **apps/test/.gitignore** — Create with Next.js defaults:
    ```
    .next/
    out/
    node_modules/
    .env.local
    ```

    **apps/test/src/app/providers.tsx** — Create client-side QueryClientProvider wrapper:
    - `'use client';` directive at top
    - Create QueryClient with `useState(() => new QueryClient({ defaultOptions: { queries: { staleTime: 60_000 } } }))`
    - Wrap children in `<QueryClientProvider client={queryClient}>`
    - Per CONTEXT.md: consumer provides their own QueryClientProvider — this is exactly that pattern

    **apps/test/src/app/layout.tsx** — Create root layout:
    - Import `Providers` from `./providers`
    - Import `Nav` from `@/components/nav`
    - Standard HTML structure with metadata (title: "LSP Indexer React — Test App")
    - Wrap children in `<Providers>` component
    - Include `<Nav />` sidebar for domain navigation
    - Use a simple 2-column layout: sidebar nav + main content area

    **apps/test/src/app/page.tsx** — Create landing page (this is a SERVER component by default in Next.js App Router):
    - Import from `@lsp-indexer/react/server`: `import { getServerUrl } from '@lsp-indexer/react/server';`
    - Import type from `@lsp-indexer/react/types`: `import type { IndexerErrorCategory } from '@lsp-indexer/react/types';`
    - Display a heading: "LSP Indexer React — Dev Playground"
    - Display a "Package Status" section showing:
      - Entry point validation: "✓ @lsp-indexer/react — imported successfully"
      - Server entry validation: "✓ @lsp-indexer/react/server — imported successfully"
      - Types entry validation: "✓ @lsp-indexer/react/types — imported successfully"
    - Display an "Environment" section showing env var status (use try/catch around getServerUrl to detect if vars are set):
      - NEXT_PUBLIC_INDEXER_URL: set/not set
      - INDEXER_URL: set/not set
    - If `getServerUrl()` throws, catch the error and show a helpful message
    - NOTE: The page.tsx is a server component — importing from `@lsp-indexer/react/server` validates that the server entry works in RSC context
    - Also import `IndexerError` from `@lsp-indexer/react` to validate the main entry import (even though the main entry has "use client", this is fine — server components can reference client module exports for type checking; the actual client code runs on client)

    **apps/test/src/components/nav.tsx** — Create navigation sidebar:
    - Simple list of links to domain playgrounds (most are placeholder for future phases)
    - Links: Home (/), Profiles (/profiles — Phase 8), Assets (/assets — Phase 9), NFTs (/nfts — Phase 9), etc.
    - Mark future pages as "Coming Soon" with muted styling
    - Use standard Next.js `<Link>` component

    **After file creation:**
    1. Run `pnpm install` from repo root — this links the workspace deps including @lsp-indexer/react
    2. Verify `@lsp-indexer/react` resolves: `ls node_modules/@lsp-indexer/react` (should be symlink to packages/react)

  </action>
  <verify>
    1. `cat pnpm-workspace.yaml` shows both `packages/*` and `apps/*`
    2. `ls apps/test/src/app/` shows layout.tsx, page.tsx, providers.tsx
    3. `ls apps/test/src/components/` shows nav.tsx
    4. `ls -la node_modules/@lsp-indexer/react` shows symlink to packages/react (after pnpm install)
    5. `cat apps/test/.env.local.example` shows all 4 env vars documented
  </verify>
  <done>
    - pnpm-workspace.yaml includes apps/*
    - apps/test exists with Next.js 15 App Router structure
    - Providers wrapper creates consumer-owned QueryClientProvider (per CONTEXT.md decision)
    - Landing page imports from all 3 entry points
    - Navigation scaffold ready for future domain pages
    - .env.local.example documents all required env vars
  </done>
</task>

<task type="auto">
  <name>Task 2: Build verification — next build, import validation, bundle check</name>
  <files>
    apps/test/src/components/connection-status.tsx
  </files>
  <action>
    **apps/test/src/components/connection-status.tsx** — Create a CLIENT component that validates runtime imports:
    - `'use client';` directive
    - Import `{ IndexerError }` from `@lsp-indexer/react` (validates main entry works in client component)
    - Display the IndexerError class name to prove import works: `IndexerError.name` should be `'IndexerError'`
    - Use `useEffect` to check if `process.env.NEXT_PUBLIC_INDEXER_URL` is available at runtime
    - Display connection status: whether the public URL env var is configured

    **Update apps/test/src/app/page.tsx** — Add the `ConnectionStatus` client component:
    - Import `ConnectionStatus` from `@/components/connection-status`
    - Add it to the page below the server-side checks
    - This validates BOTH server and client imports work on the same page

    **Run build verification:**
    1. First, ensure packages/react is built: `pnpm --filter @lsp-indexer/react build`
    2. Run `pnpm --filter test build` (which runs `next build`)
    3. This is the critical test. If `next build` succeeds:
       - All 3 entry point imports resolve correctly
       - No bundle contamination (server code not leaking to client, client code not breaking server)
       - "use client" directive works correctly
       - TypeScript types resolve across workspace boundary
    4. If `next build` fails, diagnose and fix:
       - Import resolution errors: check `transpilePackages` in next.config.ts, check workspace linking
       - "use client" errors: check that client components have the directive, server components don't import React hooks
       - Type errors: check tsconfig paths and moduleResolution settings
       - If main entry "use client" banner causes issues in server component import: change the page.tsx server-side import to use only `@lsp-indexer/react/server` and move `@lsp-indexer/react` imports to the client component only

    **Verify bundle separation:**
    After successful build, check that the server bundle doesn't include client-only code:
    - `grep -r "use client" apps/test/.next/server/` should NOT show `@lsp-indexer/react` internals leaking
    - The Next.js build output should show page routes compiled without errors

    **Add root-level build-all script** (if not already present):
    Add to root package.json scripts: `"build:all": "pnpm --filter @lsp-indexer/react build && pnpm --filter test build"`

  </action>
  <verify>
    1. `pnpm --filter test build` exits 0 (next build succeeds)
    2. Build output shows all routes compiled: `/ (server)`, etc.
    3. `ls apps/test/.next/` confirms build output exists
    4. `cat apps/test/src/components/connection-status.tsx` imports from `@lsp-indexer/react` (client entry)
    5. `cat apps/test/src/app/page.tsx` imports from `@lsp-indexer/react/server` (server entry) and `@lsp-indexer/react/types` (types entry)
  </verify>
  <done>
    - `next build` succeeds with zero errors
    - Client component successfully imports from @lsp-indexer/react (main entry)
    - Server component successfully imports from @lsp-indexer/react/server (server entry)
    - Type imports from @lsp-indexer/react/types resolve correctly
    - No bundle contamination between client and server entries
    - Connection status component validates runtime import functionality
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full set of requirements covered by this plan:

1. **FOUND-04** (Per user decision — no provider, consumer-provided QueryClientProvider): The test app creates its own QueryClient and wraps in QueryClientProvider. No IndexerProvider exists. This pattern is demonstrated and validated.
2. **FOUND-06** (Entry points without bundle contamination): `next build` succeeds with imports from all 3 entry points in both client and server contexts.
3. **FOUND-07** (Next.js test app): apps/test exists, builds successfully, imports from @lsp-indexer/react, validates hooks work context in both client and server components.

Combined with Plan 01 verification:

- FOUND-01 ✓ (ESM+CJS+DTS with "use client")
- FOUND-02 ✓ (Codegen pipeline)
- FOUND-03 ✓ (Env var config)
- FOUND-04 ✓ (Consumer-provided QueryClient, no provider)
- FOUND-05 ✓ (IndexerError with categories)
- FOUND-06 ✓ (Entry points validated in real app)
- FOUND-07 ✓ (Next.js test app exists and builds)
  </verification>

<success_criteria>

- `pnpm --filter test build` succeeds (next build exits 0)
- Landing page imports from all 3 package entry points
- Client component imports from @lsp-indexer/react work
- Server component imports from @lsp-indexer/react/server work
- Type imports from @lsp-indexer/react/types resolve
- pnpm-workspace.yaml includes apps/\*
- .env.local.example documents all required env vars
- Navigation scaffold ready for Phase 8+ domain pages
  </success_criteria>

<output>
After completion, create `.planning/phases/07-package-foundation/07-02-SUMMARY.md`
</output>
