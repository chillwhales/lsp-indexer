---
phase: 20.3-grafana-dashboard-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/grafana/provisioning/dashboards/indexer-monitoring.json
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05]

must_haves:
  truths:
    - "Pipeline step latency panel shows per-step timing breakdown"
    - "Entity throughput panel shows entities persisted per batch by entity type"
    - "Verification health panel shows valid/invalid/new address counts"
    - "Metadata fetch progress panel shows backlog depth, fetch duration, success/failure"
    - "Batch processing time panel shows total elapsed per batch"
    - "All existing panels (block height, errors, logs, container metrics) still present and unchanged"
  artifacts:
    - path: "docker/grafana/provisioning/dashboards/indexer-monitoring.json"
      provides: "Complete Grafana dashboard with 2 new rows and 8 new panels across Pipeline Performance and Verification & Metadata"
      contains: ["Pipeline Performance", "Verification & Metadata"]
  key_links:
    - from: "indexer-monitoring.json pipeline panels"
      to: "Loki datasource"
      via: "LogQL queries using step, durationMs, entityType, count structured fields"
      pattern: "compose_service.*indexer.*json.*step"
    - from: "indexer-monitoring.json verification panels"
      to: "Loki datasource"
      via: "LogQL queries using validCount, invalidCount, newCount structured fields"
      pattern: "compose_service.*indexer.*json.*VERIFY"
    - from: "indexer-monitoring.json metadata panels"
      to: "Loki datasource"
      via: "LogQL queries using unfetchedCount, durationMs, entityKey structured fields"
      pattern: "compose_service.*indexer.*json.*unfetchedCount"
---

<objective>
Add 5 new panel groups to the Grafana dashboard that surface pipeline health, entity throughput, verification status, and metadata fetch progress using structured log data from Phases 20.1 and 20.2.

Purpose: Production operators can monitor indexer health at a glance — step latencies, entity flow, verification results, and metadata backlog — all from a single dashboard.
Output: Updated `indexer-monitoring.json` with new rows and panels.
</objective>

<execution_context>
@/home/coder/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.3-grafana-dashboard-redesign/20.3-CONTEXT.md
@.planning/phases/20.2-pipeline-instrumentation/20.2-01-SUMMARY.md

<interfaces>
<!-- Existing dashboard structure — executor should preserve all existing panels exactly. -->

From docker/grafana/provisioning/dashboards/indexer-monitoring.json:
- UID: "lsp-indexer-monitoring"
- Datasources: Loki (uid: "loki"), Prometheus (uid: "prometheus")
- Template vars: $service (Loki), $project (Prometheus), $log_lines (custom)
- Existing rows (y positions):
  - y=0:  "Overview" row (not collapsed)
  - y=1:  4 stat panels (h=4): Current Block, Blocks/min, Activity, Errors
  - y=5:  "Error Analysis" row (not collapsed)
  - y=6:  2 timeseries panels (h=8): Error Rate by Service, Errors by Pipeline Step
  - y=14: "Block Progress" row (not collapsed)
  - y=15: 2 timeseries panels (h=8): Block Height Over Time, Log Entries/sec
  - y=23: "Log Explorer" row (collapsed=true, contains logs panel)
  - y=24: "Container Metrics" row (collapsed=true, contains CPU/Memory/Network/Disk panels)
- All panels use Loki for log queries, Prometheus for container metrics
- Panel pattern: { type, title, datasource, targets[{refId, datasource, expr, legendFormat?}], fieldConfig, options, gridPos }

<!-- Structured log fields available (from Phases 20.1 + 20.2): -->
Pipeline step fields: step (string), durationMs (number), blockRange (string), blockNumber (number)
Entity fields: entityType (string), count (number), strategy (string)
Verification fields: category (string), validCount (number), invalidCount (number), newCount (number)
Metadata fields: entityKey (string), unfetchedCount (number), backlogCount (number), durationMs (number), totalProcessed (number), totalFailed (number)
Batch summary fields: step="BATCH_SUMMARY", totalDurationMs (number), totalEntities (number), stepTimings (object), blockCount (number)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Pipeline Performance row with step latency, batch time, and entity throughput panels</name>
  <files>docker/grafana/provisioning/dashboards/indexer-monitoring.json</files>
  <action>
Read the existing dashboard JSON. Insert a new **non-collapsed** row "Pipeline Performance" and 3 timeseries panels after the "Block Progress" section (after y=22). Shift the two collapsed rows ("Log Explorer" and "Container Metrics") down accordingly.

**New row:** "Pipeline Performance" at gridPos { h: 1, w: 24, x: 0, y: 23 }

**Panel 1: Pipeline Step Latency** (DASH-01) — timeseries at gridPos { h: 8, w: 8, x: 0, y: 24 }
- Datasource: Loki (uid: "loki")
- LogQL query:
  ```
  max by (step) (max_over_time({compose_service="indexer"} | json | step != "" | step != "BATCH_SUMMARY" | durationMs > 0 | unwrap durationMs [$__interval]))
  ```
- legendFormat: `{{step}}`
- Visualization: timeseries with stacking mode "normal", fillOpacity 30, drawStyle "bars"
- Color mode: palette-classic
- Unit: ms
- Tooltip: mode "multi", sort "desc"
- Legend: displayMode "table", placement "bottom", calcs ["mean", "max"]

**Panel 2: Batch Processing Time** (DASH-05) — timeseries at gridPos { h: 8, w: 8, x: 8, y: 24 }
- Datasource: Loki (uid: "loki")
- LogQL query:
  ```
  max(max_over_time({compose_service="indexer"} | json | step="BATCH_SUMMARY" | unwrap totalDurationMs [$__interval]))
  ```
- legendFormat: `Total Batch Time`
- Visualization: timeseries with drawStyle "line", lineInterpolation "smooth", fillOpacity 20
- Color: fixedColor "blue", mode "fixed"
- Unit: ms
- Thresholds: green (null), yellow (1000), red (5000) — to flag slow batches

**Panel 3: Entity Throughput** (DASH-02) — timeseries at gridPos { h: 8, w: 8, x: 16, y: 24 }
- Datasource: Loki (uid: "loki")
- LogQL query:
  ```
  sum by (entityType) (sum_over_time({compose_service="indexer"} | json | step="PERSIST_RAW" | count > 0 | unwrap count [$__interval]))
  ```
- legendFormat: `{{entityType}}`
- Visualization: timeseries with drawStyle "bars", stacking mode "normal", fillOpacity 50
- Color mode: palette-classic
- Unit: short (entity count)
- Legend: displayMode "table", placement "bottom", calcs ["sum"]

**Shift collapsed rows:** Move "Log Explorer" row from y=23 to y=32, and "Container Metrics" row from y=24 to y=33. Also update inner panel y positions accordingly (add 9 to each inner panel y value).

**CRITICAL:** Preserve ALL existing panels exactly as they are. Do not modify any existing panel's query, gridPos, options, or field config. Only change y positions on the two collapsed rows and their inner panels.
  </action>
  <verify>
    <automated>jq 'def check: (.panels | map(select(.title == "Pipeline Performance" or .title == "Pipeline Step Latency" or .title == "Batch Processing Time" or .title == "Entity Throughput")) | length == 4) and (.panels | map(select(.title == "Current Block" or .title == "Block Height Over Time" or .title == "Error Rate by Service")) | length == 3); check' docker/grafana/provisioning/dashboards/indexer-monitoring.json</automated>
  </verify>
  <done>3 new pipeline panels visible in dashboard JSON under "Pipeline Performance" row. All existing panels preserved. JSON is valid.</done>
</task>

<task type="auto">
  <name>Task 2: Add Verification and Metadata row with health stats and trend panels</name>
  <files>docker/grafana/provisioning/dashboards/indexer-monitoring.json</files>
  <action>
Read the dashboard JSON (as modified by Task 1). Insert a new **non-collapsed** row "Verification & Metadata" and 5 panels after the "Pipeline Performance" section. Shift the two collapsed rows down accordingly.

**New row:** "Verification & Metadata" at gridPos { h: 1, w: 24, x: 0, y: 32 }

**Panel 4: Verification Health** (DASH-03) — stat at gridPos { h: 4, w: 8, x: 0, y: 33 }
- Datasource: Loki (uid: "loki")
- Three targets:
  - Target A: `sum(sum_over_time({compose_service="indexer"} | json | step="VERIFY" | unwrap validCount [$__range]))` — legendFormat: "Valid"
  - Target B: `sum(sum_over_time({compose_service="indexer"} | json | step="VERIFY" | unwrap invalidCount [$__range]))` — legendFormat: "Invalid"
  - Target C: `sum(sum_over_time({compose_service="indexer"} | json | step="VERIFY" | unwrap newCount [$__range]))` — legendFormat: "New"
- Visualization: stat with colorMode "background", graphMode "area", textMode "value_and_name"
- Thresholds for Target B (Invalid): green at null, red at 1 — highlights any invalid addresses
- reduceOptions calcs: ["last"]

**Panel 5: Verification Trends** (DASH-03 continued) — timeseries at gridPos { h: 8, w: 12, x: 0, y: 37 }
- Datasource: Loki (uid: "loki")
- Three targets:
  - Target A: `sum(sum_over_time({compose_service="indexer"} | json | step="VERIFY" | unwrap validCount [$__interval]))` — legendFormat: "Valid"
  - Target B: `sum(sum_over_time({compose_service="indexer"} | json | step="VERIFY" | unwrap invalidCount [$__interval]))` — legendFormat: "Invalid"
  - Target C: `sum(sum_over_time({compose_service="indexer"} | json | step="VERIFY" | unwrap newCount [$__interval]))` — legendFormat: "New"
- Visualization: timeseries drawStyle "line", lineInterpolation "smooth", fillOpacity 10
- Color mode: palette-classic
- Overrides: "Invalid" series → fixedColor "red"; "Valid" series → fixedColor "green"; "New" series → fixedColor "blue"
- Legend: displayMode "list", placement "bottom", calcs ["sum"]

**Panel 6: Metadata Fetch Backlog** (DASH-04) — stat at gridPos { h: 4, w: 8, x: 8, y: 33 }
- Datasource: Loki (uid: "loki")
- LogQL query:
  ```
  sum by (entityKey) (max_over_time({compose_service="indexer"} | json | unfetchedCount > 0 | unwrap unfetchedCount [$__range]))
  ```
- legendFormat: `{{entityKey}}`
- Visualization: stat with colorMode "value", graphMode "area", textMode "value_and_name"
- Thresholds: green (null), yellow (100), red (500) — flags growing backlogs
- reduceOptions calcs: ["last"]

**Panel 7: Metadata Fetch Performance** (DASH-04 continued) — timeseries at gridPos { h: 8, w: 12, x: 12, y: 37 }
- Datasource: Loki (uid: "loki")
- Four targets:
  - Target A (fetch duration): `max by (entityKey) (max_over_time({compose_service="indexer"} | json | entityKey != "" | durationMs > 0 | unwrap durationMs [$__interval]))` — legendFormat: "{{entityKey}} duration"
  - Target B (backlog trend): `sum by (entityKey) (max_over_time({compose_service="indexer"} | json | unfetchedCount > 0 | unwrap unfetchedCount [$__interval]))` — legendFormat: "{{entityKey}} backlog"
  - Target C (success): `sum by (entityKey) (sum_over_time({compose_service="indexer"} | json | totalProcessed > 0 | unwrap totalProcessed [$__interval]))` — legendFormat: "{{entityKey}} processed"
  - Target D (failure): `sum by (entityKey) (sum_over_time({compose_service="indexer"} | json | totalFailed > 0 | unwrap totalFailed [$__interval]))` — legendFormat: "{{entityKey}} failed"
- Visualization: timeseries drawStyle "line", lineInterpolation "smooth", fillOpacity 10
- Color mode: palette-classic
- Overrides: "failed" series → fixedColor "red"
- Unit: ms (for Target A), override Targets B/C/D to unit "short"
- Legend: displayMode "table", placement "bottom", calcs ["mean", "last"]
- Y-axis: Use overrides to put backlog/processed/failed on right axis (axisPlacement: "right" for Targets B/C/D)

**Panel 8: Avg Batch Time** — stat at gridPos { h: 4, w: 8, x: 16, y: 33 }
- Datasource: Loki (uid: "loki")
- LogQL query:
  ```
  avg(avg_over_time({compose_service="indexer"} | json | step="BATCH_SUMMARY" | unwrap totalDurationMs [$__range]))
  ```
- legendFormat: `Avg Batch Time`
- Visualization: stat with colorMode "value", graphMode "area", textMode "auto"
- Unit: ms
- Thresholds: green (null), yellow (1000), red (5000)
- reduceOptions calcs: ["mean"]

**Shift collapsed rows:** Move "Log Explorer" row from y=32 to y=45, and "Container Metrics" row from y=33 to y=46. Update inner panel y positions accordingly.

**CRITICAL:** Preserve ALL existing panels including those added by Task 1. Only change y positions on collapsed rows.
  </action>
  <verify>
    <automated>jq 'def check: (.. | objects | select(.title? == "Verification Health" or .title? == "Verification Trends" or .title? == "Metadata Fetch Backlog" or .title? == "Metadata Fetch Performance" or .title? == "Avg Batch Time") | .title) as $t | ([$t] | length > 0) and ([.. | objects | select(.title? == "Current Block")] | length > 0); check' docker/grafana/provisioning/dashboards/indexer-monitoring.json</automated>
  </verify>
  <done>5 new verification/metadata panels plus avg batch time stat visible in dashboard JSON under "Verification & Metadata" row. All existing panels and Pipeline Performance panels preserved. Full JSON validates with jq.</done>
</task>

</tasks>

<verification>
1. JSON validity: `jq . docker/grafana/provisioning/dashboards/indexer-monitoring.json > /dev/null` exits 0
2. New panels exist: "Pipeline Step Latency", "Batch Processing Time", "Entity Throughput", "Verification Health", "Verification Trends", "Metadata Fetch Backlog", "Metadata Fetch Performance", "Avg Batch Time"
3. New rows exist: "Pipeline Performance", "Verification & Metadata"
4. Existing panels preserved: "Current Block", "Blocks/min", "Block Processing Activity", "Errors (1h)", "Error Rate by Service", "Indexer Errors by Pipeline Step", "Block Height Over Time", "Log Entries/sec", "Service Logs", "CPU Usage by Container", "Memory Usage by Container", "Network I/O", "Disk I/O"
5. No Loki/Prometheus datasource UIDs changed
6. All new panels use compose_service="indexer" label selector
</verification>

<success_criteria>
- Dashboard JSON contains 2 new rows and 8 new panels (3 pipeline + 5 verification/metadata)
- All 13 existing panels remain unchanged in queries and config
- JSON parses without errors
- All LogQL queries reference structured fields from Phases 20.1/20.2
- Requirements DASH-01 through DASH-05 all have corresponding panels
</success_criteria>

<output>
After completion, create `.planning/phases/20.3-grafana-dashboard-redesign/20.3-01-SUMMARY.md`
</output>
