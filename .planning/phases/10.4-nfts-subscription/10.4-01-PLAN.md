---
phase: 10.4-nfts-subscription
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/react/src/subscriptions/nfts.ts
  - packages/react/src/index.ts
  - apps/test/src/app/subscriptions/page.tsx
autonomous: true
requirements: [SUB-02, SUB-03]

must_haves:
  truths:
    - 'Developer can call useNftSubscription() and get { data, isConnected, isSubscribed, error }'
    - 'All type params inferred — zero explicit type arguments at call sites'
    - 'NFT subscription data appears in the playground page'
    - 'Cache invalidation works when invalidate: true'
  artifacts:
    - path: 'packages/react/src/subscriptions/nfts.ts'
      provides: 'useNftSubscription hook'
      contains: 'useNftSubscription'
    - path: 'apps/test/src/app/subscriptions/page.tsx'
      provides: 'NFTs subscription demo section'
      contains: 'useNftSubscription'
  key_links:
    - from: 'packages/react/src/subscriptions/nfts.ts'
      to: '@lsp-indexer/node'
      via: 'buildNftWhere + nftKeys + parseNfts + NftSubscriptionDocument'
      pattern: 'buildNftWhere|nftKeys|parseNfts|NftSubscriptionDocument'
    - from: 'packages/react/src/subscriptions/nfts.ts'
      to: 'packages/react/src/subscriptions/use-subscription.ts'
      via: 'useSubscription call'
      pattern: 'useSubscription'
---

<objective>
Create the `useNftSubscription` hook and add a working NFTs subscription demo to the playground page.

Purpose: Domain subscription hook for NFTs. Follows the exact pattern established by profiles (10.2).

Output: `useNftSubscription` exported from `@lsp-indexer/react`, NFTs subscription demo visible in the test app.
</objective>

<execution_context>
@/home/coder/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-subscriptions/10-CONTEXT.md
@.planning/phases/10.1-subscription-foundation/10.1-01-SUMMARY.md
@.planning/phases/10.2-profiles-subscription/10.2-01-SUMMARY.md
@packages/node/src/services/nfts.ts
@packages/node/src/keys/nfts.ts
@packages/node/src/documents/nfts.ts
@packages/node/src/parsers/nfts.ts
@packages/react/src/index.ts
@apps/test/src/app/subscriptions/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useNftSubscription hook + playground demo + export</name>
  <files>
    packages/react/src/subscriptions/nfts.ts
    packages/react/src/index.ts
    apps/test/src/app/subscriptions/page.tsx
  </files>
  <action>
    **Create `packages/react/src/subscriptions/nfts.ts`:**

    Follow the exact pattern from the 10.2 profiles SUMMARY:
    - Import `Nft`, `NftFilter` from `@lsp-indexer/types`
    - Import `buildNftWhere`, `nftKeys`, `parseNfts`, `NftSubscriptionDocument` from `@lsp-indexer/node`
    - `extract: (result) => result.nft` — Hasura table extraction
    - `parser: (raw) => parseNfts(raw)`
    - `order_by: undefined` — entity domain, Hasura default sort
    - Params interface: `UseNftSubscriptionParams` with `{ filter?, limit?, enabled?, invalidate?, onData?, onReconnect? }`
    - `useQueryClient()` called unconditionally (Rules of Hooks), only passed when `invalidate: true`

    **Update `packages/react/src/index.ts`:**

    Add export:
    ```typescript
    export * from './subscriptions/nfts';
    ```

    **Add NFTs demo to playground page:**

    Update `apps/test/src/app/subscriptions/page.tsx` — add an NFTs subscription section showing:
    1. Connection/subscription status badges
    2. Data count and list of NFT names/token IDs
    3. Error display
    4. Filter input if relevant
    5. Limit selector

    **Build validation:**
    ```bash
    pnpm build
    ```

    Commit: `feat(10.4-nfts): add useNftSubscription hook + playground demo`

  </action>
  <verify>
    `pnpm build` — all packages + test app build.
    `grep "useNftSubscription" packages/react/dist/index.js` — exported.
    `grep "useNftSubscription" apps/test/src/app/subscriptions/page.tsx` — used in playground.
  </verify>
  <done>useNftSubscription hook exported from @lsp-indexer/react with full type inference. NFTs subscription demo visible in test app playground at /subscriptions.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes for all packages + test app
- `useNftSubscription` is importable from `@lsp-indexer/react`
- Hook accepts { filter?, limit?, enabled?, invalidate?, onData?, onReconnect? }
- Hook returns { data: Nft[] | null, isConnected, isSubscribed, error: Error | null }
- All types inferred — no explicit type arguments
- Playground shows NFTs subscription demo
</verification>

<success_criteria>

1. useNftSubscription created with type-safe SubscriptionConfig (extract + parser, zero assertions)
2. Hook exported from @lsp-indexer/react
3. NFTs subscription demo works in playground
4. All packages build with zero type errors
   </success_criteria>

<output>
After completion, create `.planning/phases/10.4-nfts-subscription/10.4-01-SUMMARY.md`
</output>
