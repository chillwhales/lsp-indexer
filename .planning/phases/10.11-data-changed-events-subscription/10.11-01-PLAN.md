---
phase: 10.11-data-changed-events-subscription
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/node/src/documents/data-changed-events.ts
  - packages/node/src/services/data-changed-events.ts
  - packages/node/src/graphql/graphql.ts
  - packages/node/src/graphql/gql.ts
  - packages/react/src/hooks/factories/data-changed-events/create-use-data-changed-event-subscription.ts
  - packages/react/src/hooks/factories/data-changed-events/index.ts
  - packages/react/src/hooks/factories/index.ts
  - packages/react/src/hooks/data-changed-events/queries.ts
  - packages/react/src/hooks/data-changed-events/use-data-changed-event-subscription.ts
  - packages/react/src/hooks/data-changed-events/index.ts
  - packages/react/src/hooks/types/data-changed-events.ts
  - packages/react/src/hooks/types/index.ts
  - packages/next/src/hooks/data-changed-events/queries.ts
  - packages/next/src/hooks/data-changed-events/use-data-changed-event-subscription.ts
  - packages/next/src/hooks/data-changed-events/index.ts
  - apps/test/src/app/data-changed-events/page.tsx
autonomous: true
requirements: [SUB-02, SUB-03]
gap_closure: true

must_haves:
  truths:
    - 'Developer can call useDataChangedEventSubscription() from @lsp-indexer/react and get { data, isConnected, isSubscribed, error }'
    - 'Developer can call useDataChangedEventSubscription() from @lsp-indexer/next and get identical behavior'
    - 'Include parameter narrows return type via 3-overload pattern (DataChangedEventResult<I>, DataChangedEvent, PartialDataChangedEvent)'
    - 'Results default to block-order desc sorting (block_number desc, transaction_index desc, log_index desc)'
    - 'Developer can pass sort param to override default event-domain sort'
    - 'Cache invalidation works when invalidate: true (invalidates dataChangedEventKeys.all)'
    - 'Subscription tab in /data-changed-events playground shows live data with filter/sort/include controls'
  artifacts:
    - path: 'packages/node/src/documents/data-changed-events.ts'
      provides: 'DataChangedEventSubscriptionDocument'
      contains: 'DataChangedEventSubscriptionDocument'
    - path: 'packages/node/src/services/data-changed-events.ts'
      provides: 'buildDataChangedEventSubscriptionConfig + exported buildDataChangedEventOrderBy'
      contains: 'buildDataChangedEventSubscriptionConfig'
    - path: 'packages/react/src/hooks/factories/data-changed-events/create-use-data-changed-event-subscription.ts'
      provides: 'Subscription factory with 3-overload type narrowing'
      contains: 'createUseDataChangedEventSubscription'
    - path: 'packages/react/src/hooks/data-changed-events/use-data-changed-event-subscription.ts'
      provides: 'React thin wrapper (3 lines + imports)'
      contains: 'useDataChangedEventSubscription'
    - path: 'packages/next/src/hooks/data-changed-events/use-data-changed-event-subscription.ts'
      provides: "Next.js thin wrapper with 'use client'"
      contains: 'useDataChangedEventSubscription'
    - path: 'packages/react/src/hooks/types/data-changed-events.ts'
      provides: 'UseDataChangedEventSubscriptionParams interface'
      contains: 'UseDataChangedEventSubscriptionParams'
    - path: 'apps/test/src/app/data-changed-events/page.tsx'
      provides: 'Subscription tab with status badges, filter/sort/include controls'
      contains: 'SubscriptionTab'
  key_links:
    - from: 'packages/react/src/hooks/factories/data-changed-events/create-use-data-changed-event-subscription.ts'
      to: '@lsp-indexer/node'
      via: 'buildDataChangedEventSubscriptionConfig + dataChangedEventKeys'
      pattern: 'buildDataChangedEventSubscriptionConfig|dataChangedEventKeys'
    - from: 'packages/react/src/hooks/data-changed-events/use-data-changed-event-subscription.ts'
      to: 'hooks/factories/data-changed-events'
      via: 'createUseDataChangedEventSubscription factory import'
      pattern: 'createUseDataChangedEventSubscription'
    - from: 'packages/next/src/hooks/data-changed-events/use-data-changed-event-subscription.ts'
      to: '@lsp-indexer/react'
      via: 'createUseDataChangedEventSubscription factory import from react package'
      pattern: 'createUseDataChangedEventSubscription.*@lsp-indexer/react'
    - from: 'packages/node/src/services/data-changed-events.ts'
      to: 'packages/node/src/documents/data-changed-events.ts'
      via: 'DataChangedEventSubscriptionDocument import'
      pattern: 'DataChangedEventSubscriptionDocument'
---

<objective>
Create `useDataChangedEventSubscription` hook using the factory pattern established in Phase 10.2, with directory migration of data-changed-events hooks from flat monolithic files to directory structure, dual-package export (@lsp-indexer/react + @lsp-indexer/next), and Subscription tab in the existing /data-changed-events playground page.

Purpose: Domain subscription hook for live ERC725Y data change events. This is an EVENT domain — results MUST default to block-order desc sorting (`block_number desc, transaction_index desc, log_index desc`). Follows the factory + directory migration pattern proven in 10.7–10.10.

Output: `useDataChangedEventSubscription` with 3-overload type narrowing exported from both packages, Subscription tab in playground.

**Gap closure:** Replaces outdated plan that used wrong file locations (subscriptions/ dir), no factory pattern, no include/sort, no 3-overload types, no dual-package export, no subscription document, no codegen, and wrong playground pattern.
</objective>

<execution_context>
@/home/coder/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-subscriptions/10-DOMAIN-SUBSCRIPTION-PRECEDENT.md
@.planning/phases/10-subscriptions/10-CONTEXT.md
@.planning/phases/10.7-followers-subscription/10.7-01-SUMMARY.md
@.planning/phases/10.10-encrypted-assets-subscription/10.10-01-SUMMARY.md
@packages/node/src/services/data-changed-events.ts
@packages/node/src/documents/data-changed-events.ts
@packages/react/src/hooks/data-changed-events.ts
@packages/next/src/hooks/data-changed-events.ts
@apps/test/src/app/data-changed-events/page.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From @lsp-indexer/types (data-changed-events domain types):

```typescript
export type DataChangedEvent = z.infer<typeof DataChangedEventSchema>;
export type PartialDataChangedEvent = z.infer<typeof PartialDataChangedEventSchema>;
export type DataChangedEventResult<I extends DataChangedEventInclude> = /* conditional include type */;
export type DataChangedEventFilter = z.infer<typeof DataChangedEventFilterSchema>;
export type DataChangedEventSort = z.infer<typeof DataChangedEventSortSchema>;
export type DataChangedEventInclude = z.infer<typeof DataChangedEventIncludeSchema>;
```

From packages/node/src/services/data-changed-events.ts (functions needed):

```typescript
export function buildDataChangedEventWhere(filter?: DataChangedEventFilter): Data_Changed_Bool_Exp;
// NOT currently exported — MUST be exported for subscription config:
function buildDataChangedEventOrderBy(
  sort?: DataChangedEventSort,
): Data_Changed_Order_By[] | undefined;
export function buildDataChangedEventIncludeVars(
  include?: DataChangedEventInclude,
): Record<string, boolean>;
```

From packages/node/src/keys/data-changed-events.ts:

```typescript
export const dataChangedEventKeys = { all: [...], list: [...], latest: [...], infinite: [...] };
```

From packages/react/src/hooks/types/subscription.ts:

```typescript
export type UseSubscriptionFn = /* generic subscription hook type */;
export type UseSubscriptionReturn<T> = { data: T[] | null; isConnected: boolean; isSubscribed: boolean; error: Error | null };
```

From packages/react/src/constants.ts:

```typescript
export const DEFAULT_SUBSCRIPTION_LIMIT = 10;
```

From packages/node/src/services/utils.ts (event domain shared utility):

```typescript
export function buildBlockOrderSort(direction: 'asc' | 'desc'): /* Hasura order_by array */;
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Subscription document + codegen + service config + factory + directory migration + thin wrappers + types + playground Subscription tab</name>
  <files>
    packages/node/src/documents/data-changed-events.ts
    packages/node/src/services/data-changed-events.ts
    packages/node/src/graphql/graphql.ts
    packages/node/src/graphql/gql.ts
    packages/react/src/hooks/factories/data-changed-events/create-use-data-changed-event-subscription.ts
    packages/react/src/hooks/factories/data-changed-events/index.ts
    packages/react/src/hooks/factories/index.ts
    packages/react/src/hooks/data-changed-events/queries.ts
    packages/react/src/hooks/data-changed-events/use-data-changed-event-subscription.ts
    packages/react/src/hooks/data-changed-events/index.ts
    packages/react/src/hooks/types/data-changed-events.ts
    packages/react/src/hooks/types/index.ts
    packages/next/src/hooks/data-changed-events/queries.ts
    packages/next/src/hooks/data-changed-events/use-data-changed-event-subscription.ts
    packages/next/src/hooks/data-changed-events/index.ts
    apps/test/src/app/data-changed-events/page.tsx
  </files>
  <action>
    Follow the exact pattern from 10.7 (followers — event domain) and 10.10 (encrypted-assets — directory migration). This is an EVENT DOMAIN — block-order desc default sort is mandatory.

    **Step 1: Add subscription document to `packages/node/src/documents/data-changed-events.ts`**

    Add `DataChangedEventSubscriptionDocument` using `graphql()` tagged template. Mirror `GetDataChangedEventsDocument` exactly (same field selection, same `@include` directives, same variables) but:
    - Change operation type from `query` to `subscription`
    - Remove `$offset: Int` variable (subscriptions don't paginate)
    - Remove `data_changed_aggregate` field (no aggregate in subscriptions)
    - Keep all other variables: `$where`, `$order_by`, `$limit`, plus all 32 include boolean variables (4 scalar + 10 UP `$includeUniversalProfile*` + 18 DA `$includeDigitalAsset*`)
    - Total: ~35 variables

    **Step 2: Run codegen**

    ```bash
    pnpm --filter @lsp-indexer/node codegen
    ```

    This generates `DataChangedEventSubscriptionSubscription` and `DataChangedEventSubscriptionSubscriptionVariables` types in `packages/node/src/graphql/graphql.ts` and adds the `graphql()` overload in `packages/node/src/graphql/gql.ts`.

    **Step 3: Export `buildDataChangedEventOrderBy` + add `buildDataChangedEventSubscriptionConfig` in `packages/node/src/services/data-changed-events.ts`**

    3a. Add `export` keyword to `buildDataChangedEventOrderBy` (line ~140, currently not exported).

    3b. Add `buildDataChangedEventSubscriptionConfig` function. Follow the exact pattern from `buildFollowerSubscriptionConfig` in `packages/node/src/services/followers.ts` (event domain pattern):

    ```typescript
    type RawDataChangedEventSubscriptionRow = DataChangedEventSubscriptionSubscription['data_changed'][number];

    export function buildDataChangedEventSubscriptionConfig(params: {
      filter?: DataChangedEventFilter;
      sort?: DataChangedEventSort;
      limit?: number;
      include?: DataChangedEventInclude;
    }) {
      const where = buildDataChangedEventWhere(params.filter);
      const orderBy = buildDataChangedEventOrderBy(params.sort) ?? buildBlockOrderSort('desc');
      const includeVars = buildDataChangedEventIncludeVars(params.include);

      return {
        document: DataChangedEventSubscriptionDocument,
        variables: {
          where: Object.keys(where).length > 0 ? where : undefined,
          order_by: orderBy,
          limit: params.limit,
          ...includeVars,
        },
        extract: (result: DataChangedEventSubscriptionSubscription) => result.data_changed,
        parser: (raw: RawDataChangedEventSubscriptionRow[]) =>
          params.include ? parseDataChangedEvents(raw, params.include) : parseDataChangedEvents(raw),
      };
    }
    ```

    Import `DataChangedEventSubscriptionDocument` from `../documents/data-changed-events` and `DataChangedEventSubscriptionSubscription` from `../graphql/graphql`. Import `buildBlockOrderSort` from `./utils` if not already imported.

    **CRITICAL EVENT DOMAIN PATTERN:** `buildDataChangedEventOrderBy(params.sort) ?? buildBlockOrderSort('desc')` — when no sort provided, default to block-order desc. This is what makes event domains different from entity domains (which use `undefined` for Hasura default ordering).

    **Step 4: Create subscription factory**

    Create `packages/react/src/hooks/factories/data-changed-events/create-use-data-changed-event-subscription.ts`:

    Follow the exact pattern from `packages/react/src/hooks/factories/followers/create-use-follower-subscription.ts` (event domain reference). The factory has 3 overloads:

    - Overload 1 (include provided): `params: UseDataChangedEventSubscriptionParams & { include: I; onData?: (data: DataChangedEventResult<I>[]) => void }` → `UseSubscriptionReturn<DataChangedEventResult<I>>`
    - Overload 2 (no include): `params?: Omit<UseDataChangedEventSubscriptionParams, 'include'> & { include?: never; onData?: (data: DataChangedEvent[]) => void }` → `UseSubscriptionReturn<DataChangedEvent>`
    - Overload 3 (widest): `params: UseDataChangedEventSubscriptionParams & { include?: DataChangedEventInclude; onData?: (data: PartialDataChangedEvent[]) => void }` → `UseSubscriptionReturn<PartialDataChangedEvent>`
    - Implementation: `params: UseDataChangedEventSubscriptionParams & { onData?: (data: any[]) => void } = {}` → `UseSubscriptionReturn<PartialDataChangedEvent>`

    **CRITICAL:** `onData` is per-overload, NOT on `UseDataChangedEventSubscriptionParams`. Implementation uses `any[]` for `onData` to avoid contravariance.

    Import `buildDataChangedEventSubscriptionConfig`, `dataChangedEventKeys` from `@lsp-indexer/node`.
    Import types from `@lsp-indexer/types`.
    Import `DEFAULT_SUBSCRIPTION_LIMIT` from `../../../constants`.
    Import `UseDataChangedEventSubscriptionParams`, `UseSubscriptionFn` from `../../types`.
    Use `useQueryClient()` unconditionally (Rules of Hooks), only pass when `invalidate: true`.
    Use `dataChangedEventKeys.all` for `invalidateKeys`.

    Create `packages/react/src/hooks/factories/data-changed-events/index.ts`:
    ```typescript
    export * from './create-use-data-changed-event-subscription';
    ```

    Update `packages/react/src/hooks/factories/index.ts` — add:
    ```typescript
    export * from './data-changed-events';
    ```

    **Step 5: Create subscription types**

    Create `packages/react/src/hooks/types/data-changed-events.ts`:
    ```typescript
    export interface UseDataChangedEventSubscriptionParams {
      filter?: DataChangedEventFilter;
      sort?: DataChangedEventSort;
      limit?: number;
      include?: DataChangedEventInclude;
      enabled?: boolean;
      invalidate?: boolean;
      onReconnect?: () => void;
      // NOTE: onData is NOT here — it's per-overload in the factory
    }
    ```

    Import `DataChangedEventFilter`, `DataChangedEventSort`, `DataChangedEventInclude` from `@lsp-indexer/types`.

    Update `packages/react/src/hooks/types/index.ts` — add:
    ```typescript
    export * from './data-changed-events';
    ```

    **Step 6: Directory migration — React package**

    Convert flat file to directory:
    1. Create directory `packages/react/src/hooks/data-changed-events/`
    2. Move `packages/react/src/hooks/data-changed-events.ts` → `packages/react/src/hooks/data-changed-events/queries.ts` (rename, no content changes needed — the flat file's exports become the queries module)
    3. Create `packages/react/src/hooks/data-changed-events/use-data-changed-event-subscription.ts`:
       ```typescript
       import { createUseDataChangedEventSubscription } from '../factories';
       import { useSubscription } from '../use-subscription';

       export const useDataChangedEventSubscription = createUseDataChangedEventSubscription(useSubscription);
       ```
    4. Create `packages/react/src/hooks/data-changed-events/index.ts`:
       ```typescript
       export * from './queries';
       export * from './use-data-changed-event-subscription';
       ```
    5. Delete old flat file `packages/react/src/hooks/data-changed-events.ts`
    6. The barrel `packages/react/src/hooks/index.ts` already has `export * from './data-changed-events'` — this automatically resolves to the directory's `index.ts`. No change needed.

    **Step 7: Directory migration — Next.js package**

    Convert flat file to directory:
    1. Create directory `packages/next/src/hooks/data-changed-events/`
    2. Move `packages/next/src/hooks/data-changed-events.ts` → `packages/next/src/hooks/data-changed-events/queries.ts`
    3. **CRITICAL: Fix relative import path** — after moving into a subdirectory, `../actions/data-changed-events` becomes `../../actions/data-changed-events`. Check and fix all relative imports in `queries.ts`.
    4. Create `packages/next/src/hooks/data-changed-events/use-data-changed-event-subscription.ts`:
       ```typescript
       'use client';

       import { createUseDataChangedEventSubscription } from '@lsp-indexer/react';
       import { useSubscription } from '../use-subscription';

       export const useDataChangedEventSubscription = createUseDataChangedEventSubscription(useSubscription);
       ```
       **CRITICAL:** `'use client'` directive at top. Import factory from `@lsp-indexer/react` (NOT relative). Import `useSubscription` from `../use-subscription` (local Next.js version, NOT from `@lsp-indexer/react`).
    5. Create `packages/next/src/hooks/data-changed-events/index.ts`:
       ```typescript
       export * from './queries';
       export * from './use-data-changed-event-subscription';
       ```
    6. Delete old flat file `packages/next/src/hooks/data-changed-events.ts`
    7. The barrel `packages/next/src/hooks/index.ts` already has `export * from './data-changed-events'` — auto-resolves.

    **Step 8: Add Subscription tab to playground**

    Update `apps/test/src/app/data-changed-events/page.tsx`:

    1. Import `useDataChangedEventSubscription` from both packages (with aliases):
       ```typescript
       import { useDataChangedEventSubscription as useDataChangedEventSubscriptionNext } from '@lsp-indexer/next';
       import { useDataChangedEventSubscription as useDataChangedEventSubscriptionReact } from '@lsp-indexer/react';
       ```

    2. Add to the `useHooks(mode)` function (or equivalent mode-switching pattern used on this page):
       Return `useDataChangedEventSubscription` based on mode.

    3. Create `SubscriptionTab` component following the exact pattern from the follows/page.tsx Subscription tab:
       - Accept `mode: HookMode` prop
       - Use `useDataChangedEventSubscription` from the mode-appropriate package
       - State: limit (default 10), invalidate toggle
       - Use `useListState()` (or equivalent) for filter/sort/include state if the page uses shared state
       - Display: connection status badge, subscription status badge, error display
       - Controls: filter fields (reuse existing page filter config — 9 fields), sort dropdown (4 options: newest, oldest, universalProfileName, digitalAssetName), include toggles (reuse existing DATA_CHANGED_EVENT_INCLUDE_FIELDS), limit selector, invalidate toggle
       - Results: `ResultsList` with subscription data

    4. Add tab to the tabs array:
       ```typescript
       {
         value: 'subscription',
         label: 'Subscription',
         icon: <Radio className="size-4" />,
         render: (mode) => <SubscriptionTab mode={mode} />,
       }
       ```
       Import `Radio` from `lucide-react`.

    **Step 9: Build validation**

    ```bash
    pnpm --filter @lsp-indexer/node build && pnpm --filter @lsp-indexer/react build && pnpm --filter @lsp-indexer/next build && pnpm --filter test build
    ```

    If `packages/indexer` has pre-existing build errors (unrelated typeorm/abi issues), build individual packages instead of `pnpm build`.

    Commit: `feat(10.11-data-changed-events): add useDataChangedEventSubscription with factory pattern + directory migration + dual-package export`

  </action>
  <verify>
    ```bash
    pnpm --filter @lsp-indexer/node build && pnpm --filter @lsp-indexer/react build && pnpm --filter @lsp-indexer/next build && pnpm --filter test build
    ```
    Verify exports:
    ```bash
    grep "useDataChangedEventSubscription" packages/react/dist/index.js
    grep "useDataChangedEventSubscription" packages/next/dist/index.js
    grep "buildDataChangedEventSubscriptionConfig" packages/node/dist/index.js
    grep "SubscriptionTab" apps/test/src/app/data-changed-events/page.tsx
    ```
  </verify>
  <done>
    - useDataChangedEventSubscription exported from both @lsp-indexer/react and @lsp-indexer/next with 3-overload type narrowing
    - createUseDataChangedEventSubscription factory in packages/react/src/hooks/factories/data-changed-events/
    - DataChangedEventSubscriptionDocument created with ~35 variables mirroring query document
    - buildDataChangedEventSubscriptionConfig in node service with EVENT DOMAIN block-order desc default sort
    - buildDataChangedEventOrderBy now exported from node service
    - Data-changed-events hooks migrated from flat files to directory structure in both react and next packages
    - UseDataChangedEventSubscriptionParams type created
    - Subscription tab visible in /data-changed-events playground with connection status, filter/sort/include controls, cache invalidation toggle
    - All packages build with zero type errors
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @lsp-indexer/node build` passes — subscription document + config builder + exported orderBy
- `pnpm --filter @lsp-indexer/react build` passes — factory + thin wrapper + directory migration
- `pnpm --filter @lsp-indexer/next build` passes — thin wrapper with 'use client' + directory migration
- `pnpm --filter test build` passes — playground Subscription tab
- `useDataChangedEventSubscription` importable from both `@lsp-indexer/react` and `@lsp-indexer/next`
- Hook accepts { filter?, sort?, limit?, include?, enabled?, invalidate?, onData?, onReconnect? }
- Hook returns { data: DataChangedEvent[] | null, isConnected, isSubscribed, error: Error | null }
- 3 overloads: include → DataChangedEventResult<I>, no include → DataChangedEvent, widest → PartialDataChangedEvent
- Event domain default sort: block_number desc, transaction_index desc, log_index desc
- Sort param overrides default when provided
- Cache invalidation via dataChangedEventKeys.all when invalidate: true
- Zero type assertions in subscription data path
- Old flat files deleted (packages/react/src/hooks/data-changed-events.ts, packages/next/src/hooks/data-changed-events.ts)
- Playground shows Subscription tab with all controls (4th tab alongside Latest, List, Infinite)
</verification>

<success_criteria>

1. useDataChangedEventSubscription created with factory pattern (createUseDataChangedEventSubscription) reused across both packages
2. Hook exported from both @lsp-indexer/react and @lsp-indexer/next
3. 3-overload type narrowing via include parameter — zero explicit type arguments at call sites
4. Event domain block-order desc default sort applied (block_number, transaction_index, log_index all desc)
5. DataChangedEventSubscriptionDocument mirrors query document (same fields, same @include directives)
6. Data-changed-events hooks restructured from monolithic flat files to directory pattern
7. Subscription tab works in /data-changed-events playground with filter/sort/include controls
8. All 4 packages + test app build with zero type errors
   </success_criteria>

<output>
After completion, create `.planning/phases/10.11-data-changed-events-subscription/10.11-01-SUMMARY.md`
</output>
