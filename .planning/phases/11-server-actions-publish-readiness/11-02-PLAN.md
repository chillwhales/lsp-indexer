---
phase: 11-server-actions-publish-readiness
plan: 02
type: execute
wave: 2
depends_on: ['11-01']
files_modified:
  - package.json
  - packages/types/package.json
  - packages/node/package.json
  - packages/react/package.json
  - packages/next/package.json
  - packages/types/tsup.config.ts
  - packages/node/tsup.config.ts
  - packages/react/tsup.config.ts
  - packages/next/tsup.config.ts
autonomous: true
requirements:
  - ACTION-02
  - DX-03

must_haves:
  truths:
    - 'Developer can run publint on all 4 packages and see zero errors'
    - 'Developer can run arethetypeswrong on all 4 packages and see zero errors'
    - 'Developer can import from @lsp-indexer/node in a server context without client code leaking'
    - 'Developer can npm pack each package and see only dist/ and package metadata included'
  artifacts:
    - path: 'package.json'
      provides: 'Workspace-level publish validation scripts'
      contains: 'publint'
    - path: 'packages/types/package.json'
      provides: 'Types package with validated exports map'
      contains: 'publint'
    - path: 'packages/node/package.json'
      provides: 'Node package with validated exports map'
      contains: 'publint'
    - path: 'packages/react/package.json'
      provides: 'React package with validated exports map'
      contains: 'publint'
    - path: 'packages/next/package.json'
      provides: 'Next package with validated exports map'
      contains: 'publint'
  key_links:
    - from: 'package.json scripts'
      to: 'publint + @arethetypeswrong/cli'
      via: 'devDependencies'
      pattern: 'publint'
    - from: 'packages/*/package.json exports'
      to: 'packages/*/dist/*'
      via: 'exports map conditions'
      pattern: 'dist/index'
    - from: '@lsp-indexer/next'
      to: '@lsp-indexer/node'
      via: 'server-only dependency (no client code)'
      pattern: 'import.*@lsp-indexer/node'
---

<objective>
Validate all 4 packages (`@lsp-indexer/types`, `@lsp-indexer/node`, `@lsp-indexer/react`, `@lsp-indexer/next`) for npm publish readiness using `publint` and `arethetypeswrong`, fix any issues found, verify server/client bundle separation, and confirm `npm pack` contents are clean.

Purpose: Ensure every package can be published to npm without exports map issues, missing type declarations, or bundle contamination — the final quality gate before shipping.
Output: All 4 packages pass publish validation tools with zero errors, clean npm pack output.
</objective>

<execution_context>
@/home/coder/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-server-actions-publish-readiness/11-CONTEXT.md
@.planning/phases/11-server-actions-publish-readiness/11-01-SUMMARY.md

<interfaces>
<!-- Package structure the executor needs to understand -->

**4 packages to validate (dependency order):**

```
@lsp-indexer/types  →  @lsp-indexer/node  →  @lsp-indexer/react
                                           →  @lsp-indexer/next (also depends on @lsp-indexer/react)
```

**Current exports maps (all 4 packages follow the same pattern):**

```json
{
  ".": {
    "import": { "types": "./dist/index.d.ts", "default": "./dist/index.js" },
    "require": { "types": "./dist/index.d.cts", "default": "./dist/index.cjs" }
  }
}
```

**@lsp-indexer/next has an additional entry point:**

```json
{
  "./server": {
    "import": { "types": "./dist/server.d.ts", "default": "./dist/server.js" },
    "require": { "types": "./dist/server.d.cts", "default": "./dist/server.cjs" }
  }
}
```

**All packages use tsup for builds:**

- format: ['esm', 'cjs']
- dts: true
- sourcemap: true
- @lsp-indexer/react and @lsp-indexer/next (main entry) have `banner: { js: '"use client";' }`

**Current `files` field in all 4 packages:** `"files": ["dist"]`

**Known pre-existing issue:** `packages/indexer` has pre-existing build errors (unrelated typeorm/abi issues). Build individual packages with `--filter`, NOT `pnpm build` from root.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install publish validation tools and run against all 4 packages</name>
  <files>
    package.json
    packages/types/package.json
    packages/node/package.json
    packages/react/package.json
    packages/next/package.json
    packages/types/tsup.config.ts
    packages/node/tsup.config.ts
    packages/react/tsup.config.ts
    packages/next/tsup.config.ts
  </files>
  <action>
**1. Install tools at workspace root:**

```bash
pnpm add -Dw publint @arethetypeswrong/cli
```

**2. Add validation scripts to root `package.json`:**

```json
{
  "scripts": {
    "validate:publint": "pnpm --filter @lsp-indexer/types exec publint && pnpm --filter @lsp-indexer/node exec publint && pnpm --filter @lsp-indexer/react exec publint && pnpm --filter @lsp-indexer/next exec publint",
    "validate:attw": "pnpm --filter @lsp-indexer/types exec attw --pack && pnpm --filter @lsp-indexer/node exec attw --pack && pnpm --filter @lsp-indexer/react exec attw --pack && pnpm --filter @lsp-indexer/next exec attw --pack",
    "validate:publish": "pnpm validate:publint && pnpm validate:attw"
  }
}
```

**3. Build all 4 packages (dependency order):**

```bash
pnpm --filter @lsp-indexer/types build
pnpm --filter @lsp-indexer/node build
pnpm --filter @lsp-indexer/react build
pnpm --filter @lsp-indexer/next build
```

**4. Run publint on each package:**

```bash
cd packages/types && pnpm exec publint
cd packages/node && pnpm exec publint
cd packages/react && pnpm exec publint
cd packages/next && pnpm exec publint
```

**5. Run arethetypeswrong on each package:**

```bash
cd packages/types && pnpm exec attw --pack
cd packages/node && pnpm exec attw --pack
cd packages/react && pnpm exec attw --pack
cd packages/next && pnpm exec attw --pack
```

**6. Fix any issues found.** Common publint/attw issues and fixes:

- **Missing `types` condition before `default`:** Already in place — verify.
- **`typesVersions` missing for node10 resolution:** If attw flags `node10` resolution failures, add a `typesVersions` map matching the package's actual type entry points. For packages with only a `"."` entry point:
  ```json
  "typesVersions": { "*": { ".": ["./dist/index.d.ts"] } }
  ```
  For `@lsp-indexer/next` (which also has `"./server"`), additionally add `"./server": ["./dist/server.d.ts"]`.
- **`main`/`module`/`types` top-level fields inconsistent with exports:** Verify consistency. The top-level fields should match the `"."` entry point's conditions.
- **Missing `"type": "module"` consistency:** Already set — verify.
- **Peer dependencies missing for packages that use them at runtime:** Check if `zod` needs to be a peerDependency of `@lsp-indexer/types` (it's currently a direct dep — publint may flag this since consumers need it too). If publint flags it, add as both dependency and peerDependency with `peerDependenciesMeta: { zod: { optional: false } }`.
- **Source maps in `files`:** Currently `"files": ["dist"]` — sourcemaps are inside dist/ which is fine.

If tsup config or exports map adjustments are needed, make them. The goal is zero errors from both tools across all 4 packages. Iterate: fix → rebuild → revalidate until clean.
</action>
<verify>
<automated>pnpm --filter @lsp-indexer/types build && pnpm --filter @lsp-indexer/node build && pnpm --filter @lsp-indexer/react build && pnpm --filter @lsp-indexer/next build && cd packages/types && pnpm exec publint && cd ../node && pnpm exec publint && cd ../react && pnpm exec publint && cd ../next && pnpm exec publint</automated>
</verify>
<done> - publint and @arethetypeswrong/cli installed as workspace root devDependencies - validate:publint, validate:attw, validate:publish scripts in root package.json - All 4 packages pass publint with zero errors - All 4 packages pass attw with zero errors - Any exports map / tsup config fixes applied and committed
</done>
</task>

<task type="auto">
  <name>Task 2: Bundle separation verification and npm pack audit</name>
  <files>
    packages/types/package.json
    packages/node/package.json
    packages/react/package.json
    packages/next/package.json
  </files>
  <action>
**1. Verify server/client bundle separation (ACTION-02):**

The key concern: `@lsp-indexer/node` is server-only code (GraphQL execution, fetch, etc.). When `@lsp-indexer/next` imports from `@lsp-indexer/node` in server actions (`'use server'`), Next.js should NOT ship node code to the browser.

Verify by building the test app:

```bash
cd apps/test && pnpm build
```

Check for:

- Zero "client-only code imported in server" warnings
- Zero "server-only code imported in client" warnings
- Build completes successfully

If the test app build fails due to unrelated issues (missing env vars, etc.), verify at the package level instead:

- Check that `@lsp-indexer/next` main entry has `"use client"` banner (for hooks/subscriptions)
- Check that `@lsp-indexer/next/server` entry does NOT have `"use client"` banner (for server-only exports like WebSocket proxy)
- Verify `@lsp-indexer/node` has NO `"use client"` banner (it's server-only)

**2. npm pack audit for all 4 packages:**

For each package, run:

```bash
cd packages/types && npm pack --dry-run 2>&1
cd packages/node && npm pack --dry-run 2>&1
cd packages/react && npm pack --dry-run 2>&1
cd packages/next && npm pack --dry-run 2>&1
```

Verify for each:

- Only `dist/` contents are included (_.js, _.cjs, _.d.ts, _.d.cts, _.js.map, _.cjs.map)
- `package.json` is included (automatic)
- `README.md` is included if present (automatic)
- NO source files (\*.ts from src/)
- NO test fixtures
- NO node_modules
- NO tsconfig.json, tsup.config.ts, or other config files

If source files leak through, verify the `"files": ["dist"]` field is correctly set.

**3. Final cross-package build verification:**

Build all 4 packages in dependency order one final time to confirm the full chain works:

```bash
pnpm --filter @lsp-indexer/types build && \
pnpm --filter @lsp-indexer/node build && \
pnpm --filter @lsp-indexer/react build && \
pnpm --filter @lsp-indexer/next build
```

Verify all produce ESM (.js), CJS (.cjs), and declaration (.d.ts, .d.cts) files.
</action>
<verify>
<automated>cd packages/types && npm pack --dry-run 2>&1 | head -30 && cd ../node && npm pack --dry-run 2>&1 | head -30 && cd ../react && npm pack --dry-run 2>&1 | head -30 && cd ../next && npm pack --dry-run 2>&1 | head -30</automated>
</verify>
<done> - Test app builds successfully (or package-level separation verified) - @lsp-indexer/node has no "use client" banner in output - @lsp-indexer/next main entry has "use client" banner, ./server entry does not - npm pack --dry-run shows only dist/ contents for all 4 packages - No source files, config files, or test fixtures in pack output - Full build chain succeeds: types → node → react → next
</done>
</task>

</tasks>

<verification>
1. `pnpm validate:publish` passes (runs both publint and attw on all 4 packages)
2. `npm pack --dry-run` in each package shows only dist/ contents
3. Full build chain succeeds: `pnpm --filter @lsp-indexer/types build && pnpm --filter @lsp-indexer/node build && pnpm --filter @lsp-indexer/react build && pnpm --filter @lsp-indexer/next build`
4. No "use client" banner in @lsp-indexer/node dist output: `head -1 packages/node/dist/index.js` should NOT contain "use client"
</verification>

<success_criteria>

1. All 4 packages pass publint with zero errors
2. All 4 packages pass arethetypeswrong with zero errors
3. npm pack output contains only dist/ files for each package
4. @lsp-indexer/node is server-safe — no client-side directives or browser-only code
5. Full package build chain completes without errors
   </success_criteria>

<output>
After completion, create `.planning/phases/11-server-actions-publish-readiness/11-02-SUMMARY.md`
</output>
