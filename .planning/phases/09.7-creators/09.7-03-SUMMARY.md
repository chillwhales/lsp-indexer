---
phase: 09.7-creators
plan: 03
subsystem: hooks, actions, wiring
tags: [creator, lsp4, tanstack-query, hooks, server-action, DX-05, include-narrowing]

# Dependency graph
requires:
  - phase: 09.7-02
    provides: fetchCreators service, creatorKeys query key factory, parseCreators parser
  - phase: 09.7-01
    provides: Creator types (CreatorSchema, CreatorResult<I>, UseCreatorsParams, UseInfiniteCreatorsParams)
  - phase: 09.6-generic-type-propagation
    provides: 3-overload <const I> generic pattern (DX-05)
provides:
  - React hooks (useCreators, useInfiniteCreators) with getClientUrl() for direct browser → Hasura queries
  - Next.js server action (getCreators) with 'use server' directive and getServerUrl()
  - Next.js hooks (useCreators, useInfiniteCreators) routing through server action
  - Barrel exports wired in react and next package index.ts files
affects: [09.7-04 (playground + verification)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 'Creator hooks follow established 3-overload <const I> pattern with UseCreatorsReturn<F> type aliases'
    - 'Infinite hook destructure-before-spread pattern for TS2783 prevention'

key-files:
  created:
    - packages/react/src/hooks/creators.ts
    - packages/next/src/actions/creators.ts
    - packages/next/src/hooks/creators.ts
  modified:
    - packages/react/src/index.ts
    - packages/next/src/index.ts

key-decisions:
  - 'Followed followers hook pattern exactly — consistent DX across all domains'
  - 'DEFAULT_PAGE_SIZE = 20 matching all other domains'

patterns-established:
  - 'Creator domain consumer API: 2 hooks + 1 server action (no singular hook — no natural key)'

# Metrics
duration: 2min
completed: 2026-02-23
---

# Phase 9.7 Plan 03: Creator Hooks + Server Action + Barrel Exports Summary

**2 React hooks + 1 server action + 2 Next.js hooks for LSP4 Creator domain with 3-overload DX-05 pattern, barrel exports, and clean 4-package builds**

## Performance

- **Duration:** 2 min
- **Started:** 2026-02-23T17:47:55Z
- **Completed:** 2026-02-23T17:50:50Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Created React hooks (`useCreators`, `useInfiniteCreators`) using `getClientUrl()` for direct browser → Hasura queries with TanStack Query caching and deduplication
- Created Next.js server action (`getCreators`) with `'use server'` directive routing through `getServerUrl()` to keep GraphQL endpoint hidden from client
- Created Next.js hooks (`useCreators`, `useInfiniteCreators`) routing through the server action instead of direct fetch
- All 5 exported functions use 3-overload `<const I>` generic pattern for type-safe include narrowing
- Wired barrel exports through `packages/react/src/index.ts` and `packages/next/src/index.ts`
- All 4 packages (types, node, react, next) build and typecheck with zero errors

## Task Commits

Each task was committed atomically:

1. **Task 1: Create React hooks and Next.js server action + hooks** - `73f22f0` (feat)
2. **Task 2: Wire barrel exports and validate builds** - `09bbae9` (feat)

## Files Created/Modified

- `packages/react/src/hooks/creators.ts` - 2 TanStack Query hooks (useCreators, useInfiniteCreators) for browser → Hasura directly
- `packages/next/src/actions/creators.ts` - Server action wrapping fetchCreators via getServerUrl()
- `packages/next/src/hooks/creators.ts` - 2 TanStack Query hooks routing through server action
- `packages/react/src/index.ts` - Added creator hooks barrel export
- `packages/next/src/index.ts` - Added creator action and hooks barrel exports

## Decisions Made

- **Followed followers hook pattern exactly:** Consistent DX across all domains — same overload signatures, same return type aliases, same destructure-before-spread pattern for infinite hooks.
- **DEFAULT_PAGE_SIZE = 20:** Matches all other domain hooks for consistent infinite scroll behavior.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All hooks and server action ready for 09.7-04 (playground + verification)
- `useCreators` and `useInfiniteCreators` available from both `@lsp-indexer/react` and `@lsp-indexer/next`
- `getCreators` server action exported from `@lsp-indexer/next`
- No blockers

## Self-Check: PASSED

---

_Phase: 09.7-creators_
_Completed: 2026-02-23_
