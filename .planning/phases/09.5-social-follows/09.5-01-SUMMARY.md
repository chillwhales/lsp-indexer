---
phase: 09.5-social-follows
plan: 01
subsystem: types, graphql
tags: [zod, graphql-codegen, conditional-include, follower, social, DX-04]

# Dependency graph
requires:
  - phase: 09.4-conditional-include-types
    provides: IncludeResult utility type, stripExcluded, ProfileResult generic
  - phase: 08-first-vertical-slice
    provides: ProfileSchema, ProfileIncludeSchema, profile documents pattern
provides:
  - Follower domain Zod schemas with all inferred types
  - FollowerResult<I> conditional include type with nested profile narrowing
  - FollowCount type for aggregate queries
  - GetFollowersDocument and GetFollowCountDocument GraphQL documents
  - Codegen output with Follower_Bool_Exp, GetFollowersQuery, GetFollowCountQuery
affects: [09.5-02 (parsers+services), 09.5-03 (hooks), 09.5-04 (playground)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 'FollowerResult<I> with ResolveFollowerProfile + ResolveFollowedProfile intersection types'
    - 'Dual-profile sub-includes (followerProfile + followedProfile) with independent narrowing'
    - 'Two aliased follower_aggregate queries for follow counts'

key-files:
  created:
    - packages/types/src/followers.ts
    - packages/node/src/documents/followers.ts
  modified:
    - packages/types/src/index.ts
    - packages/node/src/graphql/graphql.ts
    - packages/node/src/graphql/gql.ts

key-decisions:
  - 'FollowerResult uses dual profile resolvers (ResolveFollowerProfile + ResolveFollowedProfile) — each side independently narrowable'
  - 'GetFollowersDocument serves all list queries (followers, following, isFollowing) — service layer varies $where'
  - 'GetFollowCountDocument uses aliased follower_aggregate for separate follower/following counts'
  - '22 @include variables: 2 scalars + 2×9 profile sub-includes + 2 top-level profile toggles'

patterns-established:
  - 'Dual relation sub-includes with independent narrowing (followerProfile vs followedProfile)'
  - 'Single list document reused for multiple hooks via service-layer $where variation'

# Metrics
duration: 3min
completed: 2026-02-22
---

# Phase 9.5 Plan 01: Follower Types + Documents + Codegen Summary

**Follower domain Zod schemas with DX-04 conditional include types and two GraphQL documents for social graph queries**

## Performance

- **Duration:** 3 min
- **Started:** 2026-02-22T18:51:20Z
- **Completed:** 2026-02-22T18:54:44Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Created complete Follower domain type system with 6 Zod schemas, 12 inferred types, and conditional include types (DX-04 from the start)
- FollowerResult<I> narrowing with dual profile resolvers — followerProfile and followedProfile independently narrowable via ProfileIncludeSchema sub-includes
- Two GraphQL documents: GetFollowersDocument (22 @include variables) and GetFollowCountDocument (two aliased aggregates)
- All codegen output generated with Follower_Bool_Exp, GetFollowersQuery, GetFollowCountQuery types

## Task Commits

Each task was committed atomically:

1. **Task 1: Create Follower domain types with conditional include types** - `817fb92` (feat)
2. **Task 2: Create GraphQL documents and run codegen** - `6fdad25` (feat)

## Files Created/Modified

- `packages/types/src/followers.ts` - All Follower domain Zod schemas, inferred types, conditional include types (FollowerResult, PartialFollower)
- `packages/types/src/index.ts` - Added barrel export for followers
- `packages/node/src/documents/followers.ts` - GetFollowersDocument (list with 22 @include directives) and GetFollowCountDocument (two aliased aggregates)
- `packages/node/src/graphql/graphql.ts` - Regenerated codegen output with follower types
- `packages/node/src/graphql/gql.ts` - Regenerated codegen output with follower document mappings

## Decisions Made

- **Single list document reused:** GetFollowersDocument serves useFollowers, useInfiniteFollowers, useFollowing, useInfiniteFollowing, and useIsFollowing — the service layer varies the `$where` filter, not the document
- **Two aliased aggregates for count:** GetFollowCountDocument uses aliased `follower_aggregate` for independent follower and following counts
- **22 include variables:** 2 scalar toggles (timestamp, address) + 2 top-level profile toggles + 2×9 profile sub-field toggles = 22 total Boolean! = true defaults
- **Profile sub-fields match parseProfile:** avatar uses file_type, aggregates use followedBy_aggregate/followed_aggregate (matching existing profile document pattern)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Types and documents ready for 09.5-02 (parsers + services + query keys)
- FollowerSchema defines the parser output shape
- GetFollowersDocument and GetFollowCountDocument define the codegen input types for service functions
- No blockers

## Self-Check: PASSED

---

_Phase: 09.5-social-follows_
_Completed: 2026-02-22_
