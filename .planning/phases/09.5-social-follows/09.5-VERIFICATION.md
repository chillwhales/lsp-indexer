---
phase: 09.5-social-follows
verified: 2026-02-22T20:00:00Z
status: passed
score: 20/20 must-haves verified
---

# Phase 9.5: Social / Follows Verification Report

**Phase Goal:** Developer can use `useFollowers`, `useFollowing`, `useFollowCount`, `useInfiniteFollowers`, `useInfiniteFollowing` for LSP26 social graph data.
**Verified:** 2026-02-22
**Status:** ✅ PASSED
**Re-verification:** No — initial verification (after branch merge consolidation)

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                          | Status     | Evidence                                                                                                                                                                                                                                                                                                         |
| --- | -------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Follower, FollowerFilter, FollowerSort, FollowerInclude types exist and are importable from @lsp-indexer/types | ✓ VERIFIED | `packages/types/src/followers.ts` exports all 4 types (lines 193-198); re-exported via `packages/types/src/index.ts` line 4                                                                                                                                                                                      |
| 2   | FollowerResult<I> narrows return type based on include parameter                                               | ✓ VERIFIED | `packages/types/src/followers.ts` lines 256-265 — uses `IncludeResult` + `ResolveFollowerProfile` + `ResolveFollowedProfile` conditional types                                                                                                                                                                   |
| 3   | FollowerInclude has followerProfile and followedProfile sub-includes (ProfileIncludeSchema)                    | ✓ VERIFIED | Lines 121-130 — both fields typed as `ProfileIncludeSchema.optional()`                                                                                                                                                                                                                                           |
| 4   | FollowCount type exists with followerCount and followingCount fields                                           | ✓ VERIFIED | Lines 45-50 — `FollowCountSchema` with both fields as `z.number()`                                                                                                                                                                                                                                               |
| 5   | GraphQL documents for follower list and follow count exist with @include directives                            | ✓ VERIFIED | `packages/node/src/documents/followers.ts` — `GetFollowersDocument` (160 lines) with 22 `@include` directives; `GetFollowCountDocument` (lines 173-186) with aliased aggregates                                                                                                                                  |
| 6   | parseFollower transforms Hasura response to clean Follower type with camelCase fields                          | ✓ VERIFIED | `packages/node/src/parsers/followers.ts` lines 57-64 — maps `follower_address` → `followerAddress`, `followed_address` → `followedAddress`; nested profiles parsed via `parseProfile`                                                                                                                            |
| 7   | parseFollower supports include parameter for conditional type narrowing via stripExcluded                      | ✓ VERIFIED | Lines 36-39 (overload) + line 67 — calls `stripExcluded(result, include, ['followerAddress', 'followedAddress'])`                                                                                                                                                                                                |
| 8   | fetchFollowers translates filter/sort/include params to Hasura variables                                       | ✓ VERIFIED | `packages/node/src/services/followers.ts` lines 227-286 — uses `buildFollowerWhere`, `buildFollowerOrderBy`, `buildFollowerIncludeVars`; direction param switches primary field                                                                                                                                  |
| 9   | fetchFollowCount returns { followerCount, followingCount }                                                     | ✓ VERIFIED | Lines 301-316 — uses `GetFollowCountDocument` with aliased aggregates, returns both count fields                                                                                                                                                                                                                 |
| 10  | fetchIsFollowing returns boolean                                                                               | ✓ VERIFIED | Lines 328-365 — uses `GetFollowersDocument` with limit 1 and all includes disabled, returns `result.follower.length > 0`                                                                                                                                                                                         |
| 11  | followerKeys provides cache key factory                                                                        | ✓ VERIFIED | `packages/node/src/keys/followers.ts` — 109 lines, hierarchical TkDodo pattern with `all`, `followers()`, `followersList(...)`, `followersInfinite(...)`, `following()`, `followingList(...)`, `followingInfinite(...)`, `counts()`, `count(address)`, `isFollowings()`, `isFollowing(f, d)`                     |
| 12  | 6 React hooks exported from @lsp-indexer/react                                                                 | ✓ VERIFIED | `packages/react/src/hooks/followers.ts` — 6 exported functions: `useFollowers`, `useInfiniteFollowers`, `useFollowing`, `useInfiniteFollowing`, `useFollowCount`, `useIsFollowing`; re-exported via `packages/react/src/index.ts`                                                                                |
| 13  | 3 server actions exported from @lsp-indexer/next                                                               | ✓ VERIFIED | `packages/next/src/actions/followers.ts` — 3 functions with `'use server'`: `getFollowers`, `getFollowCount`, `getIsFollowing`; re-exported via `packages/next/src/index.ts`                                                                                                                                     |
| 14  | 6 Next.js hooks exported from @lsp-indexer/next                                                                | ✓ VERIFIED | `packages/next/src/hooks/followers.ts` — 6 exported functions: `useFollowers`, `useInfiniteFollowers`, `useFollowing`, `useInfiniteFollowing`, `useFollowCount`, `useIsFollowing`; routed through server actions; re-exported via `packages/next/src/index.ts`                                                   |
| 15  | All 4 packages build successfully                                                                              | ✓ VERIFIED | Per user context — all 4 packages + test app build successfully after branch merge                                                                                                                                                                                                                               |
| 16  | Follows playground page at /follows with 6 tabs                                                                | ✓ VERIFIED | `apps/test/src/app/follows/page.tsx` — 660 lines, 6 tabs: `followers`, `infinite-followers`, `following`, `infinite-following`, `count`, `is-following`                                                                                                                                                          |
| 17  | FollowerCard component with collapsible profile sections                                                       | ✓ VERIFIED | `apps/test/src/components/follower-card.tsx` — 104 lines, uses `Collapsible`/`CollapsibleContent`/`CollapsibleTrigger` for both `followerProfile` and `followedProfile` sections                                                                                                                                 |
| 18  | Nav sidebar shows Follows link as active                                                                       | ✓ VERIFIED | `apps/test/src/components/nav.tsx` line 46 — `{ href: '/follows', label: 'Follows', icon: Heart, available: true }`; active state via `isActive={pathname === link.href}` (line 76)                                                                                                                              |
| 19  | All address filters use \_ilike + escapeLike for case-insensitive matching                                     | ✓ VERIFIED | `packages/node/src/services/followers.ts` — 8 `_ilike` usages, all paired with `escapeLike()`: primary address (line 51), followerAddress filter (line 58), followedAddress filter (line 64), followerName (line 71), followedName (line 79), fetchFollowCount (lines 308-309), fetchIsFollowing (lines 335-336) |
| 20  | Nested sorts (followerName, followedName) map to Hasura nested order_by                                        | ✓ VERIFIED | Lines 128-142 — `followerName` → `followerUniversalProfile.lsp3Profile.name.value`, `followedName` → `followedUniversalProfile.lsp3Profile.name.value`; both default `nulls: 'last'`                                                                                                                             |

**Score:** 20/20 truths verified

### Required Artifacts

| Artifact                                     | Expected                                          | Status     | Details                                                                                              |
| -------------------------------------------- | ------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------- |
| `packages/types/src/followers.ts`            | Follower domain types + conditional include types | ✓ VERIFIED | 271 lines, 12 types exported, FollowerResult<I> conditional narrowing                                |
| `packages/node/src/documents/followers.ts`   | GraphQL documents with @include directives        | ✓ VERIFIED | 186 lines, 2 documents, 22 @include directives                                                       |
| `packages/node/src/parsers/followers.ts`     | Parser with camelCase transform + stripExcluded   | ✓ VERIFIED | 97 lines, overloaded parseFollower + parseFollowers                                                  |
| `packages/node/src/services/followers.ts`    | 3 service functions + internal builders           | ✓ VERIFIED | 366 lines, fetchFollowers/fetchFollowCount/fetchIsFollowing + buildFollowerWhere/OrderBy/IncludeVars |
| `packages/node/src/keys/followers.ts`        | Cache key factory with hierarchical keys          | ✓ VERIFIED | 109 lines, 11 key methods                                                                            |
| `packages/react/src/hooks/followers.ts`      | 6 React hooks using TanStack Query                | ✓ VERIFIED | 427 lines, all 6 hooks with proper enabled guards                                                    |
| `packages/next/src/actions/followers.ts`     | 3 server actions with 'use server'                | ✓ VERIFIED | 94 lines, 3 functions, 'use server' directive                                                        |
| `packages/next/src/hooks/followers.ts`       | 6 Next.js hooks via server actions                | ✓ VERIFIED | 383 lines, all 6 hooks routing through server actions                                                |
| `apps/test/src/app/follows/page.tsx`         | Playground page with 6 tabs                       | ✓ VERIFIED | 660 lines, all 6 tabs with mode switching (react/server)                                             |
| `apps/test/src/components/follower-card.tsx` | Card with collapsible profile sections            | ✓ VERIFIED | 104 lines, 2 collapsible profile sections, raw JSON toggle                                           |

### Key Link Verification

| From            | To             | Via                                                                                      | Status  | Details                                                     |
| --------------- | -------------- | ---------------------------------------------------------------------------------------- | ------- | ----------------------------------------------------------- |
| React hooks     | Node services  | `import { fetchFollowers, fetchFollowCount, fetchIsFollowing } from '@lsp-indexer/node'` | ✓ WIRED | All 6 hooks call corresponding service functions            |
| Next.js hooks   | Server actions | `import { getFollowers, getFollowCount, getIsFollowing } from '../actions/followers'`    | ✓ WIRED | All 6 Next.js hooks call corresponding server actions       |
| Server actions  | Node services  | `import { fetchFollowers, fetchFollowCount, fetchIsFollowing } from '@lsp-indexer/node'` | ✓ WIRED | All 3 server actions delegate to Node services              |
| Services        | Documents      | `import { GetFollowCountDocument, GetFollowersDocument } from '../documents/followers'`  | ✓ WIRED | All service functions use correct documents via `execute()` |
| Services        | Parsers        | `import { parseFollowers } from '../parsers/followers'`                                  | ✓ WIRED | `fetchFollowers` calls `parseFollowers` on response         |
| Parsers         | stripExcluded  | `import { stripExcluded } from './strip'`                                                | ✓ WIRED | `parseFollower` calls `stripExcluded` when include provided |
| Playground page | React hooks    | `import { useFollowers, ... } from '@lsp-indexer/react'`                                 | ✓ WIRED | All 6 hooks imported and used in tab components             |
| Playground page | Next.js hooks  | `import { useFollowers, ... } from '@lsp-indexer/next'`                                  | ✓ WIRED | All 6 hooks imported with aliases and used in server mode   |
| Playground page | FollowerCard   | `import { FollowerCard } from '@/components/follower-card'`                              | ✓ WIRED | Used in all 4 list/infinite tabs via `renderItem`           |
| Nav             | Follows route  | `{ href: '/follows', label: 'Follows', available: true }`                                | ✓ WIRED | Active state via `pathname === link.href`                   |

### Requirements Coverage

| Requirement                                                                 | Status      | Notes                                                              |
| --------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------ |
| QUERY-05: useFollowers, useFollowing, useFollowCount for social/follow data | ✓ SATISFIED | All 3 hooks exist in both @lsp-indexer/react and @lsp-indexer/next |
| PAGE-01: useInfinite\* hooks for offset-based infinite scroll               | ✓ SATISFIED | useInfiniteFollowers and useInfiniteFollowing in both packages     |

### Anti-Patterns Found

| File | Line | Pattern                                                       | Severity | Impact |
| ---- | ---- | ------------------------------------------------------------- | -------- | ------ |
| —    | —    | No stub patterns, TODOs, FIXMEs, or placeholder content found | —        | —      |

Note: `placeholder` hits in the playground page are all HTML input `placeholder` attributes (UI hint text), not stub indicators.

### Human Verification Required

### 1. Playground Page Visual Correctness

**Test:** Navigate to `/follows`, enter a LUKSO address, verify that follower results render in FollowerCards with collapsible profile sections
**Expected:** Follower list loads, cards show addresses/timestamps, profile sections expand on click
**Why human:** Visual rendering + real API data cannot be verified programmatically

### 2. Infinite Scroll Pagination

**Test:** In the "Infinite Followers" or "Infinite Following" tab, enter an address with many followers, scroll and click "Load more"
**Expected:** Additional pages load and append to the list
**Why human:** Requires real Hasura data + interactive scroll behavior

### 3. Server Mode vs React Mode Toggle

**Test:** Switch between "React" and "Server" mode toggle on the playground page
**Expected:** Both modes return identical data — server mode routes through Next.js server actions
**Why human:** Requires runtime execution to verify server action routing

---

_Verified: 2026-02-22_
_Verifier: Claude (gsd-verifier)_
