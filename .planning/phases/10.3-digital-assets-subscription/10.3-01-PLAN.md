---
phase: 10.3-digital-assets-subscription
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/react/src/subscriptions/digital-assets.ts
  - packages/react/src/index.ts
  - apps/test/src/app/subscriptions/page.tsx
autonomous: true
requirements: [SUB-02, SUB-03]

must_haves:
  truths:
    - 'Developer can call useDigitalAssetSubscription() and get { data, isConnected, isSubscribed, error }'
    - 'All type params inferred — zero explicit type arguments at call sites'
    - 'Digital asset subscription data appears in the playground page'
    - 'Cache invalidation works when invalidate: true'
  artifacts:
    - path: 'packages/react/src/subscriptions/digital-assets.ts'
      provides: 'useDigitalAssetSubscription hook'
      contains: 'useDigitalAssetSubscription'
    - path: 'apps/test/src/app/subscriptions/page.tsx'
      provides: 'Digital assets subscription demo section'
      contains: 'useDigitalAssetSubscription'
  key_links:
    - from: 'packages/react/src/subscriptions/digital-assets.ts'
      to: '@lsp-indexer/node'
      via: 'buildDigitalAssetWhere + digitalAssetKeys + parseDigitalAssets + DigitalAssetSubscriptionDocument'
      pattern: 'buildDigitalAssetWhere|digitalAssetKeys|parseDigitalAssets|DigitalAssetSubscriptionDocument'
    - from: 'packages/react/src/subscriptions/digital-assets.ts'
      to: 'packages/react/src/subscriptions/use-subscription.ts'
      via: 'useSubscription call'
      pattern: 'useSubscription'
---

<objective>
Create the `useDigitalAssetSubscription` hook and add a working digital assets subscription demo to the playground page.

Purpose: Domain subscription hook for digital assets. Follows the exact pattern established by profiles (10.2).

Output: `useDigitalAssetSubscription` exported from `@lsp-indexer/react`, digital assets subscription demo visible in the test app.
</objective>

<execution_context>
@/home/coder/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-subscriptions/10-CONTEXT.md
@.planning/phases/10.1-subscription-foundation/10.1-01-SUMMARY.md
@.planning/phases/10.2-profiles-subscription/10.2-01-SUMMARY.md
@packages/node/src/services/digital-assets.ts
@packages/node/src/keys/digital-assets.ts
@packages/node/src/documents/digital-assets.ts
@packages/node/src/parsers/digital-assets.ts
@packages/react/src/index.ts
@apps/test/src/app/subscriptions/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useDigitalAssetSubscription hook + playground demo + export</name>
  <files>
    packages/react/src/subscriptions/digital-assets.ts
    packages/react/src/index.ts
    apps/test/src/app/subscriptions/page.tsx
  </files>
  <action>
    **Create `packages/react/src/subscriptions/digital-assets.ts`:**

    Follow the exact pattern from the 10.2 profiles SUMMARY:
    - Import `DigitalAsset`, `DigitalAssetFilter` from `@lsp-indexer/types`
    - Import `buildDigitalAssetWhere`, `digitalAssetKeys`, `parseDigitalAssets`, `DigitalAssetSubscriptionDocument` from `@lsp-indexer/node`
    - `extract: (result) => result.digital_asset` — Hasura table extraction
    - `parser: (raw) => parseDigitalAssets(raw)`
    - `order_by: undefined` — entity domain, Hasura default sort
    - Params interface: `UseDigitalAssetSubscriptionParams` with `{ filter?, limit?, enabled?, invalidate?, onData?, onReconnect? }`
    - `useQueryClient()` called unconditionally (Rules of Hooks), only passed when `invalidate: true`

    **Update `packages/react/src/index.ts`:**

    Add export:
    ```typescript
    export * from './subscriptions/digital-assets';
    ```

    **Add digital assets demo to playground page:**

    Update `apps/test/src/app/subscriptions/page.tsx` — add a digital assets subscription section showing:
    1. Connection/subscription status badges
    2. Data count and list of digital asset names/addresses
    3. Error display
    4. Filter input if relevant
    5. Limit selector

    **Build validation:**
    ```bash
    pnpm build
    ```

    Commit: `feat(10.3-digital-assets): add useDigitalAssetSubscription hook + playground demo`

  </action>
  <verify>
    `pnpm build` — all packages + test app build.
    `grep "useDigitalAssetSubscription" packages/react/dist/index.js` — exported.
    `grep "useDigitalAssetSubscription" apps/test/src/app/subscriptions/page.tsx` — used in playground.
  </verify>
  <done>useDigitalAssetSubscription hook exported from @lsp-indexer/react with full type inference. Digital assets subscription demo visible in test app playground at /subscriptions.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes for all packages + test app
- `useDigitalAssetSubscription` is importable from `@lsp-indexer/react`
- Hook accepts { filter?, limit?, enabled?, invalidate?, onData?, onReconnect? }
- Hook returns { data: DigitalAsset[] | null, isConnected, isSubscribed, error: Error | null }
- All types inferred — no explicit type arguments
- Playground shows digital assets subscription demo
</verification>

<success_criteria>

1. useDigitalAssetSubscription created with type-safe SubscriptionConfig (extract + parser, zero assertions)
2. Hook exported from @lsp-indexer/react
3. Digital assets subscription demo works in playground
4. All packages build with zero type errors
   </success_criteria>

<output>
After completion, create `.planning/phases/10.3-digital-assets-subscription/10.3-01-SUMMARY.md`
</output>
